# 通用代码项目审查维护宪法

> 本文件是任意代码项目可复用的长期制度文件。它不是某一轮清理任务的计划，也不是只给某个 AI 用的提示词；它是所有维护者在开发前、开发中、开发后都必须执行的架构审查与维护门禁。  
> 任何人类开发者、AI / agent、外包协作者、未来接手者，只要要改这个仓库，就必须先服从这份文档，再谈实现。

---

## 1. 目标与适用范围

### 1.1 目标

这份宪法只服务一件事：

- 让任意项目在未来长期迭代中，始终保持：
  - 业务核心稳定
  - DDD 边界清楚
  - 多壳共核
  - 乐高积木式可插拔
  - 禁止手搓成熟轮子
  - 禁止重复造轮子
  - 禁止前端干后端的活
  - 禁止后端干前端的活
  - 禁止代码越改越厚、越改越绕、越改越难维护

它要防止的不是单次 bug，而是长期退化：

- 手搓轮子冲动
- 冗余代码累积
- 冗余抽象累积
- 框架倒灌核心
- 壳层偷藏业务真相
- 业务规则落错层
- 临时方案沉淀成永久负担
- 项目在岁月中逐渐堆成屎山

### 1.2 适用范围

本文件覆盖以下一切改动：

- 代码改动
- 依赖变更
- 新库引入
- 工具链调整
- 配置变更
- 运行时接入
- 协议层改动
- 数据访问改动
- 测试基础设施改动
- 文档所驱动的架构性重构

### 1.3 适用对象

本文件适用于：

- 仓库主人
- 人类开发者
- AI / agent
- 外包协作者
- 未来任何接手这个项目的人

任何人都不能因为“只是一个小改动”或“只是先临时修一下”绕过本文件。

### 1.4 通用化术语映射

为保证本文件可用于任何项目、任何语言，术语按如下方式理解：

- `前端 / 后端`：可映射为调用方 / 服务方、上游 / 下游、客户端 / 服务端、插件侧 / 核心侧。
- `domain / application / contract / adapter / shell`：是推荐分层模型；若项目使用其他分层命名，只要职责等价即可。
- `HTTP / realtime / CLI`：仅是常见入口示例，可替换为 RPC、消息队列消费者、定时任务入口、桌面入口、脚本入口等。
- 本文件中的框架与实现举例仅用于说明边界，不构成技术栈绑定。

---

## 2. 宪法的地位

### 2.1 文档层级

本仓库后续文档分为三层：

- [AGENTS.md](AGENTS.md)
  - 约束代理人格、执行纪律与会话级长期记忆
- [审查维护.md](审查维护.md)
  - 约束所有改动者的正式制度与审查门禁
- `docs/superpowers/plans/*.md`
  - 承接某一轮具体战役的执行计划

### 2.2 不能混淆的职责

本文件不是：

- PRD
- 功能设计稿
- 单轮重构计划
- 编码风格速查表
- 随便看看的人类可选建议

本文件是：

- 最高级维护制度
- 长期复用的审查标准
- 所有改动的共同上位约束

---

## 3. 核心原则与绝对红线

### 3.1 核心原则

#### 原则 1：业务核心高于技术实现

真正稳定的应该是：

- 领域模型
- 业务语义
- 核心规则
- 事件语义
- 错误语义
- 插件边界
- 多壳共享契约

真正可以演进的应该是：

- 前端技术
- 后端框架
- 库与工具链
- 适配层实现
- 运行时壳

任何改动只要让业务核心更依赖具体框架，默认就是退化。

#### 原则 2：复用优先，自研受限

凡是通用能力、基础设施、协议封装、性能敏感组件、工程工具链，默认先复用成熟生态，不默认自研；任何代码项目都不得重复造轮子、手搓成熟轮子。

业务真相自己掌握，通用问题站在巨人肩膀上。复用成熟轮子不是偷懒，而是主动利用那些已经被长期打磨、反复验证过的实现，把通用复杂性留给成熟生态，把项目代码尽量收缩到领域语义与薄适配层，而不是膨胀成难以维护的私有基础设施。

语言选型不设预设偏好；默认原则是“优先稳定成熟、长期维护成本可控、生态健康、替换路径清楚”。任何语言与框架都必须先满足边界清楚与可维护性，再谈引入。

#### 原则 3：分层必须可证明

所有改动都必须先回答：它属于 `domain / application / contract / adapter / shell` 哪一层。

说不清归属的改动，不允许直接进入实现。

#### 原则 4：多壳共核，前后端完美解耦

Web、iOS、Android、CLI 都只是壳。

它们都只能消费：

- 稳定 command
- 稳定 query
- 稳定 event
- 稳定 snapshot
- 稳定 error code

前端不许干后端的活，后端也不许干前端的活。

这句话在本项目中的具体含义是：

- 前端不得承载权限真相、成员资格真相、治理语义真相、消息成立真相
- 后端不得为某个壳层硬编码页面流程、布局语义、展示文案、壳层专属数据形状

#### 原则 5：adapter 只翻译，不决策

`API 入口 / 事件入口 / CLI / worker / repository / DB mapping / 外部服务接入` 这些都只负责：

- 协议翻译
- 鉴权接入
- IO 编排
- 错误转码
- 框架胶水

它们不负责：

- 业务裁决
- 领域规则
- 权限真相
- 成员真相
- 前端页面流程

#### 原则 6：删除优先于堆叠

遇到问题先想：

- 能不能删
- 能不能并
- 能不能收口
- 能不能复用

不要默认再包一层、再抽一层、再加一个 manager / bridge / wrapper / helper。

#### 原则 7：优雅 = 简洁 + 高效 + 清楚

本项目接受的“优雅”不是炫技，不是谜语，不是花哨。

本项目接受的优雅是：

- 简洁
- 高效
- 易读
- 边界清楚
- 重复更少
- 热路径更薄
- 扩展成本更低
- 维护者更容易理解

### 3.2 绝对红线

以下情况是不可谈判的根本红线。

任何普通例外放行、紧急豁免、延期处理都不得突破这些红线：

- 新增手搓成熟轮子
- 重复造轮子
- 未做生态调研就自研通用能力
- 前端承载权限真相、成员真相、治理真相、消息成立真相
- 后端为某个前端壳硬编码页面流程、布局语义、文案语义、壳层专属数据形状
- `contract` 混入 UI 专属字段、展示语义、页面流程语义
- handler / 事件回调 / repo 映射里偷偷承载业务规则
- 核心层直接依赖框架或基础设施实现
- 引入第二套真相、第二套入口、第二套流程、第二套轮子
- 无测试、无验证、无证据地凭感觉重构
- 以清理之名跨多个独立子系统大面积重写

如果某次改动触碰上述任一项，默认结论只能是：

- 驳回
- 或先改设计再重审

---

## 4. 分层判定法

本章使用 `domain / application / contract / adapter / shell` 作为通用判题骨架。  
若你的项目采用其他命名体系，只要职责等价，允许一一映射后执行同样门禁。

### 4.1 五层定义

#### `domain`

拥有：

- 领域模型
- 值对象
- 不变量
- 领域错误语义
- 与具体技术无关的业务事实

禁止：

- 具体 Web / RPC / 消息框架 API
- 具体数据库驱动 API
- 具体运行时对象
- 具体部署平台细节
- 浏览器 API
- HTTP / Socket / CLI 协议细节
- 数据库表结构技巧

#### `application`

拥有：

- 用例编排
- command / query / event 协调
- 权限与成员资格等业务判断的组织
- 出站 port 定义

禁止：

- 具体 SQL
- HTTP / Socket / CLI 协议实现
- 页面流程
- 框架胶水

#### `contract`

拥有：

- 多壳共享命令
- 多壳共享查询
- 多壳共享事件
- 多壳共享快照
- 稳定错误码

禁止：

- 展示文案
- UI 状态枚举
- 布局字段
- 某个壳专属的页面流程语义
- 框架类型

#### `adapter`

拥有：

- HTTP handler
- realtime handler
- CLI 接入
- repo 实现
- DB mapping
- 外部服务接入
- 协议解码
- IO 编排
- 错误转码

禁止：

- 业务真相
- 权限裁决
- 成员真相
- 消息是否成立的最终判断

#### `shell`

拥有：

- 交互编排
- 展示派生
- 页面状态
- 本地体验态
- 草稿
- 滚动位置
- 乐观态
- 连接提示

禁止：

- 权限真相
- 成员资格真相
- 治理真相
- 消息成立真相

### 4.2 判定顺序

每次改动必须按以下顺序判层：

1. 这是业务真相、领域规则或不变量吗
2. 这是业务用例编排吗
3. 这是多壳共享稳定契约吗
4. 这是协议/IO/框架/外部系统适配吗
5. 这是壳层交互或本地体验态吗

如果一个改动同时想落进两层，默认说明边界没想清楚，先停下重判。

### 4.3 依赖方向矩阵

| 层 | 可以依赖 | 禁止依赖 |
|---|---|---|
| `domain` | 无框架纯语义 | `application / contract / adapter / shell` 具体实现与框架 |
| `application` | `domain`、稳定 `contract` | `adapter / shell` 具体实现、框架 API |
| `contract` | 纯数据结构与稳定语义 | 任何壳层展示语义、入口框架类型 |
| `adapter` | `application / domain / contract` 与基础设施 | 反向把基础设施细节塞回核心 |
| `shell` | 稳定 `contract`、必要壳层适配 | 业务真相、成员真相、权限真相 |

### 4.4 真相归属矩阵

| 内容 | 唯一允许归属 |
|---|---|
| 权限真相 | `domain / application` |
| 成员资格真相 | `domain / application` |
| 消息成立真相 | `domain / application` |
| 治理语义真相 | `domain / application` |
| 稳定跨端数据契约 | `contract` |
| 协议态 / 连接态 / 运行态投影 | `adapter` |
| 草稿 / 乐观态 / 滚动位置 / 连接提示 | `shell` |

### 4.5 快速判错法

出现以下情况，默认就是边界错误：

- 前端 state 在判断“谁能发言”
- handler / 事件回调在拼业务规则
- repo / db mapping 在决定业务是否合法
- `contract` 出现 UI 字段或页面流程
- 某段代码同时谈业务规则又谈协议细节 / 存储细节 / 运行时细节

---

## 5. 复用优先与自研准入门禁

### 5.1 默认规则

任何涉及以下能力的改动，默认先走复用审查：

- 网络通信
- 连接管理
- 序列化
- 存储访问
- 缓存
- 队列
- 重试
- 背压
- 加密
- 鉴权
- 会话
- ID 生成
- 时间源
- CLI 基础设施
- 测试支撑
- 通用工具链
- 跨端共用支撑能力

这类能力在没有完成复用审查之前，不得直接进入自研实现；默认顺序必须是先问“为什么不能复用”，再问“是否值得自己写”。

### 5.2 复用审查的最小证据集

没有以下证据，默认视为“没有完成调研”：

- 候选实现列表
- 官方文档或主要资料来源
- 比较维度：
  - 性能
  - 维护性
  - 生态成熟度
  - 边界适配
  - 可替换性
- 每个候选被淘汰的明确理由
- 为什么现有仓库能力不能直接复用
- 为什么一个薄适配层是合理翻译，而不是重复包装

只写一句“我查过了”，不算完成复用审查。

任何自研通用能力的提议，在进入实现前都必须先完成这组证据；做不到这一步，不得自研。

### 5.2.1 调研来源覆盖要求

复用调研必须同时覆盖以下来源：

- 仓库现有实现
- `学习/` 中已有候选
- 目标语言生态主要成熟方案
- 官方文档或主要维护方资料

### 5.2.2 调研记录的额外必填项

复用调研记录还必须写明：

- 检索时间
- 检索范围
- 关键检索词或查询路径

只列两个候选、主观写几句淘汰理由，不能算合规调研。

### 5.3 允许自研的唯一条件

只有同时满足以下条件，才允许谨慎自研：

- 成熟方案在边界上无法适配本项目目标
- 成熟方案在性能、维护性、生态成熟度或可替换性上存在明确缺口
- 这个能力承载的是本项目自己的业务真相，而不是通用问题
- 自研后不会把核心绑死在一个难以替换的私有实现上
- 有明确验证方案证明这不是拍脑袋

少一条，都不允许自研。

### 5.4 明确禁止的自研动机

下面这些理由不成立：

- “实现看起来不复杂”
- “我想完全可控”
- “先写出来再说”
- “现有库 API 不够顺手”
- “自己写更纯”
- “顺手封一层以后可能用得到”

---

## 6. 多壳共核与前后端完美解耦

### 6.1 前端能做什么

前端只允许做：

- 表达意图
- 展示状态
- 渲染快照
- 维护草稿
- 维护滚动位置
- 维护乐观态
- 维护连接提示

### 6.2 前端不能做什么

前端不得：

- 判定权限真相
- 判定成员资格真相
- 判定治理语义
- 判定消息是否成立
- 维护一套后端之外的业务规则

### 6.3 后端能做什么

后端只负责：

- 业务规则
- 权威事实
- 权限裁决
- 成员资格
- 稳定契约
- 错误语义
- 多壳共享能力

### 6.4 后端不能做什么

后端不得：

- 为某个壳层硬编码页面流程
- 为某个壳层硬编码展示文案
- 为某个壳层硬编码布局语义
- 返回只有某个壳才能理解的 UI 形状

### 6.5 共享面的唯一合法位置

跨端共享的唯一合法通用表面是 `contract`。

任何跨端能力，都必须先收敛成：

- command
- query
- event
- snapshot
- error code

禁止先按某个壳的页面流程设计接口，再反过来包装成“通用后端”。

### 6.6 前后端解耦快速检查清单

每次改动前、改动中、改动后，都必须用下面这组问题做一次快审。

只要有一条答成“是”，默认说明边界正在退化，必须先收口再继续开发：

- 前端是不是在判断“谁能发言、谁能看见、谁能加入、谁有权限”？
- 前端是不是保存了一套后端之外的成员、权限、治理或消息成立真相？
- 前端是不是为了某个页面流程，自己发明了后端契约里不存在的业务状态机？
- 后端是不是开始返回只服务某个壳的文案、布局字段、页面步骤或 UI 状态枚举？
- `contract` 里是不是出现了按钮文案、卡片字段、颜色语义、页面流程语义或壳专属展示模型？
- `API / 事件 / CLI` handler 里是不是直接在拼业务规则，而不是只做协议翻译与接线？
- repo / db mapping / 事件 handler 里是不是在决定“这条业务事实算不算成立”？
- shell / viewmodel / presenter 里是不是开始替后端做权限裁决、成员资格裁决或治理裁决？
- 某次改动是不是只有 Web 壳能理解，未来换成 iOS / Android / CLI 就得重写业务接口？
- 删除“当前框架与驱动”之后，这段核心业务或用例是不是就完全不可测试、不可复用？

若全部答“否”，还必须继续补两句确认：

- 这次新增的只是壳层体验态、协议翻译，还是共享契约 / 应用用例 / 领域规则？
- 这次改动产生的真相，是否仍只有一个权威来源？

这份快审清单不是可选建议；它是第 6 章的落地判题器。

---

## 7. 巨人经验与通用吸收原则

本文件不要求照抄任何特定产品的技术栈，但要求吸收长期验证过的结构性原则：

- 通用复杂性集中收口
- 权威事实单点明确
- 主链路优先
- 外围能力隔离
- 本地体验态不冒充业务真相

在任何项目中，这些原则都要被翻译成可判题条款：

- 同步、连接、存储、协议复杂性不要散落手搓
- 壳层只消费稳定能力，不到处复制胶水
- 权限、成员资格、消息成立、事件顺序必须有权威事实源
- 后台能力、辅助能力、外围集成不得拖垮系统主链路

---

## 8. 审查状态与可执行动作

每次审查必须只有以下四种最终状态之一：

### `通过`

允许：

- 继续实现
- 继续提交
- 继续合并
- 继续收口

### `驳回`

禁止：

- 进入实现
- 继续提交
- 继续合并

必须：

- 回到设计
- 或补充审查证据

### `例外放行`

只允许：

- 在已批准的例外范围内继续

必须：

- 附带批准者
- 附带失效条件
- 附带补审要求
- 不得突破本文件第 3.2 节的根本红线

### `延期处理`

只允许：

- 带着显式债务记录继续

禁止：

- 伪装成通过

### 硬规则

- 没有最终审查状态的记录，视为无效记录
- 无有效审查记录的改动，不得视为完成

---

## 8.1 仓库级硬门禁

以下门禁是仓库级规则，不是建议：

- 没有有效审查记录的改动，不得合并
- 没有有效审查记录的高风险改动，不得提交到主线
- 没有完成开发后验收的改动，不得发布
- 审查状态为 `驳回` 的改动，不得继续实现、提交、合并或发布
- 审查状态为 `延期处理` 的改动，只能带着显式债务记录继续，不得冒充为 `通过`

### 8.1.1 机器门禁要求

本仓库后续必须逐步落地以下自动化门禁：

- CI 或脚本校验高风险改动是否存在对应审查记录
- CI 或脚本校验审查记录是否包含最小必填字段
- PR 模板或等价流程要求填写审查状态与记录路径
- 发布前校验是否存在未过审或逾期债务

在自动化门禁尚未全部落地前，由仓库主人或主维护者承担人工阻断责任；人工阻断不是可选动作，而是自动化缺位期间的临时替代。

在自动化门禁未落地前，任何主线合并、发布或高风险提交，如果没有仓库主人或主维护者的明确确认，默认视为违规放行，必须立即补审；必要时必须回滚。

---

## 9. 开发前审查清单

每次开发前，必须逐项回答以下问题。

任一项答不清，禁止进入实现阶段。

### 9.1 分层判定

- 这次改动属于哪一层
- 为什么属于这一层
- 哪些层会被连带影响

### 9.2 目标与非目标

- 这次到底解决什么问题
- 这次明确不解决什么
- 这是新增、修复，还是清理

### 9.3 复用预审

- 这次是否涉及通用能力或基础设施
- 仓库内是否已有现成能力
- 目标语言生态是否已有成熟方案
- 为什么不能直接复用

### 9.4 重复实现排查

- 仓库内是否已有相同职责代码
- 是否已存在相似 helper / wrapper / bridge / manager
- 这次会不会形成第二套真相、第二套流程、第二套入口

### 9.5 边界污染预判

- 会不会把业务规则塞进 adapter / shell
- 会不会把 UI 语义塞进 contract
- 会不会把框架细节带进 application / domain
- 会不会让某壳专属流程倒灌核心

### 9.6 依赖与工具链影响检查

- 是否新增依赖
- 是否调整 feature
- 是否调整工具链
- 是否增加运行时接入
- 为什么值得引入
- 会不会增加未来替换成本

### 9.7 验证计划

- 哪些测试要补或强化
- 最后用什么命令证明改动正确
- 哪些风险点需要专项验证

### 9.8 债务处理策略

- 这次会碰到哪些旧债
- 是顺手清掉
- 还是局部绕开
- 还是延期处理

### 9.9 低风险与高风险判定

只有同时满足以下条件，才允许被判为低风险改动：

- 只涉及单一层级
- 不新增依赖
- 不改工具链
- 不改运行时接入
- 不改共享契约
- 不跨层移动业务真相
- 不新增通用 helper / wrapper / bridge / manager
- 不触碰热路径
- 不涉及债务延期
- 不涉及例外放行

只要有一条不满足，默认按高风险改动处理。

### 9.10 高风险改动的独立复核

以下情况必须有独立复核，不允许作者自判通过：

- 新增依赖
- 工具链或运行时接入调整
- 跨层边界移动
- 修改 `contract`
- 触碰系统高频热路径
- 触碰权限、成员、消息成立、治理语义真相
- 申请例外放行
- 债务延期
- 跨多个模块或多个文件簇的清理型维护

独立复核人可以是：

- 仓库主人
- 主维护者
- 仓库主人明确指定的 reviewer

AI / agent 不能既是实施者又是唯一放行者。

---

## 10. 开发中审查清单

开发过程中必须持续自检，防止写着写着就漂。

### 10.1 一旦像轮子，就立刻停

写到一半如果开始出现：

- 通用 helper
- 新 manager
- 新 bridge
- 新 wrapper
- 一层看不出边界价值的封装

必须暂停，回到复用审查重新判断。

### 10.2 不允许边写边放错层

持续自问：

- 我现在写的是业务真相还是协议细节
- 这段逻辑是不是已经越过它该在的层
- 这段代码删掉框架名后还能成立吗

### 10.3 不允许偷塞业务真相

开发中最容易出现的结构性污染包括：

- 前端 state 里偷放权限或成员规则
- handler 里偷写业务规则
- repo 映射里偷做合法性判断
- contract 里偷放 UI 字段

发现即收回。

### 10.4 不允许新增重复入口、重复流程、重复真相

如果出现：

- 两个函数做同一件事
- 两套 API 表达同一业务语义
- 两个状态来源都在代表同一事实
- 新逻辑只是把旧逻辑换名重包

默认不是灵活，是冗余。

### 10.5 新抽象必须有真实压力来源

新增抽象前，必须回答：

- 它解决了哪一个已存在的重复或边界问题
- 它是否降低了理解成本
- 它是否真的服务于多壳共核或可替换性
- 如果不加这层，系统会具体哪里更差

答不出来，就不要抽。

### 10.6 热路径必须持续保持薄

对任何存在高频路径的系统都要盯住：

- 事件入口 handler 不长胖
- handler 不揉业务、序列化、存储、分发于一体
- 不跨 `await` 长持业务锁
- 分发、背压、取消、重试留在 adapter
- 业务事实是否合法、谁有资格执行，回到 `application / domain`

### 10.7 顺手清理明显冗余

开发过程中碰到：

- 重复 helper
- 无意义中转函数
- 过时包装
- 假状态
- 明显废弃路径
- 源码字符串式测试断言

不能装作没看见。

### 10.8 技术债即时登记

这次不能清掉的问题，必须立即记录，禁止只留在脑子里。

---

## 11. 开发后验收清单

开发完成后，不是“能跑就算完”，而是要证明结构没有变坏，最好还更清楚了。

### 11.1 结果必须更清楚

开发后必须检查：

- 职责是否更单一
- 边界是否更清楚
- 重复是否更少
- 核心是否更独立
- 以后增删改是否更容易

### 11.2 必须复查是否引入新轮子或新冗余

必须重新扫一遍：

- 是否新增通用私有轮子
- 是否新增重复包装
- 是否形成第二套真相
- 是否把临时方案留成长期结构

### 11.3 必须复查边界是否守住

必须明确确认：

- `domain` 无框架细节
- `application` 无协议与展示细节
- `contract` 无 UI 专属语义
- `adapter` 无业务真相
- `shell` 无权限、成员、治理、消息成立真相

### 11.4 验证必须是真实证据

完成声明必须建立在新鲜验证上。

必须写清：

- 跑了哪些命令
- 覆盖了哪些关键路径
- 还有哪些未验证风险

### 11.5 债务必须显式处理

必须说明：

- 清掉了哪些旧债
- 哪些债务这次没处理
- 为什么没处理
- 什么时候必须回头处理

### 11.6 提交必须反映边界变化

提交前必须确认：

- 工作树干净
- 没有无关格式化噪音
- 提交粒度清楚
- 中文提交信息准确说明：
  - 做了什么
  - 为什么做
  - 影响了什么边界

### 11.7 长期维护视角复盘

开发完成后最后再问一次：

- 这次改动是否让未来 CLI / 移动端 / 多壳复用更容易
- 是否减少了对具体框架的绑定
- 是否让后来者更容易理解
- 是否让核心业务更可测试、更可替换

---

## 12. 例外审批、争议裁决与紧急变更

### 12.1 谁可以批准例外

默认只有以下角色可以批准例外：

- 仓库主人
- 仓库主人明确指定的主维护者

AI / agent 不得自批例外。

### 12.2 哪些情况允许紧急豁免

仅限：

- 安全事故
- 数据损坏风险
- 生产阻断
- 必须立即止血的运行故障

### 12.3 紧急豁免也不能绕过红线

紧急豁免只允许绕过非核心流程，不能绕过根本红线：

- 不允许借紧急修复新增成熟轮子私有重写
- 不允许借紧急修复制造第二套真相
- 不允许借紧急修复把业务规则塞错层

### 12.4 紧急豁免后的必做事项

- 当前止血结束后立即补做事后审查
- 例外必须附带失效时间或失效触发点
- 例外不得永久挂起

### 12.5 有争议时如何裁决

对审查结论有争议时，必须提交固定证据集给仓库主人或主维护者裁决。

口头争论、感觉判断、资历压制都不能替代证据。

---

## 13. 证据留痕的固定落点

### 13.1 留痕不是建议，是硬规则

审查结果必须留痕。没有留痕，等于没有审查。

### 13.2 普通低风险改动

普通且低风险改动，也必须留痕。

默认落点同样是：

- `docs/superpowers/reviews/YYYY-MM-DD-<topic>-审查记录.md`

只有在已经存在当前活跃执行计划且该计划就是当前唯一实施载体时，才允许把低风险审查记录并入该计划文件；但仍必须包含完整必填字段，且提交说明中必须明确指向该记录位置。

### 13.3 必须单独落为审查记录的情况

以下情况必须单独落为审查记录：

- 跨层边界移动
- 新增依赖
- 新增基础设施能力
- 申请例外放行
- 债务延期
- 明确发现重复造轮子
- 明确发现高风险冗余或结构退化

### 13.4 固定落点

单独审查记录固定落点为：

- `docs/superpowers/reviews/YYYY-MM-DD-<topic>-审查记录.md`

### 13.5 最小记录模板

每条审查记录至少包含：

- 变更范围
- 风险等级
- 层级归属
- 复用 / 自研结论
- 检索时间
- 检索范围
- 关键检索词或查询路径
- 候选对比摘要
- 风险点
- 验证命令
- 债务结论
- 债务责任人
- 回看触发点
- 最晚处理时机
- 是否需要独立复核
- 独立复核人
- 补审要求
- 审查最终状态

---

## 14. 技术债记录与延期规则

### 14.1 默认原则

发现债务，不能装看不见。

每次开发前、开发中、开发后，只要发现：

- 重复实现
- 边界污染
- 低价值抽象
- 私有轮子
- 过时胶水
- 假状态

都必须明确选择：

- 这次清掉
- 这次局部缓解
- 这次延期处理

### 14.2 允许延期的条件

只有同时满足以下条件，才允许延期：

- 这次不处理不会破坏正确性
- 不会让主链路继续明显恶化
- 不会形成第二套真相或第二套轮子
- 已有临时止血措施
- 已留下完整记录

### 14.3 明确禁止延期的债务

默认不能延期：

- 新增手搓成熟轮子
- 新增重复造轮子
- 新增双重真相
- 新增双重入口或双重流程
- 新增核心绑死框架
- 明知会扩散的主链路退化
- 无关键验证却强行收口

### 14.4 每条延期债务的必填字段

- 债务是什么
- 出现在哪些文件与边界
- 属于哪类问题
- 为什么这次没处理
- 继续放着的风险
- 当前临时控制措施
- 债务责任人
- 回看触发点
- 最晚处理时机

### 14.5 真正清债的标准

一条债务只有同时满足以下条件，才算关闭：

- 根因已处理
- 没生成新的重复实现
- 验证已补齐
- 边界重新变清楚
- 留痕文档已更新

### 14.6 逾期债务的强制动作

任何已到“最晚处理时机”仍未处理的债务，必须立刻触发以下动作：

- 相关热点的后续改动默认不得直接 `通过`
- 必须重新审查该债务是否升级为独立清理计划
- 必须由债务责任人或仓库主人重新确认处理路径
- 在下一个版本收口前，不得继续无限顺延

如果债务已经逾期且没有复审记录，默认视为制度未执行到位。

---

## 15. 清理型维护的反失控约束

禁止借“清理”“重构”“优雅化”之名，把普通维护升级成无边界的大重写。

### 15.1 默认约束

- 一轮清理默认只打一个热点或一个热点簇
- 先做表征测试或强化现有行为验证，再做清理
- 优先删除、合并、收口
- 不优先大改写

### 15.2 明确禁止

- 一轮跨多个独立子系统的大面积重写
- 顺手把普通开发变成全仓翻新
- 为了“更高级”引入新一轮抽象膨胀
- 没有计划就改公共边界

### 15.3 必须升级为单独计划的情况

以下情况不得混在普通开发里顺手完成，必须升级为单独计划：

- 会改变多个层级的公共边界
- 会改变多个入口共享契约
- 会影响主链路行为
- 会涉及多个独立子系统联动调整

---

## 16. 固定审查模板

以下模板可直接复制到计划、审查记录或变更说明中。

```markdown
# 审查记录

## 变更范围
- 

## 层级归属
- 主归属层：
- 连带影响层：
- 归属理由：

## 风险等级
- 低风险 / 高风险：

## 复用 / 自研结论
- 是否涉及通用能力：
- 仓库现有实现：
- 检索时间：
- 检索范围：
- 关键检索词或查询路径：
- 候选实现：
- 对比维度摘要：
- 最终结论：

## 边界风险
- 

## 冗余 / 重复实现风险
- 

## 验证
- 计划命令：
- 实际命令：
- 未验证风险：

## 债务结论
- 是否新增债务：
- 是否延期：
- 债务责任人：
- 回看触发点：
- 最晚处理时机：

## 复核要求
- 是否需要独立复核：
- 独立复核人：

## 最终状态
- 通过 / 驳回 / 例外放行 / 延期处理

## 批准信息
- 批准人：
- 例外失效条件：
- 补审要求：
```

---

## 17. 最后的总原则

以后每一次维护，都不要先问“这次能不能改”，而要先问：

- 这次改动是不是让核心更独立
- 边界是不是更清楚
- 复用是不是更多
- 私有轮子是不是更少
- 冗余是不是更少
- 代码是不是更简洁、更高效、更容易理解
- 证据是不是更硬

如果答案是否定的，那么无论功能是否完成，这次改动都不应被视为真正合格。
