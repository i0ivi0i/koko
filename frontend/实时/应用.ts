import {
  创建房间实时编排,
  type 房间实时编排依赖,
  type 房间实时编排端口,
} from "../房间实时编排.js";

/**
 * 实时应用门面只暴露“房间实时协作”这一条稳定入口：
 * 1. 聊天内核和桥接层以后只认这里，不再把 `房间实时编排.ts` 当成长期 owner 入口；
 * 2. 当前先复用既有编排实现，避免在 owner 收口阶段同时改结构和改协议；
 * 3. 后续若把订阅、补发、控制面进一步拆细，只在这个门面背后演进。
 */
export type 实时应用依赖 = 房间实时编排依赖;
export type 实时应用端口 = 房间实时编排端口;

export function 创建实时应用(deps: 实时应用依赖): 实时应用端口 {
  return 创建房间实时编排(deps);
}
