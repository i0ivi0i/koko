/**
 * 过渡期入口：
 * 真正的房间快照恢复 owner 已搬到 `frontend/恢复/应用.ts`。
 * 这里保留旧壳层路径，只为了让旧 import 面在重构阶段不断线。
 * 新的恢复真相禁止继续回流到这份文件。
 */
export {
  创建恢复应用 as 创建房间快照恢复协作,
  type 房间快照恢复协作,
  type 房间快照恢复协作依赖,
} from "../../恢复/应用.js";
