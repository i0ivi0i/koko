{
  "name": "koko-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --environment jsdom",
    "test:e2e": "vitest run --environment jsdom tests/端到端测试.spec.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "lit": "^3.3.1",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "jsdom": "^26.1.0",
    "typescript": "^6.0.2",
    "vitest": "^3.2.4"
  }
}
