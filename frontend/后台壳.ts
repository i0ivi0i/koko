/**
 * 根目录只保留迁移门面：
 * 1. 继续提供既有 `./后台壳.js` 入口，避免现有懒加载与测试入口立刻炸开；
 * 2. 真实壳 owner 已经收进 `frontend/后台/壳.ts`；
 * 3. 这里显式保留 side-effect import，确保自定义元素注册仍然生效。
 */
export { 后台壳 } from "./后台/壳.js";
import "./后台/壳.js";
