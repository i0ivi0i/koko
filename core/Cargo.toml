[package]
name = "koko-core"
version = "0.1.0"
edition = "2024"

[dependencies]
time = "0.3"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync"] }
