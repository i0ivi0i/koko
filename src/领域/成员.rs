use super::领域错误;

/// 消息发送前必须先确认成员资格，避免入口层或壳层私自裁决。
///
/// 维护者说明：
/// - 这是最小成员资格不变量函数，保持纯函数可测。
/// - 只判断“是否成员”这个事实，不处理权限分级等扩展语义。
pub fn 校验成员可发言(是否成员: bool) -> Result<(), 领域错误> {
    if 是否成员 {
        Ok(())
    } else {
        Err(领域错误::成员资格不足)
    }
}
