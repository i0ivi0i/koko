# CLAUDE.md

用于减少 LLM 常见编码失误的行为约束；可按需与项目级指令合并使用。

**取舍：** 这些规则默认偏向谨慎而非速度；面对琐碎任务时，禁止机械套用而丢掉基本判断。

## 1. 禁止带着模糊理解直接编码

**禁止想当然。禁止掩饰困惑。禁止把权衡藏起来。**

开始实现前：
- 禁止在未显式说明关键假设时直接动手；拿不准就先问。
- 存在多种合理解释时，禁止静默自选其一。
- 明明有更简单的方案时，禁止不提；该明确提出异议时就提出。
- 只要还有关键点没看明白，禁止硬着头皮继续；先说清卡住在哪里，再发问。

## 2. 禁止把简单问题写复杂

**禁止超配。禁止预支未来。只写解决当前问题所需的最小代码。**

- 禁止实现超出需求范围的功能。
- 禁止为一次性代码预先搭抽象。
- 禁止加入未被要求的“灵活性”或“可配置性”。
- 禁止为实际上不可能发生的场景堆错误处理。
- 明明 50 行能解决的问题，禁止写成 200 行；写复杂了就收回来。

自问一句：“一个资深工程师会不会觉得这东西过度复杂？”如果会，禁止就这样算完，继续简化。

## 3. 禁止借题发挥，改动必须下刀精准

**禁止扩大改动面。禁止顺手清场。只动必须动的地方，只收拾自己留下的尾巴。**

修改既有代码时：
- 禁止顺手“优化”相邻代码、注释或格式。
- 禁止重构没坏、也不在本次问题范围内的东西。
- 禁止按个人口味改风格；即使你自己不会这么写，也要对齐现有风格。
- 看到与当前任务无关的死代码，禁止擅自删除；可以指出，但不要顺手处理。

当你的改动制造出孤儿项时：
- 由你这次改动造成未使用的 import、变量或函数，禁止留着不清。
- 既有死代码在未被要求前，禁止借机清理。

判准：如果某一行改动不能直接追溯到用户请求，禁止把它混进提交里。

## 4. 禁止在成功标准含糊时一路往前推

**禁止边做边猜。禁止把“差不多能用”当作完成。先定义可验证的成功标准，再循环推进直到验明为止。**

把任务改写成可验证的目标：
- “加上校验” → “先为非法输入写测试，再把测试跑绿”
- “修这个 bug” → “先写出能复现问题的测试，再把它跑绿”
- “重构 X” → “确认改前改后测试都通过”

多步骤任务中，禁止在未写出简短计划前直接展开；每一步都要带上对应的验证点：
```
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
3. [步骤] → 验证：[检查项]
```

成功标准够硬，才能让你自己闭环推进；如果标准只是“把它弄好”，禁止假装这已经足够明确。

---

**这些规则生效的标志：** diff 里的无关改动更少，因过度复杂而返工的情况更少，澄清问题出现在动手之前，而不是出错之后。
