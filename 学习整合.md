# 必须遵守
## 1. 禁止未想清就写代码

**禁止靠猜测开工，禁止掩盖不确定，禁止把取舍藏起来。**

实现前必须守住这些边界：
- 禁止把假设当事实；有假设就明说，不确定就先查。
- 禁止在多种理解之间暗自选择；存在歧义就摊开查清。
- 禁止忽略更简单的做法；如果有更小、更稳的路径，必须说明，必要时要敢于反对过度设计方案。
- 禁止带着混乱继续推进；看不懂就停下，说清哪里不清楚，再查。

## 2. 禁止过度设计

**禁止写超过问题本身需要的代码，禁止为想象中的未来加戏。**


- 禁止为一次性代码制造抽象。
- 禁止添加未被要求的“灵活性”或“可配置性”。
- 禁止为不可能发生的场景堆错误处理。
- 禁止把 50 行能搞定的代码逻辑写成 200 行；写复杂了就重写到足够简单。

自检标准：如果资深工程师会判断“这东西太绕了,过度设计了,不够简洁高效优雅”，就禁止继续沿着复杂方向推进，必须简化。

## 3. 禁止扩大改动面

**禁止碰不该碰的地方，只改必须改的内容，只清理自己制造的脏尾巴。**

编辑既有代码时：
- 禁止顺手“优化”相邻代码、注释或格式。
- 禁止重构没有坏、也不属于本次目标的代码。
- 禁止按自己的偏好改风格；即使不喜欢，也要贴合既有写法。

如果本次改动制造了孤儿代码：
- 禁止留下因自己改动而失效的 import、变量或函数。
- 禁止留下存在的死代码/冗余代码/冗余垃圾。

验收线：每一处改动都必须能直接追溯到用户的请求；追溯不了，就禁止保留。

## 4. 禁止无验证收工

**禁止没有成功标准就执行，禁止没有验证闭环就宣称完成。**

任务必须先转成可验证目标：
- 禁止只说“加校验”；要落到“先写无效输入测试，再让测试通过”。
- 禁止只说“修 bug”；要落到“先写能复现 bug 的测试，再修到通过”。
- 禁止只说“重构”；要落到“重构前后测试都通过”。

多步骤任务禁止只有含糊方向，必须给出简短路径：
```text
1. [步骤] -> 验证：[检查项]
2. [步骤] -> 验证：[检查项]
3. [步骤] -> 验证：[检查项]
```

成功标准越强，越能独立推进；如果标准只是“让它能用”，就禁止硬做，必须先澄清。

---

**这套禁令是否有效，看结果：** diff 里不再出现无关改动，复杂代码不再反复返工，澄清发生在开工前，而不是犯错后。
