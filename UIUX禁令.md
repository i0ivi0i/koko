# UIUX禁令

目标：这不是一份“界面风格建议”，而是一份把团队从网页思维里拽出来的体验禁令。

禁止把它理解成“一个能聊天的网站”；必须把它理解成“一个暂时跑在浏览器里的群聊应用”。

所以这份文档真正禁止的，不是某个按钮长什么样，而是会持续把产品做回网页的那套思维、判断方式和工作习惯。

---

## 0. 总纲

先记住五条硬裁决：

- 禁止把用户进入理解成进入页面；要把它理解成进入现场。
- 禁止把群聊核心理解成浏览内容；要把它理解成持续在场。
- 禁止把 IM 的单位理解成页；要把它理解成流。
- 禁止把应用感来源理解成装饰性；要把它理解成连续性。
- 禁止把“高级 UI”理解成像设计稿；要把它理解成顺得像身体的一部分。

后面所有禁令，都服务这五句话。

---

## 1. 世界观禁令

### 1.1 禁止把 IM 当内容网站设计

内容网站的核心是“看”；  
IM 的核心是“在”。

看，是路过。
在，是停留。

所以禁止用内容网站那套思维来做群聊：

- 不能把用户理解成访客；
- 不能把房间理解成详情页；
- 不能把消息理解成内容列表；
- 不能把首页理解成引导页；
- 不能把切换理解成跳转。

一旦这样想，产品就会天然长成网页。

### 1.2 禁止把界面当静态画面设计

网页设计师最容易犯的错，是盯着某一屏“长得好不好看”。

但 IM 不是静态画面产品，它是时间性产品。
用户感知的不是一张图，而是一整段连续体验：

- 打开时有没有立刻回到上次现场；
- 新消息来了时身体有没有被自然牵引；
- 离开底部后有没有清楚知道自己身在何处；
- 断网、重连、恢复时心里有没有失重；
- 发消息、发图、看图时有没有被切断节奏。

所以：

- 禁止只评审静态稿，不评审时间流；
- 禁止只看单屏视觉，不看 30 秒连续操作；
- 禁止只设计“正常态”，不设计恢复态、失败态、等待态、切换态。

### 1.3 禁止把“像网页”理解成视觉问题

“网页味”首先不是视觉风格问题，而是认知结构问题。

真正让用户觉得自己在用网页的，往往不是颜色、圆角、阴影，而是：

- 总在跳；
- 总在等；
- 总在找入口；
- 总在确认自己现在在哪；
- 总在理解页面关系；
- 总在担心刚才的输入和现场会不会丢。

所以禁止把去网页化先做成视觉化工程；必须先去掉这些认知负担。
禁止把视觉当第一刀；视觉只能是最后一层。

---

## 2. 体验哲学禁令

### 2.1 禁止让用户“访问房间”

禁止把用户动作理解成“访问房间”；用户真正做的是“进入房间”。

“访问”是网页词。
“进入”是应用词。

差别在这里：

- 访问，默认随时离开；
- 进入，默认持续停留；
- 访问，强调页面切换；
- 进入，强调状态延续；
- 访问，重点是内容呈现；
- 进入，重点是人在场、关系在场、节奏在场。

凡是会让“进入房间”变成“打开一个新页面”的设计，全部都属于错误方向。

### 2.2 禁止让用户感到自己在“操作浏览器”

最强的应用感，来自用户忘掉浏览器本身。

如果一个设计方案需要用户频繁意识到：

- 我是不是跳页了；
- 我是不是该点浏览器返回；
- 我是不是刷新了；
- 我是不是丢了滚动位置；
- 我是不是又开了个标签；
- 我是不是离开刚才那个上下文了；

那这个方案再好看，也已经输了。

禁止设计出让用户感知为“我正在浏览器里完成一串页面动作”的方案；
必须把体验收口到“我仍在这段会话里”。

### 2.3 禁止打断用户的在场感

群聊是“低门槛持续停留”的产品。
它不是让用户专心完成一个步骤，然后结束。

它更像一种轻持续在线状态：

- 看一眼；
- 回一句；
- 上滑看一下前文；
- 打开一张图；
- 回到输入区继续说；
- 断一下线又接回来；
- 切到后台又回来；
- 看起来没做什么，其实一直没离场。

所以：

- 禁止任何不必要的整屏重置；
- 禁止把一个自然动作拆成多个网页步骤；
- 禁止一打开媒体就像离开会话；
- 禁止错误或恢复把用户从现场中弹出去。

禁止把 IM 第一原则理解成“信息完整”；必须优先保证“在场不断”。

### 2.4 禁止牺牲节奏去换“设计感”

很多网页式设计喜欢追求：

- 大留白；
- 大标题；
- 大卡片；
- 大段说明；
- 强装饰；
- 仪式感切换。

这些东西不一定错，但在 IM 里默认有毒。

因为群聊首先是节奏型产品，不是观赏型产品。
它要服务的是：

- 快速扫读；
- 快速判断；
- 快速进入；
- 快速表达；
- 长时间停留不累；
- 高频使用不烦。

只要一种视觉处理会伤害节奏，它就不该因为“更有设计感”而被保留。

---

## 3. 判断习惯禁令

### 3.1 禁止先问“这一屏好不好看”，后问“这一分钟顺不顺”

禁止按错误顺序评审；正确顺序必须反过来：

1. 这一分钟顺不顺；
2. 这条连续动作会不会断；
3. 用户会不会慌；
4. 现场会不会丢；
5. 最后才是这一屏是否漂亮。

任何把“截图好看”放在“连续使用顺手”前面的评审，都会把产品带回网页设计。

### 3.2 禁止按页面拆问题，必须按时刻拆问题

不要问：

- 首页怎么设计？
- 房间页怎么设计？
- 查看器页怎么设计？

要问：

- 用户刚回来时第一秒看到什么最对？
- 用户读到一半来新消息时，怎样提醒最不打断？
- 用户离底浏览时，怎样回到底部最自然？
- 用户发图失败时，怎样既保留现场又保留控制权？
- 用户切到后台再回来时，怎样最快恢复确定感？

禁止按页面思维拆问题；页面思维天然会产出网页。
必须按时刻思维拆问题；时刻思维才会产出应用。

### 3.3 禁止把状态设计成附属信息

在 IM 里，状态不是点缀，它就是体验主体的一部分。

比如：

- 我现在是不是在线；
- 我是不是已经回到最新；
- 这里是不是未读起点；
- 这条消息是不是已发出；
- 这张图是不是还在传；
- 我现在是不是还在这个房间里；
- 系统是不是已经恢复稳了。

这些如果不被稳定感知，用户心里就会一直悬着。

所以禁止把关键状态做成：

- 角落里的一句浅字；
- 一闪而过的 toast；
- 只在 hover 时出现的提示；
- 要点开二级面板才知道的信息。

### 3.4 禁止让用户自己拼系统真相

一个低级但常见的网页坏习惯，是把真相拆散丢给用户自己拼：

- 看按钮像是发出去了，但不知道成功没有；
- 看到未读线，但不知道自己是不是离底；
- 看到加载中，但不知道现场还在不在；
- 看到错误，但不知道刚才内容丢没丢；
- 看到重连，但不知道能不能继续发。

这类设计本质上是在把系统判断成本外包给用户。

禁止把系统判断成本外包给用户。
产品必须主动让真相成形，让用户不必猜。

---

## 4. 塑形习惯禁令

这部分最重要。因为产品最后长成什么样，很多时候不是因为某次大设计，而是因为团队的坏习惯慢慢把它带偏。

### 4.1 禁止用“先这样也能用”来放过网页味

很多网页味就是这样长出来的：

- 先加个页面过渡一下；
- 先用一个列表页顶着；
- 先用 toast 提示一下；
- 先让用户自己点进去看；
- 先用浏览器返回回去；
- 先不处理恢复态；
- 先把错误做成红字。

每一个“先这样”，最后都会变成产品骨头的一部分。

所以只要方向上已经明显是网页思维，就不能因为“暂时也能跑”而放过。

### 4.2 禁止每个局部都各自聪明

应用感来自整体秩序，不来自局部小聪明。

常见坏习惯：

- 这里自己弹一个 toast；
- 那里自己做一个 loading；
- 另一个地方自己做一个返回；
- 每个模块各自定义一次空态；
- 每个面板各自发明一套状态文案；
- 每个入口各自决定什么时候切屏。

最后用户感受到的不是灵活，而是散、乱、碎。

所以禁止让这些事情各自发生在局部里，必须统一收口：

- 状态语言要统一；
- 切换语义要统一；
- 返回逻辑要统一；
- 提醒方式要统一；
- “从哪里来、现在在哪、下一步做什么”要统一。

### 4.3 禁止只为首屏设计，不为高频使用设计

网页容易只设计第一次打开。
IM 必须设计第 100 次、第 1000 次使用。

第一次打开时觉得“好看”，没什么了不起。
真正难的是高频使用后仍然：

- 不烦；
- 不累；
- 不乱；
- 不慌；
- 不散；
- 不需要重新理解。

所以一切设计都要问：

它是一次性展示友好，还是长期使用友好？

如果两者冲突，禁止为一次性展示让路；IM 默认站长期使用。

### 4.4 禁止把说明当设计能力

一旦一个界面需要大量解释，通常不是用户不够聪明，而是设计不够诚实。

好的 IM 体验不是靠解释成立，而是靠肌肉记忆成立。

因此：

- 禁止用大段文案去弥补交互不自然；
- 禁止用教程味空态替代清晰结构；
- 禁止用“提示用户这样理解”代替“让用户自然这样感知”。

禁止把“看懂了”当成合格线；真正好的界面必须达到“不用想就会用”。

---

## 5. 具体落地禁令

禁止让前面的哲学停留在抽象口号里；下面这些落地禁令必须写死。

### 5.1 空间结构

- 禁止居中卡片式网页骨架成为主形态。
- 禁止房间主舞台被营销区、说明区、功能区分权。
- 禁止整页滚动承接会话主流程。
- 禁止房间页长得像内容详情页。

### 5.2 导航结构

- 禁止主流程依赖浏览器返回。
- 禁止房间、查看器、面板之间频繁整页跳转。
- 禁止一切“打开一个新网页才能继续主流程”的默认设计。
- 禁止切换时整屏白掉、重置、失忆。

### 5.3 时间线

- 禁止消息流长得像资讯流、博客流、卡片 feed。
- 禁止未读、最新、发送中、失败、系统态没有明确锚点。
- 禁止时间线只顾好看，不顾扫读效率。
- 禁止让用户在时间线上经常失去方位感。

### 5.4 输入区

- 禁止输入区像网页表单。
- 禁止输入区不稳定、不固定、离主舞台太远。
- 禁止附件、重试、草稿、发送状态远离输入区。
- 禁止发送链路没有明显的连续反馈。

### 5.5 媒体

- 禁止时间线媒体默认重型、默认吵闹、默认夺主。
- 禁止打开媒体像离开群聊去另一个站点。
- 禁止正式查看器和房间上下文彻底断裂。
- 禁止预览态和正式态混成一层。

### 5.6 系统态

- 禁止关键状态只靠 toast。
- 禁止离线、重连、恢复、更新、补发没有固定感知位置。
- 禁止错误一来就先清现场。
- 禁止让用户猜“刚才那一步到底算成功还是失败”。

---

## 6. 评审前置问题

以后任何 UIUX 方案，禁止先看稿感受；先回答这七问：

1. 用户进入后，感到自己是在“看页面”还是“在现场”？
2. 这套设计的主单位是“页”还是“流”？
3. 用户在连续使用 1 分钟时，哪里会断气？
4. 关键状态是不是稳定可感知，而不是靠猜？
5. 用户会不会频繁意识到浏览器本身的存在？
6. 这套方案是在优化截图，还是在优化高频使用？
7. 去掉地址栏和标签栏之后，它还像不像一个独立 IM 应用？

只要第 1、2、5、7 任一题答错，就禁止继续往下推进，这个方案已经偏了。

---

## 7. 最后的硬裁决

以后评审一个设计方案，禁止再先问：

- 漂不漂亮；
- 潮不潮；
- 像不像某个产品；
- 够不够“高级”。

先问这三句：

- 它有没有把用户留在现场里？
- 它有没有让会话像一条不断的流？
- 它有没有让浏览器退后，让应用站到前面？

如果没有，哪怕视觉再精致，也禁止把它当成应用方案，它仍然只是一个做得比较漂亮的网页。

如果有，哪怕视觉还可以继续打磨，也可以认定它已经站在正确的产品智慧上了。
