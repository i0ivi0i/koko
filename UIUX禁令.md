# UIUX禁令

目标：让用户一上手就进入“正在使用群聊 IM 应用”的心理场，而不是感觉自己在一个网页里填表、跳页、点卡片、等刷新。

总裁决：

- 页面只是窗口，不是内容页。
- 房间是现场，不是详情页。
- 时间线是主舞台，不是列表组件。
- 输入区是主武器，不是网页表单。
- 状态要持续可见，不要只在角落里闪一下。

---

## 1. 空间感禁令

- 禁止把主界面做成居中卡片网页。聊天主舞台不能像登录页、表单页、SaaS 设置页那样被一张卡包在页面中央。
- 禁止出现明显的“网页外边框感”。进入房间后，视觉上应先感到自己进入了一个持续运行的聊天空间，而不是落在一个带大留白的内容容器里。
- 禁止首页使用官网式 Hero、功能宫格、产品介绍文案、营销区块。群聊产品首页只能服务“回到会话 / 进入房间 / 继续交流”。
- 禁止房间页出现“上方网站头图 + 中间聊天卡片 + 下方网页正文”的三段式网页结构。
- 禁止房间页和首页共用同一种“内容页骨架”。用户必须在 1 秒内感到“我已经进房间了”，而不是“我还在站点里浏览另一个页面”。
- 禁止整页滚动承载聊天主流程。默认应让消息时间线自己滚动，外层页面尽量稳定，不把浏览器网页滚动当主交互。

## 2. 导航感禁令

- 禁止把“进房间”设计成传统网页跳转体验。不能每次都像打开一个新详情页。
- 禁止把“房间 / 查看器 / 成员面板 / 媒体详情”全做成浏览器级新页面来回跳。优先用应用内层级、抽屉、侧板、覆盖层解决。
- 禁止让浏览器后退承担主导航职责。应用自己必须给出明确、稳定、可预期的返回路径。
- 禁止每次切房、重连、恢复、打开媒体时都整屏白掉再重画一次。应用壳必须尽量连续，内容切换而不是世界重建。
- 禁止顶部出现典型网站导航栏：Logo、导航菜单、帮助、关于、定价、文档、页脚等，这些都会立刻把人拉回“网页站点”心智。
- 禁止面包屑导航。IM 不是后台 CMS，也不是文档站。

## 3. 主舞台禁令

- 禁止房间主舞台被多个同权重区块平分。进入房间后，视觉第一焦点必须是消息时间线，而不是欢迎语、说明卡、公告卡、功能卡。
- 禁止消息区宽度像文章正文区。聊天阅读是扫视，不是长文阅读，气泡、头像、时间、未读线必须服务快速定位。
- 禁止把消息列表做成博客流、资讯流、瀑布流、卡片 feed。聊天时间线必须像时间线，不像内容推荐页。
- 禁止让消息、系统提示、未读分隔、跳到最新、发送中、失败态混成一团没有层次。
- 禁止未读和最新位置没有空间锚点。用户必须始终知道自己是在“最新处”“未读上方”还是“历史中段”。
- 禁止消息主舞台为了“设计感”牺牲密度，导致一屏只能看很少几条消息。

## 4. 输入区禁令

- 禁止把输入区做成普通网页表单。不能出现明显的“标题 + 输入框 + 提交按钮”表单既视感。
- 禁止输入区脱离主舞台漂在别处。它应稳定锚在用户视线下沿，像 IM 的操作控制台，而不是页面某个模块。
- 禁止把发送按钮做成唯一主通路。键盘发送、连续发言、焦点回留都必须优先考虑。
- 禁止附件、表情、补充操作分散成多个远离输入区的网页按钮区。
- 禁止输入区状态反馈过轻。上传中、失败、阻塞、可发送、草稿保留，必须在输入区附近直接感知。
- 禁止为了“干净”把输入区做得像空无一物，结果一到真实高频使用就显得脆弱、慢、没抓手。

## 5. 时间线交互禁令

- 禁止把“跳到最新”藏成二级交互。离底浏览时，回到底部必须足够直接。
- 禁止未读分隔条做得像普通分组标题，没有提醒力。
- 禁止发送中的本地消息、失败重试、上传中的媒体只靠文案提示，不给明确视觉态。
- 禁止消息 hover 才出现关键操作，导致移动端和触屏端天然残废。
- 禁止系统消息和用户消息几乎长一样。系统态应该被一眼识别，但又不能喧宾夺主。
- 禁止头像、昵称、时间、连续消息关系没有节奏，导致时间线看起来像一堆孤立卡片。

## 6. 媒体体验禁令

- 禁止让时间线里的媒体一上来就像短视频站首页，自动播一片、动效一片、注意力被整列拉走。
- 禁止正式查看器像“又打开了一个网页页面”。它应该像进入应用内的沉浸式子空间。
- 禁止媒体打开后丢失房间上下文。用户要明确知道自己仍在当前房间，只是进入了更深一层查看。
- 禁止预览态和正式态不分。时间线默认轻量，正式播放、重型加载、细节控制应收口到查看器。
- 禁止媒体卡片尺寸、比例、信息层级完全不受控，导致时间线节奏被打碎。

## 7. 系统状态禁令

- 禁止把离线、重连、同步中、恢复中、更新待接管这些关键状态只做成转瞬即逝的 toast。
- 禁止系统状态没有固定位置。用户必须形成条件反射，知道去哪里看“现在是不是正常活着”。
- 禁止错误一发生就把用户踢回首页或清空房间上下文。聊天产品首先要保留现场，再谈报错。
- 禁止恢复态只说技术术语，不说用户接下来该怎么理解当前状态。
- 禁止通知、badge、多端焦点、后台补发这些应用级信号完全隐身，做得像网页什么都没发生。

## 8. 视觉语言禁令

- 禁止使用典型 SaaS 网页审美：浅色大留白、漂浮卡片、渐变 Hero、大按钮、大标题、功能九宫格。
- 禁止套用“企业官网 + 控制台”混合风格。IM 是高频使用工具，不是展示站，也不是后台报表。
- 禁止为了“高级感”把对比度、层级、触感都磨平。聊天应用需要明确的可操作性，不需要网页式端着。
- 禁止所有模块长得一样重。房间标题、时间线、输入区、系统态、次级面板必须有明确主次。
- 禁止按钮、输入框、列表项完全沿用浏览器默认控件气质，不做应用级触感设计。
- 禁止文案口吻像网站 CTA：`立即开始`、`了解更多`、`点击查看详情`。聊天应用文案应该更短、更现场、更像操作语言。

## 9. 浏览器味禁令

- 禁止让用户频繁感到“我在刷新网页”。包括整页闪烁、布局跳变、滚动位置丢失、焦点丢失、输入内容丢失。
- 禁止依赖新标签页承接主流程。新标签只能是例外，不该是群聊使用常态。
- 禁止让地址栏变化、浏览器前进后退、整页滚动成为房间操作的主要反馈。
- 禁止把“安装成 PWA 后才像应用”当借口。即便仍在浏览器标签页里，也必须先做出应用感。

## 10. 空态、加载、错误禁令

- 禁止空态像教育页。空态的职责是把人送进会话，不是讲产品故事。
- 禁止加载只会转菊花。恢复中、入房中、补历史、重连中，各自要有贴近语义的位置和反馈。
- 禁止错误态只有红字，没有后续动作。至少要告诉用户现在还能不能继续聊、要不要重试、现场有没有保住。
- 禁止每个阶段都重新发明一套空态文案和布局。应用应有统一的状态语言。

## 11. 评审门禁

以后任何新 UI 提案，只要命中下面任一条，就默认回炉重做：

- 看起来像官网、SaaS 控制台、内容详情页，而不是聊天应用。
- 进入房间像跳新网页，而不是进入持续存在的会话空间。
- 用户必须依赖浏览器后退、刷新、滚动整页才能继续主流程。
- 消息时间线不是绝对主舞台。
- 输入区不是稳定主操作区。
- 未读、重连、离线、上传、失败、恢复没有固定可见位置。
- 打开媒体像离开聊天应用去另一个网页。
- 一旦发生加载或错误，现场感立刻断掉。

评审时必须先回答这五个问题：

1. 用户第一眼看到的主舞台到底是什么？
2. 用户在房间里最顺手的主操作到底是什么？
3. 用户如何在不离开现场的前提下切房、看媒体、回到底部、继续输入？
4. 用户在哪里持续感知未读、重连、离线、上传、恢复这些关键状态？
5. 这个设计如果去掉浏览器地址栏和标签栏，看起来还像不像一个独立 IM 应用？

只要第五问答不出“像”，就说明网页味还没清掉。
