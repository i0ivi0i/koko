# 备忘

日期：2026-04-06  
状态：记录完成，暂缓执行

## 当前优先级

1. 先完成并稳定“设备级花名匿名身份最小群聊 MVP”主链。
2. 以下安全加固项先记账，不在当前轮次立即实现。

## 下一步待办（延后）

### 1. 设备入口凭证轮换与失效

- 增加“主动轮换 `device_anonymous_token`”能力。
- 增加“吊销旧设备入口凭证”能力，防止长期泄露后持续可用。
- 轮换后需保证：
  - 匿名内部身份不变；
  - 历史消息归属不变；
  - 当前会话可平滑续接。

### 2. bootstrap 接口限流与异常告警

- 对匿名身份 bootstrap 增加基础限流（按 IP / 设备入口凭证维度）。
- 记录并告警异常模式：
  - 高频失败；
  - 高频新建设备入口；
  - 异常分布式探测行为。
- 日志字段保持结构化，至少包括：
  - `usecase`
  - `adapter`
  - `error_code`
  - `device_token_hash`（避免明文）
  - `session_id`（若有）

### 3. 传输层安全基线

- 生产环境强制 HTTPS / WSS。
- 明确 `cookie / localStorage / token` 取舍边界（当前 MVP 使用本地设备入口凭证）。
- 在文档中补“匿名入口凭证是低信任入口”的说明，为未来注册系统对齐权限模型。

## 执行门槛（开始做这些项之前）

1. 当前 MVP 回归测试持续全绿。
2. 先补对应失败测试，再动实现（TDD）。
3. 任何改动不得破坏：
  - 单一身份真相边界；
  - 单一 `event_position` 同步锚点；
  - 既有 realtime 主链语义。
