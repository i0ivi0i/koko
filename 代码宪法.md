# 代码宪法

## 1. 禁止条款

这部分不是价值宣言，而是审查时可直接判定违规的禁令。

总禁令只有一条：

**禁止任何会导致“真相漂移、边界漂移、契约漂移、日志失明、排障失焦”的实现方式进入主干。**

### 1.1 A类：架构禁令

以下行为一旦出现，默认视为架构退化：

1. 禁止前端承载业务真相  
   前端不得裁决成员资格、权限真相、消息成立、历史完整性或治理语义。

2. 禁止 adapter 承担业务规则  
   handler、socket 回调、repo、数据库映射层、后台查询层不得偷偷裁决谁能发、谁能看、谁能订阅、消息是否成立。

3. 禁止绕开用例主链直写真相  
   任何领域真相都不得绕开 `用例 -> 领域 -> 权威持久化` 主链直接落库、直接广播、直接写壳状态。

4. 禁止共享契约混入壳层语义  
   `contract` 中不得出现页面步骤、UI 文案、布局字段、壳层状态枚举、仅某壳可见的展示模型。

5. 禁止把控制面、失败通道和领域事件重新混为一谈  
   transport ack、订阅成功、补洞指令、命令拒绝都不得伪装成领域事件。

6. 禁止新增第二套命令入口、第二套事件语义、第二套同步锚点  
   任何为了“先跑通”的旁路接口、旁路事件、旁路位置编号，默认都是违规。

7. 禁止把连接状态、socket room、页面连通感当业务事实  
   “连上了”“进了 room”“看起来在线”都不得反向定义成“已加入房间”或“消息已成立”。

8. 禁止模块之间靠隐式状态或内部默契协作  
   模块不得依赖全局隐式缓存、隐式单例、隐式上下文、调用顺序默契来保持正确性。

9. 禁止把某一壳的特判数据形状倒灌进核心  
   Web、CLI、移动端都只能消费稳定契约，不允许为某个壳单独改写核心数据形状。

10. 禁止手搓成熟轮子的第二层真相  
    可做薄适配，不可在 `socketioxide`、数据库、日志系统外再包一层没有明确边界价值的私有核心。

11. 禁止黑箱 helper 侵蚀边界  
    职责说不清的 `helper / util / manager / bridge / wrapper` 不得承载跨层核心逻辑。

12. 禁止让系统依赖“默契”运转  
    系统必须依赖契约、字段、类型和显式边界运转，而不是依赖某位维护者脑中的约定。

13. 禁止新增私有信息形状  
    跨层事实不得一进某层就被压成当前壳、当前 adapter 或当前页面专用的私有对象。

14. 禁止核心命名绑定外层协议  
    领域名、用例名、事件名、错误名不得把 `http_`、`ws_`、`web_`、`socket_` 这类绑定写死进核心语义。

15. 禁止增加机制种类  
    能靠现有 `命令 / 命令结果 / 查询 / 快照 / 领域事件 / 控制面结果 / 错误码` 表达的能力，不得再发明平行机制。

16. 禁止局部最优破坏公共交换形态  
    不得为了某一页面、某一 adapter、某一临时性能点，牺牲共享契约的稳定性、可观察性、可回放性或可补洞性。

17. 禁止把修枝当成可选项  
    已识别的手搓桥接、重复协议胶水、壳层假状态、低价值包装和第二套真相，必须优先收口或删除，不能长期挂起。

18. 禁止证据不足就宣布成功  
    未形成与改动相匹配的新鲜证据前，不得宣布链路已绿、功能已稳或 bug 已修完。

19. 禁止新功能跳过设计四问  
    若不能回答“权威真相在哪层、稳定契约是什么、同步锚点是什么、断线/重试/重进如何补洞去重”，则不得进入实现。

20. 禁止把近似真相当真相  
    room、connection、adapter state、transport ack、页面连通感都不得被当作业务事实的近似替代。

### 1.2 C类：工程禁令

以下行为一旦出现，默认视为工程纪律失守：

1. 禁止编写代码时不带有价值的详细中文注释  
   关键模块、关键数据流、复杂分支、边界约束和设计原因必须有中文注释说明。

2. 禁止用注释重复表面动作  
   注释不能只写“赋值了什么”“进入 if 了什么”，必须解释职责、边界和为什么这样做。

3. 禁止做不到“代码即文档”  
   命名、模块边界、函数职责必须先自解释，不能指望靠 README 或口头传承才能理解代码。

4. 禁止项目启动后默认没有日志  
   开发环境和生产环境都必须天然有日志输出，不能依赖手动开关后才可观测。

5. 禁止关键链路没有结构化日志字段  
   核心链路若缺失 `request_id / session_id / room_id / client_message_id / event_position / usecase / adapter / error_code` 等主字段，默认不合格。

6. 禁止静默失败  
   房间打开失败、快照失败、补洞失败、消息发送失败不得被背景成功或 UI 乐观态掩盖。

7. 禁止没有验证就合入  
   未经过门禁验证的修改不得进入主干；不能靠“这次应该没问题”结束任务。

8. 禁止修 bug 不补失败测试  
   同类 bug 若没有沉淀成失败测试、根因说明和防回归门禁，就不算真正修完。

9. 禁止复制粘贴横向扩展同类业务语义  
   同一业务规则、同一错误语义、同一同步语义不得在多处复制实现。

10. 禁止巨函数、巨模块、跨层巨流程长期堆积  
    当一个函数或模块开始同时承担多层职责时，必须优先拆边界，而不是继续往里塞。

11. 禁止把“以后再整理”当借口保留屎山式临时代码  
    临时桥接、临时字段、临时状态、临时特判若没有明确清理计划，默认不允许进入主干。

12. 禁止新增无法整体删除的功能  
    新功能必须能说清自己的命令入口、事件出口、快照归宿和壳层表现，否则后续无法优雅移除。

13. 禁止输入输出接口漂移  
    同一命令、查询、事件、控制面结果不得随页面需要随意改形状、改语义、改字段稳定性。

14. 禁止让排障只能靠猜  
    如果一条链路改完后仍不能靠日志和测试顺主链追根因，这条链路就还不合格。

### 1.3 C类：可读性禁令

以下行为一旦出现，默认视为“代码即文档”原则退化：

1. 禁止命名失真  
   禁止使用 `data`、`info`、`temp`、`misc`、`manager`、`helper` 这类无法表达职责的名字承载核心语义。

2. 禁止函数名只描述技术动作、不描述业务事实  
   名称必须让维护者看出“这段代码在业务上到底做了什么”，而不是只看出“它调用了某个 API”。

3. 禁止同一业务概念在不同层反复改名  
   同一事实必须尽量沿主链保持统一关键词，避免搜索路径断裂。

4. 禁止同名对象在不同层表达不同语义  
   如果一个名字在不同层里代表不同真相，会直接制造理解错位和排障歧义。

5. 禁止模块职责不写在模块头部中文注释里  
   关键模块必须用简洁中文说明“做什么、不做什么、边界在哪里”。

6. 禁止关键不变量和边界约束只存在于人脑  
   只靠聊天记录、口头交接或某位维护者记忆保留规则，默认视为违规。

7. 禁止阅读主链像迷宫  
   一个功能若必须跳过过多碎片文件和无意义跳转才能看懂主流程，说明结构已经退化。

8. 禁止为了“复用”把主链切成过细碎片  
   主链应当清楚、连贯、可读，不能被过度抽象和碎片化拆分破坏阅读体验。

9. 禁止注释与代码脱节  
   代码改了而注释不改，或注释描述的是理想设计、代码却走另一套逻辑，默认视为违规。

10. 禁止长篇注释掩盖坏代码  
    注释用于解释职责、边界和原因，不用于替混乱结构打补丁。

11. 禁止关键能力不可搜索  
    用例名、事件名、错误码名、契约名必须稳定，不能让维护者搜不到代码归宿。

12. 禁止新增功能时不同步补齐命名、中文注释与边界说明  
    新功能若缺失这三样，不得视为达到“代码即文档”要求。

### 1.4 C类：日志禁令

以下行为一旦出现，默认视为项目开始丧失天然可观测性：

1. 禁止新增命令入口不接日志  
   新命令进入主链时，必须同时进入结构化日志主链。

2. 禁止新增查询、事件、补洞或恢复链路不接日志  
   任何新链路只要会影响用户真相或系统状态，都必须天然可观察。

3. 禁止日志字段漂移  
   同一概念不得今天叫 `room_id`、明天叫 `rid`；主字段名必须长期稳定。

4. 禁止关键日志缺少主链字段  
   核心日志若缺 `request_id / session_id / room_id / client_message_id / event_position / usecase / adapter / error_code` 中的必要字段，默认不合格。

5. 禁止只在失败时临时补日志  
   日志必须是天然基础设施，不能等出事了再加 `println!` 式探针。

6. 禁止只有 adapter 日志，没有用例与领域日志  
   否则问题会停留在“包进来了/包出去了”，无法看到真相是如何成立或失败的。

7. 禁止日志不能串出一次完整主链  
   同一条消息、同一次补洞、同一次快照重拉，必须能靠字段把证据串起来。

8. 禁止日志只有模糊文本没有结构化字段  
   “处理失败了”“执行异常”这类日志若没有上下文字段，对排障价值极低。

9. 禁止高频热路径日志完全无级别策略  
   日志不能失明，也不能退化成噪音海；必须能在保留证据的同时控制可读性。

10. 禁止生产环境为了安静直接关掉关键日志  
    生产环境允许降噪，不允许失去主链证据。

11. 禁止功能新增时不补主链日志与失败日志  
    每次增删改都必须同步补齐正常链路与失败链路的结构化日志。

12. 禁止日志体系依赖人工临时约定  
    日志字段、级别和关键事件命名必须稳定，不能靠个人习惯随手发挥。

### 1.5 C类：编码习惯禁令

以下行为一旦出现，默认视为代码债或技术债正在被主动制造：

1. 禁止一边写业务一边顺手改边界  
   修一个点时不得顺手把 `adapter` 变业务层、把前端变真相层、把 repo 变规则层。

2. 禁止把“临时方案”写成长期结构  
   临时字段、临时分支、临时桥接、临时兼容若没有明确退场机制，不得进入主干。

3. 禁止一个函数同时做裁决、存储、广播、展示准备  
   单个函数不得一锅炖完整主链，否则后续演进和排障都会迅速失控。

4. 禁止把大而泛的上下文对象到处传  
   不得用模糊的大 `Context / State / Deps` 掩盖真实依赖关系。

5. 禁止核心行为由布尔开关和分支泥团驱动  
   多个布尔参数、越来越长的 if-else 和复制旧分支改条件，都是长期复杂度炸弹。

6. 禁止错误处理统一到失真  
   错误可以统一编码，不得统一成看不出层级、原因和上下文的一团模糊失败。

7. 禁止引入依赖却说不清边界价值  
   每个新 crate 都必须能说明：替代了什么复杂度、减少了什么债务、守住了什么边界。

8. 禁止热路径和冷路径混写  
   后台查询式写法、低频恢复式写法、方便调试的慢写法，不得直接污染高频主链。

9. 禁止“先复制一份再改”  
   复制旧 handler、旧用例、旧事件、旧状态机来扩展功能，默认视为债务繁殖。

10. 禁止写完代码不做结构自审  
    提交前必须自问：这段属于哪层、是否制造第二套真相、日志和测试是否补齐、阅读路径是否更浑浊。

11. 禁止参数和返回值含糊  
    不得用模糊 `bool`、神秘字符串、大杂烩对象或临时 JSON 形状承载核心业务结果。

12. 禁止为了“统一”引入低价值包装层  
    若包装层只是在转发、改名、隐藏官方能力，却没有明确边界收益，默认不允许存在。

### 1.6 C类：性能与并发编码禁令

以下行为一旦出现，默认视为性能债、并发债或长期运行风险正在被主动引入：

1. 禁止在热路径跨 `await` 长持锁  
   `Mutex / RwLock` 只允许保护短临界区，不得拿着业务锁等待数据库、网络、广播或补洞 IO。

2. 禁止无界增长进入主链  
   无界队列、无界缓存、无界积压、无界 fanout、无界重试不得进入高频实时路径。

3. 禁止把外部 IO 包进数据库事务  
   事务只提交权威真相，不得把网络请求、广播、长耗时异步操作包进同一事务。

4. 禁止 N+1 查询进入聊天主链  
   不得按消息逐条反查发送者、按事件逐条补查、按房间逐条拼详情。

5. 禁止热路径反复序列化、反复拷贝、反复变形对象  
   同一条消息不得在多个 adapter 间多次无意义 `clone`、多次 JSON 转换、重复构造近似相同载荷。

6. 禁止 spawn 无归属异步任务  
   每个异步任务都必须知道谁创建、何时取消、失败怎么暴露、关闭时如何回收。

7. 禁止异步调用没有超时和取消语义  
   补洞、恢复、查询、外部 IO 若没有超时或取消机制，默认不允许进入长期主链。

8. 禁止热路径和冷路径共享“方便复用”的慢实现  
   后台查询式写法、恢复链路写法、调试期慢写法不得直接复用到高频消息主链。

9. 禁止性能判断没有基线和证据  
   不得用“应该够快”“以后压测再看”“感觉问题不大”作为性能决策依据。

10. 禁止优化以牺牲边界和可替换性为代价  
    不得为了局部性能把领域语义塞进 adapter、把契约压扁、把日志删掉、把测试绕过。

11. 禁止恢复链路写成分支泥团  
    补洞、重试、重连、重拉快照必须有单一路径、稳定结果类型和统一日志字段，不能散在多个补丁式 if 分支里。

12. 禁止每次迭代都默认吞一点性能债  
    新功能必须说明新增了哪些查询、锁、拷贝、序列化、积压点或尾延迟风险，不能把性能成本悄悄滚大。

### 1.7 C类：死代码与冗余代码治理禁令

以下行为一旦出现，默认视为代码基线开始腐化：

1. 禁止保留无调用路径的生产代码  
   任何已无调用者、无入口注册、无契约引用的代码不得长期留在主干。

2. 禁止用注释块保留旧实现  
   `old code`、整段注释掉的历史逻辑必须删除，历史由 Git 提供。

3. 禁止 `TODO / FIXME` 无归属  
   没有责任人、截止时间、跟踪编号的 `TODO / FIXME` 不得进入主干。

4. 禁止临时兼容分支无退场条件  
   兼容分支必须写清触发条件、退场条件和最晚删除时间。

5. 禁止同一业务语义长期并存第二套 DTO / mapper / translator  
   同义结构若超过一个迭代窗口仍并存，默认视为冗余违规。

6. 禁止同一查询语义长期并存多份 SQL 实现  
   若确需并存，必须明确“谁是主实现、谁是过渡实现、何时删除”。

7. 禁止新增职责不清的 `util / helper / manager` 黑箱模块  
   不能一句话说明“做什么、不做什么、边界在哪”，就不得存在。

8. 禁止公开 API / 导出函数长期无消费者  
   对外暴露能力若无消费方，必须删掉或收回到私有范围。

9. 禁止 Feature Flag 永久化  
   每个 Flag 必须绑定清理时间点与删除任务，过期未删视为违规。

10. 禁止调试脚本、迁移脚本、一次性脚本长期悬挂  
    无执行入口、无维护人、无生命周期说明的脚本不得常驻仓库。

11. 禁止测试夹具与测试数据无人引用却长期保留  
    无测试引用的 fixture、样本与 mock 必须删除。

12. 禁止“可能以后用”成为保留冗余实现的理由  
    预留应通过清晰扩展点实现，不得靠保留废弃逻辑实现。

13. 禁止纯重构长期只加不删  
    若连续重构只叠加代码体积且不回收旧实现，默认视为技术债扩散。

14. 禁止引入新依赖后保留等价旧依赖或旧封装  
    新旧并存必须有明确迁移窗口，窗口到期必须删旧。

15. 禁止历史错误码、历史事件类型无退场计划长期保留  
    兼容保留可以存在，但必须有版本与淘汰节奏。

16. 禁止前端保留未挂载组件、未引用状态和未使用样式  
    页面不可达、状态不可读、样式不可用即视为前端死代码。

17. 禁止后端保留未注册路由、未绑定 handler、未接入端口实现  
    无入口链路的后端能力不得停留在主干中。

18. 禁止死代码清理任务无限延期  
    已识别死代码若跨两个迭代仍未清理，必须升级为阻塞项处理。

### 1.8 C类：死代码与冗余清理强制门禁

以下门禁用于把“清理”从口号变成可执行约束：

1. 禁止关闭 `clippy -D warnings` 中的未使用与死代码告警  
   Rust 主干必须保持对 `unused / dead_code` 类问题零容忍。

2. 禁止缺失周期性依赖冗余审计  
   必须定期执行 `cargo udeps`（或等价工具）并清理无用依赖。

3. 禁止 TypeScript 关闭未使用检查  
   必须保持 `noUnusedLocals / noUnusedParameters` 为启用状态并纳入门禁。

4. 禁止 PR 省略“本次清理了什么冗余”的自审说明  
   若未清理任何冗余，必须明确写出原因与后续清理计划。

### 1.9 C类：注释同步更新强制禁令

以下禁令用于防止“代码持续演进，注释逐步失真”：

1. 禁止改代码不改注释  
   任何涉及语义变化、边界变化、字段变化、错误语义变化的改动，必须同步更新对应中文注释。

2. 禁止新增公开能力无中文注释  
   新增 `pub struct / pub enum / pub fn`、跨层接口、契约类型时，必须补齐职责与边界注释。

3. 禁止删除或重命名后保留历史注释残影  
   字段、函数、错误码、事件名删除或改名后，旧术语注释必须一并清理或替换。

4. 禁止注释与代码分支语义不一致  
   注释写“先校验成员再写事件”，代码却反序执行，视为违规。

5. 禁止把“后续再补注释”作为合入理由  
   注释同步属于完成定义的一部分，不允许延期到后续任务。

6. 禁止复杂主链缺少边界注释  
   `入口 / 用例 / 适配 / 外壳 / 领域` 的关键分支、事务链、错误映射必须有中文边界说明。

7. 禁止新增契约语义不补注释  
   新增命令、查询、快照、领域事件、控制面结果、错误码时，必须同时补齐语义注释。

8. 禁止注释只复述语法动作  
   注释必须解释“职责、边界、原因”，禁止仅写“给变量赋值”“进入 if”这类低价值描述。

9. 禁止评审跳过注释一致性检查  
   Code Review 必须显式检查“注释是否跟代码一致”，不能只看编译与测试通过。

10. 禁止出现注释漂移但继续推进后续功能  
    一旦识别注释与代码失配，必须先修正注释，再继续叠加新功能。

11. 禁止主干缺失注释同步门禁  
    提交流程必须包含“注释同步”检查，缺失该门禁视为流程违规。

12. 禁止以“大量注释”掩盖错误代码  
    注释不能替代正确实现；若代码行为错误，优先修代码，再修注释。

### 1.10 C类：注释同步工程门禁

以下门禁用于把注释同步从“倡议”变成“阻断条件”：

1. 禁止 PR 缺失“注释同步说明”  
   PR 模板必须包含“本次改动同步更新了哪些中文注释；若无，为什么无语义变化”。

2. 禁止结构性改动绕过注释校验  
   若 `.rs` 文件发生函数签名、契约字段、错误码、事件结构变化，必须检测到注释同步变更。

3. 禁止 CI 对注释漂移放行  
   CI 必须具备注释漂移检查（至少覆盖关键模块），发现失配直接失败。

4. 禁止评审记录没有注释一致性结论  
   Reviewer 结论中必须包含“注释一致/不一致”的明确判断。

5. 禁止“纯格式化提交”伪装语义改动  
   仅格式化提交可豁免注释同步，但必须在提交说明或 PR 中明确标注“无语义变化”。

