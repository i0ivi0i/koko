# 代码宪法

## 主题

1. 领域主权：禁止业务真相漂移与分层越界
2. Unix 智慧：禁止巨石化、隐式化、黑箱化
3. 工程可证：禁止无日志、无注释、无验证
4. 复用优先：禁止手搓成熟轮子与重复造轮子
5. 长期维护：禁止冗余累积、死代码滞留、规则失效

## 大纲

1. 总则禁令
2. 第一编 架构禁令
3. 第二编 工程禁令
4. 第三编 可读性禁令
5. 第四编 日志禁令
6. 第五编 编码习惯禁令
7. 第六编 性能与并发编码禁令
8. 第七编 死代码与冗余代码治理禁令
9. 第八编 死代码与冗余清理强制门禁禁令
10. 第九编 注释同步更新强制禁令
11. 第十编 注释同步工程门禁禁令
12. 第十一编 新增禁令纳管禁令
13. 第十二编 术语与阈值禁令
14. 第十三编 证据与举证禁令
15. 第十四编 条款冲突裁决禁令
16. 第十五编 应急临时处置禁令
17. 第十六编 违规状态分级与处置禁令

## 分编主题分类索引（禁止分类漂移与扩展失序）

1. `A` 架构边界类：真相归位、分层边界、契约纯度、反向依赖、解耦完整性
2. `E` 工程执行类：测试、验证、交付纪律、流程完整性
3. `R` 可读性类：命名、可搜索性、代码即文档、注释一致性
4. `L` 可观测性类：日志天然存在、日志字段、日志链路完整性
5. `C` 编码习惯类：抽象克制、重复控制、参数/返回值语义清晰
6. `P` 性能并发类：热路径、锁策略、异步边界、事务边界、基线证据
7. `D` 冗余治理类：死代码、废弃路径、历史兼容退场、依赖清理
8. `G` 门禁治理类：PR/CI/评审/发布门禁
9. `N` 注释同步类：中文注释质量、注释漂移治理、注释门禁
10. `M` 纳管机制类：新增禁令录入、分类落位、编号秩序、冲突收敛
11. `T` 术语阈值类：术语边界、阈值口径、命名裁切、数值门槛、别名控制
12. `V` 证据举证类：证据链、取证材料、举证责任、审查留痕、反证要求
13. `X` 条款裁决类：冲突识别、优先级、例外顺序、解释规则、裁决留痕
14. `H` 应急处置类：临时冻结、回退、隔离、降级、恢复条件
15. `S` 状态分级类：代码状态、交付状态、门禁状态、阻断等级、恢复门槛

## 总则禁令

禁止把这部分写成价值宣言，禁止把这部分写成离开审查就无法判定的空话。

总禁令只有一条：

**禁止任何会导致“真相漂移、边界漂移、契约漂移、日志失明、排障失焦”的实现方式进入主干。**

### 第一编 架构禁令（原 1.1 A类）

以下条款禁止架构退化：

1. 禁止前端承载业务真相  
    前端不得裁决成员资格、权限真相、消息成立、历史完整性或治理语义。

2. 禁止 adapter 承担业务规则  
    handler、socket 回调、repo、数据库映射层、后台查询层不得偷偷裁决谁能发、谁能看、谁能订阅、消息是否成立。

3. 禁止绕开用例主链直写真相  
    任何领域真相都不得绕开 `用例 -> 领域 -> 权威持久化` 主链直接落库、直接广播、直接写壳状态。

4. 禁止共享契约混入壳层语义  
    `contract` 中不得出现页面步骤、UI 文案、布局字段、壳层状态枚举、仅某壳可见的展示模型。

5. 禁止把控制面、失败通道和领域事件重新混为一谈  
    transport ack、订阅成功、补洞指令、命令拒绝都不得伪装成领域事件。

6. 禁止新增第二套命令入口、第二套事件语义、第二套同步锚点  
    任何为了“先跑通”的旁路接口、旁路事件、旁路位置编号，默认都是违规。

7. 禁止把连接状态、socket room、页面连通感当业务事实  
    “连上了”“进了 room”“看起来在线”都不得反向定义成“已加入房间”或“消息已成立”。

8. 禁止模块之间靠隐式状态或内部默契协作  
    模块不得依赖全局隐式缓存、隐式单例、隐式上下文、调用顺序默契来保持正确性。

9. 禁止把某一壳的特判数据形状倒灌进核心  
    Web、CLI、移动端都只能消费稳定契约，不得为某个壳单独改写核心数据形状。

10. 禁止手搓成熟轮子的第二层真相  
    不得以“薄适配”为名，在 `socketioxide`、数据库、日志系统外再包一层没有明确边界价值的私有核心。

11. 禁止黑箱 helper 侵蚀边界  
    职责说不清的 `helper / util / manager / bridge / wrapper` 不得承载跨层核心逻辑。

12. 禁止让系统依赖“默契”运转  
    禁止系统依赖契约、字段、类型和显式边界以外的运转方式，不得依赖某位维护者脑中的约定。

13. 禁止新增私有信息形状  
    跨层事实不得一进某层就被压成当前壳、当前 adapter 或当前页面专用的私有对象。

14. 禁止核心命名绑定外层协议  
    领域名、用例名、事件名、错误名不得把 `http_`、`ws_`、`web_`、`socket_` 这类绑定写死进核心语义。

15. 禁止增加机制种类  
    在既有 `命令 / 命令结果 / 查询 / 快照 / 领域事件 / 控制面结果 / 错误码` 仍能保持边界清晰、证据完整和语义稳定时，不得再发明平行机制。

16. 禁止局部最优破坏公共交换形态  
    不得为了某一页面、某一 adapter、某一临时性能点，牺牲共享契约的稳定性、可观察性、可回放性或可补洞性。

17. 禁止把修枝当成可选项  
    禁止已识别的手搓桥接、重复协议胶水、壳层假状态、低价值包装和第二套真相长期挂起；禁止绕开收口或删除。

18. 禁止证据不足就宣布成功  
    未形成与改动相匹配的新鲜证据前，不得宣布链路已绿、功能已稳或 bug 已修完。

19. 禁止新功能跳过设计四问  
    若不能回答“权威真相在哪层、稳定契约是什么、同步锚点是什么、断线/重试/重进如何补洞去重”，则不得进入实现。

20. 禁止把近似真相当真相  
    room、connection、adapter state、transport ack、页面连通感都不得被当作业务事实的近似替代。

21. 禁止破坏性契约变更无兼容约束  
    禁止共享契约发生破坏性变更时缺失兼容窗口、淘汰时点、跨壳回归验证或调用方影响说明。

### 第二编 工程禁令（原 1.2）

以下条款禁止工程纪律失守：

1. 禁止编写代码时不带有价值的详细中文注释  
    禁止会让维护者靠猜测才能理解的关键模块、关键数据流、复杂分支、边界约束和设计原因缺失中文注释说明。

2. 禁止用注释重复表面动作  
    禁止注释只写“赋值了什么”“进入 if 了什么”，不得缺失职责、边界和为什么这样做的说明。

3. 禁止做不到“代码即文档”  
    禁止命名、模块边界、函数职责失去自解释能力，不能指望靠 README 或口头传承才能理解代码。

4. 禁止项目启动后默认没有日志  
    禁止开发环境和生产环境缺失天然日志输出，不能依赖手动开关后才可观测。

5. 禁止关键链路没有结构化日志字段  
   核心链路若缺失 `request_id / session_id / room_id / client_message_id / event_position / usecase / adapter / error_code` 等主字段，默认不合格。

6. 禁止静默失败  
   房间打开失败、快照失败、补洞失败、消息发送失败不得被背景成功或 UI 乐观态掩盖。

7. 禁止没有验证就合入  
   未经过门禁验证的修改不得进入主干；不能靠“这次应该没问题”结束任务。

8. 禁止修 bug 不补失败测试  
   同类 bug 若没有沉淀成失败测试、根因说明和防回归门禁，就不算真正修完。

9. 禁止复制粘贴横向扩展同类业务语义  
   同一业务规则、同一错误语义、同一同步语义不得在多处复制实现。

10. 禁止巨函数、巨模块、跨层巨流程长期堆积  
    禁止一个函数或模块同时承担多层职责并继续往里塞，禁止不先拆边界。

11. 禁止把“以后再整理”当借口保留屎山式临时代码  
    临时桥接、临时字段、临时状态、临时特判若没有明确清理计划，默认不得进入主干。

12. 禁止新增无法整体删除的功能  
    禁止新增功能说不清自己的命令入口、事件出口、快照归宿和壳层表现，否则后续无法优雅移除。

13. 禁止输入输出接口漂移  
    禁止同一命令、查询、事件、控制面结果因页面诉求随意改形状、改语义、改字段稳定性。

14. 禁止让排障只能靠猜  
    如果一条链路改完后仍不能靠日志和测试顺主链追根因，这条链路就还不合格。

15. 禁止配置真相失控  
    禁止关键配置缺失来源优先级、默认值边界、环境差异说明或动态生效范围说明。

16. 禁止数据库迁移与数据回填失序  
    禁止不可审查的 schema 变更、非幂等数据回填、代码与迁移发布顺序失配，禁止缺失回滚或降级说明。

### 第三编 可读性禁令（原 1.3）

以下条款禁止“代码即文档”原则退化：

1. 禁止命名失真  
   禁止使用 `data`、`info`、`temp`、`misc`、`manager`、`helper` 这类无法表达职责的名字承载核心语义。

2. 禁止函数名只描述技术动作、不描述业务事实  
    禁止名称让维护者看不出“这段代码在业务上到底做了什么”，也不能只看出“它调用了某个 API”。

3. 禁止同一业务概念在不同层反复改名  
    禁止同一事实沿主链混用多组关键词，避免搜索路径断裂。

4. 禁止同名对象在不同层表达不同语义  
   如果一个名字在不同层里代表不同真相，会直接制造理解错位和排障歧义。

5. 禁止模块职责不写在模块头部中文注释里  
    禁止会让维护者靠猜测才能理解的关键模块缺失简洁中文说明“做什么、不做什么、边界在哪里”。

6. 禁止关键不变量和边界约束只存在于人脑  
   只靠聊天记录、口头交接或某位维护者记忆保留规则，默认视为违规。

7. 禁止阅读主链像迷宫  
    禁止一个功能跳过过多碎片文件和无意义跳转才能看懂主流程，因为这说明结构已经退化。

8. 禁止为了“复用”把主链切成过细碎片  
    禁止主链不清楚、不连贯、不易读，禁止被过度抽象和碎片化拆分破坏阅读体验。

9. 禁止注释与代码脱节  
   代码改了而注释不改，或注释描述的是理想设计、代码却走另一套逻辑，默认视为违规。

10. 禁止长篇注释掩盖坏代码  
    注释用于解释职责、边界和原因，不用于替混乱结构打补丁。

11. 禁止关键能力不可搜索  
    禁止用例名、事件名、错误码名、契约名不稳定，不能让维护者搜不到代码归宿。

12. 禁止新增功能时不同步补齐命名、中文注释与边界说明  
    新功能若缺失这三样，不得视为达到“代码即文档”要求。

### 第四编 日志禁令（原 1.4）

以下条款禁止项目丧失天然可观测性：

1. 禁止新增命令入口不接日志  
    禁止新命令进入主链却不同时进入结构化日志主链。

2. 禁止新增查询、事件、补洞或恢复链路不接日志  
    禁止任何会影响用户真相或系统状态的新链路缺失天然可观察性。

3. 禁止日志字段漂移  
    禁止同一概念今天叫 `room_id`、明天叫 `rid`；禁止主字段名不长期稳定。

4. 禁止关键日志缺少主链字段  
   核心日志若缺 `request_id / session_id / room_id / client_message_id / event_position / usecase / adapter / error_code` 中的必要字段，默认不合格。

5. 禁止只在失败时临时补日志  
    禁止日志等出事了再加 `println!` 式探针。

6. 禁止只有 adapter 日志，没有用例与领域日志  
   否则问题会停留在“包进来了/包出去了”，无法看到真相是如何成立或失败的。

7. 禁止日志不能串出一次完整主链  
    禁止同一条消息、同一次补洞、同一次快照重拉无法靠字段把证据串起来。

8. 禁止日志只有模糊文本没有结构化字段  
   “处理失败了”“执行异常”这类日志若没有上下文字段，对排障价值极低。

9. 禁止高频热路径日志完全无级别策略  
    禁止日志失明，禁止日志退化成噪音海，禁止日志不能在保留证据的同时控制可读性。

10. 禁止生产环境为了安静直接关掉关键日志  
    生产环境不得以降噪为由失去主链证据。

11. 禁止功能新增时不补主链日志与失败日志  
    禁止每次增删改不同时补齐正常链路与失败链路的结构化日志。

12. 禁止日志体系依赖人工临时约定  
    禁止日志字段、级别和关键事件命名不稳定，不能靠个人习惯随手发挥。

13. 禁止异步边界丢链路上下文  
    禁止跨 `async / spawn / retry / queue` 边界丢失 `request_id / session_id / room_id` 等主链关联字段。

### 第五编 编码习惯禁令（原 1.5）

以下条款禁止代码债或技术债被主动制造：

1. 禁止一边写业务一边顺手改边界  
   修一个点时不得顺手把 `adapter` 变业务层、把前端变真相层、把 repo 变规则层。

2. 禁止把“临时方案”写成长期结构  
   临时字段、临时分支、临时桥接、临时兼容若没有明确退场机制，不得进入主干。

3. 禁止一个函数同时做裁决、存储、广播、展示准备  
   单个函数不得一锅炖完整主链，否则后续演进和排障都会迅速失控。

4. 禁止把大而泛的上下文对象到处传  
   不得用模糊的大 `Context / State / Deps` 掩盖真实依赖关系。

5. 禁止核心行为由布尔开关和分支泥团驱动  
   多个布尔参数、越来越长的 if-else 和复制旧分支改条件，都是长期复杂度炸弹。

6. 禁止错误处理统一到失真  
   不得把错误统一成看不出层级、原因和上下文的一团模糊失败。

7. 禁止引入依赖却说不清边界价值  
    禁止引入新 crate 却说不清替代了什么复杂度、减少了什么债务、守住了什么边界。

8. 禁止热路径和冷路径混写  
   后台查询式写法、低频恢复式写法、方便调试的慢写法，不得直接污染高频主链。

9. 禁止“先复制一份再改”  
   复制旧 handler、旧用例、旧事件、旧状态机来扩展功能，默认视为债务繁殖。

10. 禁止写完代码不做结构自审  
    禁止提交前不审视这段属于哪层、是否制造第二套真相、日志和测试是否补齐、阅读路径是否更浑浊。

11. 禁止参数和返回值含糊  
    不得用模糊 `bool`、神秘字符串、大杂烩对象或临时 JSON 形状承载核心业务结果。

12. 禁止为了“统一”引入低价值包装层  
    若包装层只是在转发、改名、隐藏官方能力，却没有明确边界收益，默认不得存在。

### 第六编 性能与并发编码禁令（原 1.6）

以下条款禁止性能债、并发债或长期运行风险被主动引入：

1. 禁止在热路径跨 `await` 长持锁  
   `Mutex / RwLock` 不得用于跨 `await` 的长临界区，不得拿着业务锁等待数据库、网络、广播或补洞 IO。

2. 禁止无界增长进入主链  
   无界队列、无界缓存、无界积压、无界 fanout、无界重试不得进入高频实时路径。

3. 禁止把外部 IO 包进数据库事务  
   事务只提交权威真相，不得把网络请求、广播、长耗时异步操作包进同一事务。

4. 禁止 N+1 查询进入聊天主链  
   不得按消息逐条反查发送者、按事件逐条补查、按房间逐条拼详情。

5. 禁止热路径反复序列化、反复拷贝、反复变形对象  
   同一条消息不得在多个 adapter 间多次无意义 `clone`、多次 JSON 转换、重复构造近似相同载荷。

6. 禁止 spawn 无归属异步任务  
    禁止每个异步任务不知道谁创建、何时取消、失败怎么暴露、关闭时如何回收。

7. 禁止异步调用没有超时和取消语义  
   补洞、恢复、查询、外部 IO 若没有超时或取消机制，默认不得进入长期主链。

8. 禁止热路径和冷路径共享“方便复用”的慢实现  
   后台查询式写法、恢复链路写法、调试期慢写法不得直接复用到高频消息主链。

9. 禁止性能判断没有基线和证据  
   不得用“应该够快”“以后压测再看”“感觉问题不大”作为性能决策依据。

10. 禁止优化以牺牲边界和可替换性为代价  
    不得为了局部性能把领域语义塞进 adapter、把契约压扁、把日志删掉、把测试绕过。

11. 禁止恢复链路写成分支泥团  
    禁止补洞、重试、重连、重拉快照没有单一路径、稳定结果类型和统一日志字段，不能散在多个补丁式 if 分支里。

12. 禁止每次迭代都默认吞一点性能债  
    禁止新增功能不说明新增了哪些查询、锁、拷贝、序列化、积压点或尾延迟风险，不能把性能成本悄悄滚大。

### 第七编 死代码与冗余代码治理禁令（原 1.7）

以下条款禁止代码基线腐化：

1. 禁止保留无调用路径的生产代码  
   任何已无调用者、无入口注册、无契约引用的代码不得长期留在主干。

2. 禁止用注释块保留旧实现  
    禁止保留 `old code`、整段注释掉的历史逻辑，历史由 Git 提供。

3. 禁止 `TODO / FIXME` 无归属  
   没有责任人、截止时间、跟踪编号的 `TODO / FIXME` 不得进入主干。

4. 禁止临时兼容分支无退场条件  
    禁止兼容分支缺失触发条件、退场条件和最晚删除时间。

5. 禁止同一业务语义长期并存第二套 DTO / mapper / translator  
    同义结构若超过一个迭代窗口仍并存，默认视为冗余违规。

6. 禁止同一查询语义长期并存多份 SQL 实现  
    禁止并存实现不明确“谁是主实现、谁是过渡实现、何时删除”。

7. 禁止新增职责不清的 `util / helper / manager` 黑箱模块  
   不能一句话说明“做什么、不做什么、边界在哪”，就不得存在。

8. 禁止公开 API / 导出函数长期无消费者  
    禁止对外暴露能力在无消费方、无扩展边界说明、无生命周期说明时长期保留。

9. 禁止 Feature Flag 永久化  
    禁止每个 Flag 缺失清理时间点与删除任务，过期未删视为违规。

10. 禁止调试脚本、迁移脚本、一次性脚本长期悬挂  
    无执行入口、无维护人、无生命周期说明的脚本不得常驻仓库。

11. 禁止测试夹具与测试数据无人引用却长期保留  
    禁止保留无测试引用的 fixture、样本与 mock。

12. 禁止“可能以后用”成为保留冗余实现的理由  
    禁止预留靠保留废弃逻辑实现，禁止不通过清晰扩展点实现。

13. 禁止纯重构长期只加不删  
    若连续重构只叠加代码体积且不回收旧实现，默认视为技术债扩散。

14. 禁止引入新依赖后保留等价旧依赖或旧封装  
    禁止新旧并存缺失明确迁移窗口，窗口到期禁止不删旧。

15. 禁止历史错误码、历史事件类型无退场计划长期保留  
    禁止无版本与淘汰节奏的兼容保留。

16. 禁止前端保留未挂载组件、未引用状态和未使用样式  
    页面不可达、状态不可读、样式不可用即视为前端死代码。

17. 禁止后端保留未注册路由、未绑定 handler、未接入端口实现  
    无入口链路的后端能力不得停留在主干中。

18. 禁止死代码清理任务无限延期  
    禁止已识别死代码跨两个迭代仍未清理后继续停留在非阻塞状态；禁止遗漏升级为阻塞项处理。

### 第八编 死代码与冗余清理强制门禁禁令（原 1.8）

以下条款禁止把“清理”停留在口号：

1. 禁止关闭 `clippy -D warnings` 中的未使用与死代码告警  
    禁止 Rust 主干放任 `unused / dead_code` 类问题。

2. 禁止缺失周期性依赖冗余审计  
    禁止缺失定期执行 `cargo udeps`（或等价工具）并清理无用依赖。

3. 禁止 TypeScript 关闭未使用检查  
    禁止关闭 `noUnusedLocals / noUnusedParameters`，禁止不纳入门禁。

4. 禁止 PR 省略“本次清理了什么冗余”的自审说明  
    禁止未清理任何冗余却不写出原因与后续清理计划。

### 第九编 注释同步更新强制禁令（原 1.9）

以下条款禁止代码持续演进时注释逐步失真：

1. 禁止改代码不改注释  
    禁止任何涉及语义变化、边界变化、字段变化、错误语义变化的改动不同时更新对应中文注释。

2. 禁止新增公开能力无中文注释  
    禁止新增 `pub struct / pub enum / pub fn`、跨层接口、契约类型在存在职责、边界或误用歧义时缺失中文注释。

3. 禁止删除或重命名后保留历史注释残影  
    禁止字段、函数、错误码、事件名删除或改名后保留旧术语注释。

4. 禁止注释与代码分支语义不一致  
   注释写“先校验成员再写事件”，代码却反序执行，视为违规。

5. 禁止把“后续再补注释”作为合入理由  
   注释同步属于完成定义的一部分，不得延期到后续任务。

6. 禁止复杂主链缺少边界注释  
    禁止 `入口 / 用例 / 适配 / 外壳 / 领域` 的关键分支、事务链、错误映射缺失中文边界说明。

7. 禁止新增契约语义不补注释  
    禁止新增命令、查询、快照、领域事件、控制面结果、错误码时不同时补齐语义注释。

8. 禁止注释只复述语法动作  
    禁止注释仅写“给变量赋值”“进入 if”这类低价值描述，禁止缺失“职责、边界、原因”的解释。

9. 禁止评审跳过注释一致性检查  
    禁止 Code Review 只看编译与测试通过，禁止不显式检查“注释是否跟代码一致”。

10. 禁止出现注释漂移但继续推进后续功能  
    禁止识别注释与代码失配后先继续叠加新功能。

11. 禁止主干缺失注释同步门禁  
    禁止提交流程缺失“注释同步”检查。

12. 禁止以“大量注释”掩盖错误代码  
    注释不能替代正确实现；若代码行为错误，优先修代码，再修注释。

### 第十编 注释同步工程门禁禁令（原 1.10）

以下条款禁止把注释同步停留在倡议：

1. 禁止 PR 缺失“注释同步说明”  
    禁止 PR 模板缺失“本次改动同步更新了哪些中文注释；若无，为什么无语义变化”。

2. 禁止结构性改动绕过注释校验  
    禁止 `.rs` 文件发生函数签名、契约字段、错误码、事件结构变化后检测不到注释同步变更。

3. 禁止 CI 对注释漂移放行  
    禁止 CI 缺失注释漂移检查（至少覆盖关键模块），禁止发现失配后放行。

4. 禁止评审记录没有注释一致性结论  
    禁止 Reviewer 结论缺失“注释一致/不一致”的明确判断。

5. 禁止“纯格式化提交”伪装语义改动  
   禁止将非纯格式化提交标记为注释同步豁免；纯格式化提交也不得缺少“无语义变化”标注。

### 第十一编 新增禁令纳管禁令

以下条款禁止未来持续新增条款时分类漂移、结构失序、执行失焦。

1. 禁止新增禁令未归属主题分类  
   新增禁令不得脱离 `A / E / R / L / C / P / D / G / N / M / T / V / X / H / S` 分类体系游离存在。

2. 禁止新增禁令缺失唯一条目编号  
   新增禁令不得只写自然语言，不得缺少可检索、可引用、可追踪的唯一编号。

3. 禁止新增禁令不声明所属分编  
   新增禁令不得跨编漂移，不得在未声明分编的情况下直接入法。

4. 禁止新增禁令不更新大纲与分类索引  
   新增禁令落地后，不得遗漏大纲项与分编主题分类索引的同步更新。

5. 禁止新增禁令与现有禁令冲突却不收敛  
   出现条款冲突时，不得并行保留互斥规则，不得回避冲突合并或冲突裁决。

6. 禁止新增禁令只写口号不写触发条件  
   新增禁令不得缺失触发场景、判定标准或违规识别要点。

7. 禁止新增禁令缺失证据要求  
   新增禁令不得只写主张，不得缺少验证证据与审查落点说明。

8. 禁止新增禁令绕过审查直接进入主干  
    任何新增禁令不得在未审查、未留痕、未确认生效范围的情况下直接生效。

9. 禁止新增禁令长期“临时生效”  
    新增禁令不得以“临时条款”无限期悬挂，不得没有失效条件或固化结论，并受第十五编第1条约束。

10. 禁止新增禁令后不做历史条款回扫  
    新增禁令落地后，不得跳过对相关旧条款的重编号、重引用、重索引回扫。

### 第十二编 术语与阈值禁令

以下条款禁止术语口径漂移、禁止阈值口径漂移、禁止别名漂移。

1. 禁止同一术语在同一主链内混用多套口径。  
   术语、别名、缩写、枚举名、阈值名不得在不同层表达不同真相。

2. 禁止阈值脱离来源单独生效。  
   数值阈值、等级阈值、开关阈值不得缺少规则来源、版本来源或证据来源。

3. 禁止模糊词充当判定口径。  
   “大概、差不多、足够、接近、可能”不得充当阈值判断条件。

4. 禁止别名漂移破坏搜索路径。  
   同一概念不得在不同文件、不同层、不同接口里改写成互不相认的名字。

5. 禁止阈值变更无回扫。  
    阈值调整后不得不回扫引用点、测试点、日志点和告警点。

6. 禁止量化口径悬空。  
   涉及“一个迭代窗口”“两个迭代”“定期”“关键模块”时，不得缺失项目级执行细则或审查上下文中的明确口径；不得在宪法层写死项目节奏、工具频率或模块清单。

### 第十三编 证据与举证禁令

以下条款禁止结论脱离证据，禁止举证责任漂移。

1. 禁止主张无证据。  
   裁决、阻断、放行、回退、修复结论不得脱离证据链。

2. 禁止证据链断裂。  
   禁止证据链缺失与场景对应的必需项。日志、测试、复现步骤、输入输出、快照、事件序列为候选证据项，场景声明为必需的条目不得缺失。

3. 禁止举证责任漂移。  
   主张方不得把取证责任甩给审查方、维护方或调用方。

4. 禁止单点证据冒充全案证据。  
    单条日志、单张截图、单次复现不得单独充当结论依据。

5. 禁止伪造证据完整性。  
    截断日志、裁剪上下文、改写时间线、挑选性展示不得充当证据。

6. 禁止高风险改动无决策留痕  
    禁止高风险架构改动、破坏性契约变更或关键链路重构缺失决策记录、风险说明和验收结论。

7. 禁止事故复盘不闭环  
    禁止线上事故、主链故障或严重回归缺失根因分析、整改动作、责任归档和复验结果。

### 第十四编 条款冲突裁决禁令

以下条款禁止冲突并存，禁止裁决失序。

1. 禁止互斥条款同时生效。  
   同一事实若被多条规则互相抵触，不得并行保留。

2. 禁止缺失优先级。  
    禁止冲突条款缺失默认裁决顺序；默认裁决顺序不得脱离 `总则禁令 > 架构禁令 > 证据与举证禁令 > 门禁禁令 > 其他分编禁令` 这一基准。禁止同级冲突缺失回退顺序；回退顺序不得脱离 `证据与举证禁令 > 门禁禁令 > 架构禁令 > 其他分编禁令` 这一基准。禁止在缺失裁决理由、证据引用和受影响条款记录时机械套用固定顺序。

3. 禁止临时解释替代裁决。  
   出现冲突不得用口头解释掩盖裁决结果。

4. 禁止裁决无留痕。  
   冲突裁决不得缺少理由、证据引用和受影响条款记录。

5. 禁止临时豁免倒灌成常规。  
   裁决不得把一次性豁免写成长期规则。

### 第十五编 应急临时处置禁令

以下条款禁止临时处置无限期化，禁止应急绕开主链。

1. 禁止临时处置无限期化。  
   临时冻结、临时降级、临时隔离不得没有退场条件。

2. 禁止应急绕开权威事实。  
   应急处置不得绕开权威事实、核心契约或门禁记录。

3. 禁止应急掩盖根因。  
   临时止血不得替代根因修复和回归验证。

4. 禁止临时状态污染主干。  
   临时开关、临时分支、临时兜底不得成为默认路径。

5. 禁止恢复前不复核。  
   从临时处置恢复时不得跳过验证、回放和证据复核。

### 第十六编 违规状态分级与处置禁令

以下条款只约束代码状态、交付状态、门禁状态，禁止引入人身处罚语义。

1. 禁止混写状态层级。  
   代码状态、交付状态、门禁状态不得混为同一层级。

2. 禁止状态命名失真。  
   阻断、冻结、回退、待核查、待修复等状态不得写成模糊词。

3. 禁止无证据升降状态。  
   在证据未补齐前不得把阻断态改成放行态，在回归未通过前不得把冻结态改成可交付态。

4. 禁止跳过恢复门槛。  
   违规状态未解除时不得直接恢复到可交付态或可放行态。

5. 禁止状态变更无记录。  
   状态升级、降级、冻结、恢复不得缺少触发条件、原因和留痕记录。

