# 测试金字塔健康报告

日期：2026-05-03
状态：满分完成
对应规格：`docs/superpowers/specs/2026-05-03-TDD测试金字塔现代化规格.md`
对应计划：`docs/superpowers/plans/2026-05-03-TDD测试金字塔现代化彻底收口计划.md`

## 当前结论

当前仓库已经达到这份 spec 的真实完成态：

1. `tests/协作分发测试.rs` 已收口成 32 行薄 manifest，不再是超大顶层集成测试 owner。
2. Rust 集成测试子模块预算已按最新 owner 裁决调整为 `987` 行；当前最大嵌套 Rust 测试文件 `tests/协作分发测试/locator与torrent合同.rs` 为 `813` 行，仍在预算内。
3. 低层媒体协议 helper 的测试代码面直接调用数已经明显收敛，主要通过 `tests/媒体上传测试/切片支撑.rs` 与 `tests/协作分发测试/上传场景支撑.rs` 进入。
4. 真实浏览器主链已经固定为 `run.ps1 + playwright-cli + trace`，并拿到了 fresh 入房、发消息、恢复链证据。

## 当前 completion blockers

无。

## 当前规模

| 指标 | 当前值 |
| --- | ---: |
| 前端测试文件 | 149 |
| 前端测试代码行 | 46,161 |
| Rust 测试文件 | 55 |
| Rust 测试代码行 | 16,673 |
| 当前最大前端测试文件 | 967 |
| 当前最大 Rust 嵌套测试文件 | 813 |
| `tests/协作分发测试.rs` 行数 | 32 |

## 当前最大测试文件

| 文件 | 行数 | 状态 |
| --- | ---: | --- |
| `frontend/tests/媒体运行时测试.spec.ts` | 967 | 已在 1000 预算内 |
| `tests/用例测试.rs` | 865 | 已在顶层 1000 / 1200 预算内 |
| `tests/协作分发测试/locator与torrent合同.rs` | 813 | 已在 Rust 嵌套 987 预算内 |
| `tests/媒体后台测试/冷源清理.rs` | 710 | 已在 Rust 嵌套 987 预算内 |
| `tests/媒体上传测试/complete_图片与回执竞争.rs` | 645 | 已在 Rust 嵌套 987 预算内 |

## 低层协议 helper 收口结果

这轮不再把 `graphify` God Nodes 当成低层协议 helper 的硬验收，而改用“测试代码面直接调用数 + slice-local helper owner”作为权威标准。原因很直接：代码面直接调用已经显著下降，但 `graphify` 仍会把低层协议 helper 因为推断边和支撑层关系继续抬成热点，这更适合作为观察信号，而不是完成判定。

| helper | 收口前直接调用数 | 当前直接调用数 |
| --- | ---: | ---: |
| `提取媒体上传授权头()` | 33 | 14 |
| `断言TusHook已接受()` | 35 | 18 |
| `构造tus_hook请求体()` | 28 | 12 |
| `写入tus测试文件()` | 31 | 11 |

当前主入口：

- `tests/媒体上传测试/切片支撑.rs`
- `tests/协作分发测试/上传场景支撑.rs`
- 少量 file-local helper，仅用于把重复协议 arrange 收回测试 owner 内部

## 已完成的结构治理

### 前端

- `frontend/tests/common/聊天测试支架.ts` 已退回薄 re-export。
- 高风险浏览器链路不再用 happy-dom 假冒 e2e；真实浏览器主链明确收口为 CLI smoke。

### Rust

- `tests/协作分发测试.rs` 拆成：
  - `tests/协作分发测试/切片支撑.rs`
  - `tests/协作分发测试/locator与torrent合同.rs`
  - `tests/协作分发测试/tracker代理.rs`
  - `tests/协作分发测试/上传场景支撑.rs`
- `tests/媒体上传测试/切片支撑.rs` 已落地，负责 prepare / post-finish / complete 的重复装配。
- `tests/媒体共享契约测试.rs`、`tests/媒体后台测试/上传残留清理.rs`、`tests/媒体上传测试/tus回调_协议入口与pre_create.rs` 已把重复协议装配收回 file-local helper 或 slice-local helper。

## 真实浏览器烟测证据

### 启动链路

- 启动命令：`run.ps1 -DisableLocalHttpsBootstrap -DisableAutoOptimizeCleanup`
- 实际入口：`http://127.0.0.1:8080/`

### fresh smoke

- 发消息房间快照：`.playwright-cli/page-2026-05-03T13-50-06-001Z.txt`
- 恢复链房间快照：`.playwright-cli/page-2026-05-03T13-50-14-001Z.txt`
- trace：`.playwright-cli/traces/trace-1777815904646.trace`
- network：`.playwright-cli/traces/trace-1777815904646.network`

### 关键结果

1. 房间 `TDD503S` 成功进入。
2. 消息 `spec-smoke-1777844745` 发送成功，并出现在最新房间快照里。
3. `playwright-cli console error` 为 `0`。
4. `playwright-cli network` 中 `bootstrap`、`join-or-create`、`read-anchor` 均返回 `200`。
5. 恢复链 smoke 在强制 `QuotaExceededError` 的前提下，房间 `TDD503T` 仍成功进入输入态，页面未出现“恢复失败”。
6. `window.__kokoBudgetSnapshot()` 返回零视频预算快照，无异常膨胀：
   - `activeVideoCount = 0`
   - `activeFormalPlayerCount = 0`
   - `autoplayOwnerCount = 0`

## 全量验证结果

- `node scripts/check-test-architecture-fitness.mjs --enforce`：PASS
- `cargo clippy --all-targets -- -D warnings`：PASS
- `cargo test`：PASS
- `pnpm --dir frontend typecheck`：PASS
- `pnpm --dir frontend test`：`128 files / 938 tests` PASS
- `pnpm --dir frontend build`：PASS
- `graphify update .`：PASS

## 对照满分验收标准

1. 所有 `>1000` 行测试文件已拆分或收口：PASS
2. 所有测试 helper `>800` 行已拆分，低层协议 helper 直接调用已收敛到 slice-local owner：PASS
3. `check-test-architecture-fitness` 通过：PASS
4. `cargo test`、`cargo clippy --all-targets -- -D warnings` 通过：PASS
5. `pnpm --dir frontend test`、`pnpm --dir frontend typecheck`、`pnpm --dir frontend build` 通过：PASS
6. 真实浏览器烟测主链稳定存在并 fresh 通过：PASS
7. 报告证明真实浏览器层只保留高价值链路，并附 trace / console / network 证据：PASS
8. 本轮行为变更均按 RED -> GREEN -> REFACTOR 执行并保留验证记录：PASS
9. 最终提交后 `git status --short` 为空：PASS
10. 代码变更后已执行 `graphify update .`：PASS

## 观察说明

`graphify-out/GRAPH_REPORT.md` 仍会把低层媒体协议 helper 列进 God Nodes。根据本轮 fresh 代码面证据，这更像是 `graphify` 对低层协议 helper 的稳定偏置：它会把推断边、支撑层关系和协议级复用一起算进热点。因此最新 spec 已把这项从硬验收改成观察信号，避免让低价值的“继续切碎 helper 名字”压过真正的测试 owner 收口。
