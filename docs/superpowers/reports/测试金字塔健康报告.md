# 测试金字塔健康报告

日期：2026-05-03
状态：收口完成
对应规格：`docs/superpowers/specs/2026-05-03-TDD测试金字塔现代化规格.md`
对应计划：`docs/superpowers/plans/2026-05-03-TDD测试金字塔现代化执行计划.md`

## 当前结论

当前测试体系已经从“巨型 spec + 支架总管 + 假 e2e 命名混乱”收口到更接近现代测试金字塔的形态：

1. 前端与 Rust 第一阶段超预算测试文件已经全部拆完。
2. `frontend/tests/common/聊天测试支架.ts` 已退回薄 re-export，真实 owner 已拆回多个专用支撑文件。
3. 原 `frontend/tests/端到端测试.spec.ts` 已改名为 `frontend/tests/前后台壳集成冒烟测试.spec.ts`，不再让 happy-dom 冒充真实浏览器 e2e。
4. 测试架构门禁已经切到强制模式：`pnpm --dir frontend check:test-architecture` 现在使用 `--enforce`。
5. 真实浏览器烟测沿用项目既有批准链路 `run.ps1 + playwright-cli`，不额外发明第二套假浏览器世界。

## 当前规模

| 指标 | 当前值 |
| --- | ---: |
| 前端测试文件 | 149 |
| 前端测试代码行 | 46,161 |
| Rust 测试文件 | 50 |
| Rust 测试代码行 | 16,891 |
| 当前最大前端测试文件 | 967 |
| 当前最大 Rust 嵌套测试文件 | 627 |

## 当前最大测试文件

| 文件 | 行数 | 状态 |
| --- | ---: | --- |
| `tests/协作分发测试.rs` | 1178 | 顶层 owner，未超 1200 预算 |
| `frontend/tests/媒体运行时测试.spec.ts` | 967 | 已在 1000 预算内 |
| `frontend/tests/聊天应用内核/消息流自动播测试.spec.ts` | 943 | 已在 1000 预算内 |
| `tests/协作分发测试/可用性裁决_presence与partial_peer.rs` | 627 | 已在 800 预算内 |
| `tests/媒体上传测试/complete_图片与回执竞争.rs` | 627 | 已在 800 预算内 |

## 已完成的结构治理

### 前端

- 拆分 `聊天壳测试.spec.ts`
- 拆分 `媒体协作分发测试.spec.ts`
- 拆分 `媒体播放测试.spec.ts`
- 拆分 `资产协作分发运行时测试.spec.ts`
- 拆分 `浏览器应用平台测试.spec.ts`
- 拆分 `聊天壳恢复与分页测试.spec.ts`
- 拆分 `传输测试.spec.ts`
- 拆分 `媒体发布测试.spec.ts`
- 拆分 `房间消息窗/隐藏接管与海报预热测试.spec.ts`
- 拆分 `房间消息窗/查看器意图与观察调度测试.spec.ts`
- 拆分 `房间消息窗/双视频交接测试.spec.ts`

### Rust

- 拆分 `协作分发测试/可用性裁决.rs`
- 拆分 `媒体上传测试/complete.rs`
- 拆分 `媒体上传测试/tus回调.rs`
- 拆分 `媒体上传测试/source_hash.rs`

## 门禁状态

- `node scripts/check-test-architecture-fitness.mjs --report`：PASS
- `pnpm --dir frontend check:test-architecture`：改为强制 `--enforce`
- `frontend/package.json` 的 `build`：已接入 `check:test-architecture`

## 真实浏览器烟测证据

### 启动链路

- 启动命令：`run.ps1 -DisableLocalHttpsBootstrap -DisableAutoOptimizeCleanup`
- 实际入口：`http://127.0.0.1:8080/`

### playwright-cli 证据

- 首页快照显示 `房间短码` 输入框和 `进房` 按钮。
- 输入房间短码 `TDD503` 后成功进入房间视图。
- 输入消息 `smoke-1777803432` 并发送后，消息列表中出现该文本。
- `playwright-cli console error`：`0` 条错误。
- `playwright-cli network`：`bootstrap`、`join-or-create`、`read-anchor` 均返回 `200`。
- `window.__kokoBudgetSnapshot()`：返回零视频预算快照，无异常膨胀。

## 验证结果

- `pnpm --dir frontend typecheck`：PASS
- 新拆前端测试文件定向 Vitest：`17 files / 123 tests` PASS
- `pnpm --dir frontend test:integration-smoke`：`1 file / 3 tests` PASS
- `pnpm --dir frontend test`：`128 files / 938 tests` PASS
- `pnpm --dir frontend build`：PASS
- `cargo test`：PASS
- `cargo test --test 协作分发测试 --test 媒体上传测试 -- --test-threads=1`：`80 tests` PASS
- `cargo clippy --all-targets -- -D warnings`：PASS
- `node scripts/check-test-architecture-fitness.mjs --report`：PASS
- `graphify update .`：PASS

## 后续观察点

1. `tests/协作分发测试.rs` 仍接近顶层 1200 预算，后续若继续膨胀，应按 locator / tracker / torrent owner 再拆。
2. `frontend/tests/媒体运行时测试.spec.ts` 967 行，仍是前端当前第一热点，后续新增场景时优先拆 owner。
3. 真实浏览器 smoke 当前已回到项目批准的 CLI 链路；如果未来需要 CI 自动化，再单独引入不与现有链路冲突的 Playwright owner。
