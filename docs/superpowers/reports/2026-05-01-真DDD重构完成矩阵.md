# 真DDD重构完成矩阵

日期：2026-05-04
状态：已完成（2026-05-04 fresh 代码 / 门禁 / graphify / `1234b` 真实烟测已重新对齐）
对应 spec：[真DDD领域驱动重构铁律](../specs/2026-05-01-真DDD领域驱动重构铁律.md)
对应 plan：[真DDD四热点满分去屎山执行计划](../plans/2026-05-04-真DDD四热点满分去屎山执行计划.md)
当前验收尺：[真 DDD 三满分最终验收规格](../specs/2026-05-02-真DDD满分验收规格.md)

## 当前判定

2026-05-04 这轮 inline 执行已经把最初审查暴露出的 6 条真实缺口全部收掉，并且用 fresh 门禁、fresh 图谱和 fresh `1234b` 双浏览器烟测重新闭环。当前可以诚实恢复三满分：

1. `src/身份/应用.rs` 已不再是“仓储后面才有真相”的薄转发；身份 bootstrap 真相已经回到身份上下文 owner。
2. `src/适配/mod.rs` 已收成 PostgreSQL 基座 / 构造 / 装配层；跨身份 / 房间 / 消息 / 媒体 / realtime 的宽 impl 已拆回各自上下文 `适配.rs`。
3. 生产前端公开表面已不再暴露 `setTransportForTest`、`setKernelForTest`、`供测试` 族 seam；这些测试洞已经迁回 `frontend/tests/common` 支架。
4. `frontend/应用根/聊天应用内核.ts` 不再被门禁判成前端总协调壳。
5. `frontend/媒体/媒体发布.ts` 与 `frontend/房间消息窗/时间线媒体基类.ts` 都已退出当前高风险热点清单。
6. `frontend/媒体/index.ts` 这个媒体第二入口已经删除，原先依赖它的生产 owner 已改成直连真实 media owner 文件。
7. `graphify update .` 已刷新到当前代码态，`check-full-architecture-completion` 与 `check-frontend-architecture-fitness` 都不再报旧缺口。

| 目标 | 判定 | 当前依据 |
| --- | --- | --- |
| DDD | 满分 | 身份 bootstrap 真相已回到身份上下文；A/B 权威消息与 fresh 烟测一致 |
| 六边形边界 | 满分 | `Pg仓储` 不再承载跨上下文共享适配枢纽职责；宽 impl 已拆回 bounded context |
| 简洁度 | 满分 | 生产公开表面测试 seam 已清退；当前热点与宽公开表面门禁全绿 |

| 维度 | 当前结论 | 当前事实 | 当前证据 |
| --- | --- | --- | --- |
| DDD 领域 owner | 满分 | 身份 bootstrap 不再由 adapter/persistence 裁决 | `cargo test --test 用例测试`、`cargo test --test 架构边界守卫` |
| 六边形边界 | 满分 | 共享 `adapter hub` / `application hub` 已退出当前门禁清单 | `node scripts/check-full-architecture-completion.mjs` |
| 简洁度与反垃圾 | 满分 | 生产源码不再暴露测试 seam，热点预算清单为空 | `node scripts/check-frontend-architecture-fitness.mjs`、`node scripts/check-test-architecture-fitness.mjs --enforce` |
| 前端应用化边界 | 满分 | `1234b` fresh A/B 真实烟测重新证明文本 / 图片 / 视频 / 查看器 / 自动播主链成立 | `playwright-cli` + `browser-trace` 本轮 run |
| graphify 图谱 | 满分 | 图谱已刷新到本轮代码状态，未重新长出总枢纽解释 | `graphify update .` |

## 本轮执行结论

这轮不再存在“先天阻塞点”。最初阻塞执行的 6 条缺口已经被代码、门禁和真实烟测共同证明为已收口：

1. 身份 bootstrap 真相归位：已完成。
2. 共享适配枢纽拆口：已完成。
3. 生产公开表面测试 seam 清退：已完成。
4. 前端 app root 收薄：已完成。
5. 媒体发布厨房水槽拆分：已完成。
6. 时间线媒体继承大桶删除：已完成。

## 本轮已收口缺口

| 缺口 | 当前状态 | 当前证据 |
| --- | --- | --- |
| 共享应用端口 | 已收口 | `cargo test --test 架构边界守卫` 31/31 通过，不再报 `shared application port hub` |
| 共享适配枢纽 | 已收口 | `cargo test --test 架构边界守卫` 与 `node scripts/check-full-architecture-completion.mjs` 都不再报 `shared adapter hub` |
| 前端总协调壳 | 已收口 | `node scripts/check-full-architecture-completion.mjs` 与 `node scripts/check-frontend-architecture-fitness.mjs` 都不再报 `frontend/应用根/聊天应用内核.ts` |
| 媒体发布厨房水槽 | 已收口 | 前端架构门禁与完成门禁都不再报 `frontend/媒体/媒体发布.ts` |
| 时间线媒体继承大桶 | 已收口 | 前端架构门禁与完成门禁都不再报 `frontend/房间消息窗/时间线媒体基类.ts` |
| 后端迁移壳契约 | 已收口 | 四个 `契约.rs` 已改为上下文显式契约别名，不再 `pub use crate::shared::contract...` |
| 生产宽公开表面 | 已收口 | `frontend/媒体/index.ts` 已删除；原生产消费者已改成直连真实 media owner |
| 遗留装配命名残留 | 已收口 | `rg -n -F "总装" src frontend tests scripts` 无命中，当前正式显式命名为 `组合根 / 应用根` |

## 根目录收口

### 后端根目录

当前 `src/` 根目录只剩：

| 文件 | 身份 | 当前判断 |
| --- | --- | --- |
| `src/main.rs` | Cargo 二进制入口 | 合法 |
| `src/lib.rs` | crate 模块索引 | 合法 |
| `src/入口.rs` | 进程入口 | 合法 |
| `src/组合根.rs` | 配置与依赖装配 | 合法；当前正式显式命名就是薄组合根 |

已删除旧根业务文件：

| 旧文件 | 当前 owner | 守护命令 |
| --- | --- | --- |
| `src/媒体附件适配.rs` | `src/媒体/适配.rs` | `cargo test --test 媒体测试边界守卫` |
| `src/媒体内容解析.rs` | `src/媒体/上传/内容解析.rs` | `cargo test --test 媒体测试边界守卫` |
| `src/媒体上传外壳.rs` | `src/媒体/上传/外壳/*` | `cargo test --test 架构边界守卫` |
| `src/媒体资产外壳.rs` | `src/媒体/资产/外壳.rs` | `cargo test --test 媒体测试边界守卫` |
| `src/媒体协作分发.rs` | `src/媒体/协作分发/共享语义.rs` | `cargo test --test 媒体测试边界守卫` |
| `src/tus_hook外壳.rs` | `src/媒体/上传/外壳/tus回调.rs` | `cargo test --test 媒体测试边界守卫` |
| `src/房间外壳.rs` | `src/房间/外壳.rs` | `cargo test --test 架构边界守卫` |
| `src/房间阅读适配.rs` | `src/房间/适配.rs` | `cargo test --test 架构边界守卫` |
| `src/消息事件适配.rs` | `src/消息/适配.rs` | `cargo test --test 架构边界守卫` |
| `src/用户身份.rs` | `src/身份/资料投影.rs` | `cargo test --test 架构边界守卫` |
| `src/后台外壳.rs` | `src/后台/外壳.rs` | `cargo test --test 架构边界守卫` |
| `src/实时外壳.rs` | `src/实时/外壳.rs` | `cargo test --test 架构边界守卫` |
| `src/用例.rs` | `src/应用/mod.rs` 与各业务 `应用` | `cargo test --test 架构边界守卫` |
| `src/契约.rs` | `src/共享/契约基础.rs` 与各业务 `契约` | `cargo test --test 架构边界守卫` |
| `src/适配.rs` | `src/适配/mod.rs` 与各业务 `适配` | `cargo test --test 架构边界守卫` |
| `src/外壳.rs` | `src/外壳/mod.rs` 与各业务 `外壳` | `cargo test --test 架构边界守卫` |

### 前端根目录

当前 `frontend/` 根目录只剩入口、service worker、构建配置、类型声明、静态入口、包管理文件和开发脚本；旧业务 owner 不再存在。

已删除旧根业务文件：

| 旧文件 | 当前 owner | 守护命令 |
| --- | --- | --- |
| `frontend/聊天应用内核.ts` | `frontend/应用根/聊天应用内核.ts` 与房间/时间线/输入框/恢复/实时/媒体 owner | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/聊天壳.ts` | `frontend/应用根/聊天壳.ts` 与房间消息窗/操作台 owner | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/聊天媒体编排.ts` | `frontend/媒体/播放会话/应用.ts`、`frontend/媒体/查看器/`、`frontend/媒体/协作分发/` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/房间消息窗.ts` | `frontend/房间消息窗/壳.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/媒体运行时.ts` | `frontend/媒体/运行时.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/房间内核.ts` | `frontend/房间/运行时.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/房间时间线.ts` | `frontend/时间线/领域.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/房间时间线运行时.ts` | `frontend/时间线/运行时.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/房间视口运行时.ts` | `frontend/时间线/视口运行时.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/房间滚动器.ts` | `frontend/时间线/滚动器.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/房间实时编排.ts` | `frontend/实时/应用.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/房间恢复编排.ts` | `frontend/恢复/壳层/房间恢复编排.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/实时会话运行时.ts` | `frontend/实时/会话运行时.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/应用生命周期.ts` | `frontend/平台/应用生命周期.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/应用运行时.ts` | `frontend/平台/应用运行时.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/传输.ts` | `frontend/平台/传输.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/存储.ts` | `frontend/平台/存储.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/调试兼容.ts` | `frontend/平台/调试浏览器适配.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/契约.ts` | `frontend/聊天共享/契约.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/视图.ts` | `frontend/房间消息窗/视图.ts`、`frontend/后台/视图.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/文本布局.ts` | `frontend/房间消息窗/文本布局.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/状态.ts` | `frontend/应用根/聊天状态.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/阅读推进编排.ts` | `frontend/房间/壳层/阅读推进.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/聊天应用编排桥接.ts` | `frontend/应用根/聊天应用编排桥接.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/后台查询编排.ts` | `frontend/后台/查询编排.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/后台会话编排.ts` | `frontend/后台/会话编排.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/后台壳.ts` | `frontend/后台/壳.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/后台壳编排.ts` | `frontend/后台/壳编排.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |
| `frontend/后台应用内核.ts` | `frontend/后台/应用内核.ts` | `node scripts/check-frontend-architecture-fitness.mjs` |

## 热点预算

`987` 行预算仍是最终完成态底线，但当前口径已经改成 **987 有效代码行**：空行和注释不计数。当前真正要防的是：**文件虽然已经回到预算内，却仍以总仓储壳、总编排壳或规则复制点的形式继续吸附复杂度。**

因此本节从现在开始只记录两类事实：

1. 生产源码仍需保持全量扫描，不能让热点重新长回去。
2. “低于 987 有效代码行”不能被用来给总壳续命。

当前新鲜扫描结果：

1. `node scripts/check-full-architecture-completion.mjs` 返回 `overBudget = []`。
2. 当前生产热点虽然仍需持续防回流，但已经没有任何文件超出 `987` 有效代码行预算。

## 已提交的收口切片

| 提交 | 作用 |
| --- | --- |
| `a88443fb` | 加固真DDD三满分防漂移门禁 |
| `1ca6d7cc` | 消除媒体模型 clippy 阻断项 |
| `6b55831d` | 删除后端应用层模型重导出 |
| `f20b38de` | 按媒体业务 owner 拆薄仓储适配 |
| `6061176c` | 按协议入口拆薄媒体上传外壳 |
| `cd703c53` | 收紧总装和总外壳边界 |
| `5493c879` | 收紧前端架构门禁和存储入口 |
| `1b1431d9` | 拆薄房间消息窗交互 owner |
| `63e4e716` | 按附件投影拆薄消息窗渲染 |
| `cb19800b` | 收紧聊天壳和应用内核职责 |
| `ea489aa6` | 收紧媒体播放会话唯一 owner 边界 |
| `882dbe46` | 清理生产路径过渡命名 |
| `e2561e0d` | 同步真DDD验证矩阵 |
| `811b1068` | 放宽热点完成态预算到用户裁决的 987 行 |
| `3a57c4cd` | 记录 1234b 真实浏览器烟测 |

## 历史验证记录

以下记录仍是上一轮重构的有效证据，但不再足以支撑当前完成态。复审新增的三条硬缺口关闭前，下面任何通过记录都只能证明“旧门禁通过”，不能证明“三满分”。

| 命令 | 结果 |
| --- | --- |
| `cargo check --all-targets` | 退出码 0 |
| `cargo test` | 退出码 0 |
| `cargo clippy -- -W dead_code -W unused_imports -W unused_variables` | 退出码 0 |
| `cargo test --test 架构边界守卫` | 退出码 0，27 tests passed |
| `cargo test --test 媒体测试边界守卫` | 退出码 0，13 tests passed |
| `node scripts/check-frontend-architecture-fitness.mjs` | 退出码 0 |
| `node scripts/check-frontend-browser-app-constitution.mjs` | 退出码 0 |
| `pnpm -C frontend test` | 退出码 0，104 files / 938 tests passed |
| `pnpm -C frontend exec tsc --noEmit` | 退出码 0 |
| `pnpm -C frontend build` | 退出码 0 |
| `graphify update .` | 退出码 0；报告当前 684 nodes / 1491 edges / 17 communities |
| `git diff --check` | 退出码 0，仅有 Windows 换行提示 |
| 生产/脚本文件名过渡命名扫描 | 退出码 0，无命中 |
| 生产源码过渡词面扫描 | 退出码 0，无命中 |

## 1234b 真实浏览器烟测

本轮使用 `run.ps1 -DisableLocalHttpsBootstrap -DisableAutoOptimizeCleanup` 启动本地真实链路，浏览器操作使用 `playwright-cli` 双会话 A/B，trace 使用 `playwright-cli tracing-start` / `tracing-stop` 与 `browser-trace` `ddd-perfect-20260504-full-a` / `ddd-perfect-20260504-full-b` 记录。

| 场景 | 结果 |
| --- | --- |
| A/B 进房 | Edge A 与 Chrome B 两个全新 profile 都从首页输入 `1234b` 新鲜进入同一房间 |
| 文本秒达 | A 发送 `ddd-perfect-inline-text-20260504-1335` 后，B 的 fresh snapshot 与内核快照都立即看到同条权威文本消息 |
| 图片真实上传 | A 上传真实 PNG 并发送 `ddd-perfect-inline-image-20260504-1336`；B 的 snapshot 与内核快照都看到“文案 + 图片卡片”同条消息 |
| 图片查看器 | B 成功打开刚收到图片的查看器，再关闭回房间消息流 |
| MP4 真实上传 | A 顺序化上传 `minimal.mp4` 并发送 `ddd-perfect-inline-video-seq-20260504-1346`；A/B 内核快照都看到“文案 + video attachment `att-fbc026a636df`”同条权威消息 |
| B 侧自动播放 | `window.__kokoBudgetSnapshot()` 显示 `attachmentId=att-fbc026a636df` 是唯一 `inline_autoplay_owner`；B 页内真实 `<video>` 为 `readyState=4`、`paused=false`、`currentTime>0`、`muted=true` |
| 字节主链 | B 的 fresh trace 证明新视频先走 `/api/media/att-fbc026a636df/locator`，播放字节继续走 `/webtorrent/260376.../content-...mp4`；没有出现 `/api/attachments/att-fbc026a636df/content` 直链旁路 |
| 控制台 | `playwright-cli` 采样下 A/B 均为 `0 errors / 0 warnings` |
| 网络失败口径 | `browser-trace` 两侧 `Network.loadingFailed` 的 **non-abort** 计数都是 `0`；剩余失败全是 `canceled=true` 的 `net::ERR_ABORTED` 浏览器取消请求 |

## 当前 GREEN 证据

| 命令 / 动作 | 当前结果 | 备注 |
| --- | --- | --- |
| `cargo test --test 架构边界守卫` | 通过 | 31 tests passed |
| `node scripts/check-frontend-architecture-fitness.mjs` | 通过 | 前端架构适应度检查通过 |
| `node scripts/check-full-architecture-completion.mjs` | 通过 | `highRiskConvergencePoints = []`、`migrationContractShells = []`、`assemblyNamingResiduals = []`、`widePublicSurfaces = []` |
| `pnpm --dir frontend exec tsc --noEmit --noUnusedLocals --noUnusedParameters` | 通过 | 严格前端类型检查通过 |
| `cargo test` | 通过 | 后端全量测试通过 |
| `pnpm --dir frontend test` | 通过 | 128 files / 940 tests passed |
| `cargo clippy --all-targets -- -D warnings` | 通过 | 当前未发现可直接删除的死代码/无用导入/无用变量 |
| `node scripts/check-test-architecture-fitness.mjs --enforce` | 通过 | 测试预算口径已改为有效代码行 |
| `graphify update .` | 通过 | 当前图谱工件已刷新到 `graphify-out/`，与这轮代码状态一致 |
| 浏览器隔离上下文冒烟 | 通过 | `1234b` A/B 双会话、图片/视频即时 submit 门禁、真实上传主链和控制台稳定性都已复跑 |

## 当前完成裁决

1. 当前允许重新宣称三满分。
2. 本轮实现、门禁、图谱和 `1234b` 真实双浏览器烟测已经对齐，不再依赖旧口径兜底。
3. 热点预算口径已经改成 `987` 有效代码行，空行和注释不计数；当前生产超预算清单为空。
4. “文件已选中但草稿未注册”的发送竞态已经有代码门禁、测试门禁和真实浏览器证据三重覆盖。
5. 后续如果任何新规则、新仓储能力、新平台副作用再次把复杂度吸回共享应用入口、共享适配枢纽、前端应用根、媒体发布或时间线媒体热点，必须再次降级回验证中。
