# 真DDD重构完成矩阵

日期：2026-05-01
状态：已完成
对应 spec：[真DDD领域驱动重构铁律](../specs/2026-05-01-真DDD领域驱动重构铁律.md)
对应 plan：[真DDD领域驱动重构执行计划](../plans/2026-05-01-真DDD领域驱动重构执行计划.md)

## 判定矩阵

| 条目 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| 后端 `identity / room / message / media / realtime / recovery` 六类 owner 全部显式存在 | 已完成 | `src/lib.rs` 已挂完整业务模块；`src/恢复/mod.rs`、`src/恢复/应用.rs`、`src/实时/外壳.rs` 已补齐并受守卫测试约束 | 统一用例与旧壳层已退成门面 |
| 前端 `房间 / 时间线 / 输入框 / 媒体/播放会话 / 媒体/查看器 / 媒体/协作分发 / 恢复 / 实时 / 总装` owner 全部显式存在 | 已完成 | `frontend/媒体/查看器/应用.ts`、`frontend/媒体/协作分发/应用.ts`、`frontend/恢复/应用.ts`、`frontend/实时/应用.ts` 与既有 `房间/时间线/输入框/总装` owner 已全部落位 | `聊天媒体编排`、`房间恢复编排`、`房间实时编排` 已退成薄门面 |
| 缺失模块全部补齐 | 已完成 | `src/恢复/mod.rs`、`src/恢复/应用.rs`、`src/实时/外壳.rs`、`frontend/媒体/查看器/应用.ts`、`frontend/媒体/协作分发/应用.ts` 全部已补齐 | 本轮 plan 明确点名的结构缺口已清空 |
| 后端外壳、前端壳层、平台适配、仓储适配、实时适配都只做翻译、投影、编排 | 已完成 | `tests/架构边界守卫.rs`、`frontend/tests/聊天应用内核/架构边界与公开入口测试.spec.ts`、`frontend/tests/聊天媒体编排/架构边界测试.spec.ts`、`frontend/tests/浏览器应用平台测试.spec.ts` 全绿 | 继续允许薄门面存在，但已禁止真相回流 |
| 专项硬禁令继续有效，没有被这次重构稀释 | 已完成 | `pnpm --dir frontend build` 通过宪法检查与架构体检；真实烟测预算快照继续显示 `formalByteSource=webtorrent_official_stream` | 本轮没有把 `WebTorrent` 主链、零崩溃约束、群聊视频禁令改松 |
| 架构边界测试、负向安全测试、前端体检、后端主链测试、真实浏览器烟测全部新鲜通过 | 已完成 | 后端主链全绿；前端 `98` 个文件 `872` 条测试通过；真实浏览器 `1234b` 进房、双会话增量、真实视频上传、查看器开关、刷新恢复、伪造本地恢复态失败、伪造 session 401 均已重跑 | 系统全屏自动烟测受浏览器瞬时激活限制，继续由既有 targeted tests 守住 |
| `graphify update .` 已重新跑过，且没有新脏尾巴 | 已完成 | `graphify update .` 已刷新到 `669 nodes / 1410 edges / 33 communities`；文档回填不需要再次刷新 | 结束前工作树需继续保持干净 |

## 本轮验收证据

1. 自动化回归：
   - `cargo test --test 用例测试 --test 房间接口测试 --test 消息主链测试 --test 实时链路测试 --test 媒体测试边界守卫 --test 协作分发测试 --test 启动与迁移测试`
   - `cargo test --test 架构边界守卫`
   - `pnpm --dir frontend test`
   - `pnpm --dir frontend typecheck`
   - `pnpm --dir frontend build`
2. 真实浏览器烟测：
   - `browser-trace` 跟踪目录：`E:/koko/.o11y/ddd-refactor-desktop`
   - `playwright-cli` 桌面会话：真实进入 `1234b`，看到历史视频消息，发送文本消息后第二个独立浏览器会话实时收到。
   - `playwright-cli` 第二会话：上传 `tests/fixtures/minimal.mp4` 并发送后，首会话实时看到新视频卡片并可打开查看器。
   - 刷新恢复后仍直接回到 `1234b`，没有掉回首页，也没有把内部 `room_id` 泄露成房间标题。
   - 移动视口预算快照显示 `activeVideoCount=1`、`activeFormalPlayerCount=1`、`autoplayOwnerCount=1`，正式字节入口仍是 `webtorrent_official_stream`。
3. 负向安全验收：
   - 伪造 `koko_current_room_id` + `koko_current_room_snapshot` 为不存在房间后，刷新只得到后端 `404`，UI 留在首页，伪造房间锚点被清理，没有凭本地缓存造出假房间。
   - 伪造 `session_id=s-forged-session` 调 `POST /api/rooms/join-or-create` 和 `GET /api/rooms/{room_id}/snapshot` 都返回 `401` + `{\"code\":\"invalid_session\",\"message\":\"会话无效\"}`。
4. Trace 结论：
   - `summary.json` 记录 `2177` 个事件、`2` 次页面阶段。
   - 失败网络项全部是 `net::ERR_ABORTED`，未出现非 abort 的 fetch/media 失败；控制台错误列表为 `0`。

## 当前热点文件

- `src/用例.rs`
- `src/外壳.rs`
- `src/适配.rs`
- `frontend/聊天应用内核.ts`
- `frontend/聊天壳.ts`
- `frontend/聊天媒体编排.ts`

## 当前残余关注点

1. `src/用例.rs`、`src/外壳.rs`、`frontend/聊天应用内核.ts`、`frontend/聊天壳.ts` 仍然是仓库里的大文件，但现在已经受 owner 守卫与体检门槛约束，新增逻辑不能再倒灌回去。
2. 系统全屏真实手势路径在自动化里会被 `userActivation` 守卫主动短路，避免制造假报错；这条路径继续由既有 targeted tests 守住。
3. 浏览器 trace 里仍存在大量 `ERR_ABORTED` 的 locator/media 取消事件；它们当前没有升级成控制台错误或非 abort 网络错误，但后续仍值得单独压噪。

## 执行规则

应该：

1. 先补齐缺失 owner，再掏空旧总控。
2. 每完成一阶段就回填本矩阵。
3. 只有证据齐全，才能把条目标成“已完成”。

禁止：

1. 把“新目录已经出现”当成“真DDD重构已完成”。
2. 把“测试暂时全绿”当成“结构已经收口”。
3. 不更新矩阵就口头宣布进入下一阶段。
