# 真DDD重构完成矩阵

日期：2026-05-01
状态：进行中
对应 spec：[真DDD领域驱动重构铁律](../specs/2026-05-01-真DDD领域驱动重构铁律.md)
对应 plan：[真DDD领域驱动重构执行计划](../plans/2026-05-01-真DDD领域驱动重构执行计划.md)

## 判定矩阵

| 条目 | 状态 | 证据 | 备注 |
| --- | --- | --- | --- |
| 后端 `identity / room / message / media / realtime / recovery` 六类 owner 全部显式存在 | 进行中 | `src/lib.rs` 已挂 `identity / room / message / media / realtime`；`src/恢复/mod.rs`、`src/恢复/应用.rs` 仍缺失 | 后端恢复 owner 未补齐 |
| 前端 `房间 / 时间线 / 输入框 / 媒体/播放会话 / 媒体/查看器 / 媒体/协作分发 / 恢复 / 实时 / 总装` owner 全部显式存在 | 进行中 | `frontend/房间/应用.ts`、`frontend/时间线/应用.ts`、`frontend/输入框/应用.ts`、`frontend/恢复/应用.ts`、`frontend/实时/应用.ts`、`frontend/媒体/播放会话/应用.ts`、`frontend/总装/应用装配.ts` 已存在；`frontend/媒体/查看器/应用.ts`、`frontend/媒体/协作分发/应用.ts` 缺失 | 前端媒体 owner 未补齐 |
| 缺失模块全部补齐 | 未完成 | 缺失：`src/恢复/mod.rs`、`src/恢复/应用.rs`、`src/实时/外壳.rs`、`frontend/媒体/查看器/应用.ts`、`frontend/媒体/协作分发/应用.ts` | 这是当前最明确的结构缺口 |
| 后端外壳、前端壳层、平台适配、仓储适配、实时适配都只做翻译、投影、编排 | 进行中 | 旧总控仍厚：`src/用例.rs`、`src/外壳.rs`、`src/适配.rs`、`frontend/聊天应用内核.ts`、`frontend/聊天壳.ts`、`frontend/聊天媒体编排.ts` | 需要继续掏空 owner |
| 专项硬禁令继续有效，没有被这次重构稀释 | 进行中 | 当前 plan 已写死保留 `WebTorrent` 主链、零崩溃、群聊视频禁止事项 | 需要在最终验收再次确认 |
| 架构边界测试、负向安全测试、前端体检、后端主链测试、真实浏览器烟测全部新鲜通过 | 进行中 | 已有部分前后端测试通过；真实浏览器最终验收尚未重跑 | 最终完成前必须补齐 |
| `graphify update .` 已重新跑过，且没有新脏尾巴 | 进行中 | 最近一次代码改动后需要再刷新图谱 | 纯文档改动不触发，代码阶段结束必须执行 |

## 当前热点文件

- `src/用例.rs`
- `src/外壳.rs`
- `src/适配.rs`
- `frontend/聊天应用内核.ts`
- `frontend/聊天壳.ts`
- `frontend/聊天媒体编排.ts`

## 当前首轮结构缺口

1. 后端恢复 owner 还没落成独立模块。
2. 后端 realtime 新外壳 owner 还没落成显式文件。
3. 前端查看器与协作分发应用 owner 还没显式落位。
4. 总文件仍然过厚，说明 owner 只是部分转移，不是彻底换位。

## 执行规则

应该：

1. 先补齐缺失 owner，再掏空旧总控。
2. 每完成一阶段就回填本矩阵。
3. 只有证据齐全，才能把条目标成“已完成”。

禁止：

1. 把“新目录已经出现”当成“真DDD重构已完成”。
2. 把“测试暂时全绿”当成“结构已经收口”。
3. 不更新矩阵就口头宣布进入下一阶段。
