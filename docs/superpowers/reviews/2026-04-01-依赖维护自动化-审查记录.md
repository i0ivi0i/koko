# 审查记录

## 审查元数据
- 审查时间：2026-04-01 CST
- 仓库路径：`E:\koko`
- 审查模式：focused mode
- 审查主题：依赖保持最新稳定状态的自动化机制落地
- Git 提交：`未提交`
- 工作树状态：存在与本轮无关的未提交测试改动，本轮仅新增自动化脚本与 GitHub Actions
- 使用技能：`using-superpowers`
- 外部校准：Rust/Cargo 官方文档、Cargo Book、Edition Guide
- 总体置信度：高
- 审查最终状态：已落地

## 审查目标
- 把 Rust/Cargo 官方关于“长期保持接近最新稳定版”的建议，从口头规则变成可执行机制。
- 避免继续依赖人工记忆做 `cargo update` 和验证。

## 审查依据
- 官方建议长期提交 `Cargo.lock`，通过 `cargo update` 主动刷新锁文件
- 官方建议显式写 `rust-version`
- 官方 CI 示例明确建议至少在 `stable / beta / nightly` 上持续 build/test
- 官方明确建议把“锁文件确定性”与“latest dependencies 验证”拆开
- 官方在 latest-deps 章节中明确点名 Dependabot / Renovate 作为长期方案

## 已落地机制
- 新增 [scripts/verify-rust-workspace.ps1](/E:/koko/scripts/verify-rust-workspace.ps1)
  - 收口仓库当前完整验证环：`cargo check`、wasm check、`cargo nextest run`、`cargo test --doc`、`cargo sqlx prepare --check`、`cargo test`、`dx` web bundle
  - 支持通过 `-UpdateDeps` 先执行 `cargo update`，再执行完整验证环
- 新增 [rust-ci.yml](/E:/koko/.github/workflows/rust-ci.yml)
  - 在 `stable / beta / nightly` 上持续检查主工作区是否仍可构建和测试
  - 主门禁统一使用 `--locked`，保持依赖解析确定性
- 新增 [rust-deps-refresh.yml](/E:/koko/.github/workflows/rust-deps-refresh.yml)
  - 提供手动和每周定时的依赖刷新任务
  - 调用 [verify-rust-workspace.ps1](/E:/koko/scripts/verify-rust-workspace.ps1) 的 `-UpdateDeps` 模式执行 `cargo update` 后完整验证
  - 采用 Cargo 官方 latest-deps 示例里的 `CARGO_RESOLVER_INCOMPATIBLE_RUST_VERSIONS=allow`
  - 若 `Cargo.toml` / `Cargo.lock` 变化，输出 patch artifact，避免静默漂移
- 新增 [dependabot.yml](/E:/koko/.github/dependabot.yml)
  - 让 Cargo 依赖和 GitHub Actions 版本按周发升级 PR

## 架构判断
- 这套方案符合官方思路，但没有引入仓库私有升级框架。
- “主 CI 保证锁文件稳定可复现” 和 “定期刷新锁文件并验证最新兼容集” 被明确拆开。
- “定时 latest-deps 验证” 和 “平台侧自动发升级 PR” 也被拆开，不把版本管理全压在单一机制上。
- Web bundle 没有直接走脆弱的裸 `dx bundle`，而是继续复用仓库已有的 [dx-bundle-web.ps1](/E:/koko/scripts/dx-bundle-web.ps1)，避免把上游 wasm workaround 再散落到多个入口。

## 验证结论
- 已本地实际运行并通过：
  - [verify-rust-workspace.ps1](/E:/koko/scripts/verify-rust-workspace.ps1)
- 这证明新脚本不是空壳，确实能在当前仓库把完整维护环跑通。

## 后续建议
1. 后续若要继续强化自动维护，再考虑是否让 scheduled workflow 自动开 PR；这一步不属于 Rust 官方最低建议，不必先做。
2. 若仓库验证环变化，优先改 [verify-rust-workspace.ps1](/E:/koko/scripts/verify-rust-workspace.ps1)，不要把命令散落回多个 CI 文件。
