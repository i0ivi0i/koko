# 审查记录

## 变更范围
- 房间实时链路第一阶段的 Task 1 / Task 2 / Task 3 / Task 4 / Task 5
- 共享 `contract` 增加 `MessageAccepted`、`event_position`、`latest_event_position`
- `application / store / rt` 建立消息位置投影与房间最新事件位置读取
- `rt` 拆分 sender 命令结果与权威消息事件
- `chat / web` 收口 pending 升格与房间订阅结果过滤
- `app / rt / http` 全量阶段测试回归

## 层级归属
- 主归属层：`application / contract / adapter`
- 连带影响层：`shell`
- 归属理由：本轮核心是把“消息成立真相”和“房间同步锚点”从旧的 ack/快照混用模型，收口到稳定共享契约与应用层投影；未把 `event_position` 倒灌进 `domain`

## 风险等级
- 低风险 / 高风险：高风险

## 复用 / 自研结论
- 是否涉及通用能力：否；本轮不新增通用基础设施，只在现有 `socketioxide / Axum / SQLx / Dioxus` 链路上收口真相
- 仓库现有实现：继续复用现有 realtime / HTTP / store 主链路
- 检索时间：2026-04-03
- 检索范围：仓库现有 `src/app.rs`、`src/store.rs`、`src/rt.rs`、`src/web.rs`、`migrations/`
- 关键检索词或查询路径：`MessageCreated`、`RoomSnapshot`、`message_accepted`、`RoomStreamSubscribed`、`event_position`
- 候选实现：N/A，本轮不引入新依赖、不自研新通用能力
- 对比维度摘要：继续复用现有成熟栈，仅新增数据库位置锚点与薄应用投影
- 最终结论：通过；不新增私有 manager / bridge / wrapper

## 边界风险
- 共享 `contract` 被修改，属于高风险公共边界
- 聊天热路径从 `message_accepted` 混充历史事实，迁回 `message_created + event_position`
- `rt` 只负责把 application 输出翻译成协议回包，未新增业务裁决

## 冗余 / 重复实现风险
- 本轮未新增第二套 realtime 基础设施
- 本轮未新增第二套消息成立真相；`message_accepted` 作为命令结果类型已显式独立
- 当前未新增第二套消息确认链；`shell` 只保留体验态与订阅编排，本轮未引入私有 wrapper / manager / bridge

## 验证
- 计划命令：
  - `cargo test --test app contract::`
  - `cargo test --test app application::send_text_message_returns_message_created_event`
  - `cargo test --test app application::subscribe_room_stream_accepts_active_member`
  - `cargo test --test app store::send_text_message_persists_message_and_room_snapshot_reads_it`
  - `$env:DATABASE_URL="postgres://postgres:postgres@127.0.0.1:5432/postgres"; cargo test --test http routes::snapshot_endpoint_returns_joined_room_history`
- 实际命令：
  - `cargo test --test app contract::`
  - `cargo test --test app application::send_text_message_returns_message_created_event`
  - `cargo test --test app application::subscribe_room_stream_accepts_active_member`
  - `$env:DATABASE_URL="postgres://postgres:postgres@127.0.0.1:5432/postgres"; cargo test --test app store::send_text_message_persists_message_and_room_snapshot_reads_it`
  - `$env:DATABASE_URL="postgres://postgres:postgres@127.0.0.1:5432/postgres"; cargo test --test http routes::snapshot_endpoint_returns_joined_room_history`
  - `$env:DATABASE_URL="postgres://postgres:postgres@127.0.0.1:5432/postgres"; cargo test --test rt socket::`
  - `cargo test --test http chat_state::`
  - `cargo test --test http web_shell::`
  - `$env:DATABASE_URL="postgres://postgres:postgres@127.0.0.1:5432/postgres"; cargo test --test app`
  - `$env:DATABASE_URL="postgres://postgres:postgres@127.0.0.1:5432/postgres"; cargo test --test rt`
  - `$env:DATABASE_URL="postgres://postgres:postgres@127.0.0.1:5432/postgres"; cargo test --test http`
- 未验证风险：
  - 本阶段非目标中的 gap refill / offset backfill、多端同步、完整恢复协议仍未实现

## 债务结论
- 是否新增债务：否
- 是否延期：否
- 债务责任人：N/A
- 回看触发点：N/A
- 最晚处理时机：N/A

## 复核要求
- 是否需要独立复核：是
- 独立复核人：仓库主人

## 最终状态
- 通过

## 批准信息
- 批准人：AI 实施前治理校准；后续独立复核人为仓库主人
- 例外失效条件：N/A
- 补审要求：仓库主人需针对共享契约、聊天热路径与壳层边界做独立复核后，方可视为本阶段正式放行
