# 审查记录

## 变更范围
- `Cargo.toml`
- `Cargo.lock`
- `migrations/0002_admin_session_truth.sql`
- `src/store.rs`
- `src/support.rs`
- `tests/admin_flow.rs`

## 层级归属
- 主归属层：`adapter`
- 连带影响层：`support`、测试、数据库 migration
- 归属理由：这次只落 Postgres 后台单会话真相 helper、token 指纹 helper 和对应回归测试，没有提前接入 HTTP/session middleware。

## 风险等级
- 低风险 / 高风险：高风险

## 复用 / 自研结论
- 是否涉及通用能力：是，涉及哈希、时间窗口、数据库并发控制、测试数据库生命周期。
- 仓库现有实现：匿名会话与房间进入已使用 `sqlx + Postgres + advisory lock`；测试数据库生命周期已统一走 `sqlx::test`。
- 检索时间：2026-04-02
- 检索范围：
  - crates.io / docs.rs 元数据（通过 `cargo info`）
  - 当前依赖树与现有实现（通过 `cargo tree`、仓库源码）
  - 仓库 `docs/superpowers/plans/2026-04-02-admin-后台会话与入口实施.md`
  - `E:\koko\学习` 目录
- 关键检索词或查询路径：
  - `cargo info sha2`
  - `cargo info time`
  - `cargo tree -p sqlx`
  - `rg -n "pg_advisory_xact_lock|sqlx::test|anonymous_sessions" E:\\koko\\src E:\\koko\\tests`
  - `Get-ChildItem E:\koko\学习 -Recurse -File`
- 候选实现：
  - `sha2`
  - `time`
  - 仓库现有 `sqlx` + `Postgres` + advisory lock 事务模式
  - 仓库现有 `sqlx::test`
- 对比维度摘要：
  - token 指纹：直接复用 `sha2`，不手写哈希。
  - 3 天失活窗口：直接复用 `time` 表达窗口长度，不新造时间工具。
  - 并发互斥：复用仓库现有 `sqlx + Postgres advisory lock` 模式，不新增内存锁或第二套同步设施。
  - 测试数据库：继续复用 `sqlx::test`，不重复实现生命周期管理。
  - “后台单有效会话 + token 轮换立即失效 + probe 不续命 + 单真相行”属于项目自己的业务真相，成熟通用库不直接提供，因此只补一层薄的 Postgres adapter。
- 最终结论：复用成熟通用能力，业务特有的后台会话真相仅以最小 SQLx adapter 落地；Task 3 明确不提前引入 `tower-sessions` / `tower-sessions-sqlx-store`。

## 边界风险
- 当前 `app::AdminSessionPort` 还不能表达 `touch_last_seen` 与当前 token 指纹，因此本次只在 `PgStore` 落 helper，不提前硬接到 application / HTTP。

## 冗余 / 重复实现风险
- 未新增私有 session framework。
- 未重复实现测试数据库生命周期。
- 新增 SQL 只承接后台单会话真相，不与匿名会话表重复建第二套用户会话系统。

## 验证
- 计划命令：
  - `cargo test --test admin_flow admin_session_rotation_replaces_previous_session`
  - `cargo test --test admin_flow admin_session_expires_after_three_idle_days`
  - `cargo test --test admin_flow admin_session_becomes_invalid_after_admin_token_rotation`
  - `cargo test --test admin_flow admin_session_probe_does_not_refresh_last_seen`
  - `cargo test --test admin_flow concurrent_admin_logins_leave_only_one_active_session`
- 实际命令：
  - `$env:DATABASE_URL='postgres://postgres:postgres@127.0.0.1:5432/postgres'; cargo test --test admin_flow admin_session_rotation_replaces_previous_session`
  - `$env:DATABASE_URL='postgres://postgres:postgres@127.0.0.1:5432/postgres'; cargo test --test admin_flow admin_session_expires_after_three_idle_days`
  - `$env:DATABASE_URL='postgres://postgres:postgres@127.0.0.1:5432/postgres'; cargo test --test admin_flow admin_session_becomes_invalid_after_admin_token_rotation`
  - `$env:DATABASE_URL='postgres://postgres:postgres@127.0.0.1:5432/postgres'; cargo test --test admin_flow admin_session_probe_does_not_refresh_last_seen`
  - `$env:DATABASE_URL='postgres://postgres:postgres@127.0.0.1:5432/postgres'; cargo test --test admin_flow concurrent_admin_logins_leave_only_one_active_session`
  - `$env:DATABASE_URL='postgres://postgres:postgres@127.0.0.1:5432/postgres'; cargo test --test admin_flow`
  - `cargo test --test http_flow app_error_code_exposes_admin_session_codes`
- 未验证风险：
  - 本次没有把 `PgStore` helper 接回 `app::AdminSessionPort` / HTTP 登录路由，这是明确留给后续任务的接线工作。

## 债务结论
- 是否新增债务：否
- 是否延期：是，保留既有接口缺口到后续任务处理
- 债务责任人：仓库主人 / 后续 Task 执行者
- 回看触发点：开始接入后台登录路由或 `AdminSessionPort` 真正落地时
- 最晚处理时机：Task 4 开始前

## 复核要求
- 是否需要独立复核：是
- 独立复核人：仓库主人

## 最终状态
- 通过

## 批准信息
- 批准人：待仓库主人复核
- 例外失效条件：无
- 补审要求：若后续要把 `PgStore` 直接实现为 `app::AdminSessionPort`，必须先补 `touch_last_seen` 与 token 指纹输入的上层契约。
