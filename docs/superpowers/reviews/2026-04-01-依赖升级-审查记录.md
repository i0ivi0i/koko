# 审查记录

## 审查元数据
- 审查时间：2026-04-01 CST
- 仓库路径：`E:\koko`
- 审查模式：repo mode
- 审查主题：依赖全面升级与阻塞点复核
- Git 提交：`未提交`
- 工作树状态：存在与本轮无关的未提交改动；本轮只围绕 `Cargo.toml`、`Cargo.lock` 与新增审查/计划文档推进
- 使用技能：`using-superpowers`、`writing-plans`
- 并行审查：使用多位 subagent 做官方最佳实践、最新版本与本地依赖图复核；最终结论以主工作区实测为准
- 外部校准：已使用 Rust/Cargo 官方文档、crates.io 元信息与 Dioxus 相关官方资料
- 成熟生态复用是否已评估：是
- 总体置信度：高
- 审查最终状态：已完成本轮升级并完成验证

## 审查目标
- 审查仓库当前 Rust 依赖是否明显落后于最新稳定版。
- 在不硬撬被上游生态钉住部分的前提下，把可安全升级的依赖升到最新稳定版。
- 识别升级后仍存在的阻塞点和下一轮最值得继续清理的依赖热点。

## 审查范围
- 依赖与锁文件：`Cargo.toml`、`Cargo.lock`
- 仓库既有升级入口：`scripts/dx-bundle-web.ps1`
- 验证链路：`cargo check`、`cargo check --target wasm32-unknown-unknown`、`cargo nextest run`、`cargo test --doc`、`cargo sqlx prepare --check`、`cargo test`

## 审查依据
- Rust/Cargo 官方建议：
  - 提交 `Cargo.lock` 保证可复现构建
  - 显式声明 `rust-version`
  - 用 `cargo update` / `cargo tree` / `cargo metadata` 做升级与依赖图审查
  - 主线验证使用锁文件，升级检查通过显式更新锁文件完成
- 项目内约束：
  - `审查维护.md` 要求新增依赖/结构性变动必须单独落审查记录
  - 仓库已有 web bundle 合法入口是 `scripts/dx-bundle-web.ps1`

## 总体结论
这仓库并不存在“顶层依赖大面积落后”的情况。真正需要升级的直接依赖主要是 [Cargo.toml](/E:/koko/Cargo.toml) 里的 `reqwest` 与 `axum-extra`，它们现在已经分别推进到 `0.13.x` 与 `0.12.x` 轨道，并在本轮通过 [Cargo.lock](/E:/koko/Cargo.lock) 实际解析到 `reqwest 0.13.2`、`axum-extra 0.12.5`。同时，锁文件也已刷新到当前约束下的最新兼容版本，`wasm-bindgen/web-sys/js-sys` 族同步来到 `0.2.116 / 0.3.93 / 0.3.93`，且主工作区编译、测试和仓库规定的 web bundle 入口全部通过。

## 已完成升级 1
- 标题：直接依赖已补齐到当前可落地的最新稳定轨道
- 结果：
  - [Cargo.toml](/E:/koko/Cargo.toml) 中 `reqwest` 已进入 `0.13` 轨道，并解析到 `0.13.2`
  - [Cargo.toml](/E:/koko/Cargo.toml) 中 `axum-extra` 已进入 `0.12` 轨道，并解析到 `0.12.5`
  - [Cargo.toml](/E:/koko/Cargo.toml) 已显式声明 `rust-version = "1.94.1"`
- 判断：这三项都符合 Rust/Cargo 官方升级治理建议，且没有引入新的分层退化

## 已完成升级 2
- 标题：锁文件已刷新到当前约束允许的最新兼容版本
- 结果：
  - [Cargo.lock](/E:/koko/Cargo.lock) 已更新 `hyper 1.9.0`
  - [Cargo.lock](/E:/koko/Cargo.lock) 已更新 `wasm-bindgen 0.2.116`
  - [Cargo.lock](/E:/koko/Cargo.lock) 已更新 `wasm-bindgen-futures 0.4.66`
  - [Cargo.lock](/E:/koko/Cargo.lock) 已更新 `js-sys 0.3.93`
  - [Cargo.lock](/E:/koko/Cargo.lock) 已更新 `web-sys 0.3.93`
- 判断：这次不是只改 manifest；锁文件和实际解析版本已同步向前推进

## 仍需后续治理的观察项
- 标题：仍有两项传递依赖受上游链路限制，未到 crates 当前可用更新
- 现状：
  - `cargo update --verbose` 仍显示 `generic-array 0.14.7 -> 0.14.9`
  - `cargo update --verbose` 仍显示 `matchit 0.8.4 -> 0.8.6`
- 判断：
  - `generic-array 0.14.7` 主要被 `sqlx 0.8.6` 与当前 websocket/tooling 链钉住
  - `matchit 0.8.4` 明确来自 `axum 0.8.8` 上游，不是仓库漏跑升级
- 建议：继续关注 `sqlx` 与 `axum` 的上游发布节奏；当前不建议为这两项手撬 `[patch]`

## 测试与验证结论
- 已运行并通过：
  - `cargo update`
  - `cargo check`
  - `cargo check --target wasm32-unknown-unknown`
  - `cargo nextest run`
  - `cargo test --doc`
  - `cargo sqlx prepare --check`
  - `cargo test`
  - `.\scripts\dx-bundle-web.ps1`
- 结论：
  - 依赖升级后，主工作区、wasm 目标、SQLx 离线元数据、测试集和仓库标准 web bundle 入口均未回归

## 后续建议
1. 下一轮如果继续做“全面升级”，优先目标是继续盯 `sqlx -> generic-array` 与 `axum -> matchit` 这两条上游阻塞链。
2. 在 `dioxus` 发布新的稳定版之前，不要无证据去手撬 `Dioxus -> wasm-bindgen/web-sys/js-sys` 组合；应继续以仓库现有 bundle 脚本为准。
3. 后续依赖升级继续保持当前节奏：先改 manifest，随后 `cargo update`，最后跑完整验证环，而不是只看 `cargo check`。
