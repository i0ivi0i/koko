# 房间发现与进入收口 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让聊天壳的“搜索已存在房间”“完整码直达”“不存在即创建”三条路径在前端交互上闭环，并与已批准的房间 PRD 对齐。

**Architecture:** 保持当前 Rust 后端真相不变，继续复用 `src/app.rs` / `src/store.rs` 已有的搜索与 `join_or_create_room_by_code` 能力，不新增新接口、不改数据库语义。这轮只收口壳层：`src/chat.rs` 负责搜索派生状态，`src/view.rs` 负责分区结果与直达动作 UI，`src/web.rs` 负责把输入态翻译成已有 `/api/rooms/search` 与 `/api/rooms/join` 契约，并把稳定错误码转成中文提示；任何“房间是否存在、该加入还是创建”的真相都继续由后端返回决定。

**Tech Stack:** Rust 2024, Dioxus 0.7, axum 0.8, reqwest, serde, chrono, uuid, SQLx tests, PowerShell, cargo test

---

## File Structure

- `E:/koko/src/chat.rs`
  - 给壳层搜索状态增加最小派生 helper：
    - 当前查询是否已构成完整合法房间码
    - 当前搜索结果中“我已加入”与“可加入房间”的分组视图
  - 复用领域 `RoomCode::new`，不在壳层手抄一套格式规则
- `E:/koko/src/view.rs`
  - 把搜索页改成“搜索 + 直达”混合入口
  - 渲染分区搜索结果
  - 渲染完整房间码主动作
  - 更新本地化文案与空态说明
- `E:/koko/src/web.rs`
  - 承接“提交完整房间码进入”动作
  - 解析 `/api/rooms/join` 与 `/api/rooms/search` 的错误码
  - 把 `invalid_room_code` 与普通网络失败区分成稳定中文提示
- `E:/koko/public/assets/theme.css`
  - 给搜索页补最小样式：
    - 分区标题
    - 主动作区
    - 辅助提示 / 格式错误提示
  - 复用现有 Telegram 风格 token，不另起一套视觉体系
- `E:/koko/tests/http_cases/chat_state.rs`
  - 锁定搜索派生状态，不让 view/web 自己再发明分区判断
- `E:/koko/tests/http_cases/view_render.rs`
  - 锁定搜索页 copy、分区结果、完整码主动作和空态文案
- `E:/koko/tests/http_cases/routes.rs`
  - 补一条 join 接口的非法房间码错误码回归，证明后端已满足前端所需契约
- `E:/koko/docs/superpowers/specs/2026-04-03-房间发现与进入-design.md`
  - 只读参考，不修改；实施时按该 PRD 对照验收

**Boundary note:** 本轮默认不改 `E:/koko/src/app.rs`、`E:/koko/src/store.rs`、数据库迁移和实时主通道。若实施中发现后端契约与 PRD 不符，再单开后续修补，不在本计划里顺手扩散。

**Reuse note:** 壳层判断“完整合法房间码”时必须直接复用 `E:/koko/src/domain.rs` 的 `RoomCode::new`，不允许在 `src/view.rs` / `src/web.rs` 手抄 `1 个字母 + 4 个数字` 的私有校验逻辑。

---

### Task 1: 锁定壳层搜索派生状态

**Files:**
- Modify: `E:/koko/tests/http_cases/chat_state.rs`
- Modify: `E:/koko/src/chat.rs`
- Test: `E:/koko/tests/http_cases/chat_state.rs`

- [ ] **Step 1: 写失败测试，锁定完整码识别与搜索分组**

在 `E:/koko/tests/http_cases/chat_state.rs` 新增最小状态回归：

```rust
#[test]
fn search_query_can_report_when_it_forms_a_complete_room_code() {
    let mut state = ChatState::awaiting_bootstrap();
    state.set_search_query("A1234");
    assert!(state.search_query_forms_complete_room_code());

    state.set_search_query("A12");
    assert!(!state.search_query_forms_complete_room_code());
}

#[test]
fn search_results_split_into_joined_and_discoverable_sections() {
    let mut state = ChatState::awaiting_bootstrap();
    state.apply_search_results(vec![
        RoomSearchResult {
            room_id: Uuid::from_u128(1),
            room_code: "A1234".to_string(),
            display_title: "A1234".to_string(),
            latest_preview: "joined".to_string(),
            latest_message_at: None,
            is_joined: true,
        },
        RoomSearchResult {
            room_id: Uuid::from_u128(2),
            room_code: "A1299".to_string(),
            display_title: "A1299".to_string(),
            latest_preview: "discover".to_string(),
            latest_message_at: None,
            is_joined: false,
        },
    ]);

    assert_eq!(state.joined_search_results().len(), 1);
    assert_eq!(state.discoverable_search_results().len(), 1);
}
```

- [ ] **Step 2: 运行测试确认 RED**

Run:

```powershell
cargo test --test http search_query_can_report_when_it_forms_a_complete_room_code
cargo test --test http search_results_split_into_joined_and_discoverable_sections
```

Expected:

- `ChatState` 还没有这些 helper，编译失败

- [ ] **Step 3: 写最小实现**

在 `E:/koko/src/chat.rs`：

- 引入 `crate::domain::RoomCode`
- 增加一个共享 helper，避免 view/web 再复制规则：

```rust
pub fn query_forms_complete_room_code(query: &str) -> bool {
    RoomCode::new(query).is_ok()
}
```

- 在 `impl ChatState` 中增加：

```rust
pub fn search_query_forms_complete_room_code(&self) -> bool {
    query_forms_complete_room_code(self.search_query())
}

pub fn joined_search_results(&self) -> Vec<RoomSearchResult> {
    self.search
        .results
        .iter()
        .filter(|room| room.is_joined)
        .cloned()
        .collect()
}

pub fn discoverable_search_results(&self) -> Vec<RoomSearchResult> {
    self.search
        .results
        .iter()
        .filter(|room| !room.is_joined)
        .cloned()
        .collect()
}
```

并补一条简短中文注释，说明这些 helper 只服务壳层表现，不是后端业务真相。

- [ ] **Step 4: 运行测试确认 GREEN**

Run:

```powershell
cargo test --test http search_query_can_report_when_it_forms_a_complete_room_code
cargo test --test http search_results_split_into_joined_and_discoverable_sections
cargo test --test http search_input_updates_without_mutating_remote_truth
```

Expected:

- 以上测试 PASS

- [ ] **Step 5: Commit**

```bash
git add src/chat.rs tests/http_cases/chat_state.rs
git commit -m "收口聊天壳搜索派生状态"
```

---

### Task 2: 重做搜索页文案、分区结果与完整码主动作

**Files:**
- Modify: `E:/koko/tests/http_cases/view_render.rs`
- Modify: `E:/koko/src/view.rs`
- Modify: `E:/koko/public/assets/theme.css`

- [ ] **Step 1: 写失败测试，锁定新 UI 结构与 copy**

在 `E:/koko/tests/http_cases/view_render.rs` 新增或改写断言，至少覆盖这三条：

```rust
#[test]
fn join_by_code_groups_joined_and_discoverable_results() {
    let html = render_chat_page(join_by_code_state());

    assert!(html.contains("data-search-section=\"joined\""));
    assert!(html.contains("data-search-section=\"discoverable\""));
    assert!(html.contains(">我已加入<"));
    assert!(html.contains(">可加入房间<"));
}

#[test]
fn join_by_code_empty_state_explains_direct_entry_for_complete_room_code() {
    let mut state = bootstrapped_state();
    state.show_join_by_code();
    state.set_search_query("A1234");
    let html = render_chat_page(state);

    assert!(html.contains("没有已存在的匹配房间"));
    assert!(html.contains("完整房间码可直接进入"));
    assert!(html.contains("进入房间"));
}

#[test]
fn room_search_copy_explains_search_and_direct_entry_together() {
    let html = render_chat_page(join_by_code_state());

    assert!(html.contains("搜索已存在房间"));
    assert!(html.contains("或输入完整房间码直接进入"));
}
```

并把现有 `join_by_code_state()` 改成同时包含已加入结果和陌生结果，避免只测到单分支。

- [ ] **Step 2: 运行测试确认 RED**

Run:

```powershell
cargo test --test http join_by_code_groups_joined_and_discoverable_results
cargo test --test http join_by_code_empty_state_explains_direct_entry_for_complete_room_code
cargo test --test http room_search_copy_explains_search_and_direct_entry_together
```

Expected:

- 现有 SSR 结构没有分区、没有主动作、copy 仍是旧文案，断言失败

- [ ] **Step 3: 写最小 UI 实现**

在 `E:/koko/src/view.rs`：

- 给 `ChatPage` / `JoinByCodeScreen` 新增 prop：

```rust
#[props(default)] on_submit_room_code: Option<EventHandler<()>>,
```

- `JoinByCodeScreen` 里先从 `state` 取：

```rust
let joined_results = state.joined_search_results();
let discoverable_results = state.discoverable_search_results();
let can_submit_room_code = state.search_query_forms_complete_room_code();
```

- 把结果区改成两个 section，仅在对应列表非空时渲染：

```rust
section { "data-search-section": "joined", ... }
section { "data-search-section": "discoverable", ... }
```

- 在搜索框下面加主动作区，只在 `can_submit_room_code` 为真时显示：

```rust
button {
    class: "tg-search__action tg-search__action--primary",
    r#type: "button",
    onclick: move |_| {
        if let Some(handler) = on_submit_room_code.as_ref() {
            handler.call(());
        }
    },
    "进入房间"
}
```

- 更新 join 页标题、副标题和空态文案，让“搜索已存在房间 + 完整码直达”同时成立
- 若查询非空但未构成完整房间码，可显示一条克制 hint，例如：

```rust
"搜索已存在房间，或输入完整房间码直接进入"
```

在 `E:/koko/public/assets/theme.css` 仅补最小样式：

- `.tg-search-panel__actions`
- `.tg-search-section`
- `.tg-search-section__title`
- `.tg-search-panel__hint--error`（若第三任务需要）

禁止为了分区列表再造新的壳布局体系；直接复用现有搜索列表和按钮 token。

- [ ] **Step 4: 运行测试确认 GREEN**

Run:

```powershell
cargo test --test http join_by_code_groups_joined_and_discoverable_results
cargo test --test http join_by_code_empty_state_explains_direct_entry_for_complete_room_code
cargo test --test http room_search_copy_explains_search_and_direct_entry_together
cargo test --test http shell_primary_copy_is_localized_for_chinese_users
```

Expected:

- 以上测试 PASS

- [ ] **Step 5: Commit**

```bash
git add src/view.rs public/assets/theme.css tests/http_cases/view_render.rs
git commit -m "收口房间搜索页文案分区与直达动作"
```

---

### Task 3: 接通完整码直达动作并稳定区分错误提示

**Files:**
- Modify: `E:/koko/tests/http_cases/routes.rs`
- Modify: `E:/koko/src/web.rs`
- Modify: `E:/koko/src/view.rs`

- [ ] **Step 1: 写失败测试，锁定 join 错误码与前端错误文案 helper**

先在 `E:/koko/tests/http_cases/routes.rs` 增加一条 HTTP 回归，证明后端已返回稳定错误码：

```rust
#[sqlx::test]
async fn join_room_endpoint_returns_invalid_room_code_error_payload(
    pool: sqlx::PgPool,
) -> sqlx::Result<()> {
    let harness = HttpHarness::new(pool).await;
    let (_session, cookie) = bootstrap_session_with_cookie(&harness).await;

    let response = harness
        .router
        .clone()
        .oneshot(
            Request::builder()
                .method("POST")
                .uri("/api/rooms/join")
                .header("content-type", "application/json")
                .header(COOKIE, &cookie)
                .body(Body::from(r#"{ "room_code": "ABCDE" }"#))
                .unwrap(),
        )
        .await
        .unwrap();

    assert_eq!(response.status(), StatusCode::BAD_REQUEST);
    let payload: serde_json::Value =
        serde_json::from_slice(&to_bytes(response.into_body(), usize::MAX).await.unwrap()).unwrap();
    assert_eq!(payload["code"], "invalid_room_code");
    Ok(())
}
```

再在 `E:/koko/src/web.rs` 的单元测试里补一条错误文案 helper 测试：

```rust
#[test]
fn room_entry_error_message_prioritizes_stable_error_codes() {
    assert_eq!(
        room_entry_error_message(Some(AppErrorCode::InvalidRoomCode), "bad request"),
        "房间码不合法，请输入 1 个字母 + 4 个数字，例如 A1234"
    );
}
```

- [ ] **Step 2: 运行测试确认 RED**

Run:

```powershell
cargo test --test http join_room_endpoint_returns_invalid_room_code_error_payload
cargo test room_entry_error_message_prioritizes_stable_error_codes
```

Expected:

- `room_entry_error_message` 还不存在，编译失败
- 路由测试应先 PASS 或暴露现有返回不稳的问题；若它先 PASS，保留该测试作为护栏继续往下做

- [ ] **Step 3: 写最小 adapter 实现**

在 `E:/koko/src/web.rs`：

- 定义一个私有 payload：

```rust
#[derive(Debug, Clone, Deserialize)]
struct HttpErrorPayload {
    code: AppErrorCode,
}
```

- 定义一个私有错误类型：

```rust
struct WebRequestError {
    code: Option<AppErrorCode>,
    detail: String,
}
```

- 让 `load_room_search_results` 和 `join_room_by_code` 在非 2xx 时优先解析 JSON 错误码，而不是直接 `error_for_status()`
- 增加 helper：

```rust
fn room_entry_error_message(code: Option<AppErrorCode>, detail: &str) -> String
```

其中至少保证：

- `InvalidRoomCode` -> `房间码不合法，请输入 1 个字母 + 4 个数字，例如 A1234`
- 其他错误回退到现有通用 copy

- 在 `view::ChatPage` 调用里把 `on_submit_room_code` 接上：

```rust
on_submit_room_code: move |_| {
    let query = room_search_query();
    if !crate::chat::query_forms_complete_room_code(&query) {
        set_shell_error(
            shell_error,
            Some("房间码不合法，请输入 1 个字母 + 4 个数字，例如 A1234".to_string()),
        );
        return;
    }

    room_action.set(RoomAction::Join(normalize_room_code_query(&query)));
    set_shell_error(shell_error, None);
}
```

- 若 `RoomAction::Join` 失败，优先用 `room_entry_error_message(...)` 显示，而不是继续只拼 `打开房间失败：...`

这里必须守住边界：

- 前端只负责“是否展示直达动作”和“如何展示错误”
- 具体是加入已有房间还是创建新房间，仍然完全由 `/api/rooms/join` 返回的快照决定

- [ ] **Step 4: 运行测试确认 GREEN**

Run:

```powershell
cargo test room_entry_error_message_prioritizes_stable_error_codes
cargo test --test http join_room_endpoint_returns_invalid_room_code_error_payload
cargo test --test http join_by_code_empty_state_explains_direct_entry_for_complete_room_code
```

Expected:

- 以上测试 PASS

- [ ] **Step 5: Commit**

```bash
git add src/web.rs src/view.rs tests/http_cases/routes.rs
git commit -m "接通完整房间码直达并稳定错误提示"
```

---

### Task 4: 最终回归与边界复核

**Files:**
- Modify: `E:/koko/src/chat.rs`
- Modify: `E:/koko/src/view.rs`
- Modify: `E:/koko/src/web.rs`
- Modify: `E:/koko/public/assets/theme.css`
- Modify: `E:/koko/tests/http_cases/chat_state.rs`
- Modify: `E:/koko/tests/http_cases/view_render.rs`
- Modify: `E:/koko/tests/http_cases/routes.rs`

- [ ] **Step 1: 跑房间壳层 targeted 回归**

Run:

```powershell
cargo test --test http search_query_can_report_when_it_forms_a_complete_room_code
cargo test --test http search_results_split_into_joined_and_discoverable_sections
cargo test --test http join_by_code_groups_joined_and_discoverable_results
cargo test --test http join_by_code_empty_state_explains_direct_entry_for_complete_room_code
cargo test --test http room_search_copy_explains_search_and_direct_entry_together
cargo test room_entry_error_message_prioritizes_stable_error_codes
```

Expected:

- 全部 PASS

- [ ] **Step 2: 跑后端契约护栏**

Run:

```powershell
cargo test --test app search_rooms_by_code_matches_case_insensitive_prefix_and_marks_membership
cargo test --test app join_or_create_room_by_code_reuses_existing_room_without_recreating_it
```

Expected:

- 继续 PASS，证明壳层收口没有倒逼改坏后端真相

- [ ] **Step 3: 跑 HTTP 集成护栏**

Run:

```powershell
$env:DATABASE_URL="postgres://koko:koko_local@127.0.0.1:5432/koko_test"
cargo test --test http room_search_endpoint_returns_case_insensitive_matches
cargo test --test http join_room_endpoint_returns_invalid_room_code_error_payload
Remove-Item Env:DATABASE_URL
```

Expected:

- 若本机测试库可用，则两条 PASS
- 若当前环境没有可用 Postgres，记录为“计划内验证命令已给出，执行时需先提供测试库”

- [ ] **Step 4: 跑工作树与格式化复核**

Run:

```powershell
$env:DATABASE_URL="postgres://koko:koko_local@127.0.0.1:5432/koko_test"
cargo test --test http
Remove-Item Env:DATABASE_URL
git status --short
```

Expected:

- 若本机测试库可用，`cargo test --test http` 全通过；否则保留 Step 3 的定向验证结果并明确记录数据库前提
- `git status --short` 只剩本任务相关改动，无额外格式化噪音

- [ ] **Step 5: Commit**

```bash
git add src/chat.rs src/view.rs src/web.rs public/assets/theme.css tests/http_cases/chat_state.rs tests/http_cases/view_render.rs tests/http_cases/routes.rs
git commit -m "完成房间发现与进入的前端交互收口"
```
