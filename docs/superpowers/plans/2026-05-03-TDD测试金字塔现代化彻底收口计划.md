# TDD 测试金字塔现代化彻底收口 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. Use `superpowers:subagent-driven-development` only if the owner explicitly authorizes subagents. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把当前仓库从“测试现代化主体已完成，但仍有少数 completion blockers”收口到 [2026-05-03-TDD测试金字塔现代化规格.md](/E:/koko/docs/superpowers/specs/2026-05-03-TDD测试金字塔现代化规格.md) 的真实完成态。

**Architecture:** 这次不再重复第一阶段大治理，也不再把独立 `e2e/` 包当成硬要求。根据最新裁决，真实浏览器烟测主链已经正式收口为 `run.ps1 + playwright-cli + trace`。因此本计划只修 2 个代码 blocker：`tests/协作分发测试.rs` 仍大于 `1000` 行，以及媒体协议测试 helper 仍滞留 `graphify` God Nodes。最后用 fresh CLI smoke、全量验证、健康报告和 `graphify update .` 完成闭环。

**Tech Stack:** Rust、Cargo、TypeScript、Vitest、PowerShell、Playwright CLI、graphify。

---

## 0. 当前 blockers

根据最新 spec 与仓库现状，离“彻底完成”还差 2 件事：

1. `tests/协作分发测试.rs` 仍超过 spec 完成态允许的 `1000` 行上限。
2. `提取媒体上传授权头()`、`断言TusHook已接受()`、`构造tus_hook请求体()`、`写入tus测试文件()` 仍在 `graphify-out/GRAPH_REPORT.md` 的 `God Nodes` 里，说明这些 helper 还被太多测试直接依赖。

不再把“缺失 repo-native `e2e/` 包”视为 blocker。真实浏览器主链已经改裁为 `run.ps1 + playwright-cli + trace`，并且最终验证必须走这条链。

## 1. 文件责任图

### 新增文件

| 文件 | 责任 |
| --- | --- |
| `docs/superpowers/plans/2026-05-03-TDD测试金字塔现代化彻底收口计划.md` | 本次彻底收口计划；只覆盖最后 2 个代码 blocker 与最终验证 |
| `tests/协作分发测试/切片支撑.rs` | 协作分发顶层集成测试的 slice-local arrange 支撑；只负责 tracker 上游、测试应用、join_ticket、受控 URL 归一化 |
| `tests/协作分发测试/locator与torrent合同.rs` | 从 `tests/协作分发测试.rs` 拆出的 locator / torrent / announce / join_ticket 合同测试 |
| `tests/协作分发测试/tracker代理.rs` | 从 `tests/协作分发测试.rs` 拆出的同源 tracker 代理入口与门禁测试 |
| `tests/协作分发测试/上传场景支撑.rs` | 协作分发 slice 对 prepare -> tus hook -> complete -> ready message 的 arrange helper |
| `tests/媒体上传测试/切片支撑.rs` | 媒体上传 slice-local arrange helper；负责 prepare / post-finish / complete 场景装配，不负责业务断言 |

### 修改文件

| 文件 | 责任 |
| --- | --- |
| `scripts/check-test-architecture-fitness.mjs` | 把完成态门禁收紧到当前 spec 字面要求：`>1000` 顶层测试文件直接 fail |
| `docs/superpowers/reports/测试金字塔健康报告.md` | 从旧的“收口完成”描述修正为真实完成态视角，并记录 CLI smoke 主链 |
| `tests/协作分发测试.rs` | 缩成薄 manifest，只保留 `mod` 声明和必要共享引入 |
| `tests/测试支撑/媒体.rs` | 停止作为全局媒体协议总出口；只保留薄支撑，供 slice-local helper 调用 |
| `tests/测试支撑/媒体/tus.rs` | 保留最薄协议 helper，但减少测试文件直接 import 面 |
| `tests/测试支撑/媒体/fixture.rs` | 保留最薄临时文件 fixture，但减少测试文件直接 import 面 |
| `tests/媒体上传测试/*.rs` | 从直接四连调用协议 helper，改为走 `tests/媒体上传测试/切片支撑.rs` 的 arrange helper |
| `tests/协作分发测试/*.rs` | 从直接四连调用协议 helper，改为走 `tests/协作分发测试/上传场景支撑.rs` 的 arrange helper |

---

## Task 1: 把完成态门禁收紧到当前 spec 字面要求

**Files:**
- Modify: `scripts/check-test-architecture-fitness.mjs`
- Modify: `docs/superpowers/reports/测试金字塔健康报告.md`
- Test: `node scripts/check-test-architecture-fitness.mjs --enforce`

- [ ] **Step 1: 读当前 spec 完成态与现有门禁实现**

Run:

```powershell
rg -n "所有 `>1000`|graphify 高连接业务节点|run\\.ps1 \\+ playwright-cli" docs/superpowers/specs/2026-05-03-TDD测试金字塔现代化规格.md
Get-Content scripts/check-test-architecture-fitness.mjs
Get-Content docs/superpowers/reports/测试金字塔健康报告.md
```

Expected: 明确“现在的脚本还停留在第一阶段 1200/1000 预算，不等于 spec 完成态”。

- [ ] **Step 2: 先写 RED 规则，只针对真实 blocker**

在 `scripts/check-test-architecture-fitness.mjs` 里加入完成态顶层上限检查：

```javascript
const 完成态顶层文件硬上限 = 1000;

function 收集完成态顶层超限(relativePath, lines) {
  if (!是Rust顶层集成测试(relativePath) && !relativePath.startsWith("frontend/tests/")) {
    return [];
  }
  if (lines.length <= 完成态顶层文件硬上限) {
    return [];
  }
  return [{
    file: relativePath,
    label: "不满足 spec 完成态",
    detail: `${relativePath}: ${lines.length} lines > ${完成态顶层文件硬上限}`,
  }];
}

const violations = records.flatMap(record => [
  ...收集行数违规(record.relativePath, record.lineArray),
  ...收集完成态顶层超限(record.relativePath, record.lineArray),
  ...收集only违规(record.relativePath, record.lineArray),
  ...收集skip违规(record.relativePath, record.lineArray),
]);
```

不要再加入“缺失 `e2e/` 包就 fail”的规则，因为最新 spec 已经不要求这个。

- [ ] **Step 3: 运行 RED，确认脚本真的抓到 `tests/协作分发测试.rs`**

Run:

```powershell
node scripts/check-test-architecture-fitness.mjs --enforce
```

Expected: FAIL，且至少包含：

```text
tests/协作分发测试.rs
```

如果还继续绿，说明完成态门禁仍然没锁死。

- [ ] **Step 4: 修正健康报告当前状态**

修改 `docs/superpowers/reports/测试金字塔健康报告.md`，至少做到：

1. `状态` 不再写 `收口完成`。
2. `当前结论` 明确写出还剩 2 个 blockers。
3. 真实浏览器烟测部分明确写：当前权威主链是 `run.ps1 + playwright-cli + trace`，不是独立 `e2e/` 包。

示意：

```markdown
## 当前 completion blockers

1. `tests/协作分发测试.rs` 仍高于 1000 行。
2. 媒体协议 helper 仍在 graphify God Nodes 中。
```

- [ ] **Step 5: 验证本任务**

Run:

```powershell
node scripts/check-test-architecture-fitness.mjs --enforce
git diff --check
```

Expected: 脚本 FAIL 是正确的 RED；`git diff --check` PASS。

- [ ] **Step 6: Commit**

```powershell
git add scripts/check-test-architecture-fitness.mjs docs/superpowers/reports/测试金字塔健康报告.md
git commit -m "收紧测试现代化当前完成态门禁"
```

---

## Task 2: 把 `tests/协作分发测试.rs` 拆到 1000 行以下

**Files:**
- Create: `tests/协作分发测试/切片支撑.rs`
- Create: `tests/协作分发测试/locator与torrent合同.rs`
- Create: `tests/协作分发测试/tracker代理.rs`
- Modify: `tests/协作分发测试.rs`
- Test: `cargo test --test 协作分发测试`

- [ ] **Step 1: Characterization**

Run:

```powershell
cargo test --test 协作分发测试
```

Expected: PASS，作为拆分前基线。

- [ ] **Step 2: 搬迁顶层共享 helper 到 slice-local 支撑**

Create `tests/协作分发测试/切片支撑.rs`:

```rust
pub(crate) struct 测试服务器 { /* 从顶层直接搬迁 */ }
pub(crate) struct 假Tracker上游 { /* 从顶层直接搬迁 */ }

pub(crate) async fn 启动假tracker上游() -> 假Tracker上游 { /* 原实现 */ }
pub(crate) async fn 启动协作分发测试应用(...) -> 测试服务器 { /* 原实现 */ }
pub(crate) fn 签发测试join_ticket(...) -> String { /* 原实现 */ }
pub(crate) fn 构造tracker首帧(...) -> String { /* 原实现 */ }
pub(crate) async fn 发送tracker首帧(...) { /* 原实现 */ }
pub(crate) fn 归一化受控地址(url: &str) -> String { /* 原实现 */ }
```

这一步只搬 helper，不改测试逻辑，不做语义清理。

- [ ] **Step 3: 拆 locator / torrent / announce 合同测试**

Create `tests/协作分发测试/locator与torrent合同.rs`，移动这些测试：

```text
locator会返回协作分发片段但不泄漏仓储私货
同一视频对发送者与群友返回同一套流媒体主链真相
图片locator会返回blob_asset而不是只给original_url
torrent接口会返回稳定metainfo并与locator对齐
locator会返回announce_web_seed与短时join_ticket
未显式配置tracker公网地址时locator会优先返回同源相对announce路径
```

Run:

```powershell
cargo test --test 协作分发测试 locator会返回协作分发片段但不泄漏仓储私货
cargo test --test 协作分发测试 torrent接口会返回稳定metainfo并与locator对齐
```

Expected: PASS。

- [ ] **Step 4: 拆 tracker 代理测试**

Create `tests/协作分发测试/tracker代理.rs`，移动这些测试：

```text
同源tracker代理入口会响应websocket握手而不是404
同源tracker代理首帧缺少join_ticket会拒绝而不是放行到tracker
同源tracker代理首帧join_ticket的info_hash不匹配会拒绝
同源tracker代理首帧join_ticket有效会放行到tracker_upstream
```

Run:

```powershell
cargo test --test 协作分发测试 同源tracker代理入口会响应websocket握手而不是404
cargo test --test 协作分发测试 同源tracker代理首帧join_ticket有效会放行到tracker_upstream
```

Expected: PASS。

- [ ] **Step 5: 把顶层文件缩成薄 manifest**

`tests/协作分发测试.rs` 最终只保留：

```rust
#[path = "协作分发测试/可用性裁决.rs"]
mod availability_ruling_tests;
#[path = "协作分发测试/内容读取.rs"]
mod content_read_tests;
#[path = "协作分发测试/分发元数据.rs"]
mod distribution_metadata_tests;
#[path = "协作分发测试/投影一致性.rs"]
mod projection_consistency_tests;
#[path = "协作分发测试/locator与torrent合同.rs"]
mod locator_and_torrent_contract_tests;
#[path = "协作分发测试/tracker代理.rs"]
mod tracker_proxy_tests;
#[path = "协作分发测试/切片支撑.rs"]
mod distribution_slice_support;
```

不允许顶层继续夹带长段 inline 测试。

- [ ] **Step 6: 验证本任务**

Run:

```powershell
cargo test --test 协作分发测试
node scripts/check-test-architecture-fitness.mjs --enforce
git diff --check
```

Expected:

1. `cargo test` PASS。
2. 门禁不再因为 `tests/协作分发测试.rs` 报超限。
3. `git diff --check` PASS。

- [ ] **Step 7: Commit**

```powershell
git add tests/协作分发测试.rs tests/协作分发测试
git commit -m "拆分协作分发顶层集成测试"
```

---

## Task 3: 给媒体协议 helper 降温，退出 graphify God Nodes

**Files:**
- Create: `tests/媒体上传测试/切片支撑.rs`
- Create: `tests/协作分发测试/上传场景支撑.rs`
- Modify: `tests/测试支撑/媒体.rs`
- Modify: `tests/测试支撑/媒体/tus.rs`
- Modify: `tests/测试支撑/媒体/fixture.rs`
- Modify: `tests/媒体上传测试/*.rs`
- Modify: `tests/协作分发测试/*.rs`
- Test: `cargo test --test 媒体上传测试 --test 协作分发测试`

- [ ] **Step 1: 读当前高连接 helper 的直接调用面**

Run:

```powershell
rg -n "提取媒体上传授权头|断言TusHook已接受|构造tus_hook请求体|写入tus测试文件" tests -g "*.rs"
Get-Content tests/测试支撑/媒体.rs
Get-Content tests/测试支撑/媒体/tus.rs
Get-Content tests/测试支撑/媒体/fixture.rs
```

Expected: 明确调用面主要集中在 `媒体上传测试` 和 `协作分发测试` 两个 slice。

- [ ] **Step 2: 先写 slice-local arrange helper，不动业务断言**

Create `tests/媒体上传测试/切片支撑.rs`:

```rust
pub(crate) struct 已准备上传场景 {
    pub attachment_id: String,
    pub upload_id: String,
    pub authorization: String,
    pub temp_file: String,
}

pub(crate) async fn 准备图片post_finish场景(...) -> 已准备上传场景 { /* arrange only */ }
pub(crate) async fn 准备视频post_finish场景(...) -> 已准备上传场景 { /* arrange only */ }
pub(crate) async fn 发送post_finish并断言协议接受(...) { /* 仅协议层 OK + {} */ }
```

Create `tests/协作分发测试/上传场景支撑.rs`:

```rust
pub(crate) struct Ready视频消息场景 {
    pub session_id: String,
    pub room_id: String,
    pub attachment_id: String,
}

pub(crate) async fn 创建ready视频消息场景(...) -> Ready视频消息场景 { /* arrange only */ }
```

这里允许封装重复 Arrange，但不允许把 locator / torrent / 消息业务断言藏进去。

- [ ] **Step 3: 收窄公共媒体协议出口**

把 `tests/测试支撑/媒体.rs` 从“全测试可随手 import 的总出口”改成“给 slice-local helper 用的薄出口”：

```rust
pub(crate) use fixture::{写入tus测试文件, 最小mp4字节, 最小png字节, 最小webp字节};
pub(crate) use tus::{提取媒体上传授权头, 断言TusHook已接受, 构造tus_hook请求体};
```

如果需要保留其他导出，也必须说明它们仍然是协议/fixture 薄出口，而不是第二业务层。

- [ ] **Step 4: 批量替换直接四连调用**

优先替换这些文件：

```text
tests/媒体上传测试/complete_图片与回执竞争.rs
tests/媒体上传测试/complete_视频与类型守卫.rs
tests/媒体上传测试/tus回调_协议入口与pre_create.rs
tests/媒体上传测试/tus回调_post_finish与terminate.rs
tests/媒体上传测试/source_hash.rs
tests/媒体上传测试/abandon.rs
tests/协作分发测试/内容读取.rs
tests/协作分发测试/分发元数据.rs
tests/协作分发测试/投影一致性.rs
tests/协作分发测试/locator与torrent合同.rs
```

替换规则：

1. `提取媒体上传授权头 + 写入tus测试文件 + 构造tus_hook请求体 + 断言TusHook已接受` 这组重复 Arrange，改成 1 个 slice-local helper。
2. 业务断言继续保留在测试本体。
3. 不新增“超大万能上传 helper”。

- [ ] **Step 5: Characterization and GREEN**

Run:

```powershell
cargo test --test 媒体上传测试
cargo test --test 协作分发测试
```

Expected: PASS。

- [ ] **Step 6: 用 graphify 直接验证降温结果**

Run:

```powershell
graphify update .
Get-Content graphify-out/GRAPH_REPORT.md -TotalCount 50
```

Expected: `Graph Report` 顶部 `God Nodes` 区块里不再出现这 4 个 helper。  
如果仍在 Top 10，继续减少测试文件对它们的直接 import，直到退出。

- [ ] **Step 7: Commit**

```powershell
git add tests/测试支撑/媒体.rs tests/测试支撑/媒体 tests/媒体上传测试 tests/协作分发测试 graphify-out
git commit -m "给媒体测试协议支撑降温收口"
```

---

## Task 4: 最终验证、CLI smoke 证据和健康报告收口

**Files:**
- Modify: `docs/superpowers/reports/测试金字塔健康报告.md`
- Test: project-wide verification

- [ ] **Step 1: 先准备 fresh 真实浏览器 smoke**

使用项目批准链路，不建 repo 内固定 smoke 脚本：

```powershell
powershell -ExecutionPolicy Bypass -File .\run.ps1 -DisableLocalHttpsBootstrap -DisableAutoOptimizeCleanup
playwright-cli close-all
playwright-cli open http://127.0.0.1:8080/
playwright-cli tracing-start
playwright-cli snapshot --filename=tddspec-home.yaml
```

Expected: 本地栈可用，首页能看到 `房间短码` 输入框和 `进房` 按钮。

- [ ] **Step 2: 运行进房 + 发消息 smoke**

Run:

```powershell
playwright-cli fill "getByRole('textbox', { name: '房间短码' })" "TDD503R"
playwright-cli click "getByRole('button', { name: '进房' })"
playwright-cli fill "getByRole('textbox', { name: '输入消息' })" "spec-smoke-<timestamp>"
playwright-cli click "getByRole('button', { name: '发送' })"
playwright-cli snapshot --filename=tddspec-after-send.yaml
playwright-cli console error
playwright-cli network
```

Expected:

1. 发送后的快照里能看到房间短码和新消息。
2. `console error` 为 `0`。
3. `network` 至少包含 `bootstrap`、`join-or-create`、`read-anchor` 的 `200`。

- [ ] **Step 3: 运行恢复链 smoke**

Run:

```powershell
playwright-cli close-all
playwright-cli open about:blank
playwright-cli run-code "async page => {
  await page.addInitScript(() => {
    const raw = window.localStorage.setItem.bind(window.localStorage);
    window.localStorage.setItem = ((key, value) => {
      if (String(key).includes('koko_current_room')) {
        throw new DOMException('quota', 'QuotaExceededError');
      }
      return raw(key, value);
    });
  });
  await page.goto('http://127.0.0.1:8080/');
}"
playwright-cli fill "getByRole('textbox', { name: '房间短码' })" "TDD503Q"
playwright-cli click "getByRole('button', { name: '进房' })"
playwright-cli snapshot --filename=tddspec-recovery.yaml
playwright-cli console error
```

Expected: 能成功进入房间输入态，页面不出现“恢复失败”。

- [ ] **Step 4: 收尾浏览器证据**

Run:

```powershell
playwright-cli eval "() => globalThis.__kokoBudgetSnapshot ? globalThis.__kokoBudgetSnapshot() : null"
playwright-cli tracing-stop
playwright-cli close-all
```

Expected: 预算快照可读；trace 成功落盘。

- [ ] **Step 5: 跑最终全量验证**

Run:

```powershell
node scripts/check-test-architecture-fitness.mjs --enforce
cargo clippy --all-targets -- -D warnings
cargo test
pnpm --dir frontend typecheck
pnpm --dir frontend test
pnpm --dir frontend build
graphify update .
git -c core.quotepath=false diff --check
git -c core.quotepath=false status --short
```

Expected:

1. 所有命令 PASS。
2. `status --short` 只剩待提交改动；提交后必须干净。
3. `graphify-out/GRAPH_REPORT.md` 顶部 `God Nodes` 不再包含那 4 个媒体协议 helper。

- [ ] **Step 6: 更新健康报告为真实完成态**

`docs/superpowers/reports/测试金字塔健康报告.md` 必须至少写清：

1. 当前最大测试文件中，`tests/协作分发测试.rs` 已降到 `1000` 行以下。
2. 媒体协议 helper 已退出 `God Nodes`。
3. 真实浏览器主链使用的是 `run.ps1 + playwright-cli + trace`。
4. fresh smoke 的房间短码、消息文本、trace 文件、console/network 结果。

不要再写“独立 `e2e/` 包缺失所以未完成”。

- [ ] **Step 7: 对照 spec 完成态逐条打勾**

手工核对 [规格](/E:/koko/docs/superpowers/specs/2026-05-03-TDD测试金字塔现代化规格.md:524) 的 10 条完成条件，并把每条证据写进健康报告。

- [ ] **Step 8: Commit**

```powershell
git add docs/superpowers/reports/测试金字塔健康报告.md scripts tests graphify-out
git commit -m "完成TDD测试金字塔现代化彻底收口"
```

---

## 2. 执行顺序裁决

这次必须按下面顺序做，不能乱序：

1. 先把门禁收紧到最新 spec 的字面要求。
2. 先拆 `tests/协作分发测试.rs`，让 `>1000` 顶层文件这个 blocker 消失。
3. 再给媒体协议 helper 降温，因为这一步最容易牵出批量测试重跑和 graphify 迭代。
4. 最后再跑 fresh CLI smoke、更新健康报告、做全量验证和提交。

原因：真实浏览器主链方向已经裁定，不再是 blocker；现在真正会让完成态继续失真的，只剩顶层巨型文件和 helper hotspot 两件事。

## 3. 自审记录

### Round 1 - 需求意图

检查点：这份计划是否只围绕最新裁决后的真实 blocker，而不是继续把 `e2e/` 包当成硬条件。

结论：通过。计划已从旧版的 3 个 blocker 收紧成 2 个代码 blocker，并把真实浏览器主链固定为 `run.ps1 + playwright-cli + trace`。
修正点：删掉了旧版里“建立 repo-native `e2e/` 层”的整段任务。

### Round 2 - 架构边界

检查点：这份计划是否避免把“helper 降温”做成新的万能测试层。

结论：通过。计划要求 `tests/媒体上传测试/切片支撑.rs` 和 `tests/协作分发测试/上传场景支撑.rs` 只做 slice-local arrange，业务断言仍留在测试本体。  
修正点：明确 `tests/测试支撑/媒体.rs` 只能收窄成薄出口，不能继续当全局媒体协议总线。

### Round 3 - 执行闭环

检查点：这份计划能否真实证明“spec 内容都彻底完成”，而不是只改几句文案。

结论：通过。计划以完成态门禁 RED 开局，拆顶层 Rust 测试、给 helper 降温、最后用 fresh CLI smoke + 全量验证 + graphify + 干净工作树闭环。
修正点：把最终 smoke 从固定 repo 脚本改成显式 `playwright-cli` 命令链，避免再把高频变化业务绑死在仓库内测试脚本上。
