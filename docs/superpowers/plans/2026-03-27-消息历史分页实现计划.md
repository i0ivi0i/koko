# 消息历史分页实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把房间消息历史从全量返回改成最新页加载 + 向上加载更老消息的一阶段分页。

**Architecture:** 契约层增加最小分页参数，仓储层按消息锚点分页读取，前端状态层持有 `has_more` 与最早消息锚点。只改现有文件，不新增 `.rs` 文件。

**Tech Stack:** Rust, axum, sqlx, Dioxus, SSR tests

---

### Task 1: 契约层分页模型

**Files:**
- Modify: `D:/100-工作/200-交易/量化交易/koko/contract/src/lib.rs`

- [x] Step 1: 先写失败测试，约束消息历史请求/响应包含分页字段
- [x] Step 2: 跑 `cargo test -p koko-contract`
- [x] Step 3: 最小实现 `RoomMessagesQuery` / `has_more`
- [x] Step 4: 再跑 `cargo test -p koko-contract`

### Task 2: 后端分页查询

**Files:**
- Modify: `D:/100-工作/200-交易/量化交易/koko/server/src/message_repo.rs`
- Modify: `D:/100-工作/200-交易/量化交易/koko/server/src/http.rs`

- [x] Step 1: 先写失败测试，覆盖最新页和 `before_message_id` 两种读取
- [x] Step 1.1: 补同时间戳锚点分页测试，固定 `(created_at, id)` 排序语义
- [x] Step 1.2: 补非法/跨房间 `before_message_id` 返回 `400` 测试
- [x] Step 2: 跑 `cargo test -p koko-server`
- [x] Step 3: 最小实现分页 SQL 和 HTTP 查询参数解析
- [x] Step 4: 再跑 `cargo test -p koko-server`

### Task 3: 前端分页状态与客户端调用

**Files:**
- Modify: `D:/100-工作/200-交易/量化交易/koko/web/src/client.rs`
- Modify: `D:/100-工作/200-交易/量化交易/koko/web/src/state.rs`
- Modify: `D:/100-工作/200-交易/量化交易/koko/web/src/app.rs`
- Modify: `D:/100-工作/200-交易/量化交易/koko/web/src/chat.rs`

- [x] Step 1: 先写失败测试，约束“初始最新页 + prepend 更老消息 + 不重复”
- [x] Step 2: 跑 `cargo test -p koko-web`
- [x] Step 3: 最小实现分页请求与状态合并
- [x] Step 4: 再跑 `cargo test -p koko-web`

### Task 4: 整体验证与收尾

**Files:**
- Modify: 如测试需要的现有文件

- [x] Step 1: 跑 `cargo test -p koko-contract`
- [x] Step 2: 跑 `cargo test -p koko-server`
- [x] Step 3: 跑 `cargo test -p koko-web`
- [x] Step 4: 跑 `cargo check -p koko-web --target wasm32-unknown-unknown`
- [ ] Step 5: 提交中文 commit
