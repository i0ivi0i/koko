# Video.js 唯一播放器真相收口 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` or `supxcode` when implementing this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把群聊时间线自动播放、点击进入全屏、退出全屏回到时间线，统一收口到同一颗 `Video.js v10` 播放器真相，彻底删除“当前 owner 走原生 `<video>` 直达、其他视频走查看器 `Video.js`”这条双轨分流。

**Architecture:** 本轮不是继续修补“当前 owner 直达全屏”的旁路，而是把它收回到唯一 canonical `Video.js v10` player/container。时间线、沉浸、全屏只是同一颗播放器在不同宿主槽位之间移动和升格；`playback.src / currentTime / autoplay owner / WebTorrent 主链` 继续只有一套业务真相。更关键的是，这颗 singleton player 的生命周期 owner 只能有一个明确模块，不能再由 `房间消息窗.ts / 聊天壳.ts / 媒体查看器.ts / 查看器会话协作.ts` 分层共管。

**Tech Stack:** TypeScript, Lit, existing media runtime / media sessions, `Video.js v10`, native Fullscreen API, Vitest, Playwright CLI / Chrome DevTools CLI.

---

## 0. 为什么要重写计划

这份计划**替代** [2026-04-25-当前自动播视频直达全屏修复计划.md](E:/koko/docs/superpowers/plans/2026-04-25-%E5%BD%93%E5%89%8D%E8%87%AA%E5%8A%A8%E6%92%AD%E8%A7%86%E9%A2%91%E7%9B%B4%E8%BE%BE%E5%85%A8%E5%B1%8F%E4%BF%AE%E5%A4%8D%E8%AE%A1%E5%88%92.md)。

旧计划的问题不是“步骤写得不够细”，而是**前提错了**：

1. 它默认接受“当前自动播 owner 可以绕开 `Video.js`，直接拿时间线原生 `<video>` 进全屏”。
2. 这样虽然解决了“点当前热视频更快”，却把系统做成了两种播放器真相：
   - 当前 owner：原生 `<video>` 直达全屏
   - 非当前 owner：`Video.js` 查看器冷开
3. 这已经在真实房间 `1234b` 冒烟中被证明是活分流，而不是纸面风险。

真实证据已经明确：

1. 同一份视频字节会因为 `inlineAutoplayOwnerAttachmentId` 不同，走两种播放器路径。
2. 分流点就在 [E:/koko/frontend/房间消息窗.ts:1115](E:/koko/frontend/房间消息窗.ts:1115) 到 [E:/koko/frontend/房间消息窗.ts:1156](E:/koko/frontend/房间消息窗.ts:1156)。
3. 当前测试还把这条分流写死了：
   - [E:/koko/frontend/tests/房间消息窗媒体查看器测试.spec.ts:2943](E:/koko/frontend/tests/房间消息窗媒体查看器测试.spec.ts:2943)
   - [E:/koko/frontend/tests/房间消息窗媒体查看器测试.spec.ts:2966](E:/koko/frontend/tests/房间消息窗媒体查看器测试.spec.ts:2966)
   - [E:/koko/frontend/tests/聊天壳测试.spec.ts:901](E:/koko/frontend/tests/聊天壳测试.spec.ts:901)

所以本轮不是继续优化那条旁路，而是直接把它删掉，回到唯一真相：

**全应用只有一颗活着的 `Video.js v10` player/container。**

但这里必须先过一个架构决策闸门，防止执行时再自相矛盾：

1. `Video.js v10` 能 seek 到某个时间点并暂停在该帧，这没有问题。
2. 但**同一颗全局唯一 live player**，不能在同一时刻给多条可见消息卡片同时提供各自独立、真实、活着的暂停视频帧。
3. 所以“全局唯一 live `Video.js` player”与“每条非 owner 卡片继续保持自己的独立 live `<video>` 暂停帧”不能同时作为正式真相成立。
4. 本计划选择前者：**严格收口到全局唯一 live `Video.js` player/container**。
5. 因此，spec 里凡是把“非 owner 卡片也继续拥有自己的活 `<video>` 暂停帧”写成正式真相的句子，都必须退场或改写成“只读帧投影 / 宿主槽位表达”，不能继续保留双真相。

官方依据也支持这条方向：

1. Video.js 文档明确把 `Player.Container` 定义为播放器的物理表面，fullscreen 应该作用在 container 上，而不是到处临时再长一颗新 video。  
   来源：[media-container](https://videojs.org/docs/framework/html/reference/player-container)
2. Video.js fullscreen 特性明确写的是“先尝试 container，再回退到 media element”。  
   来源：[Fullscreen](https://videojs.org/docs/framework/html/reference/feature-fullscreen)
3. Video.js 的“打开 modal 时创建、关闭时 dispose”建议，适用于真正冷开的 modal player，不适用于当前已经热着的时间线播放器表面。  
   来源：[Player Workflows](https://videojs.org/guides/player-workflows/)

---

## 1. 最终裁决

这次实现前先把红线写死：

1. **禁止** 当前自动播 owner 继续走原生 `<video>` 直达全屏。
2. **禁止** 非当前 owner 继续走独立 `Video.js` 查看器，而当前 owner 走另一套播放器。
3. **禁止** 时间线里同时存在“活的原生播放 surface”和“活的 `Video.js` viewer surface”两套正式视频播放器真相。
4. **禁止** 为了兼容旧路径，保留“原生 `<video>` 直达全屏”和“`Video.js` 冷开查看器”双活并存。
5. **禁止** 把 DOM 元素、浏览器表面句柄或壳层细节塞进共享 `媒体查看器打开请求` 契约。
6. **禁止** 为了实现唯一 player 真相，再长一套新的 manager / service / registry 私有框架。
7. **禁止** 伤到 WebTorrent 主链、自动播放续播位置真相、帮助任务和做种语义。
8. **禁止** 把“非 owner 卡片也要有画面”偷换成“非 owner 卡片也继续拥有自己的 live `<video>` 播放表面”。

允许且推荐的最终形态：

1. 时间线当前自动播 owner 使用同一颗 canonical `Video.js` player/container 挂到消息槽位。
2. 点击当前 owner 时，同一颗 player/container 直接 fullscreen。
3. 点击非当前 owner 时，同一颗 player 只是切换 attachment/source，并移动到目标宿主槽位或沉浸层。
4. 退出 fullscreen 后，同一颗 player 回到时间线宿主槽位，继续同一条 `currentTime`。
5. 非 owner 卡片如果还需要保留画面，只能是**不拥有独立 live player 真相**的只读帧投影，不能退化回第二颗正式播放器。

---

## 2. 影响文件与职责

**先改文档，再改测试，再改实现。**

### 文档

- Modify: `docs/superpowers/specs/2026-04-25-视频唯一播放与自动播视觉连续性.md`
  - 现有 spec 还保留“时间线轻量 `<video>` + 查看器 Video.js”的旧裁决，需要先改成“唯一 `Video.js` player/container 真相”。

### 壳层与编排

- Modify: `frontend/房间消息窗.ts`
  - 删除当前 `inlineAutoplayOwnerAttachmentId -> 原生 <video> 直达全屏` 分流。
  - 改成只表达“哪个消息槽位愿意承接 canonical player”。
- Modify: `frontend/聊天壳.ts`
  - 保留 `MEDIA_OPEN_REQUESTED` 作为唯一显式打开媒体入口。
  - 增加壳层私有的宿主槽位接管编排，不改共享 contract。
- Modify: `frontend/聊天媒体编排.ts`
  - 让“点击当前/非当前视频”的编排都指向同一个 canonical player owner。
  - 禁止当前热视频点击再触发 `ENTER_RECOVERING` 冷开链。

### 播放器与查看器

- Add: `frontend/媒体/全局唯一播放器.ts`
  - 作为 singleton `Video.js v10` player/container 的**唯一生命周期 owner**。
  - 只持有：唯一实例、当前宿主槽位、fullscreen 前后归位、接管 attachment/source 的最小状态。
  - 不持有：locator / WebTorrent / 自动播候选裁决 / 第二套业务命令 / 第二套时间戳缓存。
- Modify: `frontend/媒体/媒体查看器.ts`
  - 从“默认创建一个 viewer player”收口到“调用 `全局唯一播放器.ts` 接管或冷开同一颗 canonical Video.js player”。
- Modify: `frontend/媒体/videojs播放器壳.ts`
  - 只保留 `Video.js` 壳能力；宿主迁移的生命周期判断回到 `全局唯一播放器.ts`。
- Modify: `frontend/媒体/壳层/查看器会话协作.ts`
  - 继续负责“请求如何贴齐播放真相”，但不再把当前热会话强行做成 `recovering` 冷开。
- Modify: `frontend/媒体/媒体播放.ts`
  - 复核 `surface = "viewer" | "inline_autoplay"` 的现有设计，必要时收口为“同一 canonical player 的展示投影”，不新增第三种 surface。

### 测试

- Modify: `frontend/tests/房间消息窗媒体查看器测试.spec.ts`
- Modify: `frontend/tests/聊天壳测试.spec.ts`
- Modify: `frontend/tests/聊天媒体编排测试.spec.ts`
- Modify: `frontend/tests/媒体查看器测试.spec.ts`
- Optional Modify: `frontend/tests/聊天应用内核测试.spec.ts`

---

## 3. Task 0: 先过架构决策闸门，消灭文档自相矛盾

**Files:**
- Modify: `docs/superpowers/specs/2026-04-25-视频唯一播放与自动播视觉连续性.md`
- Modify: `docs/superpowers/plans/2026-04-25-VideoJs唯一播放器真相收口计划.md`

- [ ] **Step 1: 把互斥真相写明白**

必须明确写死：

1. “全局唯一 live `Video.js` player/container”与“每条非 owner 卡片继续拥有自己的 live `<video>` 暂停帧”不能同时成立。
2. 本轮执行选择前者。
3. 因此前一版 spec 里凡是把“非 owner 卡片继续维持独立 live `<video>`”写成正式真相的地方，都要退场或改写成“只读帧投影 / 宿主槽位表达”。

- [ ] **Step 2: 把 Telegram 风格状态转换写成产品参考，而不是技术 owner**

必须明确：

1. Telegram 的可借鉴之处是状态转换体验：点当前视频放大、退出回原位、切另一条视频时接管同一条会话。
2. Telegram 不是浏览器端 `Video.js` / Fullscreen API 的技术 authority。
3. 浏览器实现仍以 `Video.js v10 / Chrome / MDN` 的 container-first fullscreen 和媒体 API 为硬边界。

- [ ] **Step 3: grep 自查文档不再同时保留两套真相**

Run:

```powershell
rg -n "轻量 <video>|活跃播放表面|直达全屏|Video.js|暂停预览态" docs/superpowers/specs/2026-04-25-视频唯一播放与自动播视觉连续性.md docs/superpowers/plans/2026-04-25-VideoJs唯一播放器真相收口计划.md
```

Expected:

- 不再同时出现“严格 singleton live player”与“多条非 owner 卡片各有 live `<video>`”两种正式裁决

---

## 4. Task 1: 先改 spec，防止执行漂移

**Files:**
- Modify: `docs/superpowers/specs/2026-04-25-视频唯一播放与自动播视觉连续性.md`

- [ ] **Step 1: 先把旧裁决标出来**

在 spec 中标出这些已经不再成立的旧句子：

1. “时间线继续用轻量 `<video>`，并让非 owner 卡片也继续拥有自己的 live 暂停帧”
2. “查看器 / 全屏继续由唯一 Video.js v10 播放器壳承载，但当前热会话优先直接接管当前活跃播放表面”
3. 所有默认接受“原生 `<video>` 直达全屏”的段落
4. 所有会让“全局唯一 live player”与“多条 live `<video>`”同时成立的段落

- [ ] **Step 2: 写入新的硬裁决**

把 spec 收口成下面这句话的展开版：

```md
群聊里任何正式视频播放都只认同一颗 Video.js v10 player/container。
时间线自动播、点击进入沉浸/全屏、退出后回到消息流，都只是同一颗播放器的宿主槽位切换与 fullscreen 升格。
非 owner 卡片如果还要表达画面，只能是宿主槽位或只读帧投影，不能再拥有独立 live 播放器表面。
```

必须新增“禁止”表述：

1. 禁止当前 owner 保留原生 `<video>` 直达全屏旁路
2. 禁止消息流视频再按 owner / 非 owner 使用不同播放器
3. 禁止双活 player truth
4. 禁止把“非 owner 也要有画面”偷换成“非 owner 也继续拥有自己的 live `<video>`”

- [ ] **Step 3: 自查 spec 是否还残留双轨措辞**

Run:

```powershell
rg -n "轻量 <video>|原生 <video>|直达全屏|Video.js" docs/superpowers/specs/2026-04-25-视频唯一播放与自动播视觉连续性.md
```

Expected:

- 只剩“唯一 Video.js v10 player/container 真相”的一致表述

---

## 5. Task 2: 先写失败测试，钉死当前 P0 根因

**Files:**
- Modify: `frontend/tests/房间消息窗媒体查看器测试.spec.ts`
- Modify: `frontend/tests/聊天壳测试.spec.ts`
- Modify: `frontend/tests/聊天媒体编排测试.spec.ts`
- Modify: `frontend/tests/媒体查看器测试.spec.ts`

- [ ] **Step 1: 写失败测试，证明当前 owner / 非 owner 走两种播放器**

至少新增一组明确失败断言：

```ts
it("当前 owner 与非 owner 点击视频时，必须命中同一颗 canonical Video.js player", async () => {
  expect(document.body.querySelectorAll("video-player[data-player-shell='videojs']")).toHaveLength(1);
});
```

细化断言：

1. 点击当前自动播 owner 后，不允许只对 `video.message-video-preview` 直接 `requestFullscreen`
2. 点击非当前 owner 后，不允许新增第二颗 `video-player[data-player-shell='videojs']`
3. 前后两次点击命中的必须是同一套 player owner，而不是两条分流

- [ ] **Step 2: 写失败测试，证明当前 owner 点击不再允许走原生 `<video>` 旁路**

把现有这类断言改红：

```ts
expect(requestFullscreen.mock.instances.at(0)).toBe(preview);
expect(viewer.打开).not.toHaveBeenCalled();
```

改成新的期望：

1. fullscreen 目标应是 canonical Video.js container 或其挂载容器
2. 当前 owner 点击后也必须经过同一颗 Video.js player 的接管逻辑
3. 但不能新增第二颗 player

- [ ] **Step 3: 写失败测试，证明不会再先 `ENTER_RECOVERING` 再重建 player**

在编排测试中补：

```ts
expect(sendToSession).not.toContainEqual({ type: "ENTER_RECOVERING" });
```

适用场景：

1. 当前自动播 owner 已经热播
2. 用户只是点击想看大
3. 此时只能接管同一条活会话，不能再把它先打回 recovering

- [ ] **Step 4: 写失败测试，证明 `currentTime` 连续**

新增断言：

1. 当前 owner 点击前后不触发新的 `loadstart` / `emptied`
2. `currentTime` 不回到 `0`
3. 退出 fullscreen 后，仍回到原消息槽位继续同一条时间线

- [ ] **Step 5: 写 Telegram 风格状态转换矩阵失败测试**

至少补四组高风险场景：

1. fullscreen 中原时间线宿主槽位被虚拟列表卸载时，不能生成第二颗 player
2. fullscreen 中自动播 owner 变化时，同一颗 canonical player 只能迁移 owner，不能重建
3. 退出 fullscreen 时原槽位已不存在，只允许归位到新的 canonical 宿主或优雅关闭，不能回退第二套查看器
4. 点击另一条非当前视频时，必须是同一颗 canonical player 让位和接管，不能留下原 owner 的活 player

- [ ] **Step 6: 跑定向测试，确认先红**

Run:

```powershell
pnpm --dir frontend test -- tests/房间消息窗媒体查看器测试.spec.ts
pnpm --dir frontend test -- tests/聊天壳测试.spec.ts
pnpm --dir frontend test -- tests/聊天媒体编排测试.spec.ts
pnpm --dir frontend test -- tests/媒体查看器测试.spec.ts
```

Expected:

- 至少一组断言因当前双轨实现而失败

---

## 6. Task 3: 删除 `房间消息窗.ts` 的原生直达全屏旁路

**Files:**
- Modify: `frontend/房间消息窗.ts:1036-1156`

- [ ] **Step 1: 删掉当前原生直达全屏路径**

删除或退役这些符号：

1. `启动时间线视频直达全屏接管(...)`
2. `请求时间线视频直达全屏(...)`
3. `读取当前自动播Owner视频(...)`
4. `尝试直接接管当前自动播视频全屏(...)`

注意：

1. 这是删除旧真相，不是给它再套一层兼容壳
2. 不能保留“以防回滚”的双活分支

- [ ] **Step 2: 改成只上报“打开视频意图 + 当前宿主槽位事实”**

`打开媒体查看器(...)` 改成：

1. 始终发统一的“打开视频”意图
2. 同时只提供壳层私有的宿主槽位上下文
3. 不再自己决定 fullscreen

- [ ] **Step 3: 给删除点补中文注释**

解释为什么：

1. 之前的原生 `<video>` 直达全屏会制造第二套播放器真相
2. 当前文件只能表达消息流宿主槽位，不再持有 fullscreen/player owner

---

## 7. Task 4: 让聊天壳/编排只保留一个显式打开入口，但允许壳层私有接管上下文

**Files:**
- Modify: `frontend/聊天壳.ts`
- Modify: `frontend/聊天媒体编排.ts`

- [ ] **Step 1: 保持 `MEDIA_OPEN_REQUESTED` 为唯一显式入口**

禁止新增：

1. `INLINE_VIDEO_FULLSCREEN_REQUESTED`
2. `CURRENT_OWNER_FULLSCREEN_REQUESTED`
3. 任何第二套业务命令

- [ ] **Step 2: 给壳层增加“当前宿主槽位接管”私有上下文**

要求：

1. 只能存在于 Web 壳内部
2. 不能写进 `媒体查看器打开请求`
3. 不能污染共享 contract

可接受形态：

1. 闭包依赖
2. 壳层私有回调
3. 当前消息槽位注册表

- [ ] **Step 3: `聊天媒体编排.ts` 区分“热接管”与“冷开恢复”**

当前：

```ts
启动查看器起始附件会话(baseRequest);
```

改成：

1. 当前热视频接管路径：跳过 `启动查看器起始附件会话(...)`
2. 普通视频冷开路径：保留旧恢复逻辑

- [ ] **Step 4: 跑编排定向测试**

Run:

```powershell
pnpm --dir frontend test -- tests/聊天媒体编排测试.spec.ts
```

Expected:

- 当前热视频点击时不再触发 `ENTER_RECOVERING`
- 非当前视频冷开路径仍然通过

---

## 8. Task 5: 把 `全局唯一播放器.ts` 收口成唯一 canonical Video.js owner

**Files:**
- Add: `frontend/媒体/全局唯一播放器.ts`
- Modify: `frontend/媒体/媒体查看器.ts`
- Modify: `frontend/媒体/videojs播放器壳.ts`
- Modify: `frontend/媒体/壳层/查看器会话协作.ts`
- Optional Modify: `frontend/媒体/媒体播放.ts`

- [ ] **Step 1: 先把 singleton lifecycle owner 固定到 `全局唯一播放器.ts`**

这个模块是唯一 owner，只允许持有：

1. 唯一 `Video.js` 实例和 container 引用
2. 当前宿主槽位
3. fullscreen 前后的归位状态
4. 当前 attachment/source 接管

其他模块只允许：

1. `媒体查看器.ts` 发起接管意图
2. `videojs播放器壳.ts` 提供壳能力
3. `房间消息窗.ts` 注册宿主槽位
4. `聊天媒体编排.ts / 查看器会话协作.ts` 投影播放真相

- [ ] **Step 2: 先写出接管接口的最小目标**

目标不是再造一个新框架，而是让同一颗 player 能做三件事：

1. 挂到时间线当前 owner 槽位 inline autoplay
2. 从 inline 直接 fullscreen
3. 退出 fullscreen 后回到 inline 槽位

禁止：

1. 第二颗 `Video.js` player
2. 第二个活 `<video>`
3. 新的长期常驻 overlay player

- [ ] **Step 3: `媒体查看器.ts` 优先复用现有 player，而不是默认 `createVideoJsPlayerShell(...)`**

现有代码：

```ts
接管当前查看器(
  generation,
  createVideoJsPlayerShell(startItem, 视口占用生命周期, 运行时钩子)
);
```

改成：

1. 先检查当前是否已经存在 canonical Video.js player
2. 如果存在，执行“宿主槽位迁移 + fullscreen 接管”
3. 只有不存在时，才首次创建

- [ ] **Step 4: `videojs播放器壳.ts` 支持 host/container 迁移**

需要补足但不能过度设计的能力：

1. 读取当前 container
2. 挂接到新的宿主槽位
3. 对当前 container 发 fullscreen
4. fullscreen 退出后恢复到上一个 host

不要做：

1. 自己再发明第二套 player lifecycle manager
2. 自己缓存第二份 `src/currentTime`
3. 保留“旧 host 里的原生 `<video>`”作为兜底活路径

- [ ] **Step 5: `查看器会话协作.ts` 只负责播放真相投影，不再把热会话打冷**

要求：

1. 当前热 owner 的 viewer 打开请求不再被改写成 `src: ""`
2. 不再对热会话走“等待恢复后再开”的逻辑
3. 仍然保留对 expired / degraded / deleted / no_seed 的真相保护

- [ ] **Step 6: 必要时收口 `媒体播放.ts` 的 `surface` 语义**

如果 `surface = "viewer" | "inline_autoplay"` 已经足够表达“同一 player 在不同宿主表面”，就只补注释和测试；
如果不够，再做最小语义收口，但：

1. **禁止** 新增第三种长期 surface
2. **禁止** 让 surface 重新变成第二套来源裁决真相

---

## 9. Task 6: 浏览器烟测与回归门禁

**Files:**
- No new code by default
- Use local browser / CLI tooling

- [ ] **Step 1: 跑前端全量门禁**

Run:

```powershell
pnpm --dir frontend test
pnpm --dir frontend typecheck
pnpm --dir frontend build
```

Expected:

- 全绿

- [ ] **Step 2: 跑真实浏览器烟测**

场景至少覆盖：

1. `https://localhost`
2. 房间 `1234b`
3. 用真实 MP4，包括 `D:\200-生活\230-照片备份\233-Telegram\色色\VID_20260425_035419_127.mp4`
4. 当前自动播 owner 点击
5. 非当前视频点击
6. fullscreen 退出回到时间线
7. 再次滚动后续播
8. fullscreen 中原宿主槽位被虚拟列表卸载
9. fullscreen 中 owner 切换到另一条视频

必须观察的证据：

1. DOM 内只有一颗活着的正式 `Video.js` player/container
2. fullscreen 前后不新增第二个 `<video>`
3. 不出现新的 `loadstart` / `emptied`
4. `currentTime` 连续
5. `WebTorrent` 主链不被改写
6. 宿主槽位变化时仍只有同一颗 canonical player

- [ ] **Step 3: 查日志与控制台**

关注：

1. 不能新增 `join_ticket_invalid` 风暴
2. 不能新增 `MaxListenersExceededWarning`
3. 不能新增 fullscreen promise rejection 风暴

---

## 10. Task 7: 清理、复核、提交

**Files:**
- Modify only files touched above

- [ ] **Step 1: 自审有没有留下兼容链垃圾**

必须确认：

1. 不再存在“原生 `<video>` 直达全屏”旧旁路
2. 不再存在“当前 owner 一条路，非当前 owner 另一条路”的双活逻辑
3. 不再存在只为迁移暂时留下、但没有删除计划的 wrapper / helper / flag

- [ ] **Step 2: 复核工作树**

Run:

```powershell
git status --short
```

Expected:

- 只剩本轮必要改动
- 不带入无关格式化噪音

- [ ] **Step 3: 如果本轮改了代码文件，更新 graphify**

Run:

```powershell
graphify update .
```

- [ ] **Step 4: 提交**

```powershell
git add docs/superpowers/specs/2026-04-25-视频唯一播放与自动播视觉连续性.md `
        docs/superpowers/plans/2026-04-25-VideoJs唯一播放器真相收口计划.md `
        frontend/媒体/全局唯一播放器.ts `
        frontend/房间消息窗.ts `
        frontend/聊天壳.ts `
        frontend/聊天媒体编排.ts `
        frontend/媒体/媒体查看器.ts `
        frontend/媒体/videojs播放器壳.ts `
        frontend/媒体/壳层/查看器会话协作.ts `
        frontend/媒体/媒体播放.ts `
        frontend/tests/房间消息窗媒体查看器测试.spec.ts `
        frontend/tests/聊天壳测试.spec.ts `
        frontend/tests/聊天媒体编排测试.spec.ts `
        frontend/tests/媒体查看器测试.spec.ts `
        frontend/tests/聊天应用内核测试.spec.ts
git commit -m "计划：收口 Video.js 唯一播放器真相"
```

---

## 11. 自审

### 这份计划为什么不算过度设计

1. 它不是再造一个播放器框架，而是**删掉第二条播放器真相**。
2. 它不新增新的业务命令、不改共享 contract、不碰后端和 WebTorrent。
3. 它把复杂度集中在“同一颗 canonical player 的宿主迁移”，这是问题本身，不是额外附会。

### 这份计划为什么能防漂移

1. 先改 spec，再改测试，再改代码，先把边界钉死。
2. 当前 P0 根因已经写成失败测试，执行时不可能靠嘴说“现在统一了”。
3. 每一步都写了“禁止什么”，避免实现时又想顺手留兼容链。
4. singleton player 的生命周期 owner 已经明确落到 `frontend/媒体/全局唯一播放器.ts`，不会再由多层共管。
5. Telegram 风格的状态转换已经被写成失败测试矩阵，不再只测“点一下能全屏”这一个表层动作。

### 这份计划的完成标准

只有同时满足下面四条，才算完成：

1. 群聊消息流里的正式视频播放器只剩同一颗 `Video.js v10`。
2. 当前 owner 点击全屏是一键直达，但不是旁路，而是同一颗 canonical player 升格。
3. 非当前视频点击不再冷开第二真相，而是切换同一颗 canonical player。
4. 自动播放、续播、全屏、退出全屏、再回时间线都保持同一条 `playback.src / currentTime` 真相。
