# 主题化花名池扩容 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把匿名身份花名空间从当前 `64` 扩到第一版 `10000+`，同时保留 `names` 作为短组合生成轮子，并让完整梗句池承担中文网络味。

**Architecture:** `src/用户身份.rs` 继续是匿名当前资料投影 owner，只保留加载、校验、随机路由和 `names` 薄适配；大词库外置到 `assets/anonymous_alias_themes.json`，不把源码写成段子仓。短组合走 `names(adjectives, nouns)`，完整梗句只作为业务数据直接抽取，不新增第二套组合器。

**Tech Stack:** Rust 2021, serde, serde_json, std::sync::OnceLock, names crate, uuid crate, PostgreSQL bootstrap integration tests

---

## File Map

### Create

- `assets/anonymous_alias_themes.json`

### Modify

- `src/用户身份.rs`
- `src/适配.rs`

### Test

- `src/用户身份.rs` 内部 `#[cfg(test)]` 单元测试
- `tests/房间接口测试.rs` 现有 bootstrap 回归测试

### Keep Unchanged Unless Evidence Forces Otherwise

- `Cargo.toml`：`serde / serde_json / names / uuid` 已存在，不为本次扩容新增依赖。
- `src/契约.rs`：前台仍只看 `session_id + display_alias`，不改共享契约。
- `migrations/*`：花名池是静态业务数据，不改数据库 schema。
- `frontend/*`：主题不对前台暴露，不改前端。

### Ownership Lock

- `assets/anonymous_alias_themes.json` 是业务词库真相，包含 `combo_themes` 和 `phrase_themes`。
- `src/用户身份.rs` 是生成适配 owner，负责解析、校验、随机抽源、调用 `names`、返回 `当前资料投影`。
- `src/适配.rs` 只消费 `当前资料投影` 并持久化 `theme_key + display_alias`，不参与花名生成决策。

## Task 1: 先写红测锁住数量、结构和生成边界

**Files:**
- Modify: `src/用户身份.rs`

- [ ] **Step 1: 添加单元测试模块，先引用还不存在的校验 helper**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn 花名词库组合空间至少一万() {
        let catalog = 花名词库();

        assert!(
            计算花名空间(catalog) >= 10_000,
            "花名空间不能再退回当前 64 级别"
        );
    }

    #[test]
    fn 花名词库主题键唯一且词条非空() {
        let catalog = 花名词库();

        校验花名词库(catalog).expect("静态花名词库必须在启动前可校验");
    }

    #[test]
    fn 组合花名继续通过_names_轮子生成() {
        let catalog = 花名词库();
        let combo = catalog
            .combo_themes
            .iter()
            .find(|theme| theme.key == "programmer_mystic")
            .expect("应存在程序员玄学主题");

        let alias = 按组合主题生成花名(combo);

        assert!(!alias.trim().is_empty());
        assert!(
            combo.adjectives.iter().any(|word| alias.contains(word.trim_end_matches('的')))
                || combo.nouns.iter().any(|word| alias.contains(word)),
            "生成结果应该来自主题词库，而不是硬编码兜底"
        );
    }

    #[test]
    fn 完整梗句池可以直接产出花名() {
        let catalog = 花名词库();
        let phrase_theme = catalog
            .phrase_themes
            .iter()
            .find(|theme| theme.key == "classic_meme_phrase")
            .expect("应存在经典完整梗句主题");

        let projection = 按完整梗句主题生成资料投影(phrase_theme);

        assert_eq!(projection.theme_key, "classic_meme_phrase");
        assert!(phrase_theme.phrases.contains(&projection.display_alias));
    }
}
```

- [ ] **Step 2: 跑红测，确认当前缺少外置词库和 helper**

Run: `cargo test --lib 花名词库 -- --nocapture`

Expected: FAIL，错误应集中在 `花名词库 / 计算花名空间 / 校验花名词库 / 按完整梗句主题生成资料投影` 尚不存在，不能因为别的模块编译问题失败。

- [ ] **Step 3: 提交红测**

```bash
git add src/用户身份.rs
git commit -m "锁定万级花名池扩容红测"
```

## Task 2: 外置万级主题词库

**Files:**
- Create: `assets/anonymous_alias_themes.json`

- [ ] **Step 1: 创建 `assets/anonymous_alias_themes.json`，按 spec 写入两类主题**

Required shape:

```json
{
  "version": 1,
  "combo_themes": [
    {
      "key": "programmer_mystic",
      "adjectives": ["404但嘴硬的", "CPU渡劫的", "缓存过期的"],
      "nouns": ["电子木鱼", "赛博功德", "量子签筒"]
    }
  ],
  "phrase_themes": [
    {
      "key": "classic_meme_phrase",
      "phrases": ["龙卷风摧毁停车场", "乌鸦坐飞机", "你那李白吗"]
    }
  ]
}
```

The real file must satisfy:

- At least `12` combo themes.
- Each combo theme has at least `30` `adjectives`.
- Each combo theme has at least `30` `nouns`.
- Total combo space is at least `10_800`.
- Total phrase count is at least `200`.
- Total alias space is at least `11_000`.
- Required combo keys: `wildlife`, `cosmos`, `snack`, `weather`, `novel_xianxia`, `novel_literary`, `wuxia_jianghu`, `cyber_abstract`, `programmer_mystic`, `history_politics`, `short_video_meme`, `worker_mood`.
- Required phrase key: `classic_meme_phrase`.

- [ ] **Step 2: 写词库时按梗语法扩词，不整站搬运**

Use the spec grammar buckets:

```text
经典动作招式反转
灾难级日常夸张
赛博玄学
程序员精神状态
网文与小说叙事
历史人物跨时空错位
短视频嘴替句式
发疯文学与牛马文学
```

Implementation notes:

- 可以使用 `秦始皇 / 曹操 / 孔明 / 拿破仑` 这类历史人物真名和意象。
- 可以使用 `CPU / DDL / 404 / bug / +1 / 2.5次元` 这类网络符号。
- 不使用具体小说、动漫、游戏 IP 角色名作为权威词条。
- 不把默认系统昵称做成违法教唆、露骨色情、群体仇恨或现实个体攻击池。

- [ ] **Step 3: 暂不提交，等 Task 3 的加载校验能证明词库有效后一起提交**

No command in this step.

## Task 3: 实现外置词库加载与校验

**Files:**
- Modify: `src/用户身份.rs`
- Create: `assets/anonymous_alias_themes.json`

- [ ] **Step 1: 把旧的四主题常量替换成 JSON 词库模型**

```rust
use names::{Generator, Name};
use serde::Deserialize;
use std::collections::HashSet;
use std::sync::OnceLock;
use uuid::Uuid;

const 花名词库_JSON: &str = include_str!("../assets/anonymous_alias_themes.json");
const 组合花名权重: u8 = 85;
const 最小花名空间: usize = 10_000;
const 最小组合主题数: usize = 12;
const 最小单主题词条数: usize = 30;
const 最小完整梗句数: usize = 200;

static 花名词库缓存: OnceLock<花名词库文件> = OnceLock::new();

#[derive(Debug, Deserialize)]
struct 花名词库文件 {
    version: u32,
    combo_themes: Vec<组合主题词典>,
    phrase_themes: Vec<完整梗句主题>,
}

#[derive(Debug, Deserialize)]
struct 组合主题词典 {
    key: String,
    adjectives: Vec<String>,
    nouns: Vec<String>,
}

#[derive(Debug, Deserialize)]
struct 完整梗句主题 {
    key: String,
    phrases: Vec<String>,
}
```

- [ ] **Step 2: 把 `当前资料投影.theme_key` 从 `&'static str` 改成 `String`**

```rust
pub(crate) struct 当前资料投影 {
    pub(crate) theme_key: String,
    pub(crate) display_alias: String,
}
```

Reason:

- JSON 加载出来的主题键是业务数据，不要为了维持 `&'static str` 去泄漏字符串或重建私有静态表。

- [ ] **Step 3: 实现解析、缓存、数量计算和校验 helper**

```rust
fn 花名词库() -> &'static 花名词库文件 {
    花名词库缓存.get_or_init(|| {
        let catalog = 解析花名词库(花名词库_JSON).expect("静态花名词库 JSON 必须可解析");
        校验花名词库(&catalog).expect("静态花名词库必须满足数量和结构约束");
        catalog
    })
}

fn 解析花名词库(raw: &str) -> Result<花名词库文件, String> {
    serde_json::from_str(raw).map_err(|err| format!("解析花名词库失败: {err}"))
}

fn 计算花名空间(catalog: &花名词库文件) -> usize {
    let combo_total = catalog
        .combo_themes
        .iter()
        .map(|theme| theme.adjectives.len() * theme.nouns.len())
        .sum::<usize>();
    let phrase_total = catalog
        .phrase_themes
        .iter()
        .map(|theme| theme.phrases.len())
        .sum::<usize>();

    combo_total + phrase_total
}

fn 校验花名词库(catalog: &花名词库文件) -> Result<(), String> {
    if catalog.version != 1 {
        return Err(format!("不支持的花名词库版本: {}", catalog.version));
    }
    if catalog.combo_themes.len() < 最小组合主题数 {
        return Err("组合主题数量不足".to_string());
    }
    if 计算花名空间(catalog) < 最小花名空间 {
        return Err("花名空间不足一万".to_string());
    }

    let phrase_count = catalog
        .phrase_themes
        .iter()
        .map(|theme| theme.phrases.len())
        .sum::<usize>();
    if phrase_count < 最小完整梗句数 {
        return Err("完整梗句数量不足".to_string());
    }

    let mut keys = HashSet::new();
    for theme in &catalog.combo_themes {
        校验主题键(&mut keys, &theme.key)?;
        校验词条列表(&theme.key, "adjectives", &theme.adjectives, 最小单主题词条数)?;
        校验词条列表(&theme.key, "nouns", &theme.nouns, 最小单主题词条数)?;
    }
    for theme in &catalog.phrase_themes {
        校验主题键(&mut keys, &theme.key)?;
        校验词条列表(&theme.key, "phrases", &theme.phrases, 1)?;
    }

    Ok(())
}
```

- [ ] **Step 4: 实现主题键和词条校验 helper**

```rust
fn 校验主题键(keys: &mut HashSet<String>, key: &str) -> Result<(), String> {
    if key.trim().is_empty() {
        return Err("主题键不能为空".to_string());
    }
    if !keys.insert(key.to_string()) {
        return Err(format!("主题键重复: {key}"));
    }
    Ok(())
}

fn 校验词条列表(
    key: &str,
    field: &str,
    values: &[String],
    min_len: usize,
) -> Result<(), String> {
    if values.len() < min_len {
        return Err(format!("{key}.{field} 词条数量不足"));
    }
    if values.iter().any(|value| value.trim().is_empty()) {
        return Err(format!("{key}.{field} 存在空词条"));
    }
    Ok(())
}
```

- [ ] **Step 5: 跑红测，确认校验已能读到词库并转绿**

Run: `cargo test --lib 花名词库 -- --nocapture`

Expected: PASS

- [ ] **Step 6: 提交外置词库与加载校验**

```bash
git add assets/anonymous_alias_themes.json src/用户身份.rs
git commit -m "外置万级主题化花名词库"
```

## Task 4: 切换随机生成链到 combo/phrase 双输入源

**Files:**
- Modify: `src/用户身份.rs`
- Modify: `src/适配.rs`

- [ ] **Step 1: 实现 `随机分配资料投影()` 的 85/15 路由**

```rust
pub(crate) fn 随机分配资料投影() -> 当前资料投影 {
    let catalog = 花名词库();
    let entropy = Uuid::new_v4();
    let bytes = entropy.as_bytes();

    if bytes[0] % 100 < 组合花名权重 || catalog.phrase_themes.is_empty() {
        return 按组合主题生成资料投影(catalog, bytes[1] as usize);
    }

    按完整梗句生成资料投影(catalog, bytes[1] as usize, bytes[2] as usize)
}
```

- [ ] **Step 2: 实现组合主题路径，继续用 `names`**

```rust
fn 按组合主题生成资料投影(catalog: &花名词库文件, entropy: usize) -> 当前资料投影 {
    let theme = &catalog.combo_themes[entropy % catalog.combo_themes.len()];
    当前资料投影 {
        theme_key: theme.key.clone(),
        display_alias: 按组合主题生成花名(theme),
    }
}

fn 按组合主题生成花名(theme: &组合主题词典) -> String {
    let adjectives = theme.adjectives.iter().map(String::as_str).collect::<Vec<_>>();
    let nouns = theme.nouns.iter().map(String::as_str).collect::<Vec<_>>();
    let mut generator = Generator::new(&adjectives, &nouns, Name::Plain);

    generator
        .next()
        .map(|alias| alias.replace('-', ""))
        .unwrap_or_else(|| format!("{}{}", theme.adjectives[0], theme.nouns[0]))
}
```

- [ ] **Step 3: 实现完整梗句路径**

```rust
fn 按完整梗句生成资料投影(
    catalog: &花名词库文件,
    theme_entropy: usize,
    phrase_entropy: usize,
) -> 当前资料投影 {
    let theme = &catalog.phrase_themes[theme_entropy % catalog.phrase_themes.len()];
    按完整梗句主题生成资料投影_带索引(theme, phrase_entropy)
}

fn 按完整梗句主题生成资料投影(theme: &完整梗句主题) -> 当前资料投影 {
    按完整梗句主题生成资料投影_带索引(theme, 0)
}

fn 按完整梗句主题生成资料投影_带索引(
    theme: &完整梗句主题,
    phrase_entropy: usize,
) -> 当前资料投影 {
    当前资料投影 {
        theme_key: theme.key.clone(),
        display_alias: theme.phrases[phrase_entropy % theme.phrases.len()].clone(),
    }
}
```

- [ ] **Step 4: 更新 `src/适配.rs` 的持久化绑定，适配 `theme_key: String`**

```rust
.bind(&projection.theme_key)
.bind(&projection.display_alias)
```

Target area:

- `impl 仓储端口 for Pg仓储/引导匿名身份`

- [ ] **Step 5: 跑生成链和 bootstrap 回归**

Run: `cargo test --lib 花名词库 -- --nocapture`

Expected: PASS

Run: `cargo test --test 房间接口测试 bootstrap接口会返回稳定花名快照 -- --exact`

Expected: PASS

- [ ] **Step 6: 提交生成链切换**

```bash
git add src/用户身份.rs src/适配.rs
git commit -m "切换主题花名到外置词库生成链"
```

## Task 5: 扩展回归覆盖并审计旧小词库退场

**Files:**
- Modify: `src/用户身份.rs`
- Verify: `src/用户身份.rs`, `assets/anonymous_alias_themes.json`

- [ ] **Step 1: 补一个抽样测试，确认随机生成结果稳定可用**

```rust
#[test]
fn 随机分配资料投影会产出主题键和花名() {
    for _ in 0..100 {
        let projection = 随机分配资料投影();

        assert!(!projection.theme_key.trim().is_empty());
        assert!(!projection.display_alias.trim().is_empty());
    }
}
```

- [ ] **Step 2: 搜索确认旧小词库常量已退场**

Run: `rg -n "动物主题形容词|宇宙主题形容词|零食主题形容词|天气主题形容词|主题词库" src/用户身份.rs`

Expected: no matches

- [ ] **Step 3: 搜索确认没有新增第二套花名生成器**

Run: `rg -n "生成花名|display_alias|names::Generator|Generator::new" src`

Expected:

- `names::Generator` / `Generator::new` 只在 `src/用户身份.rs` 的组合主题路径出现。
- 其它模块只消费 `display_alias`，不生成花名。

- [ ] **Step 4: 跑目标测试**

Run: `cargo test --lib 花名 -- --nocapture`

Expected: PASS

Run: `cargo test --test 房间接口测试 bootstrap接口会返回稳定花名快照 -- --exact`

Expected: PASS

- [ ] **Step 5: 提交测试与旧词库退场审计**

```bash
git add src/用户身份.rs
git commit -m "补齐主题花名池生成回归"
```

## Task 6: 全量验证、graphify 刷新与收尾

**Files:**
- Modify: `graphify-out/*` if graph rebuild updates it
- Verify: code and docs

- [ ] **Step 1: 跑 Rust 目标回归**

Run: `cargo test --lib -- --nocapture`

Expected: PASS

Run: `cargo test --test 房间接口测试 bootstrap接口会返回稳定花名快照 -- --exact`

Expected: PASS

Run: `cargo test --test 用例测试 -- --nocapture`

Expected: PASS

- [ ] **Step 2: 做词库与依赖边界审计**

Run: `rg -n "anonymous_alias_themes|combo_themes|phrase_themes|Generator::new|随机分配资料投影" src assets tests`

Expected:

- `assets/anonymous_alias_themes.json` 是唯一大词库。
- `src/用户身份.rs` 是唯一生成 owner。
- `src/适配.rs` 只消费 `随机分配资料投影()` 结果。

Run: `git diff --check`

Expected: PASS

- [ ] **Step 3: 刷新 graphify**

Run: `python -c "from graphify.watch import _rebuild_code; from pathlib import Path; _rebuild_code(Path('.'))"`

Expected: graph rebuild completes without error

- [ ] **Step 4: 复核工作树**

Run: `git status --short`

Expected: only intended `assets/anonymous_alias_themes.json`, `src/用户身份.rs`, `src/适配.rs`, and graphify outputs remain if not yet committed.

- [ ] **Step 5: 最终提交**

```bash
git add assets/anonymous_alias_themes.json src/用户身份.rs src/适配.rs graphify-out
git commit -m "实现万级主题化花名池"
```

## Execution Notes

- 不新增 Rust 依赖；`serde_json`、`names`、`uuid` 都已在 `Cargo.toml`。
- 不新增 `.rs` 文件；继续收口在 `src/用户身份.rs`。
- 不把主题暴露给前台；`theme_key` 仍然只进数据库当前资料投影。
- 不做房间级唯一性、热更新、后台配置、手动选主题。
- 不把完整梗句拆进 adjective/noun；完整梗句是直接花名业务数据。
- 如果实现时发现 `names` 对动态 `Vec<&str>` 有 API 限制，先查 crate 文档或源码，再决定最薄适配；不要手搓第二套组合算法。
