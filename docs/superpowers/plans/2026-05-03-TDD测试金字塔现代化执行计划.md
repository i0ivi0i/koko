# TDD 测试金字塔现代化执行计划 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. Use `superpowers:subagent-driven-development` only if the owner explicitly authorizes subagents. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `koko` 当前偏重、巨型测试文件较多的测试体系，收口成符合官方 TDD 最佳实践的金字塔结构：低层快测为主、集成测试适量、真实浏览器烟测少而准。

**Architecture:** 测试代码也按 DDD owner 管理：领域/应用/契约优先低层验证，adapter/shell 做薄集成，真实浏览器只验证低层无法证明的用户体验。计划先建立测试架构体检和健康报告，再按 helper god node 与最大测试文件排序拆分，最后补最小 Playwright 层并把门禁转为强制。

**Tech Stack:** Rust、Cargo test、clippy、TypeScript、Vitest、happy-dom、Playwright、Node.js 门禁脚本、graphify、PowerShell。

---

## 0. 执行前事实

依据：

- `docs/superpowers/specs/2026-05-03-TDD测试金字塔现代化规格.md`
- `graphify-out/GRAPH_REPORT.md`
- `frontend/package.json`
- `scripts/check-frontend-architecture-fitness.mjs`
- 当前测试规模扫描

当前测试规模：

| 指标 | 当前值 |
| --- | ---: |
| 生产源文件 | 197 |
| 生产代码行 | 49,301 |
| 测试文件 | 151 |
| 测试代码行 | 62,565 |
| 测试/生产行数比 | 1.27 |
| 超 500 行测试文件 | 41 |
| 超 1000 行测试文件 | 15 |

当前最大测试文件：

| 文件 | 行数 | 第一治理动作 |
| --- | ---: | --- |
| `frontend/tests/聊天壳测试.spec.ts` | 2655 | 按入口、草稿、房间切换、媒体桥接、错误恢复拆分 |
| `frontend/tests/媒体协作分发测试.spec.ts` | 2518 | 按 tracker、runtime、票据、预算、source 选择拆分 |
| `frontend/tests/媒体播放测试.spec.ts` | 1989 | 按播放源、会话状态、错误恢复、预算投影拆分 |
| `tests/协作分发测试/可用性裁决.rs` | 1941 | 保留表驱动，按可用性裁决场景拆模块 |
| `frontend/tests/浏览器应用平台测试.spec.ts` | 1854 | 按生命周期、缓存、通知、跨上下文、service worker 拆分 |
| `frontend/tests/资产协作分发运行时测试.spec.ts` | 1387 | 按状态机、生命周期、票据续租、选片策略、预算拆分 |
| `frontend/tests/common/聊天测试支架.ts` | 1270 | 先拆支架，降低后续拆 spec 的冲突 |

当前 graphify 风险：

- `创建图片消息传输()`、`提取媒体上传授权头()`、`构造tus_hook请求体()`、`断言TusHook已接受()`、`写入tus测试文件()` 进入高连接节点。
- 这些节点不是自动判错，但必须确认它们只是 fixture，而不是测试层第二业务模型。

当前禁止：

- 禁止通过删除有价值断言来降行数。
- 禁止把大 spec 拆成无 owner 的碎片。
- 禁止让 `frontend/tests/common` 继续变成第二应用内核。
- 禁止把 happy-dom 当真实浏览器烟测。
- 禁止提交 RED 状态；RED 只在本地证明门禁或 characterization 有效。
- 禁止未跑新鲜验证就宣称测试现代化满分。

---

## 1. 文件责任图

### 新增文件

| 文件 | 责任 |
| --- | --- |
| `scripts/check-test-architecture-fitness.mjs` | 测试体系体检：测试文件预算、`.only`、长期 skip、helper god node、巨型测试清单 |
| `docs/superpowers/reports/测试金字塔健康报告.md` | 当前和每轮执行后的测试金字塔快照、最大文件排行、RED/GREEN 证据、浏览器烟测证据 |
| `frontend/tests/common/测试文本测量.ts` | OffscreenCanvas / 文本测量 shim，只服务测试环境 |
| `frontend/tests/common/假存储.ts` | fake Storage 与平台本地存储支撑 |
| `frontend/tests/common/假实时.ts` | fake Socket / 实时事件支撑 |
| `frontend/tests/common/假传输.ts` | fake 前端传输端口，不含 DOM 操作 |
| `frontend/tests/common/聊天壳DOM支架.ts` | 聊天壳 DOM 查询、输入、按钮点击、草稿注入 |
| `frontend/tests/common/恢复编排支架.ts` | 房间恢复编排测试场景 |
| `frontend/tests/common/实时编排支架.ts` | 实时编排测试场景 |
| `frontend/tests/common/阅读推进支架.ts` | 阅读推进测试场景 |
| `e2e/package.json` | Playwright 最小真实浏览器层依赖与脚本 |
| `e2e/playwright.config.ts` | 浏览器烟测配置、trace、截图、video 策略 |
| `e2e/tests/房间入场.spec.ts` | 最小入房真实浏览器烟测 |
| `e2e/tests/文本消息.spec.ts` | 最小消息发送真实浏览器烟测 |

### 修改文件

| 文件 | 责任 |
| --- | --- |
| `frontend/package.json` | 增加 `check:test-architecture`，后期把强门禁接入 build |
| `frontend/tests/common/聊天测试支架.ts` | 分阶段退场；先改成 re-export 过渡，最后删除 |
| `frontend/tests/聊天壳测试.spec.ts` | 拆分后删除旧巨型文件 |
| `frontend/tests/媒体协作分发测试.spec.ts` | 拆分后删除旧巨型文件 |
| `frontend/tests/媒体播放测试.spec.ts` | 拆分后删除旧巨型文件 |
| `frontend/tests/资产协作分发运行时测试.spec.ts` | 拆分后删除旧巨型文件 |
| `frontend/tests/浏览器应用平台测试.spec.ts` | 拆分后删除旧巨型文件 |
| `frontend/tests/聊天壳恢复与分页测试.spec.ts` | 拆分后删除旧巨型文件 |
| `frontend/tests/传输测试.spec.ts` | 拆分后删除旧巨型文件 |
| `tests/协作分发测试.rs` | 只保留顶层集成测试 crate owner 与模块声明 |
| `tests/协作分发测试/可用性裁决.rs` | 拆分表驱动场景 |
| `tests/媒体上传测试/complete.rs` | 按图片、视频、错误语义、并发闸门拆分 |

---

## Task 1: 建立测试架构体检脚本和基线报告

**Files:**
- Create: `scripts/check-test-architecture-fitness.mjs`
- Create: `docs/superpowers/reports/测试金字塔健康报告.md`
- Modify: `frontend/package.json`
- Test: `node scripts/check-test-architecture-fitness.mjs --report`
- Test: `node scripts/check-test-architecture-fitness.mjs --enforce`

- [ ] **Step 1: 读现有架构体检脚本风格**

Run:

```powershell
Get-Content scripts/check-frontend-architecture-fitness.mjs -TotalCount 220
Get-Content frontend/package.json
```

Expected: 读到现有中文函数命名、仓库根目录解析、动态文件扫描风格。

- [ ] **Step 2: 写 RED 版测试体检脚本**

Create `scripts/check-test-architecture-fitness.mjs` with this shape:

```javascript
import { readdirSync, readFileSync, statSync } from "node:fs";
import { dirname, extname, join, relative, resolve } from "node:path";
import { fileURLToPath } from "node:url";

const 当前脚本路径 = fileURLToPath(import.meta.url);
const 仓库根目录 = resolve(dirname(当前脚本路径), "..");
const enforce = process.argv.includes("--enforce");

const 预算 = {
  rustTopLevelIntegration: 1200,
  rustIntegrationModule: 800,
  vitestNormal: 800,
  vitestComplex: 1000,
  helper: 800,
};

const forbiddenPatterns = [
  { pattern: /\b(?:test|it|describe)\.only\s*\(/, label: "禁止 only 测试" },
  { pattern: /\b(?:test|it|describe)\.skip\s*\(/, label: "禁止无治理 skip" },
];

function 枚举文件(dir, predicate, output = []) {
  for (const entry of readdirSync(dir)) {
    const full = join(dir, entry);
    const stat = statSync(full);
    if (stat.isDirectory()) {
      if (!["node_modules", "dist", "target"].includes(entry)) {
        枚举文件(full, predicate, output);
      }
      continue;
    }
    if (predicate(full)) output.push(full);
  }
  return output;
}

function 读取行数(file) {
  return readFileSync(file, "utf8").split(/\r?\n/).length;
}
```

Script must:

1. Scan `tests/**/*.rs` and `frontend/tests/**/*.ts`.
2. Print top 30 largest test files.
3. In `--enforce`, fail when any file exceeds current hard budget.
4. In `--report`, exit 0 but print the same violations.
5. Detect `.only`.
6. Detect `.skip` without `TODO(` on the same line or previous line.
7. Mark helper files under `frontend/tests/common/**` separately.

- [ ] **Step 3: Run RED enforce**

Run:

```powershell
node scripts/check-test-architecture-fitness.mjs --enforce
```

Expected: FAIL. It must list at least:

```text
frontend/tests/聊天壳测试.spec.ts
frontend/tests/媒体协作分发测试.spec.ts
tests/协作分发测试/可用性裁决.rs
frontend/tests/common/聊天测试支架.ts
```

If it passes, the gate is wrong; fix the script until it catches the current debt.

- [ ] **Step 4: Run report mode GREEN**

Run:

```powershell
node scripts/check-test-architecture-fitness.mjs --report
```

Expected: PASS with current debt report. This lets us commit the observability tool before all debt is removed, without pretending the debt is fixed.

- [ ] **Step 5: Add package script**

Modify `frontend/package.json`:

```json
"check:test-architecture": "node ../scripts/check-test-architecture-fitness.mjs --report"
```

Do not wire `--enforce` into `build` yet. That happens only after the first-wave cleanup is green.

- [ ] **Step 6: Write baseline health report**

Create `docs/superpowers/reports/测试金字塔健康报告.md`:

```markdown
# 测试金字塔健康报告

日期：2026-05-03
状态：基线

## 当前结论

当前测试数量可接受，但巨型测试文件、测试支架 god node 和真实浏览器层缺口需要治理。

## 最大测试文件

| 文件 | 行数 | 状态 |
| --- | ---: | --- |
| frontend/tests/聊天壳测试.spec.ts | 2655 | 待拆 |

## RED/GREEN 证据

- RED: `node scripts/check-test-architecture-fitness.mjs --enforce` 抓到当前超预算测试文件。
- GREEN: `node scripts/check-test-architecture-fitness.mjs --report` 成功输出基线。
```

- [ ] **Step 7: Verify task**

Run:

```powershell
node scripts/check-test-architecture-fitness.mjs --report
pnpm --dir frontend typecheck
git diff --check
```

Expected: all pass.

- [ ] **Step 8: Commit**

```powershell
git add scripts/check-test-architecture-fitness.mjs frontend/package.json docs/superpowers/reports/测试金字塔健康报告.md
git commit -m "建立测试金字塔体检基线"
```

---

## Task 2: 拆分 `frontend/tests/common/聊天测试支架.ts`

**Files:**
- Create: `frontend/tests/common/测试文本测量.ts`
- Create: `frontend/tests/common/假存储.ts`
- Create: `frontend/tests/common/假实时.ts`
- Create: `frontend/tests/common/假传输.ts`
- Create: `frontend/tests/common/聊天壳DOM支架.ts`
- Create: `frontend/tests/common/恢复编排支架.ts`
- Create: `frontend/tests/common/实时编排支架.ts`
- Create: `frontend/tests/common/阅读推进支架.ts`
- Modify: `frontend/tests/common/聊天测试支架.ts`
- Modify: imports in tests that use the moved helpers
- Test: `pnpm --dir frontend test -- tests/聊天壳测试.spec.ts tests/聊天壳恢复与分页测试.spec.ts tests/realtime真实链路.spec.ts`

- [ ] **Step 1: Read exports and references**

Run:

```powershell
rg -n "export (function|class|interface|type|const)|^export \\{" frontend/tests/common/聊天测试支架.ts
rg -n "聊天测试支架" frontend/tests -g "*.ts"
```

Expected: understand each exported helper's owner before moving it.

- [ ] **Step 2: Characterization GREEN before moving**

Run:

```powershell
pnpm --dir frontend test -- tests/聊天壳测试.spec.ts tests/聊天壳恢复与分页测试.spec.ts tests/聊天壳未读恢复测试.spec.ts
```

Expected: PASS. This proves current behavior before moving code.

- [ ] **Step 3: Move test text measurement**

Create `frontend/tests/common/测试文本测量.ts`:

```typescript
export function 安装测试文本测量画布(): void {
  class 假二维上下文 {
    font = "16px Microsoft YaHei";
    measureText(text: string): { width: number } {
      const px = Number(this.font.match(/(\d+(?:\.\d+)?)px/)?.[1] ?? "16");
      return { width: text.length * px * 0.58 };
    }
  }
  class 假OffscreenCanvas {
    getContext(kind: string): 假二维上下文 | null {
      return kind === "2d" ? new 假二维上下文() : null;
    }
  }
  Object.defineProperty(globalThis, "OffscreenCanvas", {
    value: 假OffscreenCanvas,
    configurable: true,
    writable: true,
  });
}
```

Import it from a single test setup owner, not every spec.

- [ ] **Step 4: Move fake storage and fake realtime**

Create `假存储.ts` and `假实时.ts`. Move only code, do not alter behavior.

Expected exports:

```typescript
export function createFakeStorage(): Storage;
export class 假Socket { /* existing public API unchanged */ }
```

- [ ] **Step 5: Move fake transport**

Create `假传输.ts`. It may import `假Socket`, contract types and `前端传输端口`, but must not import `聊天壳` or DOM helpers.

Expected exports:

```typescript
export class 假传输 implements 前端传输端口 { /* existing API unchanged */ }
export function 创建房间快照(...): 房间快照;
export function 创建传输错误(...): Error;
```

- [ ] **Step 6: Move DOM helpers**

Create `聊天壳DOM支架.ts`. It may import `聊天壳` and Lit component helpers, but must not define fake transport or fake socket.

Expected exports:

```typescript
export function 读取聊天快照供测试(el: 聊天壳): 聊天状态;
export function 读取操作台主输入(el: 聊天壳): HTMLTextAreaElement;
export function 输入房间短码到操作台(el: 聊天壳, roomCode: string): void;
export function 输入消息到操作台(el: 聊天壳, message: string): void;
```

- [ ] **Step 7: Move scenario builders**

Create:

```text
frontend/tests/common/恢复编排支架.ts
frontend/tests/common/实时编排支架.ts
frontend/tests/common/阅读推进支架.ts
```

Each file owns one scenario builder:

```typescript
export function 创建恢复编排测试场景(...);
export function 创建实时编排测试场景(...);
export function 创建阅读推进测试场景(...);
```

- [ ] **Step 8: Convert old harness to temporary re-export**

Keep `frontend/tests/common/聊天测试支架.ts` temporarily as:

```typescript
export * from "./测试文本测量.js";
export * from "./假存储.js";
export * from "./假实时.js";
export * from "./假传输.js";
export * from "./聊天壳DOM支架.js";
export * from "./恢复编排支架.js";
export * from "./实时编排支架.js";
export * from "./阅读推进支架.js";
```

This is allowed only inside this task as a migration bridge. The final task deletes it after all imports are direct.

- [ ] **Step 9: Verify task**

Run:

```powershell
pnpm --dir frontend typecheck
pnpm --dir frontend test -- tests/聊天壳测试.spec.ts tests/聊天壳恢复与分页测试.spec.ts tests/聊天壳未读恢复测试.spec.ts tests/realtime真实链路.spec.ts
node scripts/check-test-architecture-fitness.mjs --report
git diff --check
```

Expected: all pass; `聊天测试支架.ts` no longer exceeds the helper budget unless still temporary re-export.

- [ ] **Step 10: Commit**

```powershell
git add frontend/tests/common frontend/tests frontend/package.json docs/superpowers/reports/测试金字塔健康报告.md
git commit -m "拆分前端聊天测试支架"
```

---

## Task 3: 拆分 `frontend/tests/聊天壳测试.spec.ts`

**Files:**
- Create: `frontend/tests/聊天壳/入口与启动测试.spec.ts`
- Create: `frontend/tests/聊天壳/草稿与输入测试.spec.ts`
- Create: `frontend/tests/聊天壳/房间切换测试.spec.ts`
- Create: `frontend/tests/聊天壳/媒体桥接测试.spec.ts`
- Create: `frontend/tests/聊天壳/错误恢复测试.spec.ts`
- Delete: `frontend/tests/聊天壳测试.spec.ts`
- Modify: imports to use direct helpers from `frontend/tests/common/*`
- Test: new split specs

- [ ] **Step 1: Read current describe groups**

Run:

```powershell
rg -n "describe\\(|it\\(|test\\(" frontend/tests/聊天壳测试.spec.ts
```

Expected: identify behavior clusters before moving.

- [ ] **Step 2: Characterization before moving**

Run:

```powershell
pnpm --dir frontend test -- tests/聊天壳测试.spec.ts
```

Expected: PASS.

- [ ] **Step 3: Move entrance/startup tests**

Create `frontend/tests/聊天壳/入口与启动测试.spec.ts`. Move only tests about custom element setup, initial snapshot, lifecycle start.

Run:

```powershell
pnpm --dir frontend test -- tests/聊天壳/入口与启动测试.spec.ts
```

Expected: PASS.

- [ ] **Step 4: Move draft/input tests**

Create `frontend/tests/聊天壳/草稿与输入测试.spec.ts`. Move tests around composer text, draft restart, attachment input.

Run:

```powershell
pnpm --dir frontend test -- tests/聊天壳/草稿与输入测试.spec.ts
```

Expected: PASS.

- [ ] **Step 5: Move room switching tests**

Create `frontend/tests/聊天壳/房间切换测试.spec.ts`. Move tests about join/create room, leave room, active room switching.

Run:

```powershell
pnpm --dir frontend test -- tests/聊天壳/房间切换测试.spec.ts
```

Expected: PASS.

- [ ] **Step 6: Move media bridge tests**

Create `frontend/tests/聊天壳/媒体桥接测试.spec.ts`. Move tests that verify shell delegates to media publisher/player/viewer ports.

Run:

```powershell
pnpm --dir frontend test -- tests/聊天壳/媒体桥接测试.spec.ts
```

Expected: PASS.

- [ ] **Step 7: Move error recovery tests**

Create `frontend/tests/聊天壳/错误恢复测试.spec.ts`. Move transport, room, and local storage error recovery cases.

Run:

```powershell
pnpm --dir frontend test -- tests/聊天壳/错误恢复测试.spec.ts
```

Expected: PASS.

- [ ] **Step 8: Delete old giant spec and remove direct imports**

Delete `frontend/tests/聊天壳测试.spec.ts`. Update any imports that still point at `聊天测试支架.ts` to direct helper owner files where practical.

- [ ] **Step 9: Verify task**

Run:

```powershell
pnpm --dir frontend test -- tests/聊天壳
pnpm --dir frontend typecheck
node scripts/check-test-architecture-fitness.mjs --report
git diff --check
```

Expected: all pass; no `frontend/tests/聊天壳测试.spec.ts`.

- [ ] **Step 10: Commit**

```powershell
git add frontend/tests/聊天壳 frontend/tests/common frontend/tests
git commit -m "拆分聊天壳巨型测试"
```

---

## Task 4: 拆分协作分发前端巨型测试

**Files:**
- Create: `frontend/tests/媒体协作分发/tracker与票据测试.spec.ts`
- Create: `frontend/tests/媒体协作分发/运行时生命周期测试.spec.ts`
- Create: `frontend/tests/媒体协作分发/source选择测试.spec.ts`
- Create: `frontend/tests/媒体协作分发/预算投影测试.spec.ts`
- Create: `frontend/tests/资产协作分发/状态机测试.spec.ts`
- Create: `frontend/tests/资产协作分发/票据续租测试.spec.ts`
- Create: `frontend/tests/资产协作分发/生命周期测试.spec.ts`
- Delete after split: `frontend/tests/媒体协作分发测试.spec.ts`
- Delete after split: `frontend/tests/资产协作分发运行时测试.spec.ts`

- [ ] **Step 1: Read current test groups**

Run:

```powershell
rg -n "describe\\(|it\\(|test\\(" frontend/tests/媒体协作分发测试.spec.ts frontend/tests/资产协作分发运行时测试.spec.ts
```

- [ ] **Step 2: Characterization**

Run:

```powershell
pnpm --dir frontend test -- tests/媒体协作分发测试.spec.ts tests/资产协作分发运行时测试.spec.ts
```

Expected: PASS.

- [ ] **Step 3: Split tracker and ticket tests**

Move tracker URL, join ticket, expected-close dampening tests into `tracker与票据测试.spec.ts`.

Run:

```powershell
pnpm --dir frontend test -- tests/媒体协作分发/tracker与票据测试.spec.ts
```

- [ ] **Step 4: Split runtime lifecycle tests**

Move create/start/stop/dispose/retry lifecycle tests into `运行时生命周期测试.spec.ts`.

Run:

```powershell
pnpm --dir frontend test -- tests/媒体协作分发/运行时生命周期测试.spec.ts
```

- [ ] **Step 5: Split source selection and budget tests**

Move preview-first, whole-file, WebTorrent source, and budget projection tests into separate files.

Run:

```powershell
pnpm --dir frontend test -- tests/媒体协作分发/source选择测试.spec.ts tests/媒体协作分发/预算投影测试.spec.ts
```

- [ ] **Step 6: Split asset runtime tests by new production owners**

Move tests to match production owners:

| Production owner | Test file |
| --- | --- |
| `frontend/媒体/资产协作分发状态机.ts` | `frontend/tests/资产协作分发/状态机测试.spec.ts` |
| `frontend/媒体/资产协作分发生命周期.ts` | `frontend/tests/资产协作分发/生命周期测试.spec.ts` |
| `frontend/媒体/资产协作分发票据续租.ts` | `frontend/tests/资产协作分发/票据续租测试.spec.ts` |

Run:

```powershell
pnpm --dir frontend test -- tests/资产协作分发
```

- [ ] **Step 7: Delete old giant specs**

Delete:

```text
frontend/tests/媒体协作分发测试.spec.ts
frontend/tests/资产协作分发运行时测试.spec.ts
```

- [ ] **Step 8: Verify task**

Run:

```powershell
pnpm --dir frontend test -- tests/媒体协作分发 tests/资产协作分发
pnpm --dir frontend typecheck
node scripts/check-test-architecture-fitness.mjs --report
git diff --check
```

- [ ] **Step 9: Commit**

```powershell
git add frontend/tests/媒体协作分发 frontend/tests/资产协作分发 frontend/tests
git commit -m "拆分媒体协作分发测试"
```

---

## Task 5: 拆分媒体播放、媒体发布和传输测试

**Files:**
- Split: `frontend/tests/媒体播放测试.spec.ts`
- Split: `frontend/tests/媒体发布测试.spec.ts`
- Split: `frontend/tests/传输测试.spec.ts`
- Create directories: `frontend/tests/媒体播放/`, `frontend/tests/媒体发布/`, `frontend/tests/传输/`

- [ ] **Step 1: Read current groups**

Run:

```powershell
rg -n "describe\\(|it\\(|test\\(" frontend/tests/媒体播放测试.spec.ts frontend/tests/媒体发布测试.spec.ts frontend/tests/传输测试.spec.ts
```

- [ ] **Step 2: Characterization**

Run:

```powershell
pnpm --dir frontend test -- tests/媒体播放测试.spec.ts tests/媒体发布测试.spec.ts tests/传输测试.spec.ts
```

Expected: PASS.

- [ ] **Step 3: Split media playback tests**

Target files:

```text
frontend/tests/媒体播放/播放源选择测试.spec.ts
frontend/tests/媒体播放/播放会话状态测试.spec.ts
frontend/tests/媒体播放/错误恢复测试.spec.ts
frontend/tests/媒体播放/预算投影测试.spec.ts
```

Run:

```powershell
pnpm --dir frontend test -- tests/媒体播放
```

- [ ] **Step 4: Split media publisher tests**

Target files:

```text
frontend/tests/媒体发布/草稿状态测试.spec.ts
frontend/tests/媒体发布/上传调度测试.spec.ts
frontend/tests/媒体发布/完成发布测试.spec.ts
frontend/tests/媒体发布/诊断映射测试.spec.ts
```

Run:

```powershell
pnpm --dir frontend test -- tests/媒体发布
```

- [ ] **Step 5: Split transport tests**

Target files:

```text
frontend/tests/传输/房间契约测试.spec.ts
frontend/tests/传输/媒体契约测试.spec.ts
frontend/tests/传输/阅读契约测试.spec.ts
frontend/tests/传输/恢复契约测试.spec.ts
frontend/tests/传输/错误码测试.spec.ts
```

Run:

```powershell
pnpm --dir frontend test -- tests/传输
```

- [ ] **Step 6: Delete old files and verify**

Delete:

```text
frontend/tests/媒体播放测试.spec.ts
frontend/tests/媒体发布测试.spec.ts
frontend/tests/传输测试.spec.ts
```

Run:

```powershell
pnpm --dir frontend test -- tests/媒体播放 tests/媒体发布 tests/传输
pnpm --dir frontend typecheck
node scripts/check-test-architecture-fitness.mjs --report
git diff --check
```

- [ ] **Step 7: Commit**

```powershell
git add frontend/tests/媒体播放 frontend/tests/媒体发布 frontend/tests/传输 frontend/tests
git commit -m "拆分媒体播放发布与传输测试"
```

---

## Task 6: 拆分 Rust 协作分发和媒体上传巨型测试

**Files:**
- Split: `tests/协作分发测试/可用性裁决.rs`
- Modify: `tests/协作分发测试.rs`
- Split: `tests/媒体上传测试/complete.rs`
- Test: `cargo test --test 协作分发测试`
- Test: `cargo test --test 媒体上传测试`

- [ ] **Step 1: Read module structure**

Run:

```powershell
Get-Content tests/协作分发测试.rs -TotalCount 180
Get-ChildItem tests/协作分发测试 -File
Get-Content tests/媒体上传测试/complete.rs -TotalCount 220
```

- [ ] **Step 2: Characterization**

Run:

```powershell
cargo test --test 协作分发测试
cargo test --test 媒体上传测试
```

Expected: PASS.

- [ ] **Step 3: Split availability ruling**

Create:

```text
tests/协作分发测试/可用性裁决_冷源.rs
tests/协作分发测试/可用性裁决_peer.rs
tests/协作分发测试/可用性裁决_做种.rs
tests/协作分发测试/可用性裁决_删除.rs
```

Keep table-driven style. Shared setup belongs in `tests/协作分发测试/支撑.rs`, not in `tests/common`.

Modify `tests/协作分发测试.rs`:

```rust
mod 可用性裁决_冷源;
mod 可用性裁决_peer;
mod 可用性裁决_做种;
mod 可用性裁决_删除;
```

Run:

```powershell
cargo test --test 协作分发测试
```

- [ ] **Step 4: Split media upload complete tests**

Create:

```text
tests/媒体上传测试/complete_图片.rs
tests/媒体上传测试/complete_视频.rs
tests/媒体上传测试/complete_错误.rs
tests/媒体上传测试/complete_并发.rs
```

Run:

```powershell
cargo test --test 媒体上传测试
```

- [ ] **Step 5: Verify task**

Run:

```powershell
cargo test --test 协作分发测试
cargo test --test 媒体上传测试
cargo clippy --all-targets -- -D warnings
node scripts/check-test-architecture-fitness.mjs --report
git diff --check
```

- [ ] **Step 6: Commit**

```powershell
git add tests/协作分发测试.rs tests/协作分发测试 tests/媒体上传测试
git commit -m "拆分Rust协作分发与媒体上传测试"
```

---

## Task 7: 拆分浏览器平台、恢复分页和房间消息窗巨型测试

**Files:**
- Split: `frontend/tests/浏览器应用平台测试.spec.ts`
- Split: `frontend/tests/聊天壳恢复与分页测试.spec.ts`
- Split: `frontend/tests/房间消息窗/隐藏接管与海报预热测试.spec.ts`
- Split: `frontend/tests/房间消息窗/查看器意图与观察调度测试.spec.ts`
- Split: `frontend/tests/房间消息窗/双视频交接测试.spec.ts`

- [ ] **Step 1: Characterization**

Run:

```powershell
pnpm --dir frontend test -- tests/浏览器应用平台测试.spec.ts tests/聊天壳恢复与分页测试.spec.ts tests/房间消息窗/隐藏接管与海报预热测试.spec.ts tests/房间消息窗/查看器意图与观察调度测试.spec.ts tests/房间消息窗/双视频交接测试.spec.ts
```

Expected: PASS.

- [ ] **Step 2: Split browser platform tests**

Create:

```text
frontend/tests/浏览器应用平台/生命周期测试.spec.ts
frontend/tests/浏览器应用平台/缓存测试.spec.ts
frontend/tests/浏览器应用平台/通知测试.spec.ts
frontend/tests/浏览器应用平台/跨上下文测试.spec.ts
frontend/tests/浏览器应用平台/service-worker测试.spec.ts
```

Run:

```powershell
pnpm --dir frontend test -- tests/浏览器应用平台
```

- [ ] **Step 3: Split recovery and pagination tests**

Create:

```text
frontend/tests/聊天壳/恢复测试.spec.ts
frontend/tests/聊天壳/分页测试.spec.ts
frontend/tests/聊天壳/历史入口测试.spec.ts
```

Run:

```powershell
pnpm --dir frontend test -- tests/聊天壳/恢复测试.spec.ts tests/聊天壳/分页测试.spec.ts tests/聊天壳/历史入口测试.spec.ts
```

- [ ] **Step 4: Split room message pane media tests**

Create:

```text
frontend/tests/房间消息窗/隐藏接管测试.spec.ts
frontend/tests/房间消息窗/海报预热测试.spec.ts
frontend/tests/房间消息窗/查看器意图测试.spec.ts
frontend/tests/房间消息窗/观察调度测试.spec.ts
frontend/tests/房间消息窗/双视频owner交接测试.spec.ts
frontend/tests/房间消息窗/播放连续性测试.spec.ts
```

Run:

```powershell
pnpm --dir frontend test -- tests/房间消息窗
```

- [ ] **Step 5: Delete old giant files and verify**

Delete old split files. Then run:

```powershell
pnpm --dir frontend test -- tests/浏览器应用平台 tests/聊天壳 tests/房间消息窗
pnpm --dir frontend typecheck
node scripts/check-test-architecture-fitness.mjs --report
git diff --check
```

- [ ] **Step 6: Commit**

```powershell
git add frontend/tests/浏览器应用平台 frontend/tests/聊天壳 frontend/tests/房间消息窗 frontend/tests
git commit -m "拆分浏览器平台与房间消息窗测试"
```

---

## Task 8: 删除测试支架总入口并开启强制预算

**Files:**
- Delete: `frontend/tests/common/聊天测试支架.ts`
- Modify: all imports still referencing it
- Modify: `frontend/package.json`
- Modify: `scripts/check-test-architecture-fitness.mjs`
- Modify: `docs/superpowers/reports/测试金字塔健康报告.md`

- [ ] **Step 1: Find remaining re-export usage**

Run:

```powershell
rg -n "聊天测试支架" frontend/tests -g "*.ts"
```

Expected: list should be small. Replace each import with direct helper owner.

- [ ] **Step 2: Delete old common entry**

Delete:

```text
frontend/tests/common/聊天测试支架.ts
```

Run:

```powershell
pnpm --dir frontend typecheck
pnpm --dir frontend test
```

Expected: PASS.

- [ ] **Step 3: Switch test architecture script to enforce**

Modify `frontend/package.json`:

```json
"check:test-architecture": "node ../scripts/check-test-architecture-fitness.mjs --enforce"
```

If some 800-1000 line second-wave files remain, they may be report-only warnings. All `>1000` first-wave files must be gone.

- [ ] **Step 4: Verify enforce**

Run:

```powershell
pnpm --dir frontend check:test-architecture
node scripts/check-test-architecture-fitness.mjs --enforce
```

Expected: PASS.

- [ ] **Step 5: Commit**

```powershell
git add frontend/package.json scripts/check-test-architecture-fitness.mjs frontend/tests docs/superpowers/reports/测试金字塔健康报告.md
git commit -m "开启测试架构强制门禁"
```

---

## Task 9: 建立最小 Playwright 真实浏览器层

**Files:**
- Create: `e2e/package.json`
- Create: `e2e/playwright.config.ts`
- Create: `e2e/tests/房间入场.spec.ts`
- Create: `e2e/tests/文本消息.spec.ts`
- Modify if needed: `docs/superpowers/reports/测试金字塔健康报告.md`

- [ ] **Step 1: Confirm no existing e2e owner**

Run:

```powershell
Get-ChildItem -LiteralPath . -Directory
rg -n "playwright|@playwright/test|browser-trace" . -g "package.json" -g "*.ts" -g "*.md"
```

Expected: no existing `e2e` package. If one exists, extend it instead of creating a second layer.

- [ ] **Step 2: Create e2e package**

Create `e2e/package.json`:

```json
{
  "name": "koko-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test"
  }
}
```

Then install the current stable Playwright test runner at execution time:

```powershell
pnpm --dir e2e add -D @playwright/test
```

Record the resolved version from `e2e/package.json` / lockfile in the health report. Do not hard-code a stale remembered version into the plan.

- [ ] **Step 3: Create Playwright config**

Create `e2e/playwright.config.ts`:

```typescript
import { defineConfig, devices } from "@playwright/test";

export default defineConfig({
  testDir: "./tests",
  timeout: 60_000,
  expect: { timeout: 10_000 },
  retries: 0,
  reporter: [["list"], ["html", { open: "never" }]],
  use: {
    baseURL: process.env.KOKO_E2E_BASE_URL ?? "http://127.0.0.1:8080",
    trace: "retain-on-failure",
    screenshot: "only-on-failure",
    video: "retain-on-failure",
  },
  projects: [
    { name: "chromium", use: { ...devices["Desktop Chrome"] } },
  ],
});
```

- [ ] **Step 4: Write room entry smoke**

Create `e2e/tests/房间入场.spec.ts`:

```typescript
import { expect, test } from "@playwright/test";

test("用户能进入稳定烟测房间并看到群聊应用", async ({ page }) => {
  const errors: string[] = [];
  page.on("console", (msg) => {
    if (msg.type() === "error") errors.push(msg.text());
  });

  await page.goto("/");
  await page.getByRole("textbox").first().fill("1234b");
  await page.getByRole("button").filter({ hasText: /进入|加入|创建/ }).first().click();

  await expect(page.locator("body")).toContainText(/1234b|房间|消息/);
  expect(errors).toEqual([]);
});
```

During execution, adjust selectors only after reading actual UI markup. Prefer roles and visible text; do not use private class names unless there is no stable semantic surface.

- [ ] **Step 5: Write text message smoke**

Create `e2e/tests/文本消息.spec.ts`:

```typescript
import { expect, test } from "@playwright/test";

test("用户能发送一条文本消息并在时间线看到它", async ({ page }) => {
  const text = `e2e-${Date.now()}`;
  await page.goto("/");
  await page.getByRole("textbox").first().fill("1234b");
  await page.getByRole("button").filter({ hasText: /进入|加入|创建/ }).first().click();

  await page.getByRole("textbox").last().fill(text);
  await page.keyboard.press("Enter");

  await expect(page.getByText(text)).toBeVisible();
});
```

- [ ] **Step 6: Install and run smoke**

Run:

```powershell
pnpm --dir e2e install
pnpm --dir e2e exec playwright install chromium
pnpm --dir e2e test
```

Expected: PASS when local app is running. If the app is not running, record that the smoke layer is created but runtime smoke is blocked by missing server, then do not claim browser verification.

- [ ] **Step 7: Commit**

```powershell
git add e2e docs/superpowers/reports/测试金字塔健康报告.md
git commit -m "建立最小真实浏览器烟测层"
```

---

## Task 10: 全量验证、报告升级和完成态收口

**Files:**
- Modify: `docs/superpowers/reports/测试金字塔健康报告.md`
- Modify if all criteria pass: `docs/superpowers/reports/2026-05-01-真DDD重构完成矩阵.md`
- Test: all project verification commands

- [ ] **Step 1: Run final test architecture gate**

Run:

```powershell
node scripts/check-test-architecture-fitness.mjs --enforce
pnpm --dir frontend check:test-architecture
```

Expected: PASS.

- [ ] **Step 2: Run backend verification**

Run:

```powershell
cargo check
cargo clippy --all-targets -- -D warnings
cargo test
```

Expected: PASS.

- [ ] **Step 3: Run frontend verification**

Run:

```powershell
pnpm --dir frontend typecheck
pnpm --dir frontend test
pnpm --dir frontend build
node scripts/check-frontend-architecture-fitness.mjs
pnpm --dir frontend check:browser-app-constitution
```

Expected: PASS.

- [ ] **Step 4: Run browser smoke if server is available**

Run:

```powershell
pnpm --dir e2e test
```

Expected: PASS. If it cannot run because the local app is not started, start through the repo's normal Win11 launcher and rerun. Do not fake this with Vitest.

- [ ] **Step 5: Update health report**

Update `docs/superpowers/reports/测试金字塔健康报告.md`:

```markdown
## 完成态证据

- `node scripts/check-test-architecture-fitness.mjs --enforce`: PASS
- `cargo test`: PASS
- `pnpm --dir frontend test`: PASS
- `pnpm --dir e2e test`: PASS or blocked with reason

## 最大测试文件

| 文件 | 行数 | 状态 |
| --- | ---: | --- |
```

No `>1000` file may remain without dated temporary exception.

- [ ] **Step 6: Update completion matrix only if honest**

Only if all criteria pass, add:

```text
TDD 测试现代化：满分
测试金字塔结构：满分
测试简洁度：满分
```

If e2e browser smoke is blocked or still missing, keep status as `验证中`.

- [ ] **Step 7: Update graphify after code changes**

Run:

```powershell
graphify update .
```

Expected: updated graph or a clear graphify protection message. Protection is not product failure; record it.

- [ ] **Step 8: Final cleanliness checks**

Run:

```powershell
git -c core.quotepath=false diff --check
git -c core.quotepath=false status --short
```

Expected: no unstaged garbage except intended staged changes before commit; final status clean after commit.

- [ ] **Step 9: Commit**

```powershell
git add scripts frontend tests e2e docs graphify-out
git commit -m "完成TDD测试金字塔现代化收口"
```

---

## 11. 自审记录

### Round 1 - 需求意图

检查点：本计划是否是 spec 后的“下一步”，而不是直接动手乱拆。

结论：通过。计划先建测试体检和健康报告，再按支架 god node 与最大文件分批拆分，最后补真实浏览器层和完成态门禁。修正点：没有把“测试多”当成错误，也没有安排删覆盖率式清理。

### Round 2 - 架构边界

检查点：计划是否保持 DDD / 六边形测试分层，避免测试支架成为第二业务真相。

结论：通过。计划先拆 `frontend/tests/common/聊天测试支架.ts`，把 fake transport、fake realtime、DOM helper、恢复/实时/阅读场景分离；生产 owner 与测试 owner 对齐。修正点：明确旧 `聊天测试支架.ts` 只能短期 re-export，最终必须删除。

### Round 3 - 执行闭环

检查点：每个任务是否有 RED/GREEN/验证/提交，最终是否能证明满分。

结论：通过。Task 1 要求 `--enforce` 先 RED、`--report` 先 GREEN；每个拆分任务先 characterization，再搬迁，再删除旧入口；最终用 `cargo`、`pnpm`、架构门禁、Playwright、graphify 和 clean tree 收口。修正点：Playwright 层若本地服务没启动，不允许假装通过，完成矩阵必须保持验证中。
