# 省心省力网络实现计划 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 [2026-04-27-省心省力网络 spec](/E:/koko/docs/superpowers/specs/2026-04-27-%E7%9C%81%E5%BF%83%E7%9C%81%E5%8A%9B%E7%BD%91%E7%BB%9C.md) 把 `koko` 的网络真相彻底收口成“浏览器只认一个 HTTPS 入口”，同时保证未来正式部署到 Linux 公网服务器时不依赖 `run.ps1`、`https.ps1`、Windows 证书流程或本地 Caddy 特判，并且把 `cloudflared` 明确列为禁用项，只保留 Cloudflare 公网代理/CDN 这条正式路径。

**Architecture:** 这轮不再围绕“某个端口能不能通”打补丁，而是同时收口 4 条 owner 链：浏览器公开入口 owner、Tus 上传下一跳 owner、WebTorrent announce owner、Cloudflare CDN 公开缓存平面 owner。Rust 后端和同源代理负责唯一公开契约，前端只负责把相对路径收口成当前 origin 的绝对地址，本地 Windows 脚本只允许做开发适配层，Cloudflare 边缘层只负责缓存策略，禁止再发明产品真相。

**Tech Stack:** Rust, Axum, Reqwest, TypeScript, Vitest, PowerShell, Caddy（仅本地 HTTPS 适配）, `tusd`, `bittorrent-tracker`, graphify.

---

## 0. 实施前先锁死 6 条红线

后续每个任务都必须同时服从下面 6 条：

1. **禁止** 再把 `127.0.0.1`、`localhost`、`:1081`、`:7072`、`:7073` 暴露给浏览器契约。
2. **禁止** 让 `run.ps1` / `https.ps1` / Windows 本地证书流程成为正式 Linux 部署前置条件。
3. **禁止** 出现“页面入口是 HTTPS，但上传或 announce 下一跳掉回 HTTP / 内部地址”的双真相。
4. **禁止** 为了兼容旧逻辑，让浏览器同一能力同时接受“相对路径一套、内部绝对地址一套、Windows 特判再一套”。
5. **禁止** 在本项目正式公网方案里继续引入 `cloudflared`。
6. **禁止** 把 Cloudflare 免费 CDN 的公开主入口设计成 `:2052`、`:2053`、`:2082`、`:2083`、`:2086`、`:2087`、`:2095`、`:2096`、`:8443`、`:8880` 这类默认不适合免费缓存主平面的端口。
7. **禁止** 把 `session_id`、用户 token、一次性 query 混进希望全网共享缓存的媒体公开读路径。

---

## 1. 文件地图与 owner 锁定

### 1.1 浏览器公开契约 owner

- `src/媒体上传外壳.rs`
  - `prepare` 返回值、`/files` 同源 Tus 代理、`Location` 改写入口。
- `src/媒体协作分发.rs`
  - 同源 announce 路径常量和分发表面归一。
- `src/外壳.rs`
  - `/api/swarm/announce` 同源代理与浏览器可见入口约束。
- `src/总装.rs`
  - 启动配置读取，决定哪些环境变量属于公开入口真相，哪些只属于内部 sidecar 通信。

### 1.2 前端消费 owner

- `frontend/传输.ts`
  - 平台组合根，只负责把相对路径收口成当前 origin。
- `frontend/媒体/适配/媒体HTTP接口.ts`
  - `prepareMediaUpload`、`loadMediaLocator` 的地址归一和 `AbortSignal` 透传。
- `frontend/平台/传输运行时.ts`
  - 浏览器侧唯一 `baseUrl` owner，禁止页面/功能各自猜地址。

### 1.3 本地开发适配层

- `run.ps1`
  - 只负责 Win11 开发编排，不允许继续发明产品公开入口。
- `https.ps1`
  - 只负责本地 HTTPS bootstrap，不允许被写成 Linux 部署真相。
- `tests/启动器脚本检查.ps1`
- `tests/powershell/https-script.tests.ps1`

### 1.4 验证面

- Rust:
  - `tests/媒体上传测试/公网地址推导.rs`
  - `tests/协作分发测试.rs`
- Frontend:
  - `frontend/tests/传输测试.spec.ts`
  - `frontend/tests/媒体共享契约测试.spec.ts`
  - `frontend/tests/图片上传诊断测试.spec.ts`
  - `frontend/媒体/媒体发布.ts`
  - `frontend/tests/媒体发布测试.spec.ts`
- Docs / config:
  - `.env.example`
  - Create: `docs/部署/Linux公网单入口最小部署.md`

---

## 2. Task 1: 先把“浏览器同源契约”写成失败测试

**Files:**
- Modify: `tests/媒体上传测试/公网地址推导.rs`
- Modify: `tests/协作分发测试.rs`
- Modify: `frontend/tests/传输测试.spec.ts`
- Modify: `frontend/tests/媒体共享契约测试.spec.ts`
- Modify: `frontend/tests/图片上传诊断测试.spec.ts`

- [ ] **Step 1: 写 Rust 红测，锁死 prepare 与 announce 的公开真相**

至少补下面这些断言：

```rust
assert_eq!(body["tus_endpoint"].as_str(), Some("/files"));
assert_eq!(announce_urls, vec!["/api/swarm/announce"]);
```

重点：

1. 默认浏览器 contract 优先返回同源相对路径。
2. 显式公网配置存在时，仍允许返回同源绝对地址，但**禁止** 回落到内部端口。
3. `SWARM_TRACKER_PUBLIC_URL` 缺省时，测试必须证明浏览器看到的是 `/api/swarm/announce`，不是 `ws://127.0.0.1:7072`。

- [ ] **Step 2: 写前端红测，锁死“相对路径只在一处收口”**

至少补下面这些断言：

```ts
expect(result.tus_endpoint).toBe("https://app.example.com/files");
expect(locator.distribution?.announce_urls).toEqual([
  "https://app.example.com/api/swarm/announce",
]);
```

重点：

1. `媒体HTTP接口` 允许后端返回 `/files` 和 `/api/swarm/announce`。
2. `frontend/传输.ts` / `frontend/平台/传输运行时.ts` 只在平台层统一做 `new URL(relative, baseUrl)`。
3. `图片上传诊断测试` 里凡是继续把 `http://127.0.0.1:1081/files/...` 当成正常浏览器路径的断言，都要先改红。

- [ ] **Step 3: 跑定向测试确认先红**

Run:

```powershell
cargo test 公网地址推导 --test 媒体上传测试 -- --nocapture
cargo test 协作分发测试 -- --nocapture
pnpm --dir frontend test -- --run frontend/tests/传输测试.spec.ts frontend/tests/媒体共享契约测试.spec.ts frontend/tests/图片上传诊断测试.spec.ts
```

Expected:

1. 至少出现“当前仍返回内部绝对地址”这一类失败。
2. 失败必须直指网络真相，而不是测试拼错。

---

## 3. Task 2: 收口后端公开地址 owner，优先下发同源路径

**Files:**
- Modify: `src/媒体上传外壳.rs`
- Modify: `src/媒体协作分发.rs`
- Modify: `src/外壳.rs`
- Modify: `src/总装.rs`
- Modify: `tests/媒体上传测试/公网地址推导.rs`
- Modify: `tests/协作分发测试.rs`

- [ ] **Step 1: 重新读清当前 owner 边界**

必须逐行复核：

1. `读取媒体_tus对外地址` 何时返回内部绝对地址。
2. `读取协作分发配置` / 分发响应何时把 announce 写成裸 `ws://127.0.0.1:7072`。
3. `src/外壳.rs` 当前 `/api/swarm/announce` 代理是否已经可以承接浏览器唯一入口。

- [ ] **Step 2: 改成“浏览器 contract 优先相对路径”**

目标实现：

```rust
let browser_tus_endpoint = "/files";
let browser_announce_url = "/api/swarm/announce";
```

要求：

1. prepare 默认返回 `/files`，而不是 `http://host:1081/files`。
2. 协作分发表面默认返回 `/api/swarm/announce`，而不是裸 tracker upstream。
3. 显式公网配置只允许影响“公开入口名”，**禁止** 把内部地址再次塞回浏览器。

- [ ] **Step 3: 保住非浏览器内部通信边界**

要求：

1. `MEDIA_TUS_INTERNAL_BASE_URL` 仍只服务后端 -> tusd。
2. `SWARM_TRACKER_UPSTREAM_URL` 仍只服务 Rust 代理。
3. `SWARM_SEEDER_TRACKER_URL` 仍只服务 sidecar announce。
4. **禁止** 这 3 类 internal URL 再污染返回给浏览器的 contract。

- [ ] **Step 4: 跑 Task 2 定向测试转绿**

Run:

```powershell
cargo test 公网地址推导 --test 媒体上传测试 -- --nocapture
cargo test 协作分发测试 -- --nocapture
```

- [ ] **Step 5: 中文 commit**

```powershell
git add src/媒体上传外壳.rs src/媒体协作分发.rs src/外壳.rs src/总装.rs tests/媒体上传测试/公网地址推导.rs tests/协作分发测试.rs
git commit -m "后端：收口浏览器公开地址同源契约"
```

---

## 4. Task 3: 收口 Tus 同源代理，禁止 `Location` 再泄漏内部地址

**Files:**
- Modify: `src/媒体上传外壳.rs`
- Modify: `tests/媒体上传测试/公网地址推导.rs`
- Modify: `run.ps1`
- Modify: `https.ps1`
- Modify: `tests/启动器脚本检查.ps1`
- Modify: `tests/powershell/https-script.tests.ps1`

- [ ] **Step 1: 写代理层红测**

在现有 Rust / PowerShell 测试里补下面这组失败用例：

```rust
assert_eq!(location, "https://im.example.com/files/upload-1");
assert_ne!(location, "http://127.0.0.1:1081/files/upload-1");
```

重点：

1. 浏览器通过 `/files` 进入时，返回的 `Location` 必须回到当前浏览器入口。
2. **禁止** 再出现 `http://.../files/...`、`127.0.0.1:1081/files/...`。

- [ ] **Step 2: 在 Rust 代理层改写 `Location`**

目标实现：

```rust
if header_name == LOCATION {
    rewritten = 根据当前请求公开入口改写到浏览器可见地址(...);
}
```

要求：

1. 代理层只改写浏览器可见 `Location`，不改 sidecar 内部路由。
2. forwarded 头存在时优先信任公网协议/authority。
3. 没有 forwarded 头时，以当前请求 host/proto 推导浏览器下一跳。
4. **禁止** 再把 `tusd` 原始 header 生吞活剥转给浏览器。

- [ ] **Step 3: 本地 Win11 启动器只做开发适配**

要求：

1. `run.ps1` 在启用本地 HTTPS bootstrap 时，默认把 `TUSD_BEHIND_PROXY` 打开，避免本地 HTTPS 测试天然跑偏。
2. `run.ps1` / `https.ps1` 的日志和入口摘要必须优先打印 `https://局域网IP/`，**禁止** 再把内部 Tus URL 冒充成给测试用户的入口。
3. 这些改动都只能属于本地开发适配层，**禁止** 反向变成 Linux 正式部署依赖。

- [ ] **Step 4: 跑 Task 3 定向测试转绿**

Run:

```powershell
cargo test 公网地址推导 --test 媒体上传测试 -- --nocapture
pwsh -NoProfile -File .\tests\启动器脚本检查.ps1
pwsh -NoProfile -File .\tests\powershell\https-script.tests.ps1
```

- [ ] **Step 5: 中文 commit**

```powershell
git add src/媒体上传外壳.rs run.ps1 https.ps1 tests/媒体上传测试/公网地址推导.rs tests/启动器脚本检查.ps1 tests/powershell/https-script.tests.ps1
git commit -m "上传：收口Tus同源代理与本地HTTPS适配"
```

---

## 5. Task 4: 前端只认平台层 baseUrl，禁止页面/功能各猜一套地址

**Files:**
- Modify: `frontend/传输.ts`
- Modify: `frontend/媒体/适配/媒体HTTP接口.ts`
- Modify: `frontend/媒体/媒体发布.ts`
- Modify: `frontend/平台/传输运行时.ts`
- Modify: `frontend/tests/传输测试.spec.ts`
- Modify: `frontend/tests/媒体共享契约测试.spec.ts`
- Modify: `frontend/tests/图片上传诊断测试.spec.ts`
- Modify: `frontend/tests/媒体发布测试.spec.ts`

- [ ] **Step 1: 让前端测试先证明“相对路径只在一处收口”**

至少补下面两类断言：

```ts
expect(createTransport).toHaveBeenCalledWith("https://lan.example");
expect(result.tus_endpoint).toBe("https://lan.example/files");
```

- [ ] **Step 2: 删除前端对内部绝对地址的隐式正常化**

要求：

1. `媒体HTTP接口` 继续允许相对路径和同源绝对路径。
2. 对 `127.0.0.1` / `localhost` / 内部端口的浏览器 contract，要么在测试里显式标成错误输入，要么在诊断层给出更清楚错误。
3. **禁止** 再让页面壳、媒体壳、dev seeder 各自拼接一份 announce / tus 地址。

- [ ] **Step 2.1: 锁死 Cloudflare 免费代理上传边界**

要求：

1. `frontend/媒体/媒体发布.ts` 必须明确记录或验证：如果未来上传继续走 Cloudflare 公开代理域名，单请求体不能想当然地超过 `100 MB`。
2. 如果当前 Tus / Uppy 组合还无法证明“单请求体稳定低于 `100 MB`”，就必须在实现或文档里明确标红，**禁止** 宣称“已适配 Cloudflare 免费代理上传”。
3. `frontend/tests/媒体发布测试.spec.ts` 至少要补一条边界测试或显式注释锁，防止后续有人把大文件上传行为误宣传成“免费 CDN 自动兜底”。
4. **禁止** 把 `512 MB` 以上视频默认当成“免费 Cloudflare CDN 可完整缓存”的已成立事实。

- [ ] **Step 3: 跑 Task 4 定向测试转绿**

Run:

```powershell
pnpm --dir frontend test -- --run frontend/tests/传输测试.spec.ts frontend/tests/媒体共享契约测试.spec.ts frontend/tests/图片上传诊断测试.spec.ts frontend/tests/媒体发布测试.spec.ts
```

- [ ] **Step 4: 中文 commit**

```powershell
git add frontend/传输.ts frontend/媒体/适配/媒体HTTP接口.ts frontend/媒体/媒体发布.ts frontend/平台/传输运行时.ts frontend/tests/传输测试.spec.ts frontend/tests/媒体共享契约测试.spec.ts frontend/tests/图片上传诊断测试.spec.ts frontend/tests/媒体发布测试.spec.ts
git commit -m "前端：收口同源地址解析与网络诊断"
```

---

## 6. Task 5: 把 Linux 公网部署与 Cloudflare 最小契约写成文档和样例，不再只会跑 `run.ps1`

**Files:**
- Modify: `.env.example`
- Create: `docs/部署/Linux公网单入口最小部署.md`
- Modify: `src/总装.rs`
- Modify: `tests/协作分发测试.rs`
- Modify: `tests/媒体上传测试/公网地址推导.rs`
- Modify: `frontend/tests/媒体发布测试.spec.ts`

- [ ] **Step 1: 改 `.env.example`，禁止继续示范错误公网地址**

目标：

```dotenv
APP_PORT=8080
SWARM_TRACKER_PUBLIC_URL=/api/swarm/announce
MEDIA_TUS_PUBLIC_ENDPOINT=/files
```

如果最终实现决定让这两个变量默认留空并按请求推导，也必须把注释写清楚：

1. 哪些是浏览器公开 contract
2. 哪些只是 sidecar internal URL
3. Linux 部署时最少需要哪些环境变量

- [ ] **Step 2: 写 Linux 最小部署文档**

文档必须回答下面 8 个问题：

1. Linux 正式部署最少需要哪些环境变量
2. 哪些是公开入口变量，哪些是内部 sidecar 变量
3. 为什么本项目正式部署**禁止** 使用 `cloudflared`
4. 为什么正式部署不依赖 `run.ps1` / `https.ps1` / Windows 证书导入
5. 为什么 Cloudflare CDN 免费公开主入口必须优先锁定 `443`
6. 哪些端口虽然能被 Cloudflare 代理，但**禁止** 被我们当成免费缓存主平面
7. `Cache Rules`、`Tiered Cache`、`100 MB` 上传上限、`512 MB` 缓存上限分别影响什么
8. Cloudflare 公网代理/CDN 和源站内部端口为什么必须彻底分层

- [ ] **Step 3: 补配置层回归测试**

至少补下面这类断言：

```rust
assert_eq!(config.协作分发.tracker_public_url, "/api/swarm/announce");
assert_eq!(config.tus.public_endpoint, Some("/files".to_string()));
```

或者，如果最终选择“缺省为空、运行时按请求推导”，测试也必须明确这个裁决，**禁止** 留成模糊默认。

- [ ] **Step 3.1: 把 Cloudflare 边界写成可复查证据**

要求：

1. 计划中的 Linux 部署文档必须明确写出：本项目正式方案**禁止** `cloudflared`。
2. 必须明确写出：Cloudflare 支持代理的额外端口，不等于免费缓存主平面推荐端口。
3. 必须明确写出：共享媒体 URL **禁止** 混入 `session_id`、用户 token、一人一份 query。
4. 必须明确写出：如果未来上传通过 Cloudflare 公开代理域名进入源站，实际实现阶段必须证明单请求体不超过 `100 MB`，否则不得宣称该链路正式就绪。

- [ ] **Step 4: 跑 Task 5 定向验证**

Run:

```powershell
cargo test 读取媒体_tus侧车配置 -- --nocapture
cargo test 协作分发配置 -- --nocapture
```

- [ ] **Step 5: 中文 commit**

```powershell
git add .env.example docs/部署/Linux公网单入口最小部署.md src/总装.rs tests/协作分发测试.rs tests/媒体上传测试/公网地址推导.rs
git commit -m "文档：补齐Linux公网单入口最小部署契约"
```

---

## 7. Task 6: 跑真实同构验收，证明 LAN 与未来公网是同一套脑子

**Files:**
- Modify: `docs/superpowers/plans/2026-04-27-省心省力网络实现计划.md`

- [ ] **Step 1: 跑 Rust + Frontend 定向测试**

Run:

```powershell
cargo test 公网地址推导 --test 媒体上传测试 -- --nocapture
cargo test 协作分发测试 -- --nocapture
pnpm --dir frontend test -- --run frontend/tests/传输测试.spec.ts frontend/tests/媒体共享契约测试.spec.ts frontend/tests/图片上传诊断测试.spec.ts frontend/tests/媒体发布测试.spec.ts
pwsh -NoProfile -File .\tests\启动器脚本检查.ps1
pwsh -NoProfile -File .\tests\powershell\https-script.tests.ps1
```

- [ ] **Step 2: 跑本地 HTTPS / LAN 真烟测**

Run:

```powershell
.\run.ps1
```

手动/脚本化确认：

1. 局域网设备只访问 `https://开发电脑局域网IP/`
2. prepare 返回 `/files` 或同源 HTTPS 绝对地址
3. Tus `Location` 不再出现 `127.0.0.1` / `:1081` / `http://`
4. announce 只走 `/api/swarm/announce`

- [ ] **Step 3: 记录正式 Linux 部署等价检查项**

至少写成 checklist：

1. 浏览器入口 `https://正式域名/`
2. `/files` 和 `/api/swarm/announce` 继续挂在同一入口下
3. 服务器内部 sidecar URL 不泄漏给浏览器
4. 不依赖 `run.ps1` / `https.ps1`
5. 正式公网部署没有再引入 `cloudflared`
6. Cloudflare 免费 CDN 主入口仍然锁在 `443`
7. 共享媒体 URL 不带 `session_id` 这类 cache key 污染源
8. 超过 `512 MB` 的媒体不再被文档或实现假装成“免费 CDN 可完整缓存”

- [ ] **Step 4: 更新 graph 并复核工作树**

Run:

```powershell
graphify update .
git status --short
```

---

## 8. 自审清单

写完代码后，必须逐条自审：

1. 还有没有任何返回给浏览器的 `127.0.0.1`、`localhost`、`:1081`、`:7072`、`:7073`
2. `prepare`、Tus proxy `Location`、announce 返回值是否已经只剩一套公开真相
3. 本地 `run.ps1` / `https.ps1` 是否仍然只是开发适配层，而不是产品真相 owner
4. `.env.example` 和 Linux 部署文档是否还在教人配置错误的公网入口
5. 有没有为了兼容旧逻辑保留双真相分支、平行 owner、额外包装垃圾
6. 中文注释是否只解释边界和为什么，不是在复读动作
7. Cloudflare 公网代理/CDN、被禁用的 `cloudflared`、源站内部端口这 3 层是否还被混写成同一个概念

---

## 9. 完成定义

只有同时满足下面 8 条，才允许宣布“省心省力网络整顿完成”：

1. Rust 定向红测先红后绿
2. Frontend 定向红测先红后绿
3. PowerShell 启动器测试全绿
4. LAN HTTPS 真烟测下，浏览器网络面板不再出现内部地址泄漏
5. Tus 上传创建/续传/完成全程不再掉回 HTTP 或内部端口
6. announce 只认 `/api/swarm/announce`
7. Linux 公网部署最小契约已经写成文档和样例，不再只靠 `run.ps1`
8. `cloudflared` 已被明确写成禁用项，没有再留“以后也可以上”的后门
9. Cloudflare 免费 CDN 端口、缓存键、上传上限、缓存上限边界已经写进文档与计划
10. `git status --short` 干净，并完成中文提交
