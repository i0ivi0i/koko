# 群聊视频自动播放先闪烁再续播根因收口 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task in this session. Use `superpowers:subagent-driven-development` only when the owner explicitly permits parallel agents. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 真正修复群聊里“自动播放进入前先闪烁消失一瞬间，再开始自动续播”的 bug，保证新进房首轮上滑 `G -> F -> E -> D -> C -> B -> A` 时不再黑闪、不再先掉画面后续播。

**Architecture:** 这次不再把问题定义成单纯“冷路径首帧不够快”，而是收口成一个更底层的根因：自动播放热路径里仍然存在多张画面表面和多条字节读取路径在竞争同一张视频卡片。修复方向不是继续给 `poster / preview / frozen / canonical` 打补丁，而是把自动播放热路径减到只剩一个稳定可见表面槽，并且只允许“唯一 live video + 它自己的同源同帧 bridge”参与揭帘事务。

**Tech Stack:** TypeScript, Lit, Video.js v10, WebTorrent, Vitest, Playwright CLI, Chrome DevTools CLI, browser-trace, `requestVideoFrameCallback`, `HTMLMediaElement.play()`, `OffscreenCanvas`, `ImageBitmap`, GitNexus, Serena

---

## 为什么旧方案还不够

本轮新证据说明，上一轮修复没有真正把自动播放热路径收纯：

1. 当前时间线视频卡片仍然同时拥有这些表面能力：
   - `poster` 图片
   - `preview <video>`
   - `frozen frame <canvas>`
   - `canonical host`
   - `stage host`
   见 `E:\koko\frontend\房间消息窗\视频附件表面渲染.ts`
2. `poster` 仍来自 `playback.thumbnailUrl ?? attachment.posterSrc ?? 默认占位图`，它本质上是静态图片，不是暂停帧，也不是已播放帧。
   见 `E:\koko\frontend\房间消息窗\附件渲染.ts`
3. 冻结帧确实不是“瞎猜的图片”，而是从同一颗视频通过 `drawImage()` 抓到 `OffscreenCanvas / ImageBitmap / HTMLCanvasElement` 的内存投影。
   见 `E:\koko\frontend\房间消息窗\视频桥接帧.ts`
4. 前端仍保留 `/api/attachments/{attachmentId}/content?variant=original|thumbnail` 内容地址构造：
   - `E:\koko\frontend\平台\传输.ts`
   - `E:\koko\frontend\媒体\壳层\快照投影协作.ts`
   - `E:\koko\frontend\媒体\播放会话\应用.ts`
5. 更关键的是，真实新进房 trace 里仍能看到视频附件的 `/api/attachments/.../content?variant=original` 206 请求。这说明自动播放附近还没纯到只剩 WebTorrent live chain。

所以，本计划要解决的根因不是“bridge frame 质量不够高”，而是：

> 同一张自动播放视频卡片在进入续播前后，仍然有多张画面和多条字节路径同时争抢可见主位；上一轮只是减少了其中一部分空窗，没有消灭这场竞争本身。

---

## 官方依据

执行过程中碰到分歧，先回到这些官方资料，不靠印象：

- [MDN: `HTMLVideoElement.requestVideoFrameCallback()`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLVideoElement/requestVideoFrameCallback)
  - 关键事实：它是在新视频帧送到 compositor 时回调，比 `canplay / playing` 更接近“用户能看见帧”。
  - 但它只是 best effort，可能晚一个 v-sync，不能被神化成绝对同步点。
- [web.dev: `requestVideoFrameCallback`](https://web.dev/articles/requestvideoframecallback-rvfc?hl=en)
  - 关键事实：它和屏幕视频帧率尽量同步，但仍可能一帧偏晚；如果揭帘事务设计错了，仍会闪。
- [MDN: `HTMLVideoElement.poster`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLVideoElement/poster)
  - 关键事实：`poster` 只是“在没有视频数据可用时显示的图片”。
- [Video.js options](https://videojs.org/guides/options)
  - 关键事实：`poster` 是播放前图片；自动播放应该走 Video.js option / player API，不靠原生 attribute 自我安慰。
- [MDN: `HTMLMediaElement.play()`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/play)
  - 关键事实：`play()` 返回 `Promise`，自动播放是否真的开始，必须看 promise 结果。
- [WebKit: New `<video>` Policies for iOS](https://webkit.org/blog/6784/new-video-policies-for-ios/)
  - 关键事实：`<video autoplay>` 只有在可见时才会开始播放；滚动出视口会暂停。可见性是浏览器事实，不是业务真相。
- [MDN: `CanvasRenderingContext2D.drawImage()`](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/drawImage)
  - 关键事实：`HTMLVideoElement / ImageBitmap / OffscreenCanvas` 都能被画到 canvas 上，但 video 至少要 `readyState > 1`。
- [MDN: `OffscreenCanvas.transferToImageBitmap()`](https://developer.mozilla.org/en-US/docs/Web/API/OffscreenCanvas/transferToImageBitmap)
  - 关键事实：`ImageBitmap` 是大图形资源，用完要消费或 `close()`，不能泄漏。
- [Video.js Player API](https://docs.videojs.com/player)
  - 关键事实：`currentTime()` 和 `dispose()` 是正式播放器生命周期能力，不应该被外层 UI 分支复制成第二套真相。

---

## 新根因模型

本计划采用下面这条根因模型，执行时必须先证实，再修复：

1. `Video.js v10` 不是“画面真相”，它只是播放器壳和 HTML5 tech 的生命周期封装。
2. 真正出像素的是浏览器的 `HTMLVideoElement + decoder + compositor`。
3. 当前自动播放热路径里，至少存在两类竞争：
   - **第二表面竞争**
     - `poster`
     - `preview video`
     - `frozen frame canvas`
     - `canonical host`
   - **第二字节路径竞争**
     - WebTorrent 正式视频链
     - `/api/attachments/.../content?variant=original|thumbnail` 内容地址链
4. 只要这两类竞争还存在，继续加 guard、timeout、cover、warmup 只会掩盖局部窗口，不会消灭“闪一下再播”的根因。

因此后续修复的第一原则是：

> 自动播放热路径里，唯一允许参与可见揭帘的只剩两样东西：
> 1. 全局唯一 live canonical video
> 2. 从这颗 live video 抓下来的同源同帧只读 bridge

`poster`、`preview video`、`/api/attachments/.../content?variant=original|thumbnail` 都必须被排除出自动播放热路径。

---

## Done When

只有同时满足下面条件，才允许宣告修好：

1. 使用**全新浏览器 profile**，首页看到空态首页，手输 `1234b` 新进房。
2. 新进房首轮从 `G -> F -> E -> D -> C -> B -> A` 逐条自动播时，不再出现“先闪烁消失一瞬间再续播”。
3. 同一轮走到 `A` 后，再向下看 `B -> C -> D -> E -> F -> G`，体验与首轮一致，不依赖“看过一次才变好”。
4. 退出房间，再次用**新 profile 或清 profile**重新进入，重复第 2 条仍不复发。
5. 自动播放热路径 DOM 证据满足：
   - 同一张卡片任意时刻只有一个 `visibleSurface`；
   - 不再出现 `previewReadyState = 0 + canonical covered = true + poster/frozen 都没占位` 这种黑闪形态；
   - 当 `visibleSurface = live` 时，对应 canonical live video 已通过 `requestVideoFrameCallback` 或严格 fallback 证明存在 committed frame。
6. 新进房 trace 中，自动播放热路径不再对视频附件发起 `/api/attachments/.../content?variant=original`。
7. 查看器、图片附件、非自动播放冷态 poster 不回归。
8. `pnpm --dir frontend test` 通过。
9. `pnpm --dir frontend typecheck` 通过。
10. `pnpm --dir frontend build` 通过。
11. 冒烟链路必须联用：
    - `playwright-cli`
    - `chrome-devtools-cli`
    - `browser-trace`
12. `gitnexus_detect_changes()` 在提交前已执行。
13. `git status --short` 在提交后只允许保留主人预先存在的脏文件，不能留下本次计划执行造成的尾巴。

---

## 架构裁决

### 裁决 1：自动播放热路径禁用第二颗 `preview <video>`

`preview <video>` 可以继续作为**非自动播放、非 owner、静态预览阶段**的冷态组件存在；但一旦某个附件进入自动播放候选 / pending owner / settled owner 热路径，它就不能再参与可见揭帘。

热路径里只能是：

- `bridge`
- `live`

而不是：

- `poster -> preview video -> canonical`
- `poster -> preview video + canonical -> canonical`
- `frozen + preview video + canonical`

### 裁决 2：自动播放热路径禁用 HTTP `original/thumbnail` 内容地址

`/api/attachments/{id}/content?variant=original|thumbnail` 可以继续服务：

- 静态封面
- 冷态 idle 卡片
- 图片附件

但不能继续服务：

- 自动播放候选预热
- pending owner 揭帘
- settled owner 续播过渡

只要 trace 里在自动播放热路径还看到视频附件 `variant=original` 请求，就视为本轮未完成。

### 裁决 3：自动播放只认一个稳定可见槽位

`VideoSurfaceSlot` 必须是唯一对外可见主位。内部可以有：

- `idle_poster`
- `bridge_frame`
- `live_video`

但不能让用户看到多个主壳争抢可见位。

### 裁决 4：揭帘条件只认“frame committed”

禁止用这些弱信号直接揭帘：

- `loadedmetadata`
- `canplay`
- `playing`
- `currentTime > 0`

它们可以帮助预热，但不能单独证明“用户现在真的能看见目标帧”。

揭帘必须认：

- `requestVideoFrameCallback` 提交的当前目标帧事实
- 或严格 fallback：`readyState >= HAVE_CURRENT_DATA` 且同一 live 节点已经建立 committed 标记

---

## 文件责任图

### 重点阅读

- `E:\koko\frontend\房间消息窗\附件渲染.ts`
- `E:\koko\frontend\房间消息窗\视频附件渲染.ts`
- `E:\koko\frontend\房间消息窗\视频附件表面渲染.ts`
- `E:\koko\frontend\房间消息窗\视频桥接帧.ts`
- `E:\koko\frontend\房间消息窗\时间线画面缓存.ts`
- `E:\koko\frontend\房间消息窗\时间线媒体基类.ts`
- `E:\koko\frontend\房间消息窗\时间线媒体协作.ts`
- `E:\koko\frontend\房间消息窗\时间线播放器宿主.ts`
- `E:\koko\frontend\媒体\视频可见槽位协议.ts`
- `E:\koko\frontend\媒体\全局唯一播放器.ts`
- `E:\koko\frontend\媒体\播放会话\应用.ts`
- `E:\koko\frontend\媒体\壳层\快照投影协作.ts`
- `E:\koko\frontend\平台\传输.ts`

### 预计修改

- Modify: `E:\koko\frontend\媒体\视频可见槽位协议.ts`
  - 引入明确的 `hotPathMode` / `visibleSurface` / `requiresCommittedLiveFrame`。
- Modify: `E:\koko\frontend\房间消息窗\视频附件渲染.ts`
  - 删除热路径里对 `preview video` 的可见位使用。
- Modify: `E:\koko\frontend\房间消息窗\视频附件表面渲染.ts`
  - 把渲染分支收成 `poster | bridge | live` 三态，不再允许 preview video 参与热路径可见位。
- Modify: `E:\koko\frontend\房间消息窗\时间线画面缓存.ts`
  - 明确 bridge 只来自同一颗 live canonical video。
- Modify: `E:\koko\frontend\房间消息窗\时间线媒体基类.ts`
  - 重新定义 warm frame / committed frame / preview evidence 的读写边界。
- Modify: `E:\koko\frontend\媒体\全局唯一播放器.ts`
  - 让 committed-frame 事实成为唯一 live reveal 依据。
- Modify: `E:\koko\frontend\媒体\壳层\快照投影协作.ts`
  - 切断自动播放热路径对 `thumbnail` 地址的依赖。
- Modify: `E:\koko\frontend\媒体\播放会话\应用.ts`
  - 追清 `/api/attachments/.../content` 进入视频自动播的入口，切断视频热路径 `original` 候选。

### 测试文件

- Create: `E:\koko\frontend\tests\媒体播放\自动播放热路径单表面协议测试.spec.ts`
- Create: `E:\koko\frontend\tests\媒体播放\视频热路径禁止HTTP原附件内容测试.spec.ts`
- Modify: `E:\koko\frontend\tests\媒体播放\视频可见槽位冷路径协议测试.spec.ts`
- Modify: `E:\koko\frontend\tests\房间消息窗\海报预热与候选预算测试.spec.ts`
- Modify: `E:\koko\frontend\tests\房间消息窗\无海报视频预览源测试.spec.ts`
- Modify: `E:\koko\frontend\tests\房间消息窗\自动播露出门禁测试.spec.ts`
- Modify: `E:\koko\frontend\tests\房间消息窗\冻结帧与隐藏接管测试.spec.ts`

---

## 任务计划

### Task 1: 重新取证真正的第二表面和第二字节路径

**Files:**
- Read: `frontend/房间消息窗/附件渲染.ts`
- Read: `frontend/媒体/壳层/快照投影协作.ts`
- Read: `frontend/媒体/播放会话/应用.ts`
- Read: `frontend/房间消息窗/视频附件渲染.ts`
- Read: `frontend/媒体/视频可见槽位协议.ts`

- [ ] **Step 1: 重新跑 GitNexus / Serena 调查，不复用旧会话印象**

Run GitNexus:

```text
context(name="渲染视频附件", file_path="frontend/房间消息窗/视频附件渲染.ts")
context(name="创建媒体快照投影协作", file_path="frontend/媒体/壳层/快照投影协作.ts")
context(name="读取视频预览候选播放源", file_path="frontend/媒体/播放会话/应用.ts")
impact(target="渲染视频附件", direction="upstream")
impact(target="创建前端传输/buildAttachmentContentUrl", direction="upstream")
```

Run Serena:

```text
find_symbol("读取时间线视频封面地址", "frontend/房间消息窗/附件渲染.ts")
find_symbol("读取时间线视频首帧预览源", "frontend/房间消息窗/附件渲染.ts")
search_for_pattern("variant=original|variant=thumbnail", "frontend")
```

Expected:

- 产出一张“第二表面 / 第二字节路径”归属表。
- 如果 blast radius 出现 HIGH/CRITICAL，先停下汇报。

- [ ] **Step 2: 用全新浏览器 profile 重新跑新进房首轮上滑烟测**

Use a brand-new Chrome user-data-dir and a new CDP port:

```powershell
"C:\Program Files\Google\Chrome\Application\chrome.exe" `
  --remote-debugging-port=9333 `
  --user-data-dir="$env:TEMP\koko-cold-fresh-entry-plan-proof" `
  https://127.0.0.1/
```

Then:

```powershell
chrome-devtools list_pages --output-format=json
playwright-cli attach --cdp=http://127.0.0.1:9333
node C:\Users\home\.claude\skills\browser-trace\scripts\start-capture.mjs 9333 codex-hotpath-proof
```

Expected:

- `chrome-devtools-cli` 能正常连上当前浏览器。
- 如果这里出现 `EPERM \\.\pipe\chrome-devtools-mcp\server.sock` 或同类宿主错误，先修 CLI 运行时，再继续本计划。
- 不允许把 CLI 长期坏着、只拿 MCP 或 Playwright 顶替，然后宣称三链路闭环。

Then join `1234b` manually from the empty home screen.

Sample `30ms / 240ms` card states while scrolling `G -> ... -> A`.

Expected:

- 明确记录 `blackFlashLikeStates` 是否还存在。
- 记录每张卡片的 `posterPresent / previewPresent / previewReadyState / canonicalCovered / frozenPresent`。

- [ ] **Step 3: 追 `/api/attachments/.../content?variant=original` 的实际触发链**

Run:

```powershell
playwright-cli network
```

Expected:

- 把所有视频附件 `variant=original` 请求单独列出来。
- 标明是 idle poster、viewer、autoplay candidate、pending owner 还是 settled owner 阶段触发的。
- 如果不能定位，先补只读诊断日志，再继续，不准直接切代码。

- [ ] **Step 4: Commit diagnostics checkpoint**

```bash
git add <only new diagnostics notes/tests if any>
git commit -m "chore: 记录自动播放热路径第二表面与第二字节路径证据"
```

如果这一步没有仓库文件改动，则跳过 commit，但必须在执行记录里写清楚“只采集了浏览器证据，未产生仓库变更”。

### Task 2: 先写失败测试，把“热路径只允许 bridge/live”钉死

**Files:**
- Create: `frontend/tests/媒体播放/自动播放热路径单表面协议测试.spec.ts`
- Create: `frontend/tests/媒体播放/视频热路径禁止HTTP原附件内容测试.spec.ts`
- Modify: `frontend/tests/媒体播放/视频可见槽位冷路径协议测试.spec.ts`

- [ ] **Step 1: 为单表面热路径写失败协议测试**

Add tests like:

```ts
it("自动播放热路径里不能让 preview video 成为可见主位", () => {
  const result = 判定播放连续性表面({
    /* 热路径 + live 未提交 + bridge 可用 */
  });
  expect(result.visibleSurface).toBe("bridge");
  expect(result.allowVisiblePreviewVideo).toBe(false);
});

it("自动播放热路径里 live 未提交时不能掉回无帧空窗", () => {
  const result = 判定播放连续性表面({
    /* hot path with no committed live frame and no bridge */
  });
  expect(result.visibleSurface).toBe("placeholder");
  expect(result.requiresCommittedLiveFrame).toBe(true);
});
```

- [ ] **Step 2: 为视频热路径禁止 HTTP original 内容地址写失败测试**

Add tests around `创建媒体快照投影协作(...)` / `播放会话应用`:

```ts
it("视频自动播候选预算不能把 /api/attachments/.../content?variant=original 当成 preview candidate", () => {
  const projection = 创建媒体快照投影协作(/* ... */);
  expect(projection.读取信息流视频预算表()["att-video-1"]?.previewVideoSrc).not.toContain("variant=original");
});
```

- [ ] **Step 3: 跑最小测试确认 RED**

```powershell
pnpm --dir frontend test -- "tests/媒体播放/自动播放热路径单表面协议测试.spec.ts" "tests/媒体播放/视频热路径禁止HTTP原附件内容测试.spec.ts" "tests/媒体播放/视频可见槽位冷路径协议测试.spec.ts"
```

Expected: fail on missing protocol fields or wrong preview/original path behavior.

- [ ] **Step 4: Commit failing tests**

```bash
git add frontend/tests/媒体播放/自动播放热路径单表面协议测试.spec.ts frontend/tests/媒体播放/视频热路径禁止HTTP原附件内容测试.spec.ts frontend/tests/媒体播放/视频可见槽位冷路径协议测试.spec.ts
git commit -m "test: 固化自动播放热路径单表面与禁用原附件内容要求"
```

### Task 3: 切断视频自动播放热路径里的 HTTP 内容路径

**Files:**
- Modify: `frontend/媒体/壳层/快照投影协作.ts`
- Modify: `frontend/媒体/播放会话/应用.ts`
- Read/possibly modify: `frontend/平台/传输.ts`

- [ ] **Step 1: 让视频附件预览地址表只服务静态 idle poster**

目标：

- `读取附件预览地址表()` 继续可以给视频冷态卡片提供 thumbnail poster。
- 但这个 `previewSrc` 不能再被自动播放预算/热路径用作 preview candidate。

Implementation sketch:

```ts
previewSrc:
  attachment.kind === "video"
    ? deps.构建附件内容地址(attachment.attachmentId, "thumbnail")
    : "";
```

保留上面逻辑，但明确注释和下游使用边界：

- 这是 idle poster only。
- 不准进入 autoplay hot path。

- [ ] **Step 2: 在 `读取信息流视频预算表()` 里禁止视频热路径消费 `previewSrc` 内容地址**

目标：

- `previewCandidate` 只能来自当前已成立的 `swarm` playback。
- 不允许 fallback 到 `buildAttachmentContentUrl(..., "original")`。
- 不允许把静态 thumbnail 地址抬成热路径视频源。

Minimal implementation:

```ts
const previewCandidate = deps.读取视频预览候选播放源(attachmentId);
// previewCandidate 只能是 swarm src；没有就返回 null
```

- [ ] **Step 3: 追清 fresh-entry 里为什么还有视频 `variant=original` 请求，并切掉自动播放触发入口**

这里不许猜。执行者必须：

1. 搜索 `buildAttachmentContentUrl(` 调用链。
2. 用浏览器 trace 标明哪个阶段发起。
3. 只切自动播放热路径，不误伤图片与静态冷态封面。

可能落点：

- `媒体查看器`
- `消息舞台投影`
- `聊天媒体编排`

如果某条 `variant=original` 只属于图片或明确非自动播场景，记录后放过；如果它属于视频自动播链，必须切掉。

- [ ] **Step 4: 跑目标测试确认 GREEN**

```powershell
pnpm --dir frontend test -- "tests/媒体播放/视频热路径禁止HTTP原附件内容测试.spec.ts" "tests/媒体查看器/视频主链与媒体信号测试.spec.ts" "tests/聊天壳/消息舞台与媒体投影测试.spec.ts"
```

- [ ] **Step 5: Commit**

```bash
git add frontend/媒体/壳层/快照投影协作.ts frontend/媒体/播放会话/应用.ts
git commit -m "refactor: 切断视频自动播放热路径里的HTTP原附件内容路径"
```

### Task 4: 删除热路径里的 `preview <video>` 可见主位

**Files:**
- Modify: `frontend/媒体/视频可见槽位协议.ts`
- Modify: `frontend/房间消息窗/视频附件渲染.ts`
- Modify: `frontend/房间消息窗/视频附件表面渲染.ts`

- [ ] **Step 1: 在协议层定义“热路径禁止 visible preview video”**

Add protocol fields like:

```ts
type VisibleSurface = "placeholder" | "bridge" | "live";

type HotPathMode = "idle" | "autoplay_candidate" | "pending_owner" | "settled_owner";

allowVisiblePreviewVideo: boolean;
requiresCommittedLiveFrame: boolean;
```

规则：

- `idle` 阶段允许 poster。
- `autoplay_candidate / pending_owner / settled_owner` 阶段，`allowVisiblePreviewVideo === false`。

- [ ] **Step 2: 把渲染层收成三态**

在 `视频附件表面渲染.ts` 中，热路径只允许：

```ts
if (visibleSurface === "placeholder") render poster/guard
if (visibleSurface === "bridge") render frozen-frame bridge
if (visibleSurface === "live") render canonical host
```

禁止出现：

- `preview video + canonical`
- `poster + preview video + canonical`
- `frozen + preview video + canonical`

- [ ] **Step 3: 删除 `视频附件渲染.ts` 里热路径对 `shouldRenderPreviewVideo` 的依赖**

保留 `preview video` 仅用于非热路径静态预览场景；一旦进入自动播放热路径，`shouldRenderPreviewVideo` 必须被压成 `false`。

- [ ] **Step 4: 跑协议与房间消息窗测试确认 GREEN**

```powershell
pnpm --dir frontend test -- "tests/媒体播放/自动播放热路径单表面协议测试.spec.ts" "tests/房间消息窗/海报预热与候选预算测试.spec.ts" "tests/房间消息窗/自动播露出门禁测试.spec.ts" "tests/房间消息窗/无海报视频预览源测试.spec.ts" "tests/媒体查看器/全局播放器归位与时间线交接测试.spec.ts"
```

- [ ] **Step 5: Commit**

```bash
git add frontend/媒体/视频可见槽位协议.ts frontend/房间消息窗/视频附件渲染.ts frontend/房间消息窗/视频附件表面渲染.ts
git commit -m "refactor: 删除自动播放热路径里的第二视频表面"
```

### Task 5: 让 bridge 只来自 live canonical video 的同源同帧证据

**Files:**
- Modify: `frontend/房间消息窗/时间线画面缓存.ts`
- Modify: `frontend/房间消息窗/时间线媒体基类.ts`
- Modify: `frontend/媒体/全局唯一播放器.ts`
- Modify: `frontend/tests/common/房间消息窗媒体支架.ts`

- [ ] **Step 1: 区分 `ready src` 和 `committed frame`**

当前 `ready once`、`currentTime > 0`、`playing` 都太弱。需要明确：

```ts
type FrameEvidence =
  | { kind: "none" }
  | { kind: "bridge"; src: string; currentTime: number }
  | { kind: "live_committed"; src: string; mediaTime: number };
```

- [ ] **Step 2: 让 `全局唯一播放器.ts` 只在 committed frame 后发出 live reveal 事实**

优先：

```ts
video.requestVideoFrameCallback((now, metadata) => {
  markCommittedFrame({
    src,
    mediaTime: metadata.mediaTime,
    presentedFrames: metadata.presentedFrames,
  });
});
```

fallback 也必须比 `playing` 更严格，不能只靠事件名。

- [ ] **Step 3: bridge 只能由 live canonical 采样生成**

`时间线画面缓存.ts` 只允许从 canonical live video 捕获：

- 同 src
- 有效 currentTime
- `readyState >= HAVE_CURRENT_DATA`
- 非零 videoWidth/videoHeight

禁止把 poster、thumbnail、HTTP original 内容地址伪装成 bridge。

- [ ] **Step 4: 跑 focused tests**

```powershell
pnpm --dir frontend test -- "tests/房间消息窗/冻结帧与隐藏接管测试.spec.ts" "tests/房间消息窗/海报预热与候选预算测试.spec.ts" "tests/媒体播放/自动播放热路径单表面协议测试.spec.ts"
```

- [ ] **Step 5: Commit**

```bash
git add frontend/房间消息窗/时间线画面缓存.ts frontend/房间消息窗/时间线媒体基类.ts frontend/媒体/全局唯一播放器.ts frontend/tests/common/房间消息窗媒体支架.ts
git commit -m "refactor: 让自动播放bridge只承认live canonical的同帧证据"
```

### Task 6: 全量验证与真实新进房验收

**Files:**
- No production changes expected
- Use browser tools and existing tests

- [ ] **Step 1: 跑全量前端验证**

```powershell
pnpm --dir frontend test
pnpm --dir frontend typecheck
pnpm --dir frontend build
```

Expected:

- all pass
- 重点二次确认查看器、图片附件、消息舞台投影这三类未被热路径修复误伤。

- [ ] **Step 2: 用全新 profile 重新跑新进房烟测**

必须重复这一套：

1. 新 profile
2. 首页空态
3. 手输 `1234b`
4. `G -> ... -> A`
5. 记录 `30ms / 240ms` 样本

验收标准：

- `blackFlashLikeStates = []`
- 不再出现 `variant=original` 视频自动播热路径请求
- `console error = 0`

- [ ] **Step 3: 用 browser-trace 汇总二次证明**

```powershell
node C:\Users\home\.claude\skills\browser-trace\scripts\stop-capture.mjs codex-hotpath-proof
node C:\Users\home\.claude\skills\browser-trace\scripts\bisect-cdp.mjs codex-hotpath-proof
```

Expected:

- trace summary 可证明无新控制台 error。
- failed requests 若存在，只允许用户主动切换导致的 `net::ERR_ABORTED`，不允许结构性 4xx/5xx。

- [ ] **Step 4: 更新图谱**

```powershell
graphify update .
```

- [ ] **Step 5: 运行 GitNexus 变更检查**

```text
detect_changes(scope="all", repo="koko")
```

Expected:

- 变更只落在本计划预期的媒体/消息窗/测试切片。

- [ ] **Step 6: Commit final verification notes**

```bash
git add <verification artifacts that belong in repo, if any>
git commit -m "test: 验证群聊视频自动播放不再先闪烁再续播"
```

如果无仓库验证文件变化，这一步跳过，但必须在最终报告里贴出命令和结果摘要。

---

## 真实浏览器诊断脚本

执行阶段统一使用这段 shadow-DOM-aware 采样脚本，禁止再用只扫 `document.querySelectorAll("article")` 的旧脚本：

```js
const collectRoots = (root = document) => [
  root,
  ...Array.from(root.querySelectorAll("*")).flatMap((element) =>
    element.shadowRoot ? collectRoots(element.shadowRoot) : []
  ),
];

const roots = collectRoots();
const cards = roots.flatMap((root) =>
  Array.from(root.querySelectorAll(".message-video-card"))
);

return cards.map((card) => {
  const preview = card.querySelector("video.message-video-preview:not([data-canonical-player='true'])");
  const canonical = card.querySelector("video[data-canonical-player='true']");
  const host = card.querySelector(".message-video-canonical-host");
  const poster = card.querySelector(".message-video-poster");
  const frozen = card.querySelector(".message-video-frozen-frame");

  return {
    attachmentId: card.getAttribute("data-attachment-id"),
    surfaces: [
      poster ? "poster" : null,
      preview ? "preview_video" : null,
      frozen ? "bridge" : null,
      host ? `canonical:${host.getAttribute("data-covered")}` : null,
    ].filter(Boolean),
    visibleSurfaceCount: [
      poster,
      preview,
      frozen,
      host?.getAttribute("data-covered") === "false" ? host : null,
    ].filter(Boolean).length,
    previewReadyState: preview?.readyState ?? null,
    previewPaused: preview?.paused ?? null,
    canonicalReadyState: canonical?.readyState ?? null,
    canonicalPaused: canonical?.paused ?? null,
    canonicalCurrentTime: canonical?.currentTime ?? null,
    canonicalSrc: canonical?.currentSrc || canonical?.getAttribute("src") || null,
  };
});
```

---

## 计划风险

1. **风险：切掉 `preview video` 后首屏静态体验变差**
   - 对策：只切自动播放热路径；idle 非候选阶段仍允许 poster。
2. **风险：错误切断 `/api/attachments/.../content` 伤到图片或查看器**
   - 对策：先 trace 分类，只切视频自动播热路径。
3. **风险：bridge 过严导致 reveal 太慢**
   - 对策：允许 bridge 来自 canonical live frame capture，但不允许来自 poster/thumbnail/original。
4. **风险：某些浏览器 `requestVideoFrameCallback` 不可靠**
   - 对策：保留严格 fallback，但 fallback 不能退化成 `playing` 事件即揭帘。

---

## 明确不做

- 不新增第二颗正式播放器。
- 不让每张卡片长期保活一颗解码视频。
- 不恢复 HLS / DASH / 直链 / CDN 第二视频主链。
- 不拿 poster 冒充 paused frame。
- 不用 timeout / debounce 数字微调掩盖真正的 owner / surface / source 竞争。
- 不顺手重构无关媒体模块。

---

## 执行提示

1. 这不是“继续补洞”的计划；它要求先证实第二表面和第二字节路径，再切热路径。
2. 执行者如果发现真实调用链和本计划假设不一致，必须先更新计划或向主人汇报，不能硬做。
3. 每完成一个 Task，都要重新读被 touched 的文件，防止在旧上下文上叠补丁。
4. 真实烟测失败三次以上，不允许继续在同一假设上打第 4 个补丁，必须回到根因分类重新建模。
