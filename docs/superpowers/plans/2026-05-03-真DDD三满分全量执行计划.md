# 真DDD三满分全量执行计划 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. Use `superpowers:subagent-driven-development` only if the owner explicitly authorizes subagents. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `koko` 从当前 `验证中` 推进到可证明的 `DDD: 满分 / 六边形边界: 满分 / 简洁度: 满分`。

**Architecture:** 当前架构目标是模块化单体 + Rust 六边形边界。执行顺序必须先加硬门禁并产生 RED，再修边界泄漏、死代码和热点文件，最后用独立全仓复核、graphify 和 `1234b` 双浏览器真实烟测收口。

**Tech Stack:** Rust、Axum、SQLx、socketioxide、TypeScript、Lit、XState、Video.js v10、WebTorrent、Vitest、Playwright/browser-trace、graphify。

---

## 0. 当前事实

依据：

- `docs/superpowers/specs/2026-05-02-真DDD满分验收规格.md`
- `graphify-out/GRAPH_REPORT.md`
- `tests/架构边界守卫.rs`
- `tests/媒体测试边界守卫.rs`
- `scripts/check-frontend-architecture-fitness.mjs`
- `frontend/package.json`

当前不是满分，已确认缺口：

| 类别 | 当前事实 |
| --- | --- |
| 后端超 987 行 | `src/媒体/资产/外壳.rs` 1151 行 |
| 前端超 987 行 | `frontend/媒体/资产协作分发运行时.ts` 1622、`frontend/房间消息窗/壳.ts` 1434、`frontend/媒体/媒体发布.ts` 1261、`frontend/媒体/媒体查看器.ts` 1069、`frontend/媒体/媒体协作分发.ts` 1064、`frontend/媒体/videojs播放器壳.ts` 1036 |
| 内层泄漏外层类型 | `src/媒体/协作分发/共享语义.rs` 直接使用 `axum::http::{HeaderMap, Uri}` |
| TypeScript dead-code | `pnpm --dir frontend exec tsc --noEmit --noUnusedLocals --noUnusedParameters` 当前失败 |
| graphify 风险 | 684 nodes / 1491 edges / 17 communities；`as_str()`、`send_json()`、`get()` 等高连接节点需要最终复核 |

当前禁止：

- 禁止把完成矩阵提前升回 `已完成`。
- 禁止为拆热点新增 `helper / utils / wrapper / facade / common`。
- 禁止同一能力新旧路径并存。
- 禁止只让局部门禁通过就宣称满分。
- 禁止跳过 TDD：每个语义改动先 RED，再 GREEN，再 REFACTOR。

---

## 1. 文件责任图

### 新增或修改的门禁文件

| 文件 | 责任 |
| --- | --- |
| `tests/架构边界守卫.rs` | 后端内层依赖方向、全生产 `.rs` 热点、完成矩阵状态硬门禁 |
| `tests/媒体测试边界守卫.rs` | 媒体稳定语义纯净、媒体旧入口与 owner 边界守卫 |
| `scripts/check-frontend-architecture-fitness.mjs` | 前端全生产热点、前端内层/平台依赖、owner 注册、兜底命名 |
| `scripts/check-full-architecture-completion.mjs` | 独立全仓复核输出：热点、内层泄漏、兜底桶、barrel/re-export、graphify 摘要 |
| `frontend/package.json` | 把 strict unused 类型检查纳入默认验证链 |
| `docs/superpowers/reports/2026-05-01-真DDD重构完成矩阵.md` | 只在全部门禁和烟测通过后升级完成态 |

### 后端生产切分目标

| 当前文件 | 拆分方向 |
| --- | --- |
| `src/媒体/协作分发/共享语义.rs` | 移除 Axum/HTTP 类型；只保留稳定语义和值对象 |
| `src/媒体/资产/外壳.rs` | 拆出 query 解析、Range 响应、locator 响应投影、presence 上报端点 |

### 前端生产切分目标

| 当前文件 | 拆分方向 |
| --- | --- |
| `frontend/房间消息窗/壳.ts` | 只留消息视口壳；冻结帧、播放器宿主同步、媒体窗口观察、候选预热下沉 |
| `frontend/媒体/资产协作分发运行时.ts` | 拆会话生命周期、join ticket 续租、piece 选择、事件绑定、预算投影 |
| `frontend/媒体/媒体发布.ts` | 拆草稿状态、上传调度、诊断映射、完成发布编排 |
| `frontend/媒体/媒体查看器.ts` | 拆图片查看器、视频查看器、全屏适配、状态投影 |
| `frontend/媒体/媒体协作分发.ts` | 拆 WebTorrent 适配、chunk store、source 探测、peer 信号适配 |
| `frontend/媒体/videojs播放器壳.ts` | 拆 source 设置、宿主迁移、全屏适配、错误转译 |

---

## Task 1: 先让“假满分”自动失败

**Files:**
- Modify: `tests/架构边界守卫.rs`
- Modify: `tests/媒体测试边界守卫.rs`
- Modify: `scripts/check-frontend-architecture-fitness.mjs`
- Create: `scripts/check-full-architecture-completion.mjs`
- Test: `cargo test --test 架构边界守卫`
- Test: `cargo test --test 媒体测试边界守卫`
- Test: `node scripts/check-frontend-architecture-fitness.mjs`
- Test: `node scripts/check-full-architecture-completion.mjs`

- [ ] **Step 1: 读当前目标文件和调用表面**

Run:

```powershell
Get-Content tests/架构边界守卫.rs -TotalCount 760
Get-Content tests/媒体测试边界守卫.rs -TotalCount 260
Get-Content scripts/check-frontend-architecture-fitness.mjs -TotalCount 980
Get-Content docs/superpowers/specs/2026-05-02-真DDD满分验收规格.md
```

- [ ] **Step 2: 加后端全内层依赖 RED 守卫**

在 `tests/架构边界守卫.rs` 增加动态枚举：

```rust
fn 是后端内层候选文件(path: &str) -> bool {
    path.starts_with("src/领域/")
        || path.starts_with("src/应用/")
        || path.starts_with("src/共享/")
        || path.ends_with("/应用.rs")
        || path.ends_with("/契约.rs")
        || path.ends_with("/模型.rs")
        || path.ends_with("/共享语义.rs")
}

#[test]
fn 后端内层候选文件不得泄漏外层协议类型() {
    let forbidden = [
        "axum::",
        "sqlx::",
        "socketioxide",
        "tower::",
        "reqwest::",
        "HeaderMap",
        "StatusCode",
        "Uri",
        "Json<",
        "State<",
        "Extension<",
        "SocketRef",
    ];
    let violations = 枚举后端生产_rs文件()
        .into_iter()
        .filter(|path| 是后端内层候选文件(path))
        .flat_map(|path| {
            let content = 读取(&path);
            forbidden
                .iter()
                .filter(move |item| content.contains(**item))
                .map(move |item| format!("{path}: {item}"))
        })
        .collect::<Vec<_>>();

    assert!(
        violations.is_empty(),
        "后端内层候选文件泄漏外层协议类型：{violations:?}"
    );
}
```

Run:

```powershell
cargo test --test 架构边界守卫 后端内层候选文件不得泄漏外层协议类型
```

Expected: FAIL，命中 `src/媒体/协作分发/共享语义.rs` 的 `axum::` / `HeaderMap` / `Uri`。

- [ ] **Step 3: 加后端全生产 987 行 RED 守卫**

在 `tests/架构边界守卫.rs` 增加：

```rust
#[test]
fn 后端生产源码文件满分态不得超过九百八十七行() {
    let violations = 枚举后端生产_rs文件()
        .into_iter()
        .filter_map(|path| {
            let lines = 统计物理行数(&path);
            (lines > 987).then_some(format!("{path}: {lines} 行"))
        })
        .collect::<Vec<_>>();

    assert!(
        violations.is_empty(),
        "后端生产源码仍有超 987 行热点：{violations:?}"
    );
}
```

Run:

```powershell
cargo test --test 架构边界守卫 后端生产源码文件满分态不得超过九百八十七行
```

Expected: FAIL，命中 `src/媒体/资产/外壳.rs`。

- [ ] **Step 4: 加媒体共享语义纯净 RED 守卫**

在 `tests/媒体测试边界守卫.rs` 的 `协作分发共享语义必须收进_协作分发_子域` 后增加：

```rust
#[test]
fn 协作分发共享语义不得泄漏外层协议类型() {
    let owner = 读取("src/媒体/协作分发/共享语义.rs");
    for forbidden in ["axum::", "HeaderMap", "Uri", "StatusCode", "Json<", "State<"] {
        assert!(
            !owner.contains(forbidden),
            "协作分发共享语义是稳定内层表面，不得依赖外层协议类型: {forbidden}"
        );
    }
}
```

Run:

```powershell
cargo test --test 媒体测试边界守卫 协作分发共享语义不得泄漏外层协议类型
```

Expected: FAIL。

- [ ] **Step 5: 加前端全生产 987 行 RED 守卫**

在 `scripts/check-frontend-architecture-fitness.mjs` 增加全生产文件扫描函数，不再只靠 `热点文件行数上限` 数组：

```js
const 全生产源码物理行上限 = 987;

const 检查全部前端生产源码物理行数 = (files) => {
  const violations = [];
  for (const absolutePath of files) {
    const relativePath = 转成仓库相对路径(absolutePath);
    if (relativePath.endsWith(".d.ts")) {
      continue;
    }
    const physicalLineCount = 统计物理源码行数(readFileSync(absolutePath, "utf8"));
    if (physicalLineCount > 全生产源码物理行上限) {
      violations.push({
        file: relativePath,
        label: "full-score production hotspot",
        detail: `${physicalLineCount} 行超过满分上限 ${全生产源码物理行上限} 行`,
      });
    }
  }
  return violations;
};
```

并加入 `收集架构适应度违规()`。

Run:

```powershell
node scripts/check-frontend-architecture-fitness.mjs
```

Expected: FAIL，命中 6 个当前前端热点。

- [ ] **Step 6: 创建独立全仓复核脚本**

Create `scripts/check-full-architecture-completion.mjs`，只使用 Node 标准库，输出并失败：

```js
import { readFileSync, readdirSync, statSync } from "node:fs";
import { dirname, extname, join, relative, resolve } from "node:path";
import { fileURLToPath } from "node:url";

const currentFile = fileURLToPath(import.meta.url);
const root = resolve(dirname(currentFile), "..");
const toRel = (path) => relative(root, path).replaceAll("\\", "/");
const read = (path) => readFileSync(path, "utf8");

const walk = (dir, skip = new Set()) => {
  const out = [];
  for (const entry of readdirSync(dir)) {
    if (skip.has(entry)) continue;
    const path = join(dir, entry);
    const stat = statSync(path);
    if (stat.isDirectory()) out.push(...walk(path, skip));
    else out.push(path);
  }
  return out;
};

const sourceFiles = [
  ...walk(join(root, "src")).filter((p) => extname(p) === ".rs"),
  ...walk(join(root, "frontend"), new Set(["tests", "dist", "node_modules"]))
    .filter((p) => [".ts", ".js", ".mjs"].includes(extname(p)) && !p.endsWith(".d.ts")),
];

const overBudget = sourceFiles
  .map((path) => ({ path: toRel(path), lines: read(path).split(/\r?\n/).length }))
  .filter((item) => item.lines > 987);

const forbiddenInner = /axum::|sqlx::|socketioxide|tower::|reqwest::|HeaderMap|StatusCode|Uri|Json<|State<|Extension<|SocketRef/;
const isInner = (path) =>
  path.startsWith("src/领域/") ||
  path.startsWith("src/应用/") ||
  path.startsWith("src/共享/") ||
  /\/(应用|契约|模型|共享语义)\.rs$/.test(path);

const innerLeaks = sourceFiles
  .filter((path) => extname(path) === ".rs")
  .map((path) => ({ path: toRel(path), source: read(path) }))
  .filter((item) => isInner(item.path) && forbiddenInner.test(item.source))
  .map((item) => item.path);

const fallbackName = /(^|\/)[^/]*(helper|helpers|utils|misc|facade|compat|legacy|fallback|wrapper|shim|temp|old|门面|兼容|兜底|临时|旧|包装)[^/]*(\.|\/)/i;
const fallbackNames = sourceFiles.map(toRel).filter((path) => fallbackName.test(path));

const reexports = sourceFiles
  .map((path) => ({ path: toRel(path), source: read(path) }))
  .flatMap(({ path, source }) =>
    source
      .split(/\r?\n/)
      .map((line, index) => ({ path, line: index + 1, text: line.trim() }))
      .filter((item) => /\bpub use\b|export \* from|export \{.*\} from/.test(item.text))
  );

const graph = read(join(root, "graphify-out", "GRAPH_REPORT.md"));
const graphSummary = graph.match(/## God Nodes[\s\S]*?## Surprising Connections/)?.[0] ?? "";

const failures = [];
if (overBudget.length) failures.push(["overBudget", overBudget]);
if (innerLeaks.length) failures.push(["innerLeaks", innerLeaks]);
if (fallbackNames.length) failures.push(["fallbackNames", fallbackNames]);

console.log(JSON.stringify({ overBudget, innerLeaks, fallbackNames, reexports, graphSummary }, null, 2));

if (failures.length) {
  process.exitCode = 1;
}
```

Run:

```powershell
node scripts/check-full-architecture-completion.mjs
```

Expected: FAIL，输出当前超限热点和 `共享语义.rs` 泄漏。

- [ ] **Step 7: 提交 RED 门禁**

Run:

```powershell
git diff --check
git add tests/架构边界守卫.rs tests/媒体测试边界守卫.rs scripts/check-frontend-architecture-fitness.mjs scripts/check-full-architecture-completion.mjs
git commit -m "治理: 加固三满分全仓门禁"
```

Expected: commit 成功；这些门禁失败是预期 RED，不得把完成矩阵升为完成态。

---

## Task 2: TDD 修掉 `media_distribution` 六边形泄漏

**Files:**
- Modify: `src/媒体/协作分发/共享语义.rs`
- Modify: callers found by `rg -n "读取协作分发tracker对外地址|media_distribution" src tests`
- Test: `tests/媒体测试边界守卫.rs`
- Test: `tests/协作分发测试.rs`

- [ ] **Step 1: 查调用方**

Run:

```powershell
rg -n "读取协作分发tracker对外地址|media_distribution" src tests
```

- [ ] **Step 2: 确认 RED**

Run:

```powershell
cargo test --test 媒体测试边界守卫 协作分发共享语义不得泄漏外层协议类型
```

Expected: FAIL。

- [ ] **Step 3: 改稳定语义函数签名**

目标签名：

```rust
pub(crate) fn 读取协作分发tracker对外地址(
    configured_tracker_public_url: &str,
) -> String
```

用标准库字符串判断替代 `Uri`：

```rust
fn 是回环tracker公开地址(url: &str) -> bool {
    let host = url
        .split_once("://")
        .map(|(_, rest)| rest)
        .unwrap_or(url)
        .split('/')
        .next()
        .unwrap_or("")
        .trim_matches('[')
        .trim_matches(']')
        .split(':')
        .next()
        .unwrap_or("");
    matches!(host, "127.0.0.1" | "localhost" | "::1")
}
```

如果 IPv6 `::1` 的 `split(':')` 不够稳，改成一个小值对象 `Tracker公开地址`，但仍不得引入 Axum/HTTP 类型。

- [ ] **Step 4: 更新调用方**

所有调用方只传 `configured_tracker_public_url`。如果调用方原来只是为了传 `HeaderMap`，删除该参数。

- [ ] **Step 5: GREEN**

Run:

```powershell
cargo test --test 媒体测试边界守卫
cargo test --test 协作分发测试
cargo clippy --all-targets -- -D warnings
```

Expected: PASS。

- [ ] **Step 6: 提交**

```powershell
git diff --check
git add src/媒体/协作分发/共享语义.rs tests/媒体测试边界守卫.rs
git commit -m "媒体: 移除协作分发共享语义的协议类型泄漏"
```

---

## Task 3: TypeScript unused 全量清理并纳入默认验证

**Files:**
- Modify: `frontend/package.json`
- Modify: files reported by `pnpm --dir frontend exec tsc --noEmit --noUnusedLocals --noUnusedParameters`
- Test: TypeScript and Vitest

- [ ] **Step 1: 记录 RED**

Run:

```powershell
pnpm --dir frontend exec tsc --noEmit --noUnusedLocals --noUnusedParameters
```

Expected: FAIL。当前已知会命中：

- `frontend/媒体/资产协作分发运行时.ts`
- `frontend/媒体/媒体播放.ts`
- `frontend/平台/传输.ts`
- `frontend/总装/聊天壳.ts`
- `frontend/房间消息窗/壳.ts`
- 多个 `frontend/tests/**/*.spec.ts`

- [ ] **Step 2: 按 owner 清理 production unused**

只删除未使用 import、变量、参数、类型；不要顺手重构语义。每批清理后运行：

```powershell
pnpm --dir frontend exec tsc --noEmit --noUnusedLocals --noUnusedParameters
```

Expected: 错误数量下降，不引入新错误。

- [ ] **Step 3: 按测试 owner 清理 tests unused**

测试支架里的未使用端口、fake、helper 一律删除。确实需要副作用 import 时改为：

```ts
import "../path/to/side-effect-module.js";
```

- [ ] **Step 4: 纳入默认脚本**

修改 `frontend/package.json`：

```json
"typecheck": "tsc --noEmit --noUnusedLocals --noUnusedParameters"
```

不要新增第二个长期弱 typecheck。

- [ ] **Step 5: GREEN**

Run:

```powershell
pnpm --dir frontend exec tsc --noEmit --noUnusedLocals --noUnusedParameters
pnpm --dir frontend test
node scripts/check-frontend-architecture-fitness.mjs
```

Expected: PASS。

- [ ] **Step 6: 提交**

```powershell
git diff --check
git add frontend/package.json frontend/tests frontend/媒体 frontend/平台 frontend/总装 frontend/房间消息窗
git commit -m "前端: 清理未使用代码并收紧类型检查"
```

注意：上面 `git add` 要按实际改动路径细化，不能把无关文件加进去。

---

## Task 4: 拆薄后端媒体资产外壳

**Files:**
- Modify: `src/媒体/资产/外壳.rs`
- Create: `src/媒体/资产/查询解析.rs`
- Create: `src/媒体/资产/范围响应.rs`
- Create: `src/媒体/资产/定位投影.rs`
- Create: `src/媒体/资产/presence外壳.rs`
- Modify: `src/媒体/mod.rs` or `src/媒体/资产/mod.rs` if needed
- Modify: `src/外壳/mod.rs` route mount if endpoint owner moves
- Test: `tests/媒体测试边界守卫.rs`
- Test: media asset / collaboration tests

- [ ] **Step 1: 写 characterization**

Before moving code, add or identify tests that cover:

- locator response still hides top-level `original_url`
- blob/original range still supports 200/206
- presence report still updates collaboration freshness
- query parse errors still map to stable error codes

Run:

```powershell
cargo test --test 媒体测试边界守卫
cargo test --test 协作分发测试
cargo test --test blob媒体资产契约测试
```

- [ ] **Step 2: Move query parsing**

Move pure query structs/functions out of `src/媒体/资产/外壳.rs` into `src/媒体/资产/查询解析.rs`:

- `ParsedAttachmentContentQuery`
- `ParsedSessionQuery`
- `ParsedDistributionPresencePayload`
- parse functions that do not need Axum extractors

Keep `StatusCode` only if file is treated as shell. If parsing becomes stable semantic parsing, return own error enum instead.

- [ ] **Step 3: Move Range response**

Move byte range parsing and response assembly into `src/媒体/资产/范围响应.rs`:

- `标准字节范围`
- range parsing
- content-range/header construction

This module is shell/adapter support, not domain.

- [ ] **Step 4: Move locator projection**

Move response JSON projection helpers into `src/媒体/资产/定位投影.rs`:

- locator payload shaping
- nested asset/origin fields
- no DB access
- no business decision

- [ ] **Step 5: Move presence endpoint**

If presence endpoint is separable, move its handler to `src/媒体/资产/presence外壳.rs` and mount directly from `src/外壳/mod.rs` or `src/媒体/资产/mod.rs`.

- [ ] **Step 6: DELETE old code from original file**

`src/媒体/资产/外壳.rs` must not keep forwarding wrappers. Imports should prove the file now composes smaller owner modules.

- [ ] **Step 7: GREEN**

Run:

```powershell
cargo test --test 媒体测试边界守卫
cargo test --test 协作分发测试
cargo test --test blob媒体资产契约测试
cargo clippy --all-targets -- -D warnings
cargo test --test 架构边界守卫 后端生产源码文件满分态不得超过九百八十七行
```

Expected: PASS and `src/媒体/资产/外壳.rs` <= 987 physical lines.

- [ ] **Step 8: 提交**

```powershell
git diff --check
git add src/媒体/资产 src/外壳/mod.rs tests/媒体测试边界守卫.rs tests/架构边界守卫.rs
git commit -m "媒体: 按资产协议职责拆薄后端外壳"
```

---

## Task 5: 拆薄 `frontend/房间消息窗/壳.ts`

**Files:**
- Modify: `frontend/房间消息窗/壳.ts`
- Create/modify: `frontend/房间消息窗/媒体窗口.ts`
- Create/modify: `frontend/房间消息窗/时间线播放器宿主.ts`
- Create/modify: `frontend/房间消息窗/自动播候选观察器.ts`
- Create/modify: `frontend/房间消息窗/时间线画面缓存.ts`
- Test: `frontend/tests/房间消息窗/**/*.spec.ts`
- Test: `frontend/tests/浏览器应用平台测试.spec.ts`

- [ ] **Step 1: 确认现有 owner 文件**

Run:

```powershell
rg -n "class .*Owner|export const|export function" frontend/房间消息窗 -g "*.ts"
```

- [ ] **Step 2: characterization**

Run:

```powershell
pnpm --dir frontend test -- tests/房间消息窗
pnpm --dir frontend test -- tests/浏览器应用平台测试.spec.ts
```

- [ ] **Step 3: 下沉媒体窗口观察和候选预热**

Move methods related to media window observation and autoplay candidate calculation into existing owner modules. `壳.ts` should dispatch intent and render state only.

- [ ] **Step 4: 下沉播放器宿主同步**

Keep canonical player owner outside the pane. `壳.ts` may pass host refs but must not own player truth.

- [ ] **Step 5: 删除 one-hop wrappers**

Search:

```powershell
rg -n "private .*自动播|private .*播放器|private .*冻结|wrapper|helper" frontend/房间消息窗/壳.ts
```

Every one-hop wrapper must be deleted or converted into a real owner call.

- [ ] **Step 6: GREEN**

Run:

```powershell
pnpm --dir frontend test -- tests/房间消息窗
pnpm --dir frontend test -- tests/浏览器应用平台测试.spec.ts
node scripts/check-frontend-architecture-fitness.mjs
pnpm --dir frontend exec tsc --noEmit --noUnusedLocals --noUnusedParameters
```

Expected: PASS and `frontend/房间消息窗/壳.ts` <= 987 physical lines.

- [ ] **Step 7: 提交**

```powershell
git diff --check
git add frontend/房间消息窗 frontend/tests/房间消息窗 frontend/tests/浏览器应用平台测试.spec.ts scripts/check-frontend-architecture-fitness.mjs
git commit -m "前端: 拆薄房间消息窗媒体交互壳"
```

---

## Task 6: 拆薄资产协作分发运行时

**Files:**
- Modify: `frontend/媒体/资产协作分发运行时.ts`
- Create: `frontend/媒体/协作分发会话生命周期.ts`
- Create: `frontend/媒体/协作分发门票续租.ts`
- Create: `frontend/媒体/协作分发片段选择.ts`
- Create: `frontend/媒体/协作分发事件绑定.ts`
- Create: `frontend/媒体/协作分发预算投影.ts`
- Test: `frontend/tests/资产协作分发运行时测试.spec.ts`

- [ ] **Step 1: characterization**

Run:

```powershell
pnpm --dir frontend test -- tests/资产协作分发运行时测试.spec.ts
```

- [ ] **Step 2: 拆 join ticket 续租**

Move ticket refresh and announce option logic into `协作分发门票续租.ts`。不得改变 ticket TTL 语义。

- [ ] **Step 3: 拆生命周期**

Move start/stop/reuse/destroy session lifecycle into `协作分发会话生命周期.ts`。`资产协作分发运行时.ts` 只做运行时组合。

- [ ] **Step 4: 拆 piece/source/peer 投影**

Move pure source and budget derivation into `协作分发片段选择.ts` and `协作分发预算投影.ts`。

- [ ] **Step 5: 删除旧路径**

No second runtime owner. No `全局唯一WebTorrent` file. No WebTorrent wrapper core.

- [ ] **Step 6: GREEN**

Run:

```powershell
pnpm --dir frontend test -- tests/资产协作分发运行时测试.spec.ts
node scripts/check-frontend-architecture-fitness.mjs
pnpm --dir frontend exec tsc --noEmit --noUnusedLocals --noUnusedParameters
```

Expected: PASS and `frontend/媒体/资产协作分发运行时.ts` <= 987 physical lines.

- [ ] **Step 7: 提交**

```powershell
git diff --check
git add frontend/媒体 frontend/tests/资产协作分发运行时测试.spec.ts
git commit -m "前端: 按协作分发运行时职责拆薄WebTorrent owner"
```

---

## Task 7: 拆薄媒体发布链

**Files:**
- Modify: `frontend/媒体/媒体发布.ts`
- Create: `frontend/媒体/发布草稿状态.ts`
- Create: `frontend/媒体/上传调度.ts`
- Create: `frontend/媒体/上传诊断投影.ts`
- Create: `frontend/媒体/完成发布编排.ts`
- Test: media publisher tests found by `rg -n "媒体发布|上传诊断|完成发布" frontend/tests`

- [ ] **Step 1: 找测试面**

Run:

```powershell
rg -n "媒体发布|上传诊断|完成发布|prepareMediaUpload|completeMediaUpload" frontend/tests
```

- [ ] **Step 2: characterization**

Run the matching test files before moving code.

- [ ] **Step 3: 拆草稿状态**

Move draft state transitions only. No network, no DOM.

- [ ] **Step 4: 拆上传调度**

Move Uppy/Tus scheduling glue. This is adapter/shell logic; it must not decide media business truth.

- [ ] **Step 5: 拆诊断映射和完成发布**

Diagnostics are projection. Complete publish orchestration may call transport but must not duplicate backend media validity decisions.

- [ ] **Step 6: GREEN**

Run:

```powershell
pnpm --dir frontend test
node scripts/check-frontend-architecture-fitness.mjs
pnpm --dir frontend exec tsc --noEmit --noUnusedLocals --noUnusedParameters
```

Expected: PASS and `frontend/媒体/媒体发布.ts` <= 987 physical lines.

- [ ] **Step 7: 提交**

```powershell
git diff --check
git add frontend/媒体 frontend/tests
git commit -m "前端: 按草稿上传完成职责拆薄媒体发布链"
```

---

## Task 8: 拆薄媒体查看器

**Files:**
- Modify: `frontend/媒体/媒体查看器.ts`
- Create: `frontend/媒体/图片查看器.ts`
- Create: `frontend/媒体/视频查看器.ts`
- Create: `frontend/媒体/查看器全屏适配.ts`
- Create: `frontend/媒体/查看器状态投影.ts`
- Test: `frontend/tests/媒体查看器/**/*.spec.ts`
- Test: `frontend/tests/聊天应用内核/查看器会话同步测试.spec.ts`

- [ ] **Step 1: characterization**

Run:

```powershell
pnpm --dir frontend test -- tests/媒体查看器
pnpm --dir frontend test -- tests/聊天应用内核/查看器会话同步测试.spec.ts
```

- [ ] **Step 2: 拆图片查看器**

图片查看器只负责 PhotoSwipe/image surface，不碰 video player truth。

- [ ] **Step 3: 拆视频查看器**

视频查看器只编排 viewer host，不拥有 source truth；source truth 仍来自播放会话/唯一播放器。

- [ ] **Step 4: 拆全屏适配**

Move fullscreen API translation into `查看器全屏适配.ts`。失败只能退到应用内沉浸，不引入第二播放主链。

- [ ] **Step 5: GREEN**

Run:

```powershell
pnpm --dir frontend test -- tests/媒体查看器
pnpm --dir frontend test -- tests/聊天应用内核/查看器会话同步测试.spec.ts
node scripts/check-frontend-architecture-fitness.mjs
pnpm --dir frontend exec tsc --noEmit --noUnusedLocals --noUnusedParameters
```

Expected: PASS and `frontend/媒体/媒体查看器.ts` <= 987 physical lines.

- [ ] **Step 6: 提交**

```powershell
git diff --check
git add frontend/媒体 frontend/tests/媒体查看器 frontend/tests/聊天应用内核
git commit -m "前端: 拆薄媒体查看器并保持播放真相唯一"
```

---

## Task 9: 拆薄媒体协作分发适配与 Video.js 播放器壳

**Files:**
- Modify: `frontend/媒体/媒体协作分发.ts`
- Create: `frontend/媒体/WebTorrent适配.ts`
- Create: `frontend/媒体/持久ChunkStore适配.ts`
- Create: `frontend/媒体/协作分发源探测.ts`
- Create: `frontend/媒体/peer信号适配.ts`
- Modify: `frontend/媒体/videojs播放器壳.ts`
- Create: `frontend/媒体/videojsSource设置.ts`
- Create: `frontend/媒体/videojs宿主迁移.ts`
- Create: `frontend/媒体/videojs全屏适配.ts`
- Create: `frontend/媒体/videojs错误转译.ts`
- Test: `frontend/tests/视频预览协作测试.spec.ts`
- Test: `frontend/tests/房间消息窗/时间线播放器宿主owner测试.spec.ts`
- Test: `frontend/tests/媒体查看器/**/*.spec.ts`

- [ ] **Step 1: characterization**

Run:

```powershell
pnpm --dir frontend test -- tests/视频预览协作测试.spec.ts
pnpm --dir frontend test -- tests/房间消息窗/时间线播放器宿主owner测试.spec.ts
pnpm --dir frontend test -- tests/媒体查看器
```

- [ ] **Step 2: 拆 WebTorrent 适配**

`媒体协作分发.ts` 不再直接承担所有 mature wheel glue。拆出的文件是适配层，不是私有第二核心。

- [ ] **Step 3: 拆 source/peer/chunk store**

Each file must have one reason to exist:

- `持久ChunkStore适配.ts`: IndexedDB chunk store adaptation
- `协作分发源探测.ts`: source availability projection
- `peer信号适配.ts`: peer signal translation

- [ ] **Step 4: 拆 Video.js 壳**

Video.js 只做播放器壳：

- `videojsSource设置.ts`: source set and clear
- `videojs宿主迁移.ts`: host movement
- `videojs全屏适配.ts`: fullscreen translation
- `videojs错误转译.ts`: error mapping

- [ ] **Step 5: GREEN**

Run:

```powershell
pnpm --dir frontend test -- tests/视频预览协作测试.spec.ts
pnpm --dir frontend test -- tests/房间消息窗/时间线播放器宿主owner测试.spec.ts
pnpm --dir frontend test -- tests/媒体查看器
node scripts/check-frontend-architecture-fitness.mjs
pnpm --dir frontend exec tsc --noEmit --noUnusedLocals --noUnusedParameters
```

Expected: PASS and both `frontend/媒体/媒体协作分发.ts` / `frontend/媒体/videojs播放器壳.ts` <= 987 physical lines.

- [ ] **Step 6: 提交**

```powershell
git diff --check
git add frontend/媒体 frontend/tests
git commit -m "前端: 拆薄协作分发适配和Videojs播放器壳"
```

---

## Task 10: 删除第二表面、过宽 barrel 和完成矩阵冲突

**Files:**
- Modify: `src/lib.rs`
- Modify: `frontend/媒体/index.ts` if present
- Modify: any files reported by re-export scan
- Modify: `docs/superpowers/reports/2026-05-01-真DDD重构完成矩阵.md`
- Test: architecture guard and independent checker

- [ ] **Step 1: 扫描公开转发表面**

Run:

```powershell
rg -n "\bpub use\b|export \* from|export \{.*\} from" src frontend -g "*.rs" -g "*.ts"
```

- [ ] **Step 2: 逐项裁决**

For every hit, write one of:

- stable published language, keep
- test-only surface, move under tests
- one-hop forwarding, delete
- oversized barrel, replace with direct owner import

- [ ] **Step 3: 处理 `src/lib.rs`**

If `pub mod media_distribution` still points to `src/媒体/协作分发/共享语义.rs`, decide:

- keep only if it is a stable published language and pure inner semantic surface
- otherwise delete and update callers to `crate::media::distribution` owner path

- [ ] **Step 4: 完成矩阵仍保持验证中**

Do not mark full score yet. Update matrix only with newly closed gaps and remaining gaps.

- [ ] **Step 5: GREEN**

Run:

```powershell
cargo test --test 架构边界守卫
cargo test --test 媒体测试边界守卫
node scripts/check-frontend-architecture-fitness.mjs
node scripts/check-full-architecture-completion.mjs
```

Expected: all pass except completion matrix still not upgraded until final browser smoke and full verification.

- [ ] **Step 6: 提交**

```powershell
git diff --check
git add src frontend docs/superpowers/reports/2026-05-01-真DDD重构完成矩阵.md
git commit -m "治理: 收口公开表面并同步三满分矩阵"
```

---

## Task 11: 全量验证、graphify 和真实浏览器烟测

**Files:**
- Modify: `docs/superpowers/reports/2026-05-01-真DDD重构完成矩阵.md`
- No production code unless verification exposes a bug

- [ ] **Step 1: 全量命令验证**

Run fresh:

```powershell
cargo check --all-targets
cargo test
cargo clippy --all-targets -- -D warnings
cargo test --test 架构边界守卫
cargo test --test 媒体测试边界守卫
node scripts/check-frontend-architecture-fitness.mjs
node scripts/check-frontend-browser-app-constitution.mjs
node scripts/check-full-architecture-completion.mjs
pnpm --dir frontend exec tsc --noEmit --noUnusedLocals --noUnusedParameters
pnpm --dir frontend test
pnpm --dir frontend build
graphify update .
git diff --check
git status --short
```

Expected: all exit code 0; `git status --short` clean before smoke unless graphify updated files that must be committed.

- [ ] **Step 2: 复核 graphify**

Read:

```powershell
Get-Content graphify-out/GRAPH_REPORT.md -TotalCount 220
```

Must record:

- nodes / edges / communities
- god nodes
- knowledge gaps
- whether new high-connectivity helper or isolated owner appeared

- [ ] **Step 3: 启动真实链路**

Use project launcher, not ad hoc server:

```powershell
.\run.ps1 -DisableLocalHttpsBootstrap -DisableAutoOptimizeCleanup
```

Wait until backend/frontend reachable. If ports conflict, use existing cleanup scripts rather than editing business code.

- [ ] **Step 4: `1234b` 双浏览器烟测**

Use project-approved `playwright-cli` / `browser-trace` chain. Cover:

- A/B clean identities enter room `1234b`
- history loads
- A sends text, B sees it
- A uploads real PNG, B sees image card and original entry
- A uploads MP4, B sees video card
- B inline autoplay: real `<video>`, `readyState=4`, `paused=false`, `currentTime>0`, `muted=true`, `autoplay=true`
- budget: inline `activeVideoCount=1`, `activeFormalPlayerCount=1`, `autoplayOwnerCount=1`
- viewer open/close keeps only one formal player
- console 0 error / 0 warning
- MP4 network path stays `/api/media/{attachment}/torrent` + `/webtorrent/{info_hash}/...mp4`

- [ ] **Step 5: 升级完成矩阵**

Only after all commands and smoke pass, update:

`docs/superpowers/reports/2026-05-01-真DDD重构完成矩阵.md`

Allowed final wording:

```text
DDD: 满分
六边形边界: 满分
简洁度: 满分
```

Matrix must list:

- removed old/duplicate surfaces
- protocol types removed from stable semantics
- owners split and target files
- all production hotspots <= 987
- unused/dead-code cleaned
- graphify result
- `1234b` smoke evidence
- final command outputs
- `git status --short` clean

- [ ] **Step 6: Final commit**

```powershell
git diff --check
git add .
git commit -m "治理: 完成真DDD三满分全量验收"
git status --short
```

Expected: clean working tree.

---

## Final Gate

Do not say full score unless all are true:

- [ ] `cargo check --all-targets` passed.
- [ ] `cargo test` passed.
- [ ] `cargo clippy --all-targets -- -D warnings` passed.
- [ ] `cargo test --test 架构边界守卫` passed.
- [ ] `cargo test --test 媒体测试边界守卫` passed.
- [ ] `node scripts/check-frontend-architecture-fitness.mjs` passed.
- [ ] `node scripts/check-frontend-browser-app-constitution.mjs` passed.
- [ ] `node scripts/check-full-architecture-completion.mjs` passed.
- [ ] `pnpm --dir frontend exec tsc --noEmit --noUnusedLocals --noUnusedParameters` passed.
- [ ] `pnpm --dir frontend test` passed.
- [ ] `pnpm --dir frontend build` passed.
- [ ] `graphify update .` passed and report reviewed.
- [ ] `1234b` double-browser smoke passed.
- [ ] completion matrix upgraded with evidence.
- [ ] final commit created.
- [ ] `git status --short` is empty.

If any box is unchecked, status remains `验证中`.

---

## 三遍自审

第一遍：DDD 审查

结论：计划按业务 owner 和真相边界推进，不允许前端、本地缓存、播放器、adapter 或测试支架替后端 domain/application 宣布业务成立。修正点：把完成矩阵升级放到最后，而不是在门禁修完后提前写满分。

第二遍：六边形边界审查

结论：计划先让全内层依赖方向扫描 RED，再修 `media_distribution` 泄漏，避免只改表面 import。修正点：新增 `scripts/check-full-architecture-completion.mjs` 作为独立复核，不让手写门禁数组再次漏文件。

第三遍：简洁度审查

结论：计划把所有已知超 987 行生产热点逐个收口，且每个拆分都要求删除旧路径、不建 helper/common/wrapper。修正点：把 TypeScript unused 纳入默认 `typecheck`，并把 re-export/barrel 与 graphify god nodes 放进最终复核。
