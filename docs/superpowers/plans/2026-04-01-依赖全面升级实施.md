# 依赖全面升级 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 审查 `E:\koko` 的 Rust 依赖与工具链现状，把可安全升级到最新稳定版的部分升级到位，并把仍被上游生态钉住的阻塞点落到正式审查记录。

**Architecture:** 先按 Rust/Cargo 官方建议校准升级策略，再按“主工作区直接依赖 -> 锁文件 -> 构建验证 -> 阻塞记录”的顺序推进。对被 `dioxus/web-sys/wasm-bindgen` 与上游 websocket / SQLx 依赖链钉住的部分，不做无证据硬撬，只留下明确阻塞说明。

**Tech Stack:** Rust 1.94.x, Cargo, Dioxus 0.7.x, Axum 0.8.x, SQLx 0.8.x, Socketioxide 0.18.x

---

### Task 1: 审查直接依赖与官方升级边界

**Files:**
- Modify: `Cargo.toml`
- Create: `docs/superpowers/plans/2026-04-01-依赖全面升级实施.md`

- [ ] **Step 1: 核对主工作区当前直接依赖与最新稳定版差距**

Run: `cargo info <crate>` for each direct dependency
Expected: 识别哪些已是最新，哪些仍落后

- [ ] **Step 2: 只升级当前 manifest 中仍明确落后的直接依赖**

Expected:
- 不重复改已经在工作区里完成的版本升级
- 不硬撬 `dioxus/web-sys/wasm-bindgen` 这类被上游钉住的依赖簇

- [ ] **Step 3: 显式写出工具链策略（若主工作区尚未声明）**

Scope:
- `Cargo.toml` 的 `rust-version`
- 只在当前工作区尚未声明时补充

- [ ] **Step 4: 运行基础编译验证**

Run: `cargo check`
Expected: PASS

- [ ] **Step 5: 运行 wasm 编译验证**

Run: `cargo check --target wasm32-unknown-unknown`
Expected: PASS or 暴露新的真实阻塞

### Task 2: 刷新锁文件并验证主链

**Files:**
- Modify: `Cargo.lock`

- [ ] **Step 1: 刷新 lockfile 到当前约束下的最新兼容版本**

Run: `cargo update`
Expected: 仅更新当前 semver 约束允许的依赖

- [ ] **Step 2: 审查重复依赖与旧栈来源**

Run: `cargo tree -d`
Expected: 明确重复版本是正常并存还是可消除技术债

- [ ] **Step 3: 跑主测试集**

Run:
- `cargo nextest run`
- `cargo test`
- `cargo test --doc`
- `cargo test --test rt_flow`
- `cargo test --test web_shell_flow`
Expected: PASS or 暴露具体回归

- [ ] **Step 4: 跑 web bundle 验证**

Run: `.\scripts\dx-bundle-web.ps1`
Expected: PASS or 明确记录仍由外部工具链阻塞

- [ ] **Step 5: 跑 SQLx 离线元数据校验**

Run: `cargo sqlx prepare --check`
Expected: PASS or 暴露依赖升级对 SQLx 离线元数据的影响

### Task 3: 记录被生态钉住的阻塞与后续动作

**Files:**
- Create: `docs/superpowers/reviews/2026-04-01-依赖升级-审查记录.md`

- [ ] **Step 1: 记录当前不能暴力升级的依赖簇**

Scope:
- `dioxus -> web-sys/js-sys/wasm-bindgen`
- websocket test client / `tokio-tungstenite` dev stack
- 任何仍被精确版本约束钉住的传递依赖

- [ ] **Step 2: 写明原因与后续条件**

Expected:
- 为什么不能硬升
- 需要等哪个上游 release
- 下次升级前该先看哪些验证命令

- [ ] **Step 3: 提交**

Run:
- `git add Cargo.toml Cargo.lock docs/superpowers/plans/2026-04-01-依赖全面升级实施.md docs/superpowers/reviews/2026-04-01-依赖升级-审查记录.md`
- `git commit -m "中文说明"`
