# 客户端预制媒体与 WebTorrent 单文件主链设计

日期：2026-04-20  
状态：Draft  
适用范围：`koko` 群聊图片/视频上传前预制、后端轻校验、单文件存储、WebTorrent 秒开播放与长期群友互助分发。

## 1. 核心裁决

后端服务器不再承担媒体加工重活。

图片和视频都必须先在群聊用户客户端预制成一个可直接观看、可直接做种的 canonical 成品文件。后端只接收这一份成品文件，只做轻量校验、存储、hash、torrent 元信息、24 小时初始 web seed / 做种，之后按 TTL 删除服务器文件。

后端不得把“补偿客户端能力不足”变成第二条加工主链。后端可以嗅探 MIME、读取尺寸/时长/基础编码元数据、校验 hash 和播放兼容性，但不负责重编码、缩放、封装修复、抽帧、生成派生文件或兜底产出 canonical。

发进群聊的媒体必须已经是成品。客户端加工失败时，不允许把原始文件、半成品、兼容性未知文件发进群聊。

## 2. 质量原则

单文件不等于低质量。

项目的目标不是省到模糊，而是在只保存一份的前提下保留最高可观看质量：

- 图片 canonical 文件必须视觉高清优先，默认不缩到小图，不用缩略图冒充正式图。
- 视频 canonical 文件必须优先保留原分辨率、原帧率、清晰度和声音质量。
- 能无损封装就无损封装，能不重编码就不重编码。
- 只有原始格式不能稳定跨端播放时，客户端才转成浏览器友好的 canonical 视频。
- 转码时禁止为了追求小文件默认压糊；编码参数按“清晰观看”优先，而不是按“最小体积”优先。

这里的“最高质量”定义为：在浏览器可播放、WebTorrent 可分发、后端只存一份的约束内，保留最高可观看质量。它不是承诺永远保存相机原始格式或原始编码位流。

## 3. 图片主链

图片上传前，客户端生成一份高清 `canonical.webp`。

推荐规则：

- 原图是照片时，用高质量 WebP，质量参数默认接近无感损失。
- 原图是截图、文字图、透明图时，优先使用 lossless / near-lossless WebP。
- 默认保留原始像素尺寸；只有超过产品硬上限时才等比限制。
- 后端不再生成 `thumbnail.png`、`full.webp`、`asset-original` 三份，也不把原图重新编码成 canonical。
- 消息流预览、查看器大图、WebTorrent 分发都读取同一个 `canonical.webp`。

这样图片只存在一个文件真相：看图、缓存、做种、删源都围绕同一个对象。

## 4. 视频主链

视频上传前，客户端生成一份 `canonical playable video`。

推荐规则：

- 客户端先探测原始文件是否已经适合浏览器播放。
- 原始视频已经适合播放时，只做必要的无损封装或 faststart 处理。
- 原始视频不适合播放时，客户端优先用 Mediabunny 这类 web-native 成熟轮子完成 demux / mux / remux / transmux / 转码编排，用 WebCodecs 承担浏览器原生编解码。
- 默认不降分辨率、不降帧率，不把高清视频压成低清。
- 音频默认保留高质量；只有兼容性需要时才转成通用音频编码。
- 加工超过 15 分钟时，只提醒用户“仍在处理，可继续等待或取消”，不自动把半成品发出去。

这里的无损封装、faststart、转码和封面抽取都属于客户端预制职责。后端不再生成 HLS/DASH、mezzanine、视频封面多副本。需要封面时，客户端可以从 canonical 视频抽一张轻量封面作为同一发布流程的展示派生，但媒体分发真相仍只有 canonical 视频文件。

客户端视频预制轮子裁决：

- 第一优先级是直通：原始文件已经是浏览器可播放、WebTorrent 可分发的 canonical 单文件时，不重编码。
- 第二优先级是 Mediabunny 的无损 remux / faststart / transmux：能只改容器和元数据位置，就不改编码位流。
- 第三优先级是 Mediabunny + WebCodecs 转码：先用 `canEncode` / `VideoEncoder.isConfigSupported` 做能力探测，通过后才执行。
- `web-demuxer` 只作为特定格式 demux 候选或 benchmark 对照，不升级为完整视频预制主链，除非它能补上 Mediabunny 不满足的明确边界。
- WebGPU 不作为视频编码器或容器处理器；它只允许在真实 benchmark 证明帧级处理成为瓶颈后，用于缩放、旋转、滤镜、水印、色彩变换等 GPU compute / render 支路。
- ffmpeg.wasm 只能作为最后兜底，受文件大小、内存、耗时和设备能力限制，不承诺所有设备成功。

## 5. 后端职责

后端只做轻工作：

- 校验 canonical 文件类型、大小、hash、基础媒体元数据。
- 用轻量探测校验图片/视频是否符合可展示或可播放要求。
- 存储唯一 canonical 文件。
- 写入附件 ready 真相。
- 生成 WebTorrent 所需 torrent 元信息。
- 在 24 小时窗口内作为 web seed / 初始种子帮助冷启动。
- 24 小时后删除服务器文件，让长期分发回到群友 WebTorrent swarm。

后端不做：

- 图片三份派生。
- 图片重编码、缩放、EXIF 旋转落盘或 canonical 生成。
- 视频转码。
- 视频 faststart、remux、mezzanine、封面抽帧或 canonical 生成。
- HLS/DASH 分段。
- 长期保存原片或服务端母本。
- 接收加工失败后的原始附件进群聊。

## 6. WebTorrent 主链

WebTorrent 的 payload 就是唯一 canonical 文件。

所有播放入口必须共享这一份文件真相：

- 消息流自动播放。
- 点击全屏播放。
- 图片查看器。
- 视频查看器。
- 24 小时内后端 web seed 补块。
- 24 小时后群友浏览器互帮互助做种。

拿到完整文件的群友客户端应尽量保留并继续做种。浏览器侧可以申请 persistent storage，提高长期保存概率，但不能把“浏览器一定永久不清缓存”当成产品承诺。

## 7. 用户体验语义

媒体发送分成两个阶段：

1. 本地预制阶段：文件仍在输入区或草稿态，不进入群聊时间线。
2. 上传发布阶段：只有 canonical 成品才上传并进入群聊。

如果本地预制失败：

- 不发进群聊。
- 明确告诉用户当前设备无法完成该文件预制。
- 允许用户取消、重试、换设备或换文件。

如果本地预制超过 15 分钟：

- 只提醒，不自动失败。
- 用户可以继续等待或取消。
- 仍然禁止半成品进入群聊。

## 8. 与旧设计的关系

本设计覆盖旧的“后端 complete 后生成图片多副本、视频 mezzanine、HLS/DASH 分段”的主链。

保留的旧原则：

- `Video.js v10` 仍是唯一正式播放器壳。
- 长期正式协作分发主平面仍是 WebTorrent。
- 24 小时服务器辅助窗口仍是硬上限。
- 后端仍是 ready 真相和权限真相 owner。

被替换的旧原则：

- 后端不再是媒体加工 owner。
- HLS/DASH 不再是默认必产物。
- 图片不再长期保存多份派生。
- 视频不再由后端默认转码或打包。

## 9. 验证门禁

实现前必须先补测试证明这些事实：

- 图片上传成功后，后端只保存一份 canonical WebP。
- 视频上传成功后，后端只保存一份 canonical 视频。
- 后端 complete 不再调用图片重编码/多副本生成、视频转码/封装修复、封面抽帧、HLS/DASH 打包。
- 后端收到非 canonical 输入时只能拒绝，不能代替客户端加工后放行。
- 客户端预制失败时不能发进群聊。
- 预制超过 15 分钟只提醒，不自动发布半成品。
- 自动播放、全屏播放、查看器播放、WebTorrent 做种都使用同一个 canonical 文件。
- 24 小时后服务器文件删除，不影响已有完整 peer 继续做种。

## 10. 官方依据

- Uppy 支持上传前客户端处理图片，适合把图片预制前移。
- Mediabunny 提供浏览器内媒体读取、写入、转换、demux、mux、remux、transmux、转码编排，并能利用 WebCodecs 做硬件加速编解码，适合作为客户端视频 canonical 预制主轮子。
- WebCodecs 提供浏览器侧视频编解码能力，但需要 HTTPS，且不是所有浏览器都支持；它输出的是 encoded chunk，不负责 MP4/WebM 这类容器，所以必须搭配成熟 demux/mux 工具并做能力探测。
- WebGPU 是浏览器 GPU compute / render API，不是视频编码器、转码器或容器工具；它只能作为帧级处理优化支路，不能替代 WebCodecs / Mediabunny。
- web-demuxer 是 WebCodecs-first demux 工具，适合作为格式覆盖补充或对照，不应在没有 mux/remux/transcode 完整方案前成为主链。
- ffmpeg.wasm 可以作为浏览器内转码补充，但官方说明它比原生 FFmpeg 慢，且输入文件有 WebAssembly 大小限制，所以不能把它写成无条件成功的唯一依赖。
- WebTorrent 支持 web seed、piece 选择和浏览器流式播放，适合作为 canonical 单文件的秒开与长期互助分发平面。
