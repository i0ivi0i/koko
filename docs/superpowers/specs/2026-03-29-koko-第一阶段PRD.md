# Koko 第一阶段重构 PRD

## 文档定位

本文档定义 `Koko` 从零重构后的第一阶段产品要求与架构硬边界。

目标不是先堆技术，而是先冻结三类稳定真相：

- 第一阶段到底交付什么
- 哪些业务语义未来不能被技术替换破坏
- 未来如何在不制造屎山、不重复造轮子的前提下持续演进

本文档优先级高于实现细节。实现可以调整，业务边界、稳定契约和架构硬原则不能随意漂移。

---

## 1. 项目定位

`Koko` 是一个全 Rust 的群聊 / IM 基础项目。

第一阶段的目标不是一次性把群聊系统做大做全，而是先构建一个最小但稳定的业务闭环：

- Web 聊天前台可用
- Web admin 后台可用
- Rust 服务端可用
- 房间、成员关系、消息历史可持久化
- 匿名用户可进入房间并实时发送文本消息

系统必须采用“模块化单体部署形态 + Rust 六边形架构内部边界”的组合：

- 部署先保持单体，降低第一阶段复杂度
- 内部必须严格区分领域核心、应用用例、稳定契约、技术适配层和运行壳层

项目最终追求的是：

- 技术壳可替换
- 业务核心稳定
- 项目增删改像乐高积木一样可组合、可替换、可演进
- 未来换框架、换轮子、换适配实现时，不导致业务功能崩溃、失效或大面积返工

---

## 2. 第一阶段目标

第一阶段必须同时满足以下目标：

1. 建立最小聊天闭环  
   匿名会话引导、输入房间短码、加入或创建房间、进入房间后实时发送和接收文本消息。

2. 建立最小后台只读运营闭环  
   admin 后台可登录、可查看概览统计、房间列表、房间搜索、房间详情和成员列表。

3. 建立稳定持久化闭环  
   房间、成员关系、消息历史必须进入 PostgreSQL，服务重启后数据仍然可用。

4. 建立稳定架构闭环  
   第一阶段所有能力都必须通过稳定业务契约表达，而不是绑死在 HTTP、Socket.IO、SQL 或 UI 组件上。

5. 建立反膨胀基础  
   从第一阶段开始就禁止屎山式堆叠，禁止重复造轮子，禁止低价值抽象和横向复制实现。

---

## 3. 架构总原则

### 3.1 核心原则

- 业务核心绝不依赖框架
- 先定义业务语义，再接技术壳
- 所有外部能力都通过明确 port 进入核心
- adapter 只做翻译，不做业务决策
- 稳定的是契约，不是具体实现
- 默认模块化单体部署，不滥拆服务
- 默认全 Rust 技术栈
- 对群聊 / IM 项目的实时主通道，必须优先采用成熟现成的 Rust 轮子
- 第一阶段明确采用 `socketioxide` 作为实时主通道实现
- 禁止为了同类能力重复造轮子或手搓替代实时层

### 3.2 乐高式可替换原则

以下部分必须被视为“可替换技术壳”：

- Web 前台框架与 UI 组件库
- Web admin 框架与 UI 组件库
- HTTP 入口层
- 数据库访问实现
- 页面路由与状态实现
- 底层存储适配细节

以下部分必须被视为“稳定业务契约”：

- 房间语义
- 成员关系语义
- 消息语义
- 匿名会话语义
- admin 只读运营视图语义
- 领域错误语义
- 稳定实时事件语义

### 3.3 反膨胀原则

- 代码必须追求简洁、高效、优雅
- 禁止屎山式堆叠
- 禁止用“以后再整理”当借口持续堆胶水
- 禁止重复造轮子
- 禁止横向复制相同业务语义的多套实现
- 新增功能时优先复用、收口、删除重复，再考虑新增
- 抽象必须付得起长期维护成本，不能为了“高级感”提前引入低收益复杂度

---

## 4. 第一阶段交付范围

### 4.1 纳入范围

第一阶段必须交付以下能力：

- Web 聊天前台
- Web admin 后台
- Rust 服务端
- PostgreSQL 持久化
- 匿名会话引导
- 输入房间短码进入房间
- 房间不存在时自动创建
- 进入房间后的实时文本消息收发
- 房间消息历史持久化
- 房间成员关系持久化
- admin 登录
- admin 概览统计
- admin 房间列表
- admin 房间搜索
- admin 房间详情
- admin 成员列表

### 4.2 明确不纳入

第一阶段明确不做以下内容：

- 安装部署
- 发布打包
- 房间封禁 / 解封
- 全局消息长度策略
- 消息历史分页
- 提升管理员
- 降级管理员
- 禁言成员
- 移除成员
- 插件系统落地实现
- 桌面端
- 移动端
- 其他多前端壳

### 4.3 必须预留但不在第一阶段实现

- 房间治理能力
- 成员治理能力
- 消息历史分页
- 更丰富的后台运营能力
- 稳定插件接口
- 更多前端壳
- 更多查询投影
- 更复杂的实时能力

预留的意思是：

- 第一阶段不实现具体功能
- 但第一阶段不能把未来扩展路径堵死
- 新能力未来必须能挂到已有稳定边界上，而不是推翻核心架构

---

## 5. 第一阶段用户与核心场景

### 5.1 聊天用户

聊天用户的最小完整路径：

1. 打开前台
2. 系统引导并签发匿名会话
3. 用户输入房间短码
4. 若房间存在则加入，若不存在则创建并加入
5. 进入房间后看到当前房间快照
6. 在房间内实时发送文本消息
7. 实时接收其他成员的文本消息

### 5.2 后台管理员

后台管理员的最小完整路径：

1. 登录后台
2. 查看概览统计
3. 查看房间列表
4. 按房间短码搜索
5. 查看单个房间详情
6. 查看单个房间成员列表

第一阶段后台只承担只读运营视图能力，不承担治理写操作。

---

## 6. 核心领域模型

第一阶段稳定领域对象如下：

### 6.1 AnonymousSession

表示匿名用户的稳定身份。

职责：

- 表达当前会话身份
- 为加入房间、发送消息提供主体基础
- 保证匿名模式下也有稳定的业务身份边界

### 6.2 Room

表示房间本身。

职责：

- 承载稳定房间标识
- 承载房间短码
- 承载房间存在性语义

### 6.3 Member

表示某个会话与某个房间的成员关系。

职责：

- 表达“谁属于哪个房间”
- 作为未来权限、治理、成员管理的稳定挂点

### 6.4 Message

表示一条文本消息。

职责：

- 表达所属房间
- 表达发送者
- 表达消息正文
- 表达创建时间

### 6.5 AdminReadModel

表示后台只读运营视图的稳定查询语义。

职责：

- 表达概览统计
- 表达房间列表和房间详情
- 表达成员列表

约束：

- 它是查询投影，不是业务聚合根
- 它不能反向定义房间、成员、消息的业务真相

---

## 7. 稳定业务用例

第一阶段必须存在以下稳定业务用例：

- `bootstrap_anonymous_session`
- `join_or_create_room_by_code`
- `load_room_snapshot`
- `send_text_message`
- `subscribe_room_stream`
- `list_admin_rooms`
- `search_admin_rooms`
- `get_admin_room_detail`
- `list_admin_room_members`
- `get_admin_overview`

这些是稳定用例名称，不要求最终代码名字逐字一致，但要求用例边界语义必须稳定存在。

---

## 8. 稳定业务不变量

第一阶段必须满足以下不变量：

1. 没有有效会话，不能进入房间或发送消息。
2. 房间短码是用户进入房间的稳定入口语义。
3. 输入短码时，若房间存在则加入；若不存在则创建。
4. 加入房间后必须形成持久化成员关系，不能只存在于内存连接态。
5. 每条消息必须属于某个房间和某个发送者。
6. 消息必须持久化到 PostgreSQL。
7. 实时通道只负责同步事实，不负责定义事实。
8. admin 展示的是只读查询结果，不是业务真相来源。
9. 第一阶段所有业务规则必须脱离 UI、HTTP、Socket.IO 和 SQL 解释清楚。

---

## 9. 模块分层与依赖边界

第一阶段固定为 5 层：

- `domain`
- `application`
- `contract`
- `adapter`
- `shell`

### 9.1 domain

只放：

- 核心业务语义
- 领域对象
- 领域错误
- 领域不变量
- 领域服务

禁止：

- 直接依赖 HTTP
- 直接依赖 UI
- 直接依赖数据库访问库
- 直接依赖 `socketioxide`

### 9.2 application

只放：

- 用例编排
- 用例输入输出
- 对外部能力的 port 调用

禁止：

- 直接写 SQL
- 直接写页面逻辑
- 直接写具体协议处理细节

### 9.3 contract

只放：

- 稳定 DTO
- 查询参数
- 命令结构
- 实时事件类型
- 错误编码

### 9.4 adapter

只放：

- PostgreSQL repo 实现
- `socketioxide` 实时适配
- HTTP handler 适配
- admin 查询适配
- 前端 API client 适配

### 9.5 shell

只放：

- Web 前台入口
- Web admin 入口
- 服务端入口
- 运行装配
- 页面壳与路由壳

### 9.6 单向依赖规则

- `shell -> adapter -> application -> domain`
- `contract` 可被 `shell / adapter / application` 共同引用
- `domain` 不允许反向依赖其他业务层外部实现

---

## 10. 第一阶段数据与事件流

### 10.1 聊天主流程

1. 前台启动
2. 调用匿名会话引导用例
3. 用户输入短码
4. 服务端执行 `join_or_create_room_by_code`
5. 返回房间快照
6. 建立基于 `socketioxide` 的实时订阅
7. 用户发送文本消息
8. 服务端执行 `send_text_message`
9. 消息写入 PostgreSQL
10. 实时事件广播到当前房间成员
11. 前台更新房间视图

### 10.2 admin 主流程

1. 管理员登录
2. 读取概览统计
3. 读取房间列表
4. 读取搜索结果
5. 读取房间详情
6. 读取成员列表

### 10.3 真相归属

- 会话真相：会话记录
- 房间真相：房间记录
- 成员真相：成员关系记录
- 消息真相：消息记录
- 实时事件：同步手段，不是真相来源
- admin 统计：查询投影，不是真相来源

---

## 11. 持久化要求

第一阶段必须持久化以下对象：

- 匿名会话
- 房间
- 房间短码绑定关系
- 成员关系
- 文本消息

最低要求：

- 服务重启后，房间仍存在
- 服务重启后，成员关系仍存在
- 服务重启后，消息历史仍存在
- 后台仍可读取这些真相对应的只读视图

数据库优先选择：

- PostgreSQL

第一阶段对数据库访问的要求：

- 明确表达读写职责
- 不把 SQL 细节泄漏到领域层
- 不依赖数据库技巧来定义业务语义

---

## 12. 全 Rust 技术栈要求

第一阶段必须保持全 Rust 技术栈方向。

约束如下：

- 后端使用 Rust
- 前台使用 Rust Web 技术栈
- admin 使用 Rust Web 技术栈
- 群聊 / IM 的实时主通道使用成熟 Rust 轮子 `socketioxide`

以下做法视为违背方向：

- 为已有成熟 Rust 实时轮子手搓替代层
- 为通用基础设施问题随意引入额外语言和运行时
- 把核心业务能力写死在某个非 Rust 技术壳里

说明：

- “全 Rust 技术栈”不等于技术不可演进
- 它表达的是：优先使用成熟 Rust 生态解决问题，而不是在已有轮子之外重复造轮子

---

## 13. 代码质量与维护约束

第一阶段必须从一开始就满足以下代码质量约束：

- 代码简洁
- 代码高效
- 代码优雅
- 不制造屎山
- 不无节制膨胀
- 不制造重复实现
- 不把未来维护成本转嫁给后续迭代

具体约束：

- 优先删除、合并、收口和复用
- 禁止横向复制相同语义实现
- 禁止低收益抽象
- 禁止为了“以后可能用得上”提前堆复杂度
- 禁止大而全的万能模块
- 禁止把一层职责写进另一层
- 禁止页面、handler、repo、realtime adapter 偷偷长出业务规则

评估一段设计是否合格，只看三件事：

1. 换技术壳时业务是否能不动
2. 加功能时是否主要改局部
3. 删功能时是否不会扯塌整片系统

---

## 14. 未来扩展预留

第一阶段虽然不实现以下能力，但必须允许未来平滑演进：

- 房间治理
- 成员治理
- 消息历史分页
- 插件接口
- 更多后台能力
- 更多前端壳
- 更多查询投影

预留要求：

- 未来能力必须通过稳定边界进入系统
- 未来不能靠重写核心领域来接入
- 未来不能要求推翻第一阶段持久化真相
- 未来不能要求实时通道重新定义业务事实

---

## 15. 第一阶段验收标准

当且仅当以下场景全部成立，第一阶段才算完成：

### 15.1 聊天闭环

- 新用户打开前台可完成匿名引导
- 用户输入一个不存在的短码时可自动建房并进入
- 第二个用户输入同一短码时可加入同一房间
- 房间内用户之间可实时收发文本消息
- 消息刷新或服务重启后仍可读取

### 15.2 持久化闭环

- 房间数据持久存在
- 成员关系持久存在
- 消息历史持久存在

### 15.3 admin 闭环

- 管理员可登录后台
- 管理员可查看概览统计
- 管理员可查看房间列表
- 管理员可按短码搜索房间
- 管理员可查看单房间详情
- 管理员可查看该房间成员列表

### 15.4 架构闭环

- 业务核心不直接依赖框架
- 真相不散落在壳层
- `socketioxide` 只负责实时同步，不负责承载业务真相
- 第一阶段未来扩展路径未被堵死

---

## 16. 第一阶段非目标

以下内容即使技术上能做，也不应挤进第一阶段：

- 为未来治理能力提前堆完整治理系统
- 为未来插件能力提前堆完整插件平台
- 为未来多端能力提前堆跨端复杂抽象
- 为未来高复杂统计提前引入整套事件驱动 / CQRS
- 为架构表演滥拆服务
- 为“看起来先进”放弃成熟现成 Rust 轮子

第一阶段的最高原则不是“做得最多”，而是：

用最少但边界清楚的代码，交付最稳定的业务闭环。

---

## 17. 最终结论

`Koko` 第一阶段不是一个“先做个能跑的 Demo 再说”的项目，而是一个从第一天就要把业务语义、稳定契约和可替换边界立住的全 Rust 群聊 / IM 基础。

第一阶段交付最小闭环，未来再逐步扩展治理、插件、分页、多壳和更复杂的后台能力。

但无论未来技术如何演进，都必须遵守一个总原则：

技术壳可以换，轮子可以升级，适配实现可以重写；  
房间、成员、消息、会话与稳定业务契约不能塌。
