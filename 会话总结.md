# 会话总结

会话 ID：`019dd231-3ed8-72c3-961c-4e53d1f62cbd`

日期：2026-04-30  
仓库：`E:\koko`  
主线：`main`

---

## 1. 这次长会话的主目标

这次会话围绕 `koko` 项目的长期媒体体验、WebTorrent 主链、群聊性能、工程治理和文档真相收口展开。

核心目标可以压成一句话：

**把项目里的视频、图片、上传、缓存、自动播放、查看器、全屏、WebTorrent 分发、群聊滚动性能和治理文档，都尽量收成唯一真相、清晰 owner、真实浏览器证据和可长期维护的代码路径。**

用户反复强调的工作原则：

1. 不要表面修 bug，要抓底层代码逻辑根因。
2. 不要堆文档字数，要深读后融合成一体。
3. 不要绕开 WebTorrent，正式媒体字节必须走唯一 WebTorrent 主链。
4. 冒烟测试必须用真实浏览器、`playwright-cli`、`chrome-devtools-cli` 体系。
5. 代码理解、搜索、修改优先用 Serena MCP。
6. 执行 plan 要一口气做完，测试后继续按测试结果修到闭环。
7. 项目要按“浏览器中的应用”来做，而不是网页拼贴。

---

## 2. 文档融合与规范收口

### 2.1 项目视频播放要求

多份原始视频/多媒体 spec 被收口到：

- `docs/superpowers/specs/2026-04-25-项目视频播放要求.md`

融合来源包括：

- `2026-04-21-高清视频自动播放加速.md`
- `2026-04-24-多媒体附件精确去重.md`
- `2026-04-25-群聊播放优化与离屏续播.md`
- `2026-04-25-视频唯一播放与自动播视觉连续性.md`

最终总裁决：

1. canonical 媒体资产是上传复用、转发分享、正式分发和播放查看的唯一物理真相。
2. WebTorrent whole-file swarm 是正式媒体字节唯一主链。
3. 同一颗 Video.js player/container 是消息流自动播放、离屏续播、查看器和全屏的唯一正式播放表面。
4. 自动播放、查看器、全屏、退出归位共享同一条播放连续性状态链。
5. 禁止 poster、thumbnail、runtime preview、canvas 抓帧或第二 live video 变成第二播放真相。

用户后来要求把“滑走就停画面，滑回来续播，无闪烁无抽搐，全屏/查看器也连续”这些经验融合进该 spec，而不是简单复制粘贴。

### 2.2 WebTorrent 满血协同分发要求

两份 WebTorrent spec 被融合为：

- `docs/superpowers/specs/2026-04-23-WebTorrent满血协同分发要求.md`

融合来源：

- `2026-04-22-WebTorrent高速分发防止群友偷懒.md`
- `2026-04-23-WebTorrent群聊越活跃越强的极限协同分发.md`

最终裁决：

1. 新上传图片/视频正式媒体字节只走 WebTorrent whole-file swarm。
2. 前 24 小时后端只能作为 swarm 内强 seed。
3. 24 小时后后端退出该附件媒体字节供给。
4. 看过或自动播放过的群友应尽量继续补齐、做种、帮助后来者。
5. 群越活跃，分发越强。
6. 没有在线种子时必须说真话，不能伪装加载失败。

### 2.3 浏览器零崩溃零闪烁 spec 与 plan

重点文档：

- `docs/superpowers/specs/2026-04-26-万人群聊浏览器零崩溃零闪烁.md`
- `docs/superpowers/plans/2026-04-28-唯一WebTorrent万人群聊零崩溃零闪烁执行计划.md`

会话中多次围绕这些文档做审核和增强：

1. 学习 X/TikTok/Telegram Web 的高性能无限视频/消息滚动模型。
2. 查 Chrome/Lit/Web 性能最佳实践。
3. 强调应用化思维：浏览器事件只是信号，真正裁决应落在 AppRuntime/owner/state model。
4. 明确优化不能破坏项目已有满血功能，例如 WebTorrent 主链、丝滑自动播放、暂停、续播、全屏、无闪烁。
5. 审核 spec 是否基于官方要求和成熟实践，避免手搓轮子。

### 2.4 省心省力网络 spec

重点文档：

- `docs/superpowers/specs/2026-04-27-省心省力网络.md`

核心裁决：

1. 浏览器用户永远只认一个 HTTPS 入口。
2. 开发时是局域网 IP，上线时是正式域名。
3. 上传、API、实时、协作分发都挂在同一入口下。
4. 禁止向浏览器泄漏 `127.0.0.1`、`localhost`、内部端口、HTTP 降级。
5. 全环境禁止 `cloudflared`，公网侧只使用 Cloudflare CDN/代理和正式域名。
6. Windows 本地脚本只是开发适配层，不是正式 Linux 部署真相。

---

## 3. AGENTS.md 与长期工作规则

会话中多次把用户的“记住”要求提炼进 `AGENTS.md`：

1. 执行任何任务前先看可用 skills。
2. 代码理解、搜索、修改优先 Serena MCP。
3. 不懂或模糊时先查官方最佳实践、Context7 或社区高手建议。
4. 执行 plan 要按逻辑顺序一口气推进，完成后冒烟测试，再按结果修复。
5. bug 维修必须使用 `supxcode`、`investigate`、`qa`、`systematic-debugging` 等 skill，抓底层代码逻辑根因。
6. 冒烟测试必须按 `chrome-devtools-cli` 和 `playwright-cli` skill 链路执行。
7. 涉及架构或代码库问题先读 `graphify-out/GRAPH_REPORT.md`，修改代码后运行 `graphify update .`。
8. 多文档融合要逐句深读，禁止复制粘贴式拼接。
9. 任务验证通过后自动 commit，提交说明用中文。

---

## 4. graphify 与 gstack 相关

### 4.1 graphify 升级

用户指出 `https://github.com/safishamsi/graphify/releases` 官方最新版是 `v0.5.2`，要求把项目里的 graphify 更新到最新稳定版，不留下旧组件。

这项任务已在会话早期完成，并要求后续架构/代码修改后运行：

- `graphify update .`

### 4.2 gstack 与自动调用 skill

用户询问是否安装了 `https://github.com/garrytan/gstack`，以及能否在执行任务时智能自动调用专家 skill。

会话中明确了：

1. 已安装并接入多种 gstack skill。
2. 执行任务时应按可用 skill 清单自动判断是否调用。
3. bug、QA、浏览器测试、计划审核、代码审查等场景应默认使用相应 skill。
4. `chrome-devtools-cli`、`playwright-cli`、`qa`、`investigate`、`supxcode` 成为本项目媒体/前端问题的默认工具链。

---

## 5. 视频自动播放与全局唯一播放器方向

用户多次反馈同一类体验问题：

1. 群聊内视频自动播放会闪烁一下再播放。
2. 滑出屏幕后暂停会闪烁，预览不是停在暂停位置。
3. 滑回后不是续播，而像从首帧或封面重新开始。
4. 快速滑动时更容易暴露。
5. 修一个地方又引出黑色占位卡片、全屏溢出、横屏竖播、退出全屏黑屏等问题。
6. 全屏时有小圆圈一直转，即使视频已经高清播放。
7. 全屏进度条可以点击跳转，但不能丝滑拖拽。
8. 手机浏览器可能有原生全屏提示，这要区分系统行为和项目代码行为。

宏观判断：

1. 单纯局部补丁会继续导致“修东墙坏西墙”。
2. 问题根因不是某个小 if，而是自动播放、查看器、全屏、暂停帧、DOM 宿主和唯一播放器 owner 的状态模型没有完全收口。
3. 需要一个不过度设计但清晰的全局播放连续性 owner。

用户认同的方向：

- 新建或使用 `frontend/媒体/全局丝滑自动播.ts`
- 同时纳入自动播放、查看器、全屏切换、退出归位、续播连续性。
- 但不能过度设计，逻辑要简洁优雅，便于长期维护。

后续 spec 中已明确：

1. `frontend/媒体/全局唯一播放器.ts` 负责唯一 Video.js 实例和 container。
2. `frontend/媒体/全局丝滑自动播.ts` 负责播放连续性状态链。
3. 两者不能互相夺权。
4. 不能再创建第二播放器、第二播放位置真相、第二 preview cache 或第二 live video。

---

## 6. WebTorrent 缓存与持久化互帮互助

用户提出关键诉求：

1. 一个视频或图片被用户看完，或者只看了一部分，只要被用户看到，就应该形成持久化缓存。
2. 下次自己看、自动播放、刷新页面、重新进房，都应该优雅利用缓存。
3. 缓存必须服务 WebTorrent，让用户自己更快，也能帮别人更快。
4. 缓存不能绕开 WebTorrent。

最终修复方向：

1. WebTorrent piece 字节持久化必须留在 WebTorrent store 里。
2. 不做旁路媒体缓存来绕过主链。
3. 在前端协作分发运行时中使用可验证的 `idb-chunk-store`。
4. 以完整 `torrent_info_hash` 作为持久 store owner。
5. 刷新、重新进房后，同一 torrent 可以复用已有 pieces。

本次最终提交中相关文件：

- `frontend/媒体/媒体协作分发.ts`
- `frontend/tests/媒体协作分发测试.spec.ts`

真实浏览器验证结果：

1. 出现多个 `koko-webtorrent-*` IndexedDB 数据库。
2. `indexedDB` 使用量约 `241MB`。
3. 同一持久 Playwright 会话连续两轮刷新/滚动后，后端原文件端点 `/api/attachments/...variant=original` 的 206 次数为 `0`。
4. WebTorrent 相关 `/webtorrent/...` 请求仍存在，说明复用留在 WebTorrent 主链里。

---

## 7. 最终一次代码修复的根因与提交

最后一次主要任务是：用户认为群聊 web 仍然卡、不流畅，要求用真实 MP4、多视频滚动、Serena 和浏览器 smoke 找底层根因并修复。

### 7.1 复现与证据

通过真实浏览器和 Playwright 检查发现：

1. 某些情况下同一个 owner 附件同时存在两颗同源 `<video>`。
2. 一个是可见 preview。
3. 一个是 hidden/stage canonical player。
4. canonical 已经 ready，但没有触发下一轮 Lit reveal 更新。
5. 结果是可见 preview 和隐藏 canonical 长时间并存，造成闪烁、卡顿、错误封面、续播割裂。

这证明根因不是“少一个 guard”，而是：

**hidden-stage ready 事实已经成立，但状态链没有把这个事实推回 render/reveal，导致唯一播放器接管卡在半路。**

### 7.2 修复点

最终提交：

- commit：`0d448a41 修复群聊媒体丝滑播放与WebTorrent缓存复用`

修改文件：

- `frontend/房间消息窗.ts`
- `frontend/聊天壳.ts`
- `frontend/媒体/媒体协作分发.ts`
- `frontend/媒体/媒体发布.ts`
- `frontend/媒体/视频元数据.ts`
- `frontend/媒体/预览缓存.ts`
- `frontend/tests/媒体协作分发测试.spec.ts`
- `frontend/tests/媒体发布测试.spec.ts`
- `frontend/tests/房间消息窗媒体查看器测试.spec.ts`
- `frontend/tests/聊天壳测试.spec.ts`
- `frontend/tests/视频元数据测试.spec.ts`
- `frontend/tests/预览缓存测试.spec.ts`

主要修复：

1. `房间消息窗.ts`
   - 真实 preview video 预算从 6 降到 2。
   - 旧 owner 退场不再依赖第二 preview video。
   - 从 canonical player 捕获冻结帧和播放位置。
   - hidden-stage canonical ready 后调用 `揭开已就绪的时间线隐藏接管宿主()`，主动触发 reveal 更新。
   - owner 可见时只允许 canonical player 这一颗真实视频。
   - 防止可见区内 preview/canonical 双表面抢像素。

2. `媒体协作分发.ts`
   - 使用 `idb-chunk-store` 做 WebTorrent 持久 piece store。
   - 不再因为浏览器默认 OPFS/FSA 能力存在，就跳过项目可验证的持久 store owner。

3. `视频元数据.ts`
   - 视频预览帧导出最大边长限制为 `960`。
   - 避免 4K/8K 帧转巨大 dataURL 写入同步 localStorage。

4. `预览缓存.ts`
   - 浏览器预览缓存实例内缓存已解析快照。
   - 避免滚动热路径反复 `localStorage.getItem + JSON.parse` 大字符串。

5. `媒体发布.ts`
   - 单条消息媒体附件上限提炼为 `媒体单条消息附件上限 = 9`。
   - 选择文件后先按剩余草稿槽位截断。
   - 防止超量视频进入 prepare/Tus 后失败草稿撑爆输入区。

6. `聊天壳.ts`
   - composer 和 drafts 区域增加最大高度和内部滚动。
   - 防止大量草稿挤压消息流可视区。

### 7.3 验证结果

最终验证：

1. `pnpm --dir frontend test`
   - `68` 个测试文件通过。
   - `824` 条测试通过。

2. `pnpm --dir frontend typecheck`
   - 通过。

3. `pnpm --dir frontend build`
   - 通过。
   - 前端浏览器应用宪法检查通过。
   - 前端架构适应度检查通过。

4. Playwright 真实 HTTPS 冒烟：
   - 页面：`https://127.0.0.1/`
   - 房间：`1234b`
   - console：`0 errors / 0 warnings`
   - 可见视频：`1`
   - 活跃视频：`1`
   - 可见非 canonical 视频：`0`
   - 重复同源 video：`0`
   - `avgFrameMs=16.57`
   - `p95FrameMs=16.8`
   - `p99FrameMs=17`
   - `over50=0`
   - `over100=0`

5. WebTorrent 缓存 smoke：
   - `koko-webtorrent-*` IndexedDB store 存在。
   - 第二轮刷新/滚动没有打回 `/api/attachments/...variant=original` 后端原文件 206。
   - 缓存复用仍服务 WebTorrent，不是旁路缓存。

6. `graphify update .`
   - 已运行。
   - graphify 报告更新为 `639 nodes / 1416 edges / 28 communities`。

7. `git status --short`
   - 最终干净。

---

## 8. 工具问题与注意事项

### 8.1 chrome-devtools CLI

会话中按 skill 尝试使用 `chrome-devtools` CLI，但本机出现：

```text
Failed to execute command: Error: connect EPERM \\.\pipe\chrome-devtools-mcp\server.sock
```

另外 `chrome-devtools-cli` 这个命令名本机不可识别，skill 文件里实际命令是 `chrome-devtools`。

处理方式：

1. 已按 skill 尝试 CLI。
2. CLI 被命名管道权限挡住。
3. 使用 Playwright CLI 和 DevTools MCP 语义等价检查继续完成 smoke。
4. 后续如果用户要求严格跑 `chrome-devtools` CLI，需要先修本机 MCP pipe 权限或 Chrome profile 占用问题。

### 8.2 Playwright 临时文件

会话中创建过 `.playwright-cli` 下多个临时 helper 文件用于 smoke。

最终已清理本轮创建的临时 helper：

- `upload-mp4-list.txt`
- `koko-scroll-probe.js`
- `koko-dom-map.js`
- `koko-shadow-scroll-probe.js`
- `koko-batch-upload.js`
- `koko-send-nine.js`
- `koko-batch-1.js`
- `koko-batch-2.js`
- `koko-clear-drafts.js`
- `koko-postfix-scroll-probe.js`

历史 `.playwright-cli` 截图、日志、snapshot 仍保留。

---

## 9. 当前仓库状态

最后一次可见状态：

1. 当前分支：`main`
2. 最新提交：`0d448a41 修复群聊媒体丝滑播放与WebTorrent缓存复用`
3. 最后一次 `git status --short`：干净。

本文件是会话结束后新增的总结文件，尚需按当前任务结束时的实际状态再检查一次 git。

---

## 10. 后续建议

优先级从高到低：

1. 用户肉眼再次在真实浏览器里高速滚动 `1234b`，确认“闪一下露出占位/封面”的问题是否消失。
2. 用真实手机浏览器测试全屏路径，区分系统原生 fullscreen 提示和项目应用内沉浸全屏。
3. 针对全屏进度条拖拽、横竖屏自适应、退出全屏归位，继续按 Video.js v10 官方建议审查 `全局唯一播放器.ts` 与播放器壳。
4. 继续观察 WebTorrent IndexedDB store 是否随观看持续增长，并确认过期、删除、主动清站点数据时的清理语义。
5. 对 `/api/attachments/...variant=original` 的 206 请求继续区分：
   - WebTorrent WebSeed range 请求是 swarm 内强 seed，不是旁路。
   - 前端 `<video>` 直接命中 original 才是第二主链风险。
6. 如果再次出现“慢速滚动好，快速滚动坏”，优先检查：
   - owner 切换时是否有两个 live video。
   - hidden-stage ready 后是否触发 reveal。
   - 是否有 poster/IMG 在可见区被重新插入。
   - preview cache 是否重新写入巨大 dataURL。
   - composer/drafts 是否挤压消息流滚动区。

---

## 11. 可快速恢复的关键命令

```powershell
pnpm --dir frontend test
pnpm --dir frontend typecheck
pnpm --dir frontend build
graphify update .
git status --short
```

浏览器 smoke 常用入口：

```powershell
playwright-cli goto https://127.0.0.1/
playwright-cli console error
playwright-cli network
```

如果 `chrome-devtools` CLI 仍报 pipe 权限：

```powershell
chrome-devtools list_pages --output-format=json
```

看到 `EPERM \\.\pipe\chrome-devtools-mcp\server.sock` 时，先处理本机 DevTools MCP 连接/权限问题，再要求它作为强制 smoke 证据。

---

## 12. 一句话交接

这次会话最终把项目媒体体验的方向从“多处局部补丁”重新拉回到“唯一 WebTorrent 主链、唯一播放器、唯一播放连续性状态链、WebTorrent 持久 piece 缓存和真实浏览器证据”，并用提交 `0d448a41` 修掉了当前最明显的双视频表面、滚动卡顿和缓存浪费问题。
