# 部署经验总结

日期：2026-05-08  
状态：长期维护说明  
适用范围：`koko` 当前这套 `GitHub Actions + 单台 Debian 12 VPS + Cloudflare + Caddy` 自动部署链。

关联文件：

- [docs/superpowers/specs/2026-05-05-Debian12公网正式上线小白操作手册.md](docs/superpowers/specs/2026-05-05-Debian12公网正式上线小白操作手册.md)
- [ops/README.md](ops/README.md)
- [ops/compose.yaml](ops/compose.yaml)
- [ops/Caddyfile](ops/Caddyfile)
- [ops/package-release.sh](ops/package-release.sh)
- [.github/workflows/initial-deploy.yml](.github/workflows/initial-deploy.yml)
- [.github/workflows/deploy.yml](.github/workflows/deploy.yml)
- [.github/workflows/rollback.yml](.github/workflows/rollback.yml)
- [.github/workflows/release.yml](.github/workflows/release.yml)

---

## 0. 这份文档是干什么的

这不是给新手一步一步点按钮的教程。  
那份教程已经有了。

这份文档只做一件事：

**把我们这次公网自动部署真正踩出来的经验，收成一套长期不容易忘的真相。**

一句话：

**以后谁来维护这个项目，只要先看这份文档，就知道现在正式部署到底是怎么跑的，哪些坑已经踩过了，哪些坏路不能再走。**

---

## 1. 当前正式部署真相

当前这套正式主链，已经固定成下面这一条：

1. 代码放在同一个 GitHub 私有仓库。
2. 日常开发还是正常 `git push` 到 `main`。
3. GitHub Actions 负责打包、上传、远程执行部署脚本。
4. VPS 只负责接收发布包、构建镜像、启动容器、做健康检查。
5. 正式域名走 Cloudflare 橙云。
6. 源站 HTTPS 由 Caddy 负责。
7. 源站证书自动续期走 **Cloudflare DNS-01**，不是靠人工切灰云。

一句话记忆：

**GitHub 负责发命令，VPS 负责跑版本，Cloudflare 负责正式入口，Caddy 负责源站 HTTPS。**

---

## 2. 现在有哪 4 个 GitHub 按钮

当前 GitHub `Actions` 页面里，正式有 4 个按钮：

1. `Initial Deploy`
   - 第一次上线时用。
   - 作用是：首次安装 + 首次部署。

2. `Deploy`
   - 日常升级时用。
   - `push` 到 `main` 也会自动触发它。

3. `Rollback`
   - 新版本有问题时用。
   - 作用是切回旧版本。

4. `正式发版`
   - 给当前代码打正式版本牌子时用。
   - 会创建 Git tag 和 GitHub Release。

最容易搞混的一点：

**`Deploy` 不等于 `正式发版`。**

- `Deploy` 是把代码上线。
- `正式发版` 是在 GitHub 上挂一个正式版本号，例如 `v0.1.1`。

---

## 3. 版本管理到底怎么算

这个项目现在有两套“版本”，但它们各管各的，不冲突。

### 3.1 线上部署版本

这是 VPS 真正在跑的版本。

平时自动长这样：

- `main-10-61dbb6b`

意思很简单：

- `main`：来自主分支
- `10`：第几次部署
- `61dbb6b`：对应哪次代码提交

这套版本是给：

- 升级
- 回滚
- 排查线上当前到底跑哪版

用的。

### 3.2 GitHub 正式版本

这是 GitHub Releases 页面上看到的版本。

例如：

- `v0.1.0`
- `v0.1.1`

这套版本是给：

- 对外挂牌
- 留正式发布记录
- 自动生成 release notes

用的。

所以以后看到：

- 网站已经自动升级了
- 但 GitHub Releases 还是旧的 `v0.1.0`

不代表出故障。  
只是说明：

**自动部署发生了，但你还没手动点“正式发版”。**

---

## 4. VPS 上的目录真相

VPS 上正式只认 `/opt/koko` 这一套目录。

关键位置：

- `/opt/koko/releases/<version>`
  - 每个版本自己的完整发布目录
  - 旧版本不会被新版本直接覆盖

- `/opt/koko/current`
  - 当前正在运行的唯一版本指针
  - 这个符号链接指向哪一版，系统就跑哪一版

- `/opt/koko/shared`
  - 持久化数据目录
  - 放数据库、上传数据、Caddy 数据等

- `/opt/koko/env/production.env`
  - 真正生效的生产环境变量

最重要的原则：

**VPS 不是第二套源码仓库。**

也就是说：

- 不在 VPS 上 `git pull`
- 不在 VPS 上改业务源码当真相
- 只认“发布包 -> 版本目录 -> current 指针”这条链

---

## 5. 正式发布包真相

我们已经踩过“整仓打包太脏”的坑，所以现在正式收成了白名单发布包。

当前发布包只带正式部署需要的东西，例如：

- `Dockerfile`
- `.dockerignore`
- `Cargo.toml`
- `Cargo.lock`
- `build.rs`
- `src/`
- `migrations/`
- `assets/`
- `frontend/`
- `scripts/`
- `ops/`

明确不该带进发布包的东西，例如：

- `docs/`
- `tests/`
- `学习/`
- `graphify-out/`
- `frontend/tests/`
- `frontend/dist/`
- `frontend/node_modules/`
- `frontend/.tsbuildinfo`

一句话：

**源码仓可以很丰富，但正式发布包必须克制。**

---

## 6. HTTPS 真相

现在这套公网 HTTPS，不是单靠 Cloudflare，也不是单靠 Caddy，而是两段一起成立。

### 6.1 浏览器到 Cloudflare

这段走：

- Cloudflare 橙云
- `SSL/TLS = Full (strict)`

### 6.2 Cloudflare 到源站 Caddy

这段走：

- Caddy 自动 HTTPS
- Cloudflare DNS-01 自动签证书和续期

也就是说，当前正式长期形态是：

1. 域名长期橙云；
2. Cloudflare 保持 `Full (strict)`；
3. Caddy 不再依赖人工切灰云；
4. Caddy 通过 Cloudflare API 自动做 DNS-01；
5. 源站证书自动续期。

最重要的结论：

**现在不需要靠人记得切灰云来续期。**

---

## 7. 为什么不能把 Cloudflare 的 SSL/TLS 关掉

我们已经实际验证过，这条路不行。

原因很简单：

1. Caddy 会自动把 HTTP 跳到 HTTPS。
2. Cloudflare 如果设成 `Off` 或 `Flexible`，它回源时会走 HTTP。
3. 这样 Cloudflare 和 Caddy 会互相打架。

所以对这个项目来说：

- `Off` 不行
- `Flexible` 不行
- `Full` 能跑
- `Full (strict)` 才是正式形态

一句话：

**Caddy 已经有证书，不等于 Cloudflare 可以把 SSL/TLS 关掉。**

---

## 8. 这次部署里真正踩过的坑

下面这些不是理论风险，是我们已经实际踩过、并且修掉的坑。

### 8.1 不能整仓发布

一开始如果直接整仓打包，会把：

- `docs`
- `tests`
- `学习`
- 前端本地缓存垃圾

一起带上去。

现在已经改成白名单发布包。

### 8.2 `tusd` 目录权限会让上传直接炸

如果 `/opt/koko/shared/tus` 宿主机目录权限不对，  
公网 `/files` 会直接 500。

现在这件事已经收进：

- `install.sh`
- `deploy.sh`
- `healthcheck.sh`

不再靠人记得手修。

### 8.3 `install.sh` 不能反复把 current 打回占位目录

如果首次安装脚本不幂等，重复执行时会把：

- `/opt/koko/current`

错误拨回 `__bootstrap__`。

现在已经修成：

- `current` 已经是正式链接时，直接复用
- 不再乱重置

### 8.4 Docker Compose 失败残留会卡住下次升级

如果上次升级失败，会残留 replacement 容器。

下次再部署时，不先清掉，就会因为重名冲突失败。

现在：

- `deploy.sh`
- `rollback.sh`

都会先清理这些残留。

### 8.5 Cloudflare 橙云下不能继续赌默认 ACME 挑战

短期偶尔能成功，不代表长期自动续期稳。

所以现在已经正式切到：

- Cloudflare DNS-01
- 自定义 Caddy 镜像
- `caddy-dns/cloudflare`

### 8.6 GitHub Action 旧版 Node 运行时会过期

之前 GitHub 警告过：

- `actions/checkout@v4`
- `actions/setup-node@v4`

还停在旧的 Node 20 运行时。

现在已经升级到：

- `actions/checkout@v5`
- `actions/setup-node@v6`

并且工作流已经强制按 Node 24 跑。

### 8.7 本地预检绿了，不等于 Docker 里也一定绿

这次又踩到一个很典型的坑：

1. GitHub Actions 预检阶段里，`pnpm` 是正常的；
2. 但 Dockerfile 里的前端构建阶段，只写了 `corepack enable`；
3. 结果容器里实际跑成了更高版本的 `pnpm`；
4. 最后 `pnpm --dir frontend install --frozen-lockfile` 直接失败；
5. 所以自动 `Deploy` 红了，但线上旧版本其实还活着。

这一类坑的本质是：

**“同样叫前端构建”，CI 外面的工具版本和 Docker 里面的工具版本，不一定是同一个真相。**

现在已经修成：

1. Dockerfile 会从 `frontend/package.json` 读取 `packageManager`；
2. 显式激活同一版本的 `pnpm`；
3. 部署门禁会拦住没锁 `pnpm` 版本的 Dockerfile。

---

## 9. 现在为什么说它已经足够稳定

因为现在这条链不是“理论上应该可以”，而是关键坑都已经在真实环境里踩过并修过。

可以这样理解：

1. 发布包不会再把一堆研究材料和缓存垃圾带进正式构建。
2. VPS 不再靠 `git pull` 维持线上真相。
3. `current` 指针和不可变版本目录已经分清。
4. 上传链里最常见的 `tusd` 权限坑已经自动收口。
5. 升级和回滚都能处理 Compose 遗留容器。
6. Cloudflare 橙云下的证书续期已经改成长期稳定路径。
7. GitHub Actions 运行时过期警告也已经提前修掉。
8. Docker 里的前端包管理器版本，已经不再靠默认值碰运气。

一句话：

**现在这套不是零风险，但已经从“靠记忆和手工兜底”升级成“靠结构和自动化收口”。**

---

## 10. 日常到底只要怎么做

以后日常只记这几件事就够了。

### 10.1 平时升级

1. 改代码；
2. `git push` 到 `main`；
3. GitHub 自动跑 `Deploy`；
4. VPS 切到新版本。

### 10.2 第一次上线

去 GitHub `Actions` 页面，手动点：

- `Initial Deploy`

### 10.3 出问题回滚

去 GitHub `Actions` 页面，手动点：

- `Rollback`

然后填你要退回的版本号。

### 10.4 想正式挂牌子

去 GitHub `Actions` 页面，手动点：

- `正式发版`

然后填：

- `v0.1.1`
- `v0.2.0`

这种正式版本号。

### 10.5 如果自动 Deploy 红了，先看什么

先看这 3 件事，不要一上来就怀疑 Cloudflare：

1. GitHub `Actions` 里到底是 **预检失败**，还是 **远端部署失败**；
2. 如果是预检失败，先看：
   - `tests/部署门禁测试.mjs`
   - `scripts/check-deployment-architecture-fitness.mjs`
   - 前端 `pnpm install / build`
3. 如果是远端部署失败，再看：
   - `/opt/koko/current/ops/healthcheck.sh`
   - VPS 上容器状态
   - 公网 `https://kokoqun.com`

一句话：

**先分清“代码还没出 GitHub runner”还是“已经发到 VPS 了但没切成功”，不要混着查。**

### 10.6 以后怎么防这种低级错

以后碰到构建链、工具链、部署链改动，默认记住这 5 条：

1. **本地能跑，不代表 Docker 里能跑。**
2. **GitHub 预检能过，不代表远端镜像构建也能过。**
3. **凡是版本敏感工具，都要显式锁版本，不要赌默认值。**
4. **修完一次，就把防呆写进门禁和测试，不要只修眼前。**
5. **vitest 绿不等于类型安全——提交前必须 `tsc --noEmit`，测试运行时跳过类型检查，CI 不跳。**

一句话：

**不要相信“应该一样”，要把“必须一样”写死在脚本和门禁里。**

## 11. 如果你重装了 VPS，要改什么

这件事最容易让人慌，所以这里直接说最短结论。

### 11.1 如果 VPS 只是重装，IP 没变

通常 **不用去 GitHub 改一堆密钥**。

一般只要补这两件事：

1. 把原来的公钥重新装回新 VPS；
2. 再手动跑一次 `Initial Deploy`。

### 11.2 原来的部署钥匙现在放在哪里

本机：

- 私钥：`C:\Users\home\.ssh\koko_github_actions_ed25519`
- 公钥：`C:\Users\home\.ssh\koko_github_actions_ed25519.pub`

VPS：

- `/root/.ssh/authorized_keys`

GitHub Secrets：

- `VPS_HOST`
- `VPS_USER`
- `VPS_SSH_KEY`
- `CLOUDFLARE_API_TOKEN`

一句话：

**重装 VPS 后，真正要补的是“让新机器重新认识这把旧钥匙”。**

### 11.3 哪些情况通常不用改

如果下面两件事都没变：

1. VPS 的 IP 没变；
2. 还是继续用原来这把 SSH 钥匙；

那一般不用改：

- `VPS_SSH_KEY`
- `VPS_USER`
- `CLOUDFLARE_API_TOKEN`

密码也不用往 GitHub 里改。  
因为自动部署靠的是 **SSH 私钥**，不是 root 密码。

### 11.4 什么时候才需要改 GitHub 或 Cloudflare

只有下面几种情况，才需要额外改：

1. **IP 变了**
   - GitHub Secret 里改：`VPS_HOST`
   - Cloudflare 里改：域名的 `A` 记录

2. **你换了一把新的 SSH 钥匙**
   - GitHub Secret 里改：`VPS_SSH_KEY`

3. **你不再用 `root` 登录**
   - GitHub Secret 里改：`VPS_USER`

### 11.5 你如果不会装公钥，最少要给我什么

如果你是重装后的新机器，又不会自己装公钥，最少只要给我：

1. 新 VPS 的 IP；
2. 新的 root 密码。

我就能把这把旧公钥重新装回去。  
装完以后，再跑一次 `Initial Deploy` 就行。

### 11.6 以后最省事的做法

以后如果你经常重装 VPS，最省事的是：

1. 尽量不换 IP；
2. 尽量继续用原来这把 SSH 钥匙；
3. 重装后优先先把公钥装回 VPS；
4. 然后再跑部署。

这样通常不需要去 GitHub 改一堆东西。

---

## 12. 以后绝对不要再退回的坏样子

以后谁维护，都不要再把部署链退回下面这些坏样子：

1. 不要在 VPS 上 `git pull` 当部署。
2. 不要整仓打包，把 `docs/tests/学习` 全部送上去。
3. 不要让 Cloudflare 长期橙云时，还靠人工切灰云续期。
4. 不要把 `current` 和版本目录混成一锅。
5. 不要把 GitHub Release 和线上部署版本混成一个概念。
6. 不要再引入第二套公网入口真相。
7. 不要为了省事把 Cloudflare `SSL/TLS` 改成 `Off` 或 `Flexible`。
8. 不要把部署脚本写成“第一次能跑，以后重复执行就炸”。

一句话：

**部署链一旦重新依赖人工记忆、人工切换、人工补洞，就说明它退化了。**

---

## 13. 最后一句总结

这次真正留下来的经验不是某个命令，而是一条判断标准：

**自动部署的价值，不是“能部署一次”，而是“过几个月重新部署、换版本部署、回滚部署、证书续期时，不需要再把以前踩过的坑重新踩一遍”。**
